---
phase: 03-curva-de-crescimento
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/growth-reference/index.ts
  - lib/growth-reference/index.spec.ts
  - lib/growth-reference/intergrowth/weight-newborn-boys.json
  - lib/growth-reference/intergrowth/weight-newborn-girls.json
  - lib/growth-reference/intergrowth/length-newborn-boys.json
  - lib/growth-reference/intergrowth/length-newborn-girls.json
  - lib/growth-reference/intergrowth/head-circumference-newborn-boys.json
  - lib/growth-reference/intergrowth/head-circumference-newborn-girls.json
  - lib/growth-reference/preterm-transition.ts
  - lib/growth-reference/preterm-transition.spec.ts
  - components/dashboard/patients/growth/growth-chart.tsx
autonomous: false
requirements: [GROWTH-02, GROWTH-03]
must_haves:
  truths:
    - "As curvas cobrem o período de prematuro/RN com o padrão Intergrowth-21st como base, transitando para OMS no termo corrigido (D-01 / D-04 / OQ1 RESOLVED)"
    - "Para um paciente prematuro (<37 sem) com idade corrigida < 0 (antes do termo-equivalente) a medição é plotada sobre a banda Intergrowth; a partir do termo corrigido plota sobre a banda OMS (D-04, regra de transição RESEARCH A1)"
    - "getReferenceTable aceita standard='intergrowth' e devolve rows + metadados source/version/ageMin/ageMax como para OMS (D-03)"
    - "A banda Intergrowth exibe fonte + faixa etária cobertas a partir dos metadados do arquivo, igual à banda OMS (D-03)"
    - "A math de banda/posição reusa lib/lms-zscore.ts e a infra de reference-index da 03-02 sem forkar (RESEARCH Don't Hand-Roll)"
  artifacts:
    - "lib/growth-reference/intergrowth/*.json (6 arquivos: 3 indicadores × 2 sexos, RN/prematuro) com metadados source/version/ageMin/ageMax/standard=intergrowth"
    - "lib/growth-reference/index.ts estendido — getReferenceTable resolve standard='intergrowth'"
    - "lib/growth-reference/preterm-transition.ts — regra pura Intergrowth→OMS por idade corrigida"
    - "growth-chart.tsx renderiza a banda de prematuro que transita para OMS no termo corrigido"
  key_links:
    - "growth-chart consulta preterm-transition (qual standard usar por idade corrigida) → getReferenceTable(standard, indicator, sex) → lmsValueAtZ para as bandas"
    - "computePediatricAge({ correctedAgeCutoffMonths: 36 }) fornece a idade corrigida que a regra de transição consome"
  prohibitions:
    - "NÃO usar dados Intergrowth não verificados na curva clínica sem o checkpoint human-verify (mesma postura do blocker WHO — RESEARCH Pitfall 3 / OQ1)"
    - "NÃO diferir nem mover Intergrowth para 'Fora desta fase' — OQ1 está RESOLVED como entregar em Phase 3 (D-01/D-04)"
    - "NÃO forkar lib/lms-zscore.ts nem duplicar a infra de reference-index da 03-02 — estender getReferenceTable com o standard Intergrowth"
    - "NÃO criar tabela de banco para os dados Intergrowth — JSON estático versionado como o WHO (RESEARCH Storage strategy)"
    - "NÃO desenhar a banda past-ceiling — respeitar ageMin/ageMax por arquivo; clamp z±3 (D-03 / OQ2)"
    - "NÃO tocar em patient-clinical-overview.tsx (D-08)"
---

<objective>
Slice vertical "curva do prematuro": sobre a infra de LMS + reference-index + growth-chart da 03-02, adiciona a referência **Intergrowth-21st** para o período de prematuro/RN e a regra de transição Intergrowth→OMS no termo corrigido. Para um prematuro (<37 sem), a curva plota sobre a banda Intergrowth antes do termo-equivalente e sobre a banda OMS a partir do termo corrigido, mantendo os dois pontos (cronológico/corrigido) e o toggle da 03-02. Fecha o restante de GROWTH-02 (referência completa: OMS + Intergrowth com fonte/faixa) e reforça GROWTH-03 (posicionamento por idade corrigida estendida). Honra D-01/D-04 e a disposição OQ1 (RESOLVED: entregar Intergrowth em Phase 3).

Purpose: Sem a banda de prematuro, o prematuro/RN não tem referência clínica correta antes do termo — D-01/D-04 exigem Intergrowth como base desse período. Esta slice o entrega reusando toda a math/infra já construída na 03-02.
Output: 6 arquivos JSON Intergrowth versionados (metadados de fonte/faixa), `getReferenceTable` estendido para `standard='intergrowth'`, a regra pura de transição, e a banda de prematuro renderizada no growth-chart transitando para OMS.
</objective>

## Phase Goal

**As a** pediatra, **I want to** ver as medições de um prematuro/RN plotadas sobre a referência Intergrowth-21st no período de prematuro, transitando para a curva OMS no termo corrigido, **so that** eu avalie o crescimento do prematuro sobre a referência clinicamente correta de cada período.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-curva-de-crescimento/03-CONTEXT.md
@.planning/phases/03-curva-de-crescimento/03-RESEARCH.md
@.planning/phases/03-curva-de-crescimento/03-PATTERNS.md
@.planning/phases/03-curva-de-crescimento/03-UI-SPEC.md
@lib/compute-pediatric-age.ts
@lib/lms-zscore.ts
@lib/growth-reference/index.ts
</context>

## Artifacts this phase produces (Plan 04)

- **Static assets (Intergrowth):** `lib/growth-reference/intergrowth/{weight,length,head-circumference}-newborn-{boys,girls}.json` (6 files), each `{ source, standard: "intergrowth", sex, indicator, ageUnit, ageMin, ageMax, version, rows: [{ ageMonths, L, M, S }] }` — mesmo shape dos arquivos WHO da 03-02 (RN/prematuro; ageMonths pode ser negativo/fracionário relativo ao termo corrigido conforme a fonte).
- **Lib (reference index, modified):** `lib/growth-reference/index.ts` — `getReferenceTable(standard, indicator, sex)` resolve `standard === "intergrowth"` importando os 6 JSON; tipo `GrowthReferenceStandard` inclui `"intergrowth"`; `index.spec.ts` estendido.
- **Lib (transition, pure):** `lib/growth-reference/preterm-transition.ts` — `resolveReferenceStandard({ gestationalAgeWeeks, correctedAgeMonths })` → `"intergrowth" | "who"` aplicando a regra A1 (Intergrowth antes do termo corrigido, OMS a partir do termo); `.spec.ts`.
- **Components (modified):** `components/dashboard/patients/growth/growth-chart.tsx` — para prematuros, desenha a banda Intergrowth no segmento pré-termo e a banda OMS a partir do termo corrigido, com caption de proveniência por segmento.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: confirmar fonte/licença/faixas dos dados Intergrowth-21st antes da ingestão</name>
  <action>Confirmar com o médico a fonte canônica Intergrowth-21st (intergrowth21.com), a forma machine-readable das tabelas LMS (ou a extração das tabelas publicadas), os termos de redistribuição, as faixas etárias/indicadores cobertos (RN/prematuro) e a rotulagem PT-BR, e travar a ingestão dos LMS Intergrowth até essa confirmação. Se os dados provarem-se genuinamente inobteníveis em forma utilizável, ESTE checkpoint é o ponto de re-decisão de escopo — NÃO drope silenciosamente (OQ1 RESOLVED exige entregar; a indisponibilidade real vira decisão do médico aqui).</action>
  <what-built>Nenhum dado Intergrowth ingerido ainda. As tabelas Intergrowth-21st (L, M, S por indicador/sexo/idade, RN/prematuro) alimentarão uma CURVA CLÍNICA do período de prematuro — mesma postura de acurácia do blocker WHO (03-02). A auditoria da RESEARCH marcou a disponibilidade machine-readable e os termos de redistribuição como não verificados nesta sessão (A2). Este checkpoint trava a ingestão até o médico confirmar fonte, licença e faixas — e é o ponto explícito de re-decisão caso os dados sejam inobteníveis (OQ1).</what-built>
  <how-to-verify>
    1. Abrir intergrowth21.com (e intergrowth21.tghn.org) e localizar os padrões: newborn size-at-birth e/ou postnatal growth of preterm infants; confirmar que há tabelas LMS (ou percentis) para peso, comprimento e perímetro cefálico por sexo.
    2. Confirmar os termos de redistribuição/licença dos dados (o médico/produto aprova incluir os LMS versionados no repositório).
    3. Confirmar com o médico as faixas etárias/PMA cobertas e a regra de transição Intergrowth→OMS no termo corrigido (RESEARCH A1: usar Intergrowth antes do termo corrigido, OMS a partir do termo).
    4. Confirmar a rotulagem PT-BR e a granularidade das linhas (semanas de PMA / meses corrigidos).
    5. Spot-check obrigatório antes de confiar: a linha P50 (z=0) DEVE igualar o valor `M` da fonte Intergrowth para uma idade+sexo conhecidos (será testado em `lib/growth-reference/index.spec.ts`).
    6. Se as tabelas forem inobteníveis em forma utilizável: registrar aqui a decisão do médico (extrair manualmente das figuras publicadas, aceitar subconjunto de indicadores, ou re-decidir escopo) — não seguir sem essa decisão.
  </how-to-verify>
  <resume-signal>Digite "confirmado" com a fonte/licença/faixas Intergrowth aprovadas para ingerir os LMS, ou registre a re-decisão de escopo caso os dados sejam inobteníveis.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: LMS Intergrowth (JSON versionado) + index estendido + regra de transição</name>
  <files>lib/growth-reference/intergrowth/weight-newborn-boys.json, lib/growth-reference/intergrowth/weight-newborn-girls.json, lib/growth-reference/intergrowth/length-newborn-boys.json, lib/growth-reference/intergrowth/length-newborn-girls.json, lib/growth-reference/intergrowth/head-circumference-newborn-boys.json, lib/growth-reference/intergrowth/head-circumference-newborn-girls.json, lib/growth-reference/index.ts, lib/growth-reference/index.spec.ts, lib/growth-reference/preterm-transition.ts, lib/growth-reference/preterm-transition.spec.ts</files>
  <read_first>
    - lib/growth-reference/index.ts (arquivo criado em 03-02 — a estender: getReferenceTable, GrowthReferenceTable, o import estático dos JSON WHO)
    - lib/growth-reference/index.spec.ts (arquivo da 03-02 — estender com os casos Intergrowth)
    - lib/growth-reference/who/weight-for-age-boys.json (analog de shape: source/standard/sex/indicator/ageUnit/ageMin/ageMax/version/rows[{ageMonths,L,M,S}])
    - lib/lms-zscore.ts (math pura da 03-02 — reusar, NÃO forkar)
    - lib/compute-pediatric-age.ts (idade corrigida estendida a 36m — a regra de transição consome correctedAgeMonths)
    - .planning/phases/03-curva-de-crescimento/03-RESEARCH.md §"Intergrowth → WHO transition rule (D-04)" (regra A1), §"Storage strategy — static versioned JSON", §"Open Questions (RESOLVED)" OQ1
    - .planning/phases/03-curva-de-crescimento/03-PATTERNS.md §"lib/growth-reference/index.ts"
  </read_first>
  <behavior>
    - `getReferenceTable("intergrowth", "weight-for-age", "masculino")` retorna rows não-vazio + `source`/`version`/`ageMin`/`ageMax` presentes (metadados por arquivo)
    - cada um dos 6 arquivos JSON Intergrowth tem `source`, `standard: "intergrowth"`, `version`, `ageMin`, `ageMax`, `ageUnit` e rows com {ageMonths, L, M, S}
    - `lmsValueAtZ(0, row)` sobre uma linha Intergrowth conhecida iguala o `M` da fonte (spot-check P50==M, Pitfall 3)
    - `resolveReferenceStandard({ gestationalAgeWeeks: 32, correctedAgeMonths: -1 })` retorna `"intergrowth"` (antes do termo corrigido)
    - `resolveReferenceStandard({ gestationalAgeWeeks: 32, correctedAgeMonths: 2 })` retorna `"who"` (a partir do termo corrigido)
    - `resolveReferenceStandard({ gestationalAgeWeeks: 39, correctedAgeMonths: -1 })` retorna `"who"` (termo — nunca Intergrowth para não-prematuro)
  </behavior>
  <action>
    **JSON Intergrowth** (6 arquivos): ingerir da fonte confirmada no checkpoint (intergrowth21.com — newborn size-at-birth e/ou postnatal preterm). Cada arquivo no MESMO shape dos WHO da 03-02: `{ source, standard: "intergrowth", sex, indicator, ageUnit, ageMin, ageMax, version, rows: [{ ageMonths, L, M, S }] }`. Indicadores RN/prematuro: peso, comprimento, perímetro cefálico (IMC não é padrão Intergrowth de RN — omitir). `ageMonths` relativo ao termo corrigido conforme a fonte (pode ser negativo/fracionário pré-termo); respeitar `ageMin`/`ageMax` reais da fonte (D-03/OQ2). NÃO inventar valores — os números vêm da fonte confirmada no checkpoint.
    **lib/growth-reference/index.ts** (estender): adicionar `"intergrowth"` ao tipo `GrowthReferenceStandard` (ou union equivalente); importar estaticamente os 6 JSON Intergrowth; `getReferenceTable(standard, indicator, sex)` resolve `standard === "intergrowth"` para os arquivos Intergrowth (retornando o mesmo `{ rows, source, version, ageMin, ageMax, ageUnit }`). NÃO duplicar a lógica de resolução WHO — estender a mesma função/mapa.
    **lib/growth-reference/preterm-transition.ts** (novo, puro): `resolveReferenceStandard({ gestationalAgeWeeks, correctedAgeMonths }): "intergrowth" | "who"`. Regra A1: se `gestationalAgeWeeks >= 37` (não-prematuro) → sempre `"who"`; se prematuro e `correctedAgeMonths < 0` (antes do termo-equivalente / PMA 40sem) → `"intergrowth"`; se prematuro e `correctedAgeMonths >= 0` (termo corrigido em diante) → `"who"`. JSDoc citando D-04 + RESEARCH A1. Uma responsabilidade por arquivo.
    **specs**: estender `index.spec.ts` com o lookup Intergrowth + presença de metadados + spot-check P50==M em uma linha Intergrowth; criar `preterm-transition.spec.ts` cobrindo os 3 casos de comportamento (pré-termo→intergrowth, pós-termo→who, não-prematuro→who).
  </action>
  <verify>
    <automated>npx tsx --test lib/growth-reference/index.spec.ts lib/growth-reference/preterm-transition.spec.ts && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ls lib/growth-reference/intergrowth/*.json | wc -l` retorna 6
    - `grep -L '"standard": "intergrowth"' lib/growth-reference/intergrowth/*.json` retorna vazio (todo arquivo declara o standard intergrowth)
    - `grep -L '"source"' lib/growth-reference/intergrowth/*.json` retorna vazio (todo arquivo tem source)
    - `getReferenceTable("intergrowth", "weight-for-age", "masculino")` retorna `ageMin`/`ageMax`/`source`/`version` não-nulos e rows não-vazio
    - `lib/growth-reference/preterm-transition.spec.ts` verde: pré-termo→intergrowth, pós-termo→who, GA≥37→who
    - `lib/growth-reference/index.spec.ts` verde incluindo o spot-check P50==M numa linha Intergrowth
    - `yarn typecheck` passa
  </acceptance_criteria>
  <done>6 JSON Intergrowth versionados com metadados; getReferenceTable resolve o standard intergrowth reusando a infra da 03-02; regra de transição pura testada; specs verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Banda Intergrowth no growth-chart transitando para OMS no termo corrigido</name>
  <files>components/dashboard/patients/growth/growth-chart.tsx</files>
  <read_first>
    - components/dashboard/patients/growth/growth-chart.tsx (arquivo criado em 03-02 — a modificar: bandas de referência + caption de proveniência + posicionamento por idade corrigida)
    - lib/growth-reference/preterm-transition.ts (Task 1 — qual standard por idade corrigida)
    - lib/growth-reference/index.ts (Task 1 — getReferenceTable resolve intergrowth)
    - lib/lms-zscore.ts (bandas via lmsValueAtZ — 03-02)
    - lib/compute-pediatric-age.ts (idade corrigida a 36m; measured_on como `now`, { correctedAgeCutoffMonths: 36 })
    - .planning/phases/03-curva-de-crescimento/03-RESEARCH.md §"Two points without clutter (D-04)", §"Intergrowth → WHO transition rule (D-04)"
    - .planning/phases/03-curva-de-crescimento/03-UI-SPEC.md §"C5", "C6", "Color", "Copywriting Contract"
  </read_first>
  <action>
    Modificar `growth-chart.tsx` (mantém `"use client"` e todo o comportamento da 03-02: tabs por indicador, toggle percentil/z, toggle cronológica/corrigida, pontos do paciente, empty state sem sexo). Para um paciente prematuro (`gestational_age_weeks < 37`): usar `resolveReferenceStandard` por posição de idade corrigida para escolher o standard da banda — desenhar a banda Intergrowth (via `getReferenceTable("intergrowth", indicator, sex)` + `lmsValueAtZ`) no segmento pré-termo (idade corrigida < 0) e a banda OMS a partir do termo corrigido, num mesmo eixo x de idade corrigida em meses. As duas bandas são segmentos contíguos (Intergrowth até o termo, OMS a partir do termo) — respeitar `ageMin`/`ageMax` de cada arquivo (sem past-ceiling; clamp z±3 como na 03-02). Caption de proveniência por segmento: `Fonte: Intergrowth-21st · RN/prematuro` no segmento pré-termo e `Fonte: OMS (WHO) · faixa {ageMin}–{ageMax}` no segmento OMS (D-03 obrigatório para ambas). Para indicadores sem dado Intergrowth (ex.: IMC), manter só a banda OMS. Não-prematuro (`>= 37`): comportamento idêntico à 03-02 (só OMS) — a banda Intergrowth só aparece para prematuro. Ramp monocromático azul, P50 enfatizado (não um hue por centil — reusar o estilo da 03-02).
  </action>
  <verify>
    <automated>yarn typecheck && yarn build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `yarn typecheck` e `yarn build` verdes
    - `growth-chart.tsx` inicia com `"use client"` e importa `resolveReferenceStandard` de `@/lib/growth-reference/preterm-transition`
    - `grep -q "intergrowth" components/dashboard/patients/growth/growth-chart.tsx` retorna match (consulta o standard Intergrowth)
    - `grep -q "Intergrowth" components/dashboard/patients/growth/growth-chart.tsx` retorna match (caption de proveniência do segmento de prematuro)
    - a caption OMS (`ageMin`/`ageMax`) da 03-02 continua presente (banda OMS não regride)
    - `patient-clinical-overview.tsx` NÃO aparece no diff (D-08)
  </acceptance_criteria>
  <done>Prematuro plota sobre banda Intergrowth no pré-termo transitando para OMS no termo corrigido, cada segmento com sua caption de fonte/faixa; não-prematuro inalterado; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset → curva clínica | dados Intergrowth-21st LMS alimentam a curva do período de prematuro (acurácia clínica) |
| repositório → produto | inclusão dos LMS Intergrowth versionados sob termos de redistribuição a confirmar |
| client render | a banda de prematuro é client-side; consome medições já escopadas server-side (03-01) e dados de referência read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Tampering (data integrity) | dados Intergrowth LMS incorretos → curva de prematuro clinicamente enganosa | high | mitigate | checkpoint human-verify de fonte/licença/faixas (mesma postura do blocker WHO); spot-check P50==M em spec; exibir source/version/faixa por segmento (D-03) |
| T-03-14 | Repudiation/License | redistribuição indevida dos dados Intergrowth | medium | mitigate | checkpoint confirma a licença/termos de redistribuição antes de commitar os JSON (blocking-human) |
| T-03-15 | Information disclosure | curva de dado sensível de menor renderizada no cliente | medium | accept | dados de medição já escopados por profile_id+patient_id na leitura server-side (03-01); referência é conteúdo público read-only; sem novo canal de saída |
| T-03-16 | Tampering | valor fora de faixa extrapolando past-ceiling da banda de prematuro | low | mitigate | respeitar ageMin/ageMax por arquivo Intergrowth + clamp z±3 (D-03/OQ2) |
| T-03-SC | Tampering | (sem novo pacote npm neste plano) | low | accept | nenhum install neste plano; recharts já instalado com checkpoint na 03-02 |
</threat_model>

<verification>
- `yarn test` verde (index.spec.ts estendido com Intergrowth + spot-check P50==M; preterm-transition.spec.ts)
- `yarn typecheck` e `yarn build` verdes
- 6 JSON Intergrowth com metadados de fonte/faixa; `standard: "intergrowth"` em todos
- No perfil de um paciente prematuro (<37 sem): a curva mostra a banda Intergrowth no pré-termo transitando para OMS no termo corrigido, cada segmento com sua caption de fonte/faixa
- No perfil de um paciente a termo (≥37 sem): só a banda OMS (comportamento da 03-02 inalterado)
- `patient-clinical-overview.tsx` inalterado (D-08)
</verification>

<success_criteria>
- Curva cobre o período de prematuro/RN com Intergrowth-21st como base, transitando para OMS no termo corrigido (GROWTH-02 / D-01/D-04)
- Fonte + faixa etária visíveis por segmento (Intergrowth e OMS) a partir dos metadados do arquivo (GROWTH-02 / D-03)
- Transição Intergrowth→OMS posicionada pela idade corrigida estendida a 36m, reusando o motor da Phase 1 (GROWTH-03)
- Infra de LMS/reference-index da 03-02 reusada sem fork; nenhum dado Intergrowth entra na curva sem o checkpoint de fonte/licença (OQ1)
</success_criteria>

<output>
Create `.planning/phases/03-curva-de-crescimento/03-04-SUMMARY.md` when done
</output>
