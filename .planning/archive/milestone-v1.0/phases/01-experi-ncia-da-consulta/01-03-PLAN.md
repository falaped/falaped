---
phase: 01-experi-ncia-da-consulta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - actions/cases/download-case-report-pdf.ts
  - scripts/repro-report-pdf.ts
  - package.json
  - yarn.lock
autonomous: false
requirements: [CONS-04]
must_haves:
  truths:
    - "The report PDF action drops empty/whitespace-only sections and collapses runaway blank lines before calling buildReportPdf (Path B — attacks the empty-paragraph inflation, RESEARCH cause #1)"
    - "The forgotten console.log('datapdf', datapdf) that leaks full clinical report content (minors' PII) is removed (security cleanup, D-12 area / Pitfall 6)"
    - "A dev repro script renders synthetic 0.9 / ~1.05 / 2.3-page reports to disk so the 1-page, boundary, and multi-page success-criterion cases can be visually verified"
    - "The kit root cause (200pt forced footer reserve triggering early addPage + estimate≠render heightOfString drift) is fixed at source in @falaped/falaped-kit, the kit is released, and this app's pin is bumped from 0.2.7 to the new version (Path A — eliminates the ~1.05-page phantom page)"
    - "All THREE page sizes (1-page, ~1.05-page boundary, multi-page) render CLEAN — no phantom page, no trailing footer band — verified after the version bump (CONS-04 fully met)"
  artifacts:
    - path: "actions/cases/download-case-report-pdf.ts"
      provides: "Sanitized sections + console.log removed"
      contains: "filter"
    - path: "scripts/repro-report-pdf.ts"
      provides: "Dev script rendering synthetic reports at 3 page sizes for visual CONS-04 verification"
      contains: "buildReportPdf"
    - path: "package.json"
      provides: "@falaped/falaped-kit pin bumped past 0.2.7 to the Path A release"
      contains: "@falaped/falaped-kit"
  key_links:
    - from: "actions/cases/download-case-report-pdf.ts"
      to: "@falaped/falaped-kit/pdf"
      via: "buildReportPdf(datapdf) with sanitized sections"
      pattern: "buildReportPdf"
    - from: "package.json"
      to: "@falaped/falaped-kit"
      via: "version pin bumped to the Path A kit release"
      pattern: "@falaped/falaped-kit"
---

<objective>
Land CONS-04 in full. Path B (in-repo, Wave 1): sanitize the `sections` payload before `buildReportPdf` (drop empty sections, collapse `\n{3,}` → `\n\n` — attacking the empty-paragraph inflation that is RESEARCH cause #1), remove the forgotten `console.log("datapdf", datapdf)` that leaks minors' clinical content to server logs, and add a dev repro script that renders synthetic reports at 0.9 / ~1.05 / 2.3 pages so the success-criterion-#4 cases are visually verifiable. Path A (kit source fix + release + bump): the kit owner edits `@falaped/falaped-kit` at source to remove the 200pt forced footer reserve that triggers the early `addPage()` and to fix the estimate≠render `heightOfString` drift, builds and publishes a new kit version, then this app bumps `@falaped/falaped-kit` from `0.2.7` to the new version (`package.json` + lockfile) and re-verifies. Path A is the deliverable that makes the ~1.05-page boundary case pass.

Purpose: The user has confirmed `@falaped/falaped-kit` is editable and releasable this cycle, so CONS-04 is fully in scope — all three page sizes must render clean. CONS-04's root cause is in the kit (mixed flow/absolute layout, 200pt forced footer reserve, estimate≠render drift); Path B alone cannot fix the boundary phantom page, so Path A is required to meet success criterion #4. The kit SOURCE is NOT in this repo (separate published package) — the kit-edit-and-release step is a cross-repo manual operation; the in-app bump + reinstall + re-verify is automatable.
Output: A sanitized + cleaned action, a reusable dev repro script, a released kit version fixing the root cause, and this app's pin bumped to it with all three sizes verified clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experi-ncia-da-consulta/01-CONTEXT.md
@.planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md
@.planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md
@actions/cases/download-case-report-pdf.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sanitize sections (Path B) + remove the PII-leaking console.log</name>
  <files>actions/cases/download-case-report-pdf.ts</files>
  <read_first>
    - actions/cases/download-case-report-pdf.ts (lines 51-53: the current `sections` map; line 65: the `console.log("datapdf", datapdf)` to remove)
    - .planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md §"CONS-04" (root cause #1/#2/#3) + §"Code Examples" (the exact Path B sanitization) + §"Cleanup"
    - .planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md §"actions/cases/download-case-report-pdf.ts" (the two concrete edits + the "do NOT nudge kit gap constants" warning)
  </read_first>
  <action>
    Replace the `sections` build at lines 51-53. Keep the `.sort((a, b) => a.order - b.order)`, then map each section to `{ title: s.name, content: (s.content ?? "").replace(/\n{3,}/g, "\n\n").trim() }`, then `.filter((s) => s.content.length > 0)` to drop empty/whitespace-only sections (RESEARCH cause #1). Do NOT replace the empty-content fallback with `"—"` anymore (that fallback was inflating empty sections into rendered paragraphs); dropping them is the fix. If filtering could yield zero sections for a real report, leave at least the non-empty ones — an all-empty report is a separate data problem, not this fix's concern.

    Remove line 65 `console.log("datapdf", datapdf)` entirely (leaks patient name + clinical sections to server logs — LGPD-adjacent for minors' data; RESEARCH Pitfall 6 / Cleanup). Do NOT touch the `console.error` in the catch block (that is legitimate error logging without payload).

    Do NOT modify any kit gap constants or attempt to fix layout from the app side beyond input sanitization — the layout fix is Path A (Task 4), done at kit source. Masking the estimate/render drift from the app side breaks at other content lengths (RESEARCH anti-pattern). Leave `buildReportPdf(datapdf)` otherwise unchanged.
  </action>
  <verify>
    <automated>grep -q "filter" actions/cases/download-case-report-pdf.ts && grep -q "replace(/\\\\n{3,}/g" actions/cases/download-case-report-pdf.ts && [ "$(grep -c 'console.log(\"datapdf\"' actions/cases/download-case-report-pdf.ts)" -eq 0 ] && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `actions/cases/download-case-report-pdf.ts` sections build collapses `\n{3,}` → `\n\n`, trims, and `.filter`s out empty content
    - `grep -c 'console.log("datapdf"' actions/cases/download-case-report-pdf.ts` == 0
    - the `console.error` in the catch block is still present
    - `yarn typecheck` exits 0
  </acceptance_criteria>
  <done>Sections are sanitized before buildReportPdf; the PII-leaking console.log is gone; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Dev repro script for CONS-04 visual verification at 3 page sizes</name>
  <files>scripts/repro-report-pdf.ts</files>
  <read_first>
    - actions/cases/download-case-report-pdf.ts (the `datapdf` shape passed to `buildReportPdf` — the script reuses this shape with synthetic sections)
    - .planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md §"Repro sample — how to verify" (dev script calling buildReportPdf directly with controlled-length lorem; the ~1.05 boundary triggers preparePageForLastSection's early addPage)
    - package.json (scripts block — confirm `tsx` is available to run a standalone script)
  </read_first>
  <action>
    Create `scripts/repro-report-pdf.ts`: a standalone `tsx`-runnable script that imports `buildReportPdf` from `@falaped/falaped-kit/pdf`, builds three synthetic `datapdf` payloads with body text sized to fill approximately 0.9 page, ~1.05 page (the boundary that triggers the kit's early `addPage`), and 2.3 pages, and writes each returned buffer to `./tmp/repro-0.9.pdf`, `./tmp/repro-1.05.pdf`, `./tmp/repro-2.3.pdf` (create `./tmp` if absent; this dir should be gitignored or under an existing ignore — confirm). Use the same `datapdf` field shape as the action (patientName, date, sections [{title, content}], doctorName, doctorCrm, consultationDateFormatted, consultationLocation, logoFooter) with placeholder values. Pass the synthetic section content through the SAME Path B sanitization (collapse + trim + drop-empty) the action uses, so the script verifies the sanitized payload. Print the output paths to stdout. This script isolates kit behavior from the DB and is the regression check rerun after the Path A kit release (Task 4) to confirm the boundary case is clean.
  </action>
  <verify>
    <automated>test -f scripts/repro-report-pdf.ts && grep -q "buildReportPdf" scripts/repro-report-pdf.ts && yarn tsx scripts/repro-report-pdf.ts && test -f tmp/repro-1.05.pdf</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/repro-report-pdf.ts` imports `buildReportPdf` and writes three PDFs sized ~0.9 / ~1.05 / ~2.3 pages
    - running `yarn tsx scripts/repro-report-pdf.ts` exits 0 and produces `tmp/repro-0.9.pdf`, `tmp/repro-1.05.pdf`, `tmp/repro-2.3.pdf`
    - the script applies the same Path B sanitization the action uses
  </acceptance_criteria>
  <done>The repro script renders all three page-size cases to disk without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Path B visually (1-page + multi-page must be clean)</name>
  <what-built>Path B sanitization in the action, the PII log removed, and a repro script that renders 0.9 / ~1.05 / 2.3-page reports to `tmp/`.</what-built>
  <how-to-verify>
    1. Open `tmp/repro-0.9.pdf`, `tmp/repro-1.05.pdf`, `tmp/repro-2.3.pdf` (generated by Task 2, BEFORE the Path A bump).
    2. Confirm the Path B result for the 1-page (0.9) and multi-page (2.3) PDFs: NO trailing band of dead space at the foot caused by empty paragraphs, and no spurious empty pages from empty sections. These are the in-repo-fixable cases — they MUST be clean now.
    3. Inspect the ~1.05-page (boundary) PDF: per RESEARCH cause #2/#3, the kit's 200pt forced footer reserve can still push a near-full page to a phantom second page on Path B alone. This is EXPECTED to PERSIST until Task 4 (Path A kit release) lands — it is NOT a Path B defect and does NOT fail this checkpoint. Note its current state so it can be compared after Task 4.
    Reference: RESEARCH §"CONS-04 — root cause" (#1 = Path B, #2/#3 = Path A).
  </how-to-verify>
  <resume-signal>Type "path-b-clean" once the 0.9 and 2.3-page PDFs are clean (or describe the defect if they are NOT — that is a real Path B bug to fix before proceeding to Path A).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Path A — fix the kit at source, release it, then bump the pin here</name>
  <what-built>This task is the cross-repo Path A fix. The kit SOURCE is NOT in this repo (`@falaped/falaped-kit` is a separate published package — `node_modules` is dist-only). The kit edit + build + publish is therefore a manual operation in the kit repo by the kit owner. Only the in-app pin bump + reinstall + re-verify is automatable from here.</what-built>
  <how-to-verify>
    In the separate `@falaped/falaped-kit` repo (NOT this repo):
    1. In the report builder (`src` equivalent of `dist/pdf/index.js` — `buildReportPdf` / `mountLastSection` / `preparePageForLastSection` / `footerGeometry`), remove the 200pt forced footer reserve that triggers the early `addPage()`. Stop subtracting `REPORT_MIN_GAP_SECTION_TO_FOOTER` (200pt) from `contentLimit` to force a page break; reserve the footer once via the document bottom margin / `setFutureMargins`, and let pdfkit's flow model own `doc.y` and the natural page break (RESEARCH cause #2). This eliminates the phantom page at the ~1.05-page boundary.
    2. Fix the estimate≠render drift: use ONE shared `opts` (same `width`/`lineGap`/spacing) for both the `heightOfString` estimate path and the render path, or drop the separate estimate entirely and let the flow model break naturally — so the estimate and the render no longer diverge per paragraph (RESEARCH cause #3). This removes the trailing footer band.
    3. Build the kit and publish a NEW version to the registry (bump the kit's own version above 0.2.7).
    Then, back in THIS repo (automatable):
    4. Bump `"@falaped/falaped-kit"` in `package.json` from `0.2.7` to the new published version, run `yarn install` to update `yarn.lock`, and re-run `yarn tsx scripts/repro-report-pdf.ts` to regenerate `tmp/repro-0.9.pdf`, `tmp/repro-1.05.pdf`, `tmp/repro-2.3.pdf` against the new kit.
    5. Re-open all three PDFs. ALL THREE must now be CLEAN: the ~1.05 boundary case has NO phantom second page, and none has a trailing footer band. This is the CONS-04 success criterion #4 — it must PASS for all three sizes.
    Reference: RESEARCH §"Kit editability — Path A" + §"CONS-04 — root cause" #2/#3.
  </how-to-verify>
  <resume-signal>Type "path-a-released v=<new-version>" once the kit is published, the pin is bumped, and all three regenerated PDFs (0.9 / ~1.05 / 2.3) are clean. If the boundary PDF still shows a phantom page after the bump, describe it — the kit fix is incomplete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action → server logs | console.log emitted full clinical report payload (patient name + sections) — minors' PII |
| action → @falaped/falaped-kit/pdf | report content crosses into the external PDF builder |
| this app → kit registry (version bump) | bumping the `@falaped/falaped-kit` pin pulls a new published artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | console.log("datapdf", datapdf) | mitigate | Remove the log line entirely (Task 1) — it leaks patient name + clinical sections (minors' PII) to server logs. LGPD-adjacent; primary security cleanup of this plan. |
| T-01-07 | Information Disclosure | scripts/repro-report-pdf.ts output | mitigate | Script uses synthetic placeholder data only (no real patient rows); output written under `tmp/` (gitignored). No real PII rendered. |
| T-01-08 | Tampering | kit gap constants (app side) | accept | Plan forbids nudging kit constants from the APP; the layout fix is done at kit source (Path A, Task 4), not by app-side constant nudging. |
| T-01-SC | Tampering | @falaped/falaped-kit version bump | mitigate | Path A is a version bump of an existing internal/private package (not a new package). The new version is the team's own released artifact; verify the published version string + integrity hash in `yarn.lock` after `yarn install`. RESEARCH Package Legitimacy Audit notes this is a version change, not a new install surface. |
</threat_model>

<verification>
- Action sanitizes sections and no longer logs the payload (grep gates).
- Repro script renders all three page sizes (`yarn tsx scripts/repro-report-pdf.ts`).
- Human visual gate (Task 3): 0.9/2.3-page PDFs clean on Path B; boundary case noted as pending Path A.
- Path A (Task 4): kit released, pin bumped in `package.json` + `yarn.lock`, all three regenerated PDFs clean — including the ~1.05 boundary (no phantom page).
- `yarn typecheck` green.
</verification>

<success_criteria>
- Empty sections dropped + blank-line runs collapsed before buildReportPdf (Path B).
- PII-leaking console.log removed.
- CONS-04 fully met: kit root cause fixed at source + released, pin bumped, and ALL THREE sizes (1-page, ~1.05-page boundary, multi-page) render clean with no phantom page and no trailing footer band.
</success_criteria>

<output>
Create `.planning/phases/01-experi-ncia-da-consulta/01-03-SUMMARY.md` when done. Record the released kit version, the pin bump (0.2.7 → new), and confirmation that all three page sizes verify clean.
</output>
