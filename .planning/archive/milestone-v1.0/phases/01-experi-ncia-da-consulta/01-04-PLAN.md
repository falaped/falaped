---
phase: 01-experi-ncia-da-consulta
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - lib/schemas/patient.ts
  - modules/patients/types.ts
  - modules/patients/create-patient.ts
  - modules/patients/update-patient.ts
  - actions/patients/create-patient.ts
  - actions/patients/update-patient.ts
  - components/dashboard/patients/patient-form/patient-form-gestational-age-field.tsx
  - components/dashboard/patients/patient-form/patient-form-personal-section.tsx
  - components/dashboard/patients/patient-form/patient-form-defaults.ts
  - components/dashboard/patients/patient-detail-hero.tsx
  - components/dashboard/cases/case-detail-header.tsx
  - components/dashboard/cases/case-detail-content.tsx
  - modules/cases/get-case-by-id.ts
  - modules/falaped-assistant/lib/formatters.ts
autonomous: false
requirements: [CONS-01]
must_haves:
  truths:
    - "The doctor can record a child's gestational age at birth in the patient form (D-10); empty is allowed and shows no error"
    - "Opening a patient profile shows the pediatric age next to the DOB, by-extenso, banded correctly (D-06/D-07/D-08/D-11), via the Plan-02 engine"
    - "The case header shows the patient's age (abbreviated, with a tooltip of the full text) next to the consultation reference (D-05)"
    - "Preterm children show a distinct 'idade corrigida' line/chip alongside chronological age (D-10)"
    - "Missing DOB shows a 'Completar cadastro' CTA (D-09); invalid/future DOB shows a destructive warning (D-12) — never blank, never crash"
    - "The assistant context age line routes through the same engine, so all three surfaces agree (Pitfall 5)"
  artifacts:
    - path: "components/dashboard/patients/patient-form/patient-form-gestational-age-field.tsx"
      provides: "Gestational-age input field mirroring the birth-date field"
      min_lines: 30
    - path: "components/dashboard/patients/patient-detail-hero.tsx"
      provides: "Age badge rendered from computePediatricAge (replaces formatAgeFromBirthDate)"
      contains: "computePediatricAge"
    - path: "components/dashboard/cases/case-detail-header.tsx"
      provides: "Abbreviated age badge + tooltip in the case meta row"
      contains: "formatPediatricAgeAbbrev"
    - path: "lib/schemas/patient.ts"
      provides: "gestational_age_weeks field on create + update schemas (20-42 range)"
      contains: "gestational_age_weeks"
  key_links:
    - from: "components/dashboard/patients/patient-detail-hero.tsx"
      to: "lib/compute-pediatric-age.ts"
      via: "computePediatricAge(patient.birth_date, new Date(), patient.gestational_age_weeks)"
      pattern: "computePediatricAge"
    - from: "modules/falaped-assistant/lib/formatters.ts"
      to: "lib/compute-pediatric-age.ts"
      via: "thin adapter over the engine for the assistant age line"
      pattern: "computePediatricAge|formatPediatricAge"
---

<objective>
Deliver the age vertical slice end-to-end: persist gestational age (D-10) through schema → module → action → form field, and render the pediatric age from the Plan-02 engine on the patient hero (full by-extenso, D-06/D-08), the case header (abbreviated + tooltip, D-05), and the assistant context line (Pitfall 5 — one source of truth). Handle the empty/missing/invalid/future states per the UI-SPEC copy contract (D-09/D-12) and show a distinct corrected-age line for preterm children (D-10).

Purpose: This is the user-observable payoff of CONS-01 — after this slice the doctor opens a patient and sees a clinically-correct, banded age (and corrected age for preemies) everywhere it matters, with the gestational-age input wired into the existing patient form. Depends on Plan 01 (the `gestational_age_weeks` column) and Plan 02 (the engine + formatter).
Output: A gestational-age form field + persistence path, age rendering on hero/case-header/assistant, and the deprecated inline formatter converted to a thin adapter.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experi-ncia-da-consulta/01-CONTEXT.md
@.planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md
@.planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md
@.planning/phases/01-experi-ncia-da-consulta/01-UI-SPEC.md
@.planning/phases/01-experi-ncia-da-consulta/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist gestational age (schema → types → modules → actions)</name>
  <files>lib/schemas/patient.ts, modules/patients/types.ts, modules/patients/create-patient.ts, modules/patients/update-patient.ts, actions/patients/create-patient.ts, actions/patients/update-patient.ts</files>
  <read_first>
    - lib/schemas/patient.ts (lines 47-136: the `blood_type` optional-with-refine idiom to mirror for the 20-42 range; `createPatientSchema` + `updatePatientSchema`)
    - modules/patients/types.ts (the `Patient` type — add one field)
    - modules/patients/create-patient.ts (lines 6-7 PATIENT_SELECT; 9-23 payload type; 34-49 insert row) + update-patient.ts (lines 6-7 PATIENT_SELECT; 9-23 payload type; 41-42 provided-field idiom)
    - actions/patients/update-patient.ts (the safeParse + conditional `payload.X = parsed.data.X ?? null` mapping) and actions/patients/create-patient.ts
    - .planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md §"modules/patients/{create,update}-patient.ts" and §"lib/schemas/patient.ts" (the exact edits)
  </read_first>
  <behavior>
    - Schema: gestational_age_weeks accepts an integer string 20-42 -> coerces to number; empty string -> undefined (optional, no error); out of range -> error "Informe um valor entre 20 e 42 semanas." (UI-SPEC).
    - Round-trip: creating/updating a patient with gestational_age_weeks=34 persists 34; with empty leaves NULL; existing rows stay NULL (graceful).
  </behavior>
  <action>
    In `lib/schemas/patient.ts` add `gestational_age_weeks` to BOTH `createPatientSchema` and `updatePatientSchema`, modeled on the `blood_type` optional-refine idiom: accept a string, transform empty -> undefined, coerce to integer, refine to the inclusive range 20-42 with PT-BR message "Informe um valor entre 20 e 42 semanas." Keep it optional on both schemas (gestational age is never required). The `z.input`/`z.output` types update automatically.

    In `modules/patients/types.ts` add `gestational_age_weeks: number | null` to the `Patient` type. In `create-patient.ts` and `update-patient.ts`: extend the `PATIENT_SELECT` constant to include `gestational_age_weeks`, add it to the payload type, and add it to the row build — create uses `gestational_age_weeks: payload.gestational_age_weeks ?? null`; update uses the provided-field idiom `if (payload.gestational_age_weeks !== undefined) updates.gestational_age_weeks = payload.gestational_age_weeks ?? null`. Keep ownership filters (`.eq("profile_id", profileId)`) untouched.

    In `actions/patients/create-patient.ts` and `update-patient.ts`: after `safeParse`, add the conditional mapping line `if (parsed.data.gestational_age_weeks !== undefined) payload.gestational_age_weeks = parsed.data.gestational_age_weeks ?? null` mirroring the existing field mappings. Auth + paid gate + result union are already in place — do not change them.
  </action>
  <verify>
    <automated>grep -q "gestational_age_weeks" lib/schemas/patient.ts && grep -q "gestational_age_weeks" modules/patients/types.ts && grep -q "gestational_age_weeks" modules/patients/create-patient.ts && grep -q "gestational_age_weeks" modules/patients/update-patient.ts && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/schemas/patient.ts` adds `gestational_age_weeks` (20-42 range, optional, PT-BR error) to both create and update schemas
    - `modules/patients/types.ts` Patient type has `gestational_age_weeks: number | null`
    - both patient modules include `gestational_age_weeks` in PATIENT_SELECT and the row build
    - both patient actions map `gestational_age_weeks` from parsed data
    - `yarn typecheck` exits 0
  </acceptance_criteria>
  <done>Gestational age round-trips through schema, modules, and actions; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Gestational-age form field + form defaults</name>
  <files>components/dashboard/patients/patient-form/patient-form-gestational-age-field.tsx, components/dashboard/patients/patient-form/patient-form-personal-section.tsx, components/dashboard/patients/patient-form/patient-form-defaults.ts</files>
  <read_first>
    - components/dashboard/patients/patient-form/patient-form-birth-date-field.tsx (the exact Field/FieldLabel/FieldContent/Input/FieldError + Controller + inputMode="numeric" + font-mono tabular-nums + aria-describedby hint structure to mirror)
    - components/dashboard/patients/patient-form/patient-form-personal-section.tsx (lines 31-63: the `flex ... md:flex-row` row where birth-date/sex live — render the new field there)
    - components/dashboard/patients/patient-form/patient-form-defaults.ts (buildEditPatientDefaultValues + CREATE_PATIENT_DEFAULT_VALUES — add the new key)
    - .planning/phases/01-experi-ncia-da-consulta/01-UI-SPEC.md §3 "Gestational-Age Input" (label, hint, placeholder, width copy contract)
  </read_first>
  <action>
    Create `components/dashboard/patients/patient-form/patient-form-gestational-age-field.tsx` exporting `PatientFormGestationalAgeField`, mirroring `PatientFormBirthDateField`: `Field` + `FieldLabel` "Idade gestacional ao nascer" (htmlFor `patient-gestational_age_weeks`) + `FieldContent` + `Input` (`type="text"`, `inputMode="numeric"`, placeholder "Ex.: 34", `font-mono text-sm tabular-nums`, `aria-describedby="patient-gestational_age_weeks-hint"`) + a hint paragraph (`text-xs text-muted-foreground`, id matching) "Em semanas. Usada para calcular a idade corrigida de prematuros." + `FieldError`. Use `Controller` like the birth-date field; strip non-digits on change. Width `w-full md:w-[12rem]` per UI-SPEC.

    In `patient-form-personal-section.tsx`, render `<PatientFormGestationalAgeField form={form} name="gestational_age_weeks" error={form.formState.errors.gestational_age_weeks} />` in the same `md:flex-row` row after `PatientFormBirthDateField`, keeping the row balanced.

    In `patient-form-defaults.ts`: add `gestational_age_weeks: patient.gestational_age_weeks != null ? String(patient.gestational_age_weeks) : ""` to `buildEditPatientDefaultValues`, and `gestational_age_weeks: ""` to `CREATE_PATIENT_DEFAULT_VALUES`.
  </action>
  <verify>
    <automated>test -f components/dashboard/patients/patient-form/patient-form-gestational-age-field.tsx && grep -q "Idade gestacional ao nascer" components/dashboard/patients/patient-form/patient-form-gestational-age-field.tsx && grep -q "PatientFormGestationalAgeField" components/dashboard/patients/patient-form/patient-form-personal-section.tsx && grep -q "gestational_age_weeks" components/dashboard/patients/patient-form/patient-form-defaults.ts && yarn typecheck && yarn lint</automated>
  </verify>
  <acceptance_criteria>
    - `patient-form-gestational-age-field.tsx` exports `PatientFormGestationalAgeField` with the UI-SPEC label/hint/placeholder verbatim
    - the field is rendered inside `PatientFormPersonalSection`
    - `patient-form-defaults.ts` includes `gestational_age_weeks` in both edit and create defaults
    - `yarn typecheck` and `yarn lint` exit 0
  </acceptance_criteria>
  <done>The gestational-age field renders in the patient form, prefilled on edit, blank on create.</done>
</task>

<task type="auto">
  <name>Task 3: Render age on hero + case header + route assistant through the engine</name>
  <files>components/dashboard/patients/patient-detail-hero.tsx, components/dashboard/cases/case-detail-header.tsx, components/dashboard/cases/case-detail-content.tsx, modules/cases/get-case-by-id.ts, modules/falaped-assistant/lib/formatters.ts</files>
  <read_first>
    - components/dashboard/patients/patient-detail-hero.tsx (line 8 imports the deprecated `formatAgeFromBirthDate`; line 23 calls it; lines 68-77 the DOB Badge — replace the age source, keep the Badge treatment)
    - components/dashboard/cases/case-detail-header.tsx (lines 8-22 StatusBadge active treatment; lines 55-66 the `flex flex-wrap items-center gap-x-4` meta row where the abbreviated age badge goes)
    - components/dashboard/cases/case-detail-content.tsx (line 92 renders `<CaseDetailHeader detail={caseDetail} />`; this RSC is shared with Plan 05 — Plan 04 edits ONLY the header render path, Plan 05 owns the timer mount)
    - modules/cases/get-case-by-id.ts (lines 12-27 CasePatientDetail; lines 80-95 patient sub-select — add gestational_age_weeks here; Plan 05 will add the pause columns to the top-level case select)
    - modules/falaped-assistant/lib/formatters.ts (lines 9-28 deprecated formatAgeFromBirthDate; lines 80-81 the `- Idade:` consumer line — convert formatter to adapter)
    - lib/compute-pediatric-age.ts + lib/format-pediatric-age.ts (Plan 02 — engine + formatter) and 01-02-SUMMARY.md for the exact exported signatures
    - .planning/phases/01-experi-ncia-da-consulta/01-UI-SPEC.md §1 "Pediatric Age Badge" + §Copywriting Contract (full vs abbreviated, missing/invalid/future copy, corrected-age line)
  </read_first>
  <action>
    In `modules/cases/get-case-by-id.ts`: add `gestational_age_weeks` to the `CasePatientDetail` type (line ~12-27) and to the `patient:patients(...)` sub-select (line ~80-95), so the case header can compute corrected age. Keep ownership scoping unchanged.

    Patient hero (`patient-detail-hero.tsx`): replace the `formatAgeFromBirthDate(patient.birth_date)` call with `computePediatricAge(patient.birth_date, new Date(), patient.gestational_age_weeks)` and render `formatPediatricAge(result)`. Keep the existing `Badge variant="secondary"` + `CalendarIcon` treatment for "DOB · age". Handle status: "ok" -> show the badge (and, if `result.corrected` present, a second adjacent badge/line labelled "idade corrigida: ..." with the chronological prefixed "idade cronológica" only when corrected is also shown, per UI-SPEC); "missing_birth_date" -> the "Idade indisponível" empty state with the "Completar cadastro" `Button` CTA (D-09); "invalid"/"future" -> the `text-destructive` message + outline destructive chip with the exact UI-SPEC copy (D-12).

    Case header (`case-detail-header.tsx`): add an abbreviated age `Badge variant="secondary"` in the meta row (lines 55-66) using `formatPediatricAgeAbbrev(computePediatricAge(detail.patient?.birth_date, new Date(), detail.patient?.gestational_age_weeks))`, wrapped in a `Tooltip` exposing the full `formatPediatricAge` text + DOB (UI-SPEC case-header rules; corrected age appends " (corr.)" abbreviated with full text in the tooltip). Render only when patient + a valid DOB exist; otherwise omit.

    Case detail content (`case-detail-content.tsx`): no data change needed beyond what `get-case-by-id` now returns — verify `CaseDetailHeader` receives the patient with `gestational_age_weeks`. Edit only the header render path; do not add the timer mount (Plan 05 owns it).

    Assistant formatter (`modules/falaped-assistant/lib/formatters.ts`): convert `formatAgeFromBirthDate` into a thin adapter over the engine — call `computePediatricAge(birthDate)` and return `formatPediatricAge(result)` (return null on non-"ok" status so the `- Idade:` line is omitted). Do NOT extend the old inline math (Pitfall 5). Keep the function name + signature so the line-80 caller is unchanged.
  </action>
  <verify>
    <automated>grep -q "computePediatricAge" components/dashboard/patients/patient-detail-hero.tsx && grep -q "formatPediatricAgeAbbrev" components/dashboard/cases/case-detail-header.tsx && grep -q "computePediatricAge" modules/falaped-assistant/lib/formatters.ts && grep -q "gestational_age_weeks" modules/cases/get-case-by-id.ts && [ "$(grep -v '^[[:space:]]*//' modules/falaped-assistant/lib/formatters.ts | grep -c 'T12:00:00')" -eq 0 ] && yarn typecheck && yarn lint && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `patient-detail-hero.tsx` calls `computePediatricAge` (no longer the inline `formatAgeFromBirthDate` for display) and renders status states per UI-SPEC (ok / missing / invalid / future / corrected)
    - `case-detail-header.tsx` renders an abbreviated age badge with a Tooltip via `formatPediatricAgeAbbrev`
    - `modules/cases/get-case-by-id.ts` patient sub-select + `CasePatientDetail` include `gestational_age_weeks`
    - `modules/falaped-assistant/lib/formatters.ts` `formatAgeFromBirthDate` delegates to the engine and contains no `T12:00:00` noon-hack math (grep gate == 0)
    - `yarn typecheck`, `yarn lint`, `yarn test` all exit 0
  </acceptance_criteria>
  <done>Age renders correctly (full + abbreviated + corrected + edge states) on the hero and case header; the assistant uses the same engine; all checks green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visually verify the age surfaces (UI/UX)</name>
  <what-built>Gestational-age form field, age badge on the patient hero (full by-extenso), abbreviated age + tooltip on the case header, corrected-age line for preemies, and the missing/invalid/future states — all from the tested engine.</what-built>
  <how-to-verify>
    Run `yarn dev` and:
    1. Open a patient with a known DOB -> confirm the hero badge shows "DD/MM/AAAA · age by extenso" with the correct band (10-day-old -> days; 6-week-old -> "6 semanas"; 3-month-old -> "3 meses e N dias"; 2-year-old -> "2 anos e N meses"), correct singular/plural.
    2. Edit that patient, set "Idade gestacional ao nascer" to 32, save, reopen -> confirm a distinct "idade corrigida: ..." line appears and chronological is labelled "idade cronológica"; clear the field -> corrected line disappears.
    3. Open the case for that patient -> confirm the abbreviated age badge in the header with a tooltip showing full text + DOB.
    4. A patient with NO DOB -> "Idade indisponível" + "Completar cadastro" CTA (no crash, no blank). A future/invalid DOB -> destructive warning copy (verify exact PT-BR strings against UI-SPEC §Copywriting).
    5. Confirm the assistant's "- Idade:" line matches the hero's age.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any band/copy/state mismatch against the UI-SPEC.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → action → DB | Gestational age (child PII) crosses here |
| DB → render (hero/header/assistant) | Patient clinical data rendered to the owning physician only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | gestational_age_weeks input | mitigate | Zod `safeParse` at the action boundary enforces integer 20-42; invalid input returns a result-union error, never reaches the DB. |
| T-01-10 | Information Disclosure | patient age surfaces (PII) | mitigate | Reads go through existing modules scoped by `profile_id`; hero/case-header/assistant only render data already fetched for the owning physician (auth + paid gate upstream). No new fetch path bypasses scoping. |
| T-01-11 | Elevation of Privilege | create/update patient actions | mitigate | Both actions keep the existing `getAuthenticatedUser` + `profile.status === "paid"` gate; gestational age is an added field on the already-gated payload. |
| T-01-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- Gestational age round-trips (Task 1 grep + typecheck).
- Form field renders with UI-SPEC copy (Task 2).
- Age renders from the engine on all three surfaces; assistant no longer uses the noon-hack (Task 3 grep gates + tests).
- Human UI gate confirms bands, corrected age, and edge-state copy (Task 4).
</verification>

<success_criteria>
- CONS-01 observable: doctor sees a banded, by-extenso pediatric age on the profile and (abbreviated) on the case header, with corrected age for preemies and graceful missing/invalid/future states.
- Gestational age is persisted and feeds corrected age.
- All age surfaces (hero, case header, assistant) agree — one engine, no inline drift.
</success_criteria>

<output>
Create `.planning/phases/01-experi-ncia-da-consulta/01-04-SUMMARY.md` when done.
</output>
