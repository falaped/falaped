---
phase: 04-documentos-cl-nicos-novos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260710000000_referrals.sql
  - supabase/migrations/20260710000100_rls_referrals.sql
  - supabase/migrations/20260710000200_storage_referrals.sql
  - supabase/migrations/20260710000300_create_referral_templates.sql
  - lib/constants.ts
  - lib/schemas/referral.ts
  - modules/referrals/types.ts
  - modules/referrals/insert-referral.ts
  - modules/referrals/get-referrals-by-profile-id.ts
  - modules/referrals/get-referrals-by-patient-id.ts
  - modules/referrals/get-referrals-by-case-id.ts
  - modules/referrals/delete-referral.ts
  - modules/referrals/delete-referral.spec.ts
  - modules/referrals/delete-referrals-bulk.ts
  - modules/referrals/get-referral-body-segments.ts
  - modules/referrals/generate-referral-pdf.ts
  - modules/referrals/upload-referral-pdf.ts
  - modules/referrals/update-referral-pdf-path.ts
  - modules/referral-templates/types.ts
  - modules/referral-templates/create-referral-template.ts
  - modules/referral-templates/get-referral-templates-by-profile-id.ts
  - modules/referral-templates/get-referral-template-by-id-for-profile.ts
  - modules/referral-templates/delete-referral-template.ts
  - actions/referrals/generate-referral.ts
  - actions/referrals/delete-referral.ts
  - actions/referrals/delete-referrals-bulk.ts
  - actions/referrals/index.ts
  - actions/referral-templates/create-referral-template.ts
  - actions/referral-templates/delete-referral-template.ts
  - actions/referral-templates/index.ts
  - actions/index.ts
  - app/dashboard/referrals/page.tsx
  - app/dashboard/referrals/loading.tsx
  - app/dashboard/referrals/new/page.tsx
  - app/dashboard/referrals/new/referral-wizard-wrapper.tsx
  - app/api/referrals/[id]/download/route.ts
  - components/dashboard/referrals/referral-wizard.tsx
  - components/dashboard/referrals/referral-card.tsx
  - components/dashboard/referrals/referral-table.tsx
  - components/app-sidebar.tsx
autonomous: false
requirements: [DOC-01, DOC-04]
user_setup:
  - service: supabase
    why: "Nova tabela referrals + templates + bucket privado precisam ser aplicados ao banco live (project acstugafrgrqzvtuznxv) antes da verificação; tipos e RLS vêm do DB, não do config"
    dashboard_config:
      - task: "Aplicar as 4 migrations (referrals, rls_referrals, storage_referrals, create_referral_templates) via supabase db push ou MCP apply_migration"
        location: "Supabase project acstugafrgrqzvtuznxv"

must_haves:
  truths:
    - "O médico gera um encaminhamento (especialidade/serviço, motivo, resumo clínico/hipótese, urgência) e baixa um PDF auto-preenchido com nome/DOB/idade do paciente, sem página em branco extra (DOC-01)"
    - "O médico salva o encaminhamento como template e o reutiliza num novo encaminhamento (DOC-04)"
    - "Toda leitura/escrita/exclusão de encaminhamento aplica o gate paid e escopa por profile_id — outro médico não acessa nem apaga (D-15)"
  artifacts:
    - "supabase/migrations/20260710000000_referrals.sql (tabela + trigger + índice)"
    - "supabase/migrations/20260710000100_rls_referrals.sql (4 policies owner-scoped)"
    - "supabase/migrations/20260710000200_storage_referrals.sql (bucket privado + 4 storage policies)"
    - "supabase/migrations/20260710000300_create_referral_templates.sql"
    - "modules/referrals/* (insert/get×3/delete/delete-bulk/generate-pdf/upload-pdf/update-pdf-path/body-segments/types + delete spec)"
    - "actions/referrals/* + actions/referral-templates/* barrels re-exported de actions/index.ts"
    - "app/dashboard/referrals/{page,loading,new/page} + app/api/referrals/[id]/download/route.ts"
    - "components/dashboard/referrals/{referral-wizard,referral-card,referral-table}.tsx"
  key_links:
    - "generateReferralAction → generateReferralPdf (buildMedicalCertificatePdf) → insertReferral → uploadReferralPdf → updateReferralPdfPath"
    - "delete-referral module recebe profileId da action e filtra .eq(id).eq(profile_id) (IDOR anchor)"
    - "app-sidebar.tsx grupo Serviços ganha item Encaminhamentos → /dashboard/referrals"
---

## Phase Goal

**As a** pediatra, **I want to** gerar um encaminhamento (especialidade, motivo, resumo clínico, urgência) auto-preenchido com os dados do paciente e reutilizável como template, **so that** eu direciono a criança ao especialista certo em poucos cliques, com um PDF impresso corretamente.

<objective>
Entregar a fatia vertical completa do **encaminhamento (DOC-01)** e seu **template salvável (DOC-04)**: migration+RLS+storage, módulos CRUD/PDF, action com gate paid + escopo profile_id, rota top-level, wizard, card/table e item na sidebar. Clona o padrão de `prescriptions` + `prescription-templates` + `medical-certificates` (título+corpo).

Purpose: Primeiro consumidor do padrão-molde nesta fase; estabelece o esqueleto (constants bucket, actions/index barrel block, sidebar item) que as fatias seguintes seguem. Honra D-07 (picklist+livre), D-08 (Rotina/Prioritário/Urgente), D-09 (campos), D-11 (template), D-15 (IDOR/paid), D-16 (kit PDF).
Output: Encaminhamento gerável de ponta a ponta com PDF baixável e template reutilizável.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentos-cl-nicos-novos/04-CONTEXT.md
@.planning/phases/04-documentos-cl-nicos-novos/04-RESEARCH.md
@.planning/phases/04-documentos-cl-nicos-novos/04-PATTERNS.md
@.planning/phases/04-documentos-cl-nicos-novos/04-UI-SPEC.md

# Analogs a clonar (ler antes de escrever cada arquivo)
@modules/prescriptions/insert-prescription.ts
@modules/prescriptions/get-prescriptions-by-profile-id.ts
@modules/prescriptions/get-prescriptions-by-patient-id.ts
@modules/prescriptions/delete-prescription.ts
@modules/prescriptions/delete-prescription.spec.ts
@modules/prescriptions/upload-prescription-pdf.ts
@modules/prescriptions/update-prescription-pdf-path.ts
@modules/prescriptions/types.ts
@modules/medical-certificates/generate-medical-certificate-pdf.ts
@modules/medical-certificates/get-medical-certificate-body-segments.ts
@modules/prescription-templates/create-prescription-template.ts
@modules/prescription-templates/get-prescription-templates-by-profile-id.ts
@modules/prescription-templates/get-prescription-template-by-id-for-profile.ts
@modules/prescription-templates/delete-prescription-template.ts
@modules/prescription-templates/types.ts
@actions/prescriptions/generate-prescription.ts
@actions/prescriptions/delete-prescription.ts
@actions/prescriptions/delete-prescriptions-bulk.ts
@actions/prescriptions/index.ts
@actions/prescription-templates/index.ts
@actions/patient-growth/create-measurement.ts
@lib/schemas/prescription.ts
@lib/schemas/medical-certificate.ts
@lib/constants.ts
@app/dashboard/prescriptions/page.tsx
@app/dashboard/prescriptions/loading.tsx
@app/dashboard/prescriptions/new/page.tsx
@app/dashboard/prescriptions/new/prescription-wizard-wrapper.tsx
@app/api/prescriptions/[id]/download/route.ts
@components/dashboard/prescriptions/prescription-wizard.tsx
@components/dashboard/prescriptions/prescription-card.tsx
@components/dashboard/prescriptions/prescription-table.tsx
@components/dashboard/medical-certificates/medical-certificate-patient-picker-sheet.tsx
@components/ui/combobox.tsx
@components/app-sidebar.tsx
@supabase/migrations/20260315000000_prescriptions.sql
@supabase/migrations/20260604000000_rls_prescriptions.sql
@supabase/migrations/20260315010000_storage_prescriptions.sql
@supabase/migrations/20260316010000_create_prescription_templates.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations (tabela+RLS+storage+template) e constants para referrals</name>
  <files>supabase/migrations/20260710000000_referrals.sql, supabase/migrations/20260710000100_rls_referrals.sql, supabase/migrations/20260710000200_storage_referrals.sql, supabase/migrations/20260710000300_create_referral_templates.sql, lib/constants.ts</files>
  <read_first>
    supabase/migrations/20260315000000_prescriptions.sql (tabela+trigger+índice, lines 1-35),
    supabase/migrations/20260604000000_rls_prescriptions.sql (4 policies, lines 7-44),
    supabase/migrations/20260315010000_storage_prescriptions.sql (bucket+4 policies, lines 1-42),
    supabase/migrations/20260316010000_create_prescription_templates.sql (lines 1-26),
    lib/constants.ts (line 8 PRESCRIPTIONS_BUCKET pattern)
  </read_first>
  <action>
    Criar as 4 migrations clonando os analogs prescriptions, trocando o slug para `referrals`.
    Tabela `public.referrals`: id uuid pk default gen_random_uuid; profile_id uuid not null references public.profiles(id) on delete cascade; patient_id uuid null references public.patients(id) on delete set null; case_id uuid null references public.cases(id) on delete set null; payload jsonb not null default '{}'::jsonb; location_state text null; issued_at date not null; pdf_storage_path text null; created_at/updated_at timestamptz not null default now(). Índices idx_referrals_profile_id e idx_referrals_issued_at (desc). Trigger set_updated_at_referrals clonando o trigger de prescriptions. NÃO adicionar colunas dedicadas tipo orientations/warning_signs (Discretion-A: tudo em payload).
    RLS (arquivo rls_referrals): `alter table public.referrals enable row level security` + 4 policies (select/insert/update/delete) para authenticated com predicado `profile_id in (select id from public.profiles where auth_user_id = auth.uid())` (insert usa with check; update usa using + with check).
    Storage (arquivo storage_referrals): insert bucket ('referrals','referrals',false) on conflict do update set public=false; + 4 policies em storage.objects keyed a `bucket_id = 'referrals' and (storage.foldername(name))[1] in (select id::text from public.profiles where auth_user_id = auth.uid())`.
    Template table (create_referral_templates): `public.referral_templates` com id, profile_id (references profiles on delete cascade), name text not null, snapshot jsonb not null default '{}'::jsonb, created_at/updated_at, idx_referral_templates_profile_id, trigger set_updated_at_referral_templates, + RLS enable + 4 policies mesmo predicado. (Templates não têm storage/PDF.)
    Em lib/constants.ts, adicionar após PRESCRIPTIONS_BUCKET a constante exportada REFERRALS_BUCKET com o valor do id do bucket de encaminhamentos e um JSDoc no mesmo formato do vizinho (path {profile_id}/{referral_id}.pdf).
    Usar timestamps de migration crescentes e posteriores à última migration existente do repo.
  </action>
  <verify>
    <automated>test $(ls supabase/migrations/*referrals*.sql supabase/migrations/*referral_templates*.sql 2>/dev/null | wc -l) -eq 4 && grep -q "REFERRALS_BUCKET" lib/constants.ts && grep -q "enable row level security" supabase/migrations/*rls_referrals.sql && grep -q "storage.foldername(name))\[1\]" supabase/migrations/*storage_referrals.sql && echo OK</automated>
  </verify>
  <done>4 migrations existem (tabela, rls, storage, template) com RLS habilitado e storage keyed a foldername[1]; REFERRALS_BUCKET exportado de lib/constants.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Módulos referrals + referral-templates + schema + delete spec</name>
  <files>lib/schemas/referral.ts, modules/referrals/types.ts, modules/referrals/insert-referral.ts, modules/referrals/get-referrals-by-profile-id.ts, modules/referrals/get-referrals-by-patient-id.ts, modules/referrals/get-referrals-by-case-id.ts, modules/referrals/delete-referral.ts, modules/referrals/delete-referral.spec.ts, modules/referrals/delete-referrals-bulk.ts, modules/referrals/get-referral-body-segments.ts, modules/referrals/generate-referral-pdf.ts, modules/referrals/upload-referral-pdf.ts, modules/referrals/update-referral-pdf-path.ts, modules/referral-templates/types.ts, modules/referral-templates/create-referral-template.ts, modules/referral-templates/get-referral-templates-by-profile-id.ts, modules/referral-templates/get-referral-template-by-id-for-profile.ts, modules/referral-templates/delete-referral-template.ts</files>
  <read_first>
    modules/prescriptions/insert-prescription.ts, get-prescriptions-by-profile-id.ts, get-prescriptions-by-patient-id.ts, delete-prescription.ts (post-fix, lines 9-41), delete-prescription.spec.ts (lines 32-54), upload-prescription-pdf.ts (lines 10-32), update-prescription-pdf-path.ts (lines 6-21), types.ts;
    modules/medical-certificates/generate-medical-certificate-pdf.ts (lines 28-70 bodySegmentsToText + buildMedicalCertificatePdf), get-medical-certificate-body-segments.ts;
    modules/prescription-templates/{create,get-by-profile-id,get-by-id-for-profile,delete}-prescription-template.ts + types.ts;
    lib/schemas/prescription.ts, lib/schemas/medical-certificate.ts (enum-with-default lines 9-12);
    04-PATTERNS.md §"Module: …" e §"Schema"
  </read_first>
  <action>
    Uma função exportada por arquivo, `SupabaseClient` injetado como primeiro arg, named exports, JSDoc, throw `[REFERRALS]`/`[REFERRAL_TEMPLATES]`.
    Schema lib/schemas/referral.ts: referralPayloadSchema com patientName/birthDate opcionais, specialty (string min 1, msg "Informe a especialidade ou serviço de destino"), reason (min 1, "Informe o motivo"), clinicalSummary opcional, urgency enum ["rotina","prioritario","urgente"] default "rotina" (D-08). generateReferralSchema = { payload, locationState opcional, issuedAt opcional }. Mensagens PT-BR.
    Módulos CRUD: insertReferral (params profileId/patientId/caseId/payload/locationState/issuedAt → insert → return id); get-referrals-by-profile-id (.select colunas .eq profile_id .order issued_at desc, mapear patient_name = payload.patientName); get-referrals-by-patient-id (+ .eq patient_id); get-referrals-by-case-id (+ .eq case_id).
    delete-referral.ts: assinatura (supabase, referralId, profileId, pdfStoragePath) com `.delete().eq("id", referralId).eq("profile_id", profileId)` (NUNCA só id — D-15/Pitfall 5), depois storage.remove best-effort (console.error, não throw) usando REFERRALS_BUCKET. delete-referral.spec.ts clonando delete-prescription.spec.ts: node:test + tsx, mock builder retornando cadeia .eq().eq(), assert que profileId incorreto vira no-op (0 linhas, não rejeita) e que erro de storage é engolido.
    delete-referrals-bulk.ts: clonar delete-prescriptions-bulk, batched delete escopado por profile_id + single storage remove.
    get-referral-body-segments.ts: compor BodySegment[][] a partir dos campos estruturados (especialidade, motivo, resumo clínico, urgência em rótulo PT-BR — Rotina/Prioritário/Urgente), clonando get-medical-certificate-body-segments.
    generate-referral-pdf.ts: import buildMedicalCertificatePdf de "@falaped/falaped-kit/pdf"; montar body via bodySegmentsToText(getReferralBodySegments(...)) juntando com "\n\n"; chamar buildMedicalCertificatePdf({ certificateTitle: "ENCAMINHAMENTO", patientName, date, body, doctorName, doctorCrm, doctorRqe (se houver), consultationDateFormatted, consultationLocation, logoFooter }). NUNCA importar pdfkit; NUNCA buildReportPdf (Pitfall 2/4/D-16).
    upload-referral-pdf.ts e update-referral-pdf-path.ts: path `${profileId}/${referralId}.pdf`, upsert true, contentType application/pdf, bucket REFERRALS_BUCKET.
    Template modules (referral-templates): create (insert profile_id/name.trim()/snapshot, return id), get-by-profile-id (.select id,name,created_at,snapshot .eq profile_id .order name), get-by-id-for-profile (.eq id .eq profile_id — gate de ownership), delete (adicionar `.eq("profile_id", profileId)` como hardening, defense-in-depth — não id-only). types.ts com ReferralTemplateSnapshot (specialty/reason/clinicalSummary/urgency), ReferralTemplate, ReferralTemplateOption.
  </action>
  <verify>
    <automated>yarn tsc --noEmit && node --test --import tsx modules/referrals/delete-referral.spec.ts</automated>
  </verify>
  <done>typecheck passa; delete-referral.spec.ts passa (no-op em profileId incorreto, storage error engolido); delete-referral e template delete filtram por profile_id; generate-referral-pdf usa buildMedicalCertificatePdf (sem pdfkit/buildReportPdf).</done>
</task>

<task type="auto">
  <name>Task 3: Actions (barrels+root), rotas, wizard/card/table e item na sidebar</name>
  <files>actions/referrals/generate-referral.ts, actions/referrals/delete-referral.ts, actions/referrals/delete-referrals-bulk.ts, actions/referrals/index.ts, actions/referral-templates/create-referral-template.ts, actions/referral-templates/delete-referral-template.ts, actions/referral-templates/index.ts, actions/index.ts, app/dashboard/referrals/page.tsx, app/dashboard/referrals/loading.tsx, app/dashboard/referrals/new/page.tsx, app/dashboard/referrals/new/referral-wizard-wrapper.tsx, app/api/referrals/[id]/download/route.ts, components/dashboard/referrals/referral-wizard.tsx, components/dashboard/referrals/referral-card.tsx, components/dashboard/referrals/referral-table.tsx, components/app-sidebar.tsx</files>
  <read_first>
    actions/prescriptions/generate-prescription.ts (estrutura+orquestração), actions/patient-growth/create-measurement.ts (lines 34-39 paid gate), actions/prescriptions/delete-prescription.ts, delete-prescriptions-bulk.ts, actions/prescriptions/index.ts, actions/prescription-templates/index.ts, actions/index.ts (blocos existentes lines 62-75);
    app/dashboard/prescriptions/page.tsx (lines 1-70), loading.tsx, new/page.tsx (lines 1-83), new/prescription-wizard-wrapper.tsx;
    app/api/prescriptions/[id]/download/route.ts (lines 1-55, Web Response 302);
    components/dashboard/prescriptions/prescription-wizard.tsx (state machine 113-132, picker import line 33, buildPayload 252-269, buildTemplateSnapshot 271-289, handleSaveAsTemplate 291-313, handleGenerate 315-347, Salvar-template Dialog 622-663, Usar-template Sheet 702-729), prescription-card.tsx (1-55), prescription-table.tsx;
    components/dashboard/medical-certificates/medical-certificate-patient-picker-sheet.tsx (reusar MedicalCertificatePatientPickerSheet), components/ui/combobox.tsx;
    components/app-sidebar.tsx (navMain grupo Serviços lines 64-73);
    04-UI-SPEC.md (Copywriting Contract, DOC-01 interaction contract, Color urgency badges)
  </read_first>
  <action>
    Action generate-referral.ts ("use server"): preâmbulo obrigatório — createClient → getAuthenticatedUser → if (!profile) return {ok:false,error:"Sessão não encontrada."}; if (profile.status !== "paid") return {ok:false,error:"Perfil não ativo. Conclua a configuração da conta em Perfil."} (ADICIONAR a linha paid — generate-prescription só checa profile?.id; usar create-measurement como modelo, D-15). Depois generateReferralSchema.safeParse → zodErrorToUserMessage em falha. Resolver doctor/logo/location/issuedAt (clamp issuedAt > today) verbatim de generate-prescription lines 56-85 + helper formatIssuedAt. buildPdf → insertReferral → uploadReferralPdf → updateReferralPdfPath em try/catch; sucesso retorna { ok:true, pdfBase64: buffer.toString("base64"), filename: `encaminhamento-${format(new Date(),"yyyy-MM-dd")}.pdf` }; catch retorna { ok:false, error:"Erro ao gerar encaminhamento. Tente novamente." }. Exportar GenerateReferralResult.
    delete-referral.ts / delete-referrals-bulk.ts actions: threadar profile.id no módulo (IDOR anchor), revalidatePath("/dashboard/referrals"), retornar {ok:true} / {ok:true,deletedCount}. Bulk: z.array safeParse, MAX_BULK 100.
    actions/referral-templates: createReferralTemplateAction + deleteReferralTemplateAction (preâmbulo auth+paid, threadar profileId). Barrels actions/referrals/index.ts e actions/referral-templates/index.ts re-exportando actions + Result types. Em actions/index.ts adicionar dois blocos export (encaminhamentos e referral-templates) espelhando os blocos prescriptions/prescription-templates existentes — APENAS append de novos blocos, não tocar os existentes.
    Rota app/dashboard/referrals/page.tsx: server component clonando prescriptions/page — createClient → getAuthenticatedUser → redirect("/auth/login") se sem profile → getReferralsByProfileId → header `flex flex-col gap-6` (h1 "Encaminhamentos" text-2xl font-semibold tracking-tight + p "Gerencie os encaminhamentos gerados." text-sm text-muted-foreground) → empty-state dashed Card ("Nenhum encaminhamento gerado." / "Gere o primeiro encaminhamento para um paciente." / CTA Novo encaminhamento) OU <ReferralTable>. loading.tsx clona o skeleton. new/page.tsx: await searchParams para templateId/patientId/caseId (D-13), Promise.all carrega patients + referral templates + initialTemplate opcional, renderiza referral-wizard-wrapper. download route: owner-scoped fetch (.eq id .eq profile_id), signed URL 60s, Web Response.redirect(signedUrl, 302) + preâmbulo auth+paid.
    referral-wizard.tsx (client): clonar prescription-wizard state machine; reusar MedicalCertificatePatientPickerSheet (NÃO criar picker novo). Campos DOC-01: specialty = Combobox (components/ui/combobox.tsx) semeado com destinos pediátricos comuns (ORL, Oftalmologia, Neuropediatria, Fonoaudiologia, Fisioterapia, Nutrição, e "Outro" livre — D-07) + free-text; motivo/resumo = Textarea; urgência = Select Rotina/Prioritário/Urgente default Rotina (D-08). buildPayload → { patientName, birthDate, specialty, reason, clinicalSummary, urgency }. buildTemplateSnapshot + handleSaveAsTemplate (Dialog "Salvar como template" → createReferralTemplateAction → toast.success("Template salvo.") → router.refresh). handleGenerate: guard toast.error("Informe a especialidade ou serviço de destino.") se specialty vazio → generateReferralAction → base64→Blob→download → router.push("/dashboard/referrals") → router.refresh; CTA "Gerar encaminhamento" (busy "Gerando…"); sucesso toast "Encaminhamento gerado. Download iniciado."; erro toast.error(getFriendlyToastMessage(result.error)). "Usar template" Sheet aplica snapshot ao state (toast `Template "{nome}" aplicado.`).
    referral-card.tsx: clonar prescription-card (p-5, hover:border-primary/30, formatDate(issued_at), "Baixar PDF" outline Download icon mr-1.5 h-4 w-4 → /api/referrals/[id]/download, senão "PDF não disponível" text-xs text-muted-foreground). referral-table.tsx: clonar prescription-table (row+bulk delete AlertDialog "Excluir encaminhamento?"/"Excluir"→"Excluindo…" variant destructive/"Cancelar", toast.success "Encaminhamento excluído."); urgência renderiza Badge semântico (variant secondary Rotina / default Prioritário / destructive Urgente — UI-SPEC Color).
    app-sidebar.tsx: no grupo "Serviços" (items lines 64-73) adicionar um item { title: "Encaminhamentos", url: "/dashboard/referrals" } — APENAS append no array de items, não reescrever o arquivo.
  </action>
  <verify>
    <automated>yarn tsc --noEmit && yarn eslint app/dashboard/referrals components/dashboard/referrals actions/referrals actions/referral-templates --max-warnings=0 && grep -q "dashboard/referrals" components/app-sidebar.tsx && grep -q 'status !== "paid"' actions/referrals/generate-referral.ts && grep -q "buildMedicalCertificatePdf" modules/referrals/generate-referral-pdf.ts</automated>
  </verify>
  <done>typecheck+lint limpos; item Encaminhamentos na sidebar; action generate-referral tem gate paid; wizard reusa MedicalCertificatePatientPickerSheet e Combobox; card/table clonam os analogs; PDF via buildMedicalCertificatePdf.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>[BLOCKING] Checkpoint: aplicar migrations de referrals ao banco live</name>
  <action>[BLOCKING] Aplicar as 4 migrations de referrals (referrals → rls_referrals → storage_referrals → create_referral_templates) ao banco Supabase live (project acstugafrgrqzvtuznxv) via `supabase db push` (requer SUPABASE_ACCESS_TOKEN não-interativo) ou Supabase MCP `apply_migration`, e confirmar tabela + RLS + bucket privado + templates no banco antes da verificação. Sem este push, typecheck/build passam com falso-positivo (tipos vêm do config, não do banco vivo).</action>
  <what-built>Aplicação das 4 migrations de referrals (tabela + RLS + storage bucket privado + referral_templates) ao banco Supabase live (project acstugafrgrqzvtuznxv). A verificação de tipos/build passa mesmo sem o DB live (tipos vêm do config), então a aplicação real precisa acontecer aqui antes de qualquer verificação de comportamento.</what-built>
  <how-to-verify>
    1. Aplicar as migrations: `supabase db push` (requer SUPABASE_ACCESS_TOKEN não-interativo) OU via Supabase MCP apply_migration para cada arquivo (referrals → rls_referrals → storage_referrals → create_referral_templates, nessa ordem).
    2. Confirmar no Supabase que a tabela public.referrals existe com RLS habilitado (4 policies), a tabela public.referral_templates existe com RLS, e o bucket privado "referrals" existe (public=false) com 4 storage policies.
    3. `curl` a um objeto do bucket referrals sem auth deve falhar (bucket privado).
    4. Iniciar o app (yarn dev), abrir /dashboard/referrals, criar um encaminhamento a partir de um paciente (especialidade via combobox + motivo + urgência), gerar e confirmar que o PDF baixa sem página em branco extra; salvar como template e reutilizá-lo.
  </how-to-verify>
  <resume-signal>Digite "approved" quando as migrations estiverem aplicadas ao DB live e o fluxo de encaminhamento gerar PDF corretamente, ou descreva problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action | payload de encaminhamento (patient_id/case_id, campos livres) cruza aqui, não confiável |
| server action → Postgres/Storage | queries escopadas por profile_id; RLS de tabela + storage como backstop |
| TipTap/textarea livre → PDF | texto livre do médico entra no body do PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | delete-referral / delete-referrals-bulk / download route | high | mitigate | `.eq("id",…).eq("profile_id", profileId)` em toda mutação/leitura; profile.id threadado da action; delete-referral.spec.ts prova no-op cross-tenant; RLS de tabela como backstop |
| T-04-02 | Elevation of Privilege | generateReferralAction / template actions | high | mitigate | preâmbulo `getAuthenticatedUser` + gate `profile.status === "paid"` como segunda linha (D-15) |
| T-04-03 | Information Disclosure | bucket referrals | high | mitigate | bucket private (public=false) + storage RLS keyed a foldername[1] = profile id + signed URL 60s |
| T-04-04 | Tampering | patient_id/case_id não confiáveis no insert | medium | mitigate | insert escopado por profile_id + FK + RLS predicate; ownership do patient garantida por RLS |
| T-04-05 | Info Disclosure | logs com PII de menor | medium | mitigate | logar mensagem de erro `[REFERRALS] …`, nunca o conteúdo do payload |
| T-04-06 | Tampering | admin/service-role client em delete | low | accept | usar cliente user-scoped; admin client não é usado neste caminho (padrão do repo) |
</threat_model>

<verification>
- `yarn tsc --noEmit` limpo; `yarn eslint` limpo nos diretórios novos.
- `node --test --import tsx modules/referrals/delete-referral.spec.ts` passa (no-op cross-tenant + storage error engolido).
- Todas as migrations aplicadas ao DB live (checkpoint) — tabela + RLS + bucket privado + templates.
- Fluxo manual: gerar encaminhamento a partir do perfil do paciente (auto-fill) e avulso via picker → PDF baixa sem página em branco extra; salvar/reutilizar template.
- `curl` sem auth a objeto do bucket referrals falha.
</verification>

<success_criteria>
- DOC-01 entregue: encaminhamento com especialidade (picklist+livre, D-07), motivo, resumo clínico, urgência (Rotina/Prioritário/Urgente, D-08), PDF auto-preenchido sem página em branco extra.
- DOC-04 (parte referral) entregue: template salvável/reutilizável per-médico (D-11).
- D-15: gate paid + escopo profile_id em toda leitura/escrita/exclusão; delete spec passa.
- D-16: PDF via @falaped/falaped-kit/pdf (buildMedicalCertificatePdf), sem pdfkit direto.
- Item Encaminhamentos na sidebar (D-12); entrada por perfil do paciente e avulsa (D-13).
</success_criteria>

<output>
Create `.planning/phases/04-documentos-cl-nicos-novos/04-01-SUMMARY.md` when done.
</output>
