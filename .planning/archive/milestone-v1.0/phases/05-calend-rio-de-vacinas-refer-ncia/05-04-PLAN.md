---
phase: 05-calend-rio-de-vacinas-refer-ncia
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - app/dashboard/vaccines/page.tsx
  - components/dashboard/vaccines/vaccine-calendar-view.tsx
  - components/dashboard/vaccines/vaccine-column.tsx
  - components/dashboard/patients/patient-detail-toolbar.tsx
autonomous: true
requirements: [VAC-01, VAC-02]
must_haves:
  truths:
    - "From a patient's profile, the doctor opens the vaccine calendar with the child's current age band highlighted in both SUS and SBIm columns (D-02/D-03)"
    - "The whole calendar stays visible (past + future); only the current band is emphasized (border + bg-primary/10) with an 'Idade atual' badge — no filtering, no diff (D-11)"
    - "Opening the standalone /dashboard/vaccines route (no patientId) shows no highlight"
  artifacts:
    - "app/dashboard/vaccines/page.tsx (accepts ?patientId and reads the patient's birth_date)"
    - "components/dashboard/vaccines/vaccine-calendar-view.tsx (birthDate-driven current-age highlight)"
    - "components/dashboard/patients/patient-detail-toolbar.tsx (link to the vaccine calendar with patientId)"
  key_links:
    - "patient-detail-toolbar Link → /dashboard/vaccines?patientId={id}"
    - "page.tsx reads patient.birth_date + gestational_age_weeks → passes birthDate to VaccineCalendarView"
    - "computePediatricAge(birthDate, ...) → currentMonths → matching band highlighted"
---

<objective>
Final vertical slice: wire the patient-profile entry point (D-03) and the current-age-band highlight (D-02) so a doctor can open the vaccine calendar directly from a child's ficha and immediately see "where we are" emphasized in both columns — reusing the Phase 1 age engine for positioning only (D-11, no diff logic; that is Phase 6).

Purpose: Close the two-entry-point requirement (standalone + por-paciente) and make the calendar useful mid-consultation by anchoring it to the current child's age.
Output: A link from the patient toolbar, a `?patientId`-aware route that resolves the child's birth date, and a position-only age-band highlight in the view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-CONTEXT.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-03-SUMMARY.md
@lib/compute-pediatric-age.ts
</context>

<artifacts_this_phase_produces>
This plan adds NO new tables, types, routes, modules, or packages. It extends existing components/route with the highlight and entry link.

**Route extended (this plan):** `/dashboard/vaccines` now reads an optional `?patientId` searchParam, resolves the patient's `birth_date` + `gestational_age_weeks` via the existing `getPatientById` module (owner-scoped by `profile_id` as usual — the patient IS owned, only the schedule data is global), and passes `birthDate`/`gestationalAgeWeeks` to the view.

**Components extended (this plan):** `VaccineCalendarView` computes the current-age highlight from `birthDate`; `VaccineColumn` applies the highlight styling + "Idade atual" badge to the matching age band. `patient-detail-toolbar.tsx` gains a link to the calendar with the patient's id.

Full phase artifact list lives in 05-01-PLAN.md §artifacts_this_phase_produces.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patient-aware route + entry link (D-03)</name>
  <files>app/dashboard/vaccines/page.tsx, components/dashboard/patients/patient-detail-toolbar.tsx</files>
  <read_first>
    - app/dashboard/vaccines/page.tsx (add optional searchParams handling + patient read; keep auth + paid gate)
    - modules/patients/get-patient-by-id.ts (existing owner-scoped patient read — reuse; the patient is owned, scoped by profile_id as normal)
    - components/dashboard/patients/patient-detail-toolbar.tsx (the dropdown menu of patient actions using `Link href={/dashboard/.../?patientId=...}` — add a sibling link)
    - components/dashboard/patients/patient-detail-hero.tsx lines ~35-38 (the `computePediatricAge(patient.birth_date, new Date(), patient.gestational_age_weeks)` call convention to mirror)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md §C1 (two entry points, one shared view)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §page.tsx patient entry point
  </read_first>
  <behavior>
    - Standalone: `/dashboard/vaccines` (no `patientId`) → the route reads no patient and passes no `birthDate` → the view renders with no highlight (unchanged from plan 03).
    - Patient mode: `/dashboard/vaccines?patientId={id}` → the route reads that patient (owner-scoped) and passes the child's `birth_date` + `gestational_age_weeks` to the view.
    - An invalid/foreign `patientId` (not owned by the current doctor) resolves to no patient → renders standalone (no highlight), never leaks another doctor's patient.
  </behavior>
  <action>
    In `app/dashboard/vaccines/page.tsx`, accept the optional `searchParams` `patientId`. Keep the existing auth + `profile.status === "paid"` gate. When `patientId` is present, read the patient via the existing owner-scoped `getPatientById(supabase, patientId, profile.id)` (or the module's actual signature — the patient IS owned; keep its normal `profile_id` scoping so a foreign id returns nothing). If the patient resolves, pass `birthDate={patient.birth_date}` and `gestationalAgeWeeks={patient.gestational_age_weeks}` to `<VaccineCalendarView>`; otherwise pass nothing (standalone). The three schedule reads (SUS/SBIm/gestante) are unchanged and stay global.

    In `components/dashboard/patients/patient-detail-toolbar.tsx`, add a link in the existing actions area/dropdown to `/dashboard/vaccines?patientId=${encodeURIComponent(patientId)}` labelled in PT-BR (e.g. "Calendário de vacinas"), mirroring the existing `Link href` pattern already used for `select-patient`. Use a Syringe/Calendar lucide icon consistent with the sidebar.

    PT-BR strings. No new package.
  </action>
  <verify>
    <automated>grep -q 'patientId' app/dashboard/vaccines/page.tsx &amp;&amp; echo ROUTE_PATIENT_PARAM_OK</automated>
    <automated>grep -q '/dashboard/vaccines?patientId' components/dashboard/patients/patient-detail-toolbar.tsx &amp;&amp; echo TOOLBAR_LINK_OK</automated>
    <automated>grep -q 'status !== "paid"' app/dashboard/vaccines/page.tsx &amp;&amp; echo PAID_GATE_RETAINED_OK</automated>
    <automated>yarn typecheck</automated>
  </verify>
  <done>`yarn typecheck` passes. The route accepts `?patientId`, reads the owned patient, and passes `birthDate`/`gestationalAgeWeeks` to the view; a foreign id yields no patient. The patient toolbar links to the calendar with the patient's id. The paid gate is retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Current-age-band highlight, position-only (D-02/D-11)</name>
  <files>components/dashboard/vaccines/vaccine-calendar-view.tsx, components/dashboard/vaccines/vaccine-column.tsx</files>
  <read_first>
    - lib/compute-pediatric-age.ts (the tested engine — call it, never re-implement date math; it returns { status, band, totalDays, parts })
    - components/dashboard/vaccines/vaccine-calendar-view.tsx (add the highlight computation from birthDate)
    - components/dashboard/vaccines/vaccine-column.tsx (apply highlight styling to the matching band)
    - components/dashboard/patients/growth/growth-chart.tsx (age-engine consumer convention)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md §C4 (highlight spec: border-l-2 border-primary + bg-primary/10 + "Idade atual" badge in BOTH columns; do NOT filter), §Accessibility (color + text badge, not color alone)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md §Pattern 5, §Pitfall 5
  </read_first>
  <behavior>
    - Given a `birthDate`, `computePediatricAge` yields the child's age; derive `currentMonths = Math.floor((totalDays ?? 0) / 30.4375)` only when `status === "ok"`, else null.
    - The age band whose `[age_months, age_months_max ?? age_months]` window contains `currentMonths` is marked "current" in BOTH the SUS and SBIm columns.
    - When `birthDate` is absent (standalone) OR the age is invalid/missing, `currentMonths` is null → no band is highlighted.
    - The highlight never hides other bands — past and future bands stay rendered.
  </behavior>
  <action>
    In `vaccine-calendar-view.tsx`, when a `birthDate` prop is present, call `computePediatricAge(birthDate, new Date(), gestationalAgeWeeks)` (never `new Date("YYYY-MM-DD")` — Pitfall 5; the engine already does local-midnight parsing) and compute `currentMonths` per the behavior above. Pass the computed current-band signal down to both `<VaccineColumn>` instances (SUS and SBIm) so the SAME age band is emphasized across both columns. The Gestante tab is on a different axis (weeks) and gets NO child-age highlight in this phase.

    In `vaccine-column.tsx`, when a band matches the current age, apply the highlight styling from C4: `border-l-2 border-primary` + `bg-primary/10` on that band, plus an `Idade atual` badge (text + color, not color alone — Accessibility). This is position-only emphasis — NO diff, NO pending/late computation (D-11; that is Phase 6). Do NOT filter to only the current band; the whole calendar remains visible. `bg-primary/10` on the current band is the ONLY place accent appears in the columns (UI-SPEC §Color).

    Keep the standalone path unchanged (no birthDate → no highlight).
  </action>
  <verify>
    <automated>grep -q 'computePediatricAge' components/dashboard/vaccines/vaccine-calendar-view.tsx &amp;&amp; echo ENGINE_REUSED_OK</automated>
    <automated>grep -c 'new Date("' components/dashboard/vaccines/vaccine-calendar-view.tsx | grep -qx 0 &amp;&amp; echo NO_RAW_DATE_PARSE_OK</automated>
    <automated>grep -q 'Idade atual' components/dashboard/vaccines/vaccine-column.tsx &amp;&amp; echo BADGE_OK</automated>
    <automated>grep -q 'bg-primary/10' components/dashboard/vaccines/vaccine-column.tsx &amp;&amp; echo HIGHLIGHT_STYLE_OK</automated>
    <automated>yarn typecheck</automated>
  </verify>
  <done>`yarn typecheck` passes. The view reuses `computePediatricAge` (no raw `new Date("...")` parsing). In patient mode the matching age band is highlighted (border + bg-primary/10 + "Idade atual" badge) in both SUS and SBIm columns, the whole calendar stays visible, and no diff/pending logic exists. Standalone mode shows no highlight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC route | Authenticated + paid request; adds an untrusted `?patientId` searchParam |
| RSC route → Supabase (patient read) | The patient IS owner-scoped — `getPatientById` filters by `profile_id` (normal ownership), unlike the global schedule reads |
| RSC route → Supabase (schedule reads) | Global-read RLS, unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure (IDOR) | `?patientId` patient read | high | mitigate | The patient read stays owner-scoped via `getPatientById(..., profile.id)` — a foreign/guessed `patientId` returns no patient (renders standalone, no leak). This is the normal `profile_id` ownership rule; it does NOT apply to the global schedule tables, only to the owned patient row. |
| T-05-01 | Elevation of Privilege | `/dashboard/vaccines` route | high | mitigate | The auth + `profile.status === "paid"` gate is retained (verified by grep) even with the new searchParam. |
| T-05-06 | Tampering | `?patientId` searchParam | low | accept | The param is only ever passed to an owner-scoped `.eq("id", patientId).eq("profile_id", ...)` read (Supabase parameterizes the value); an arbitrary value simply returns nothing. No injection surface. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this plan (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `yarn typecheck` passes.
- Patient mode highlights the correct age band in both columns with the "Idade atual" badge; standalone shows no highlight.
- A foreign `patientId` does not render another doctor's patient (owner-scoped read).
- No re-implemented date math; the tested age engine is reused.
</verification>

<success_criteria>
- D-03: two entry points work (sidebar standalone + patient toolbar link).
- D-02/D-11: current-age band highlighted position-only in both columns, whole calendar visible, no diff logic.
- VAC-01/VAC-02 consultation experience anchored to the child's age mid-consultation.
</success_criteria>

<output>
Create `.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-04-SUMMARY.md` when done.
</output>
