---
phase: 05-calend-rio-de-vacinas-refer-ncia
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - supabase/migrations/20260720000400_seed_vaccine_schedules_gestante.sql
  - app/dashboard/vaccines/page.tsx
  - components/dashboard/vaccines/vaccine-calendar-view.tsx
  - components/dashboard/vaccines/gestante-list.tsx
autonomous: false
requirements: [VAC-03, VAC-04]
must_haves:
  truths:
    - "The doctor sees two tabs — 'Criança (SUS × SBIm)' and 'Gestante' — and the Gestante tab lists the gestante vaccines with their gestational-week window in text (VAC-03)"
    - "Gestante rows include Hepatite B, dTpa (a partir de 20 semanas), Influenza, COVID-19, VSR/Abrysvo (28–36 semanas)"
    - "The Gestante dataset shows its own provenance caption; the fixed advisory remains visible"
  artifacts:
    - "supabase/migrations/20260720000400_seed_vaccine_schedules_gestante.sql (physician-approved gestante items, gestational_weeks axis)"
    - "components/dashboard/vaccines/gestante-list.tsx"
    - "components/dashboard/vaccines/vaccine-calendar-view.tsx (now has the Tabs shell: Criança | Gestante)"
  key_links:
    - "page.tsx → getVaccineScheduleWithItems(supabase, 'gestante') third read"
    - "Tabs: Criança (two columns) | Gestante (list by vaccine)"
    - "gestante items use week_min/week_max + text window, not age_months"
---

<objective>
Third vertical slice: add the Gestante reference as a separate tab on its own gestational-week axis (VAC-03), listed by vaccine with the window in text (D-05), each dataset keeping its provenance (VAC-04). Introduces the Tabs shell (Criança | Gestante) that plan 02's two-column Criança view slots into.

Purpose: Make explicit that gestante is a different dataset on a different axis (weeks of gestation, not child age) — satisfying VAC-04 criterion 3 (each calendar clearly labeled and separated).
Output: A physician-approved gestante seed on the gestational-week axis, a Gestante list-by-vaccine component, and the top-level Tabs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-CONTEXT.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan adds NO new tables, types, or routes. It reuses `getVaccineScheduleWithItems` (plan 01) with `source="gestante"` and extends existing components.

**Migration (this plan):** `20260720000400_seed_vaccine_schedules_gestante.sql` — idempotent global insert of the gestante `vaccine_schedule_items` on the gestational_weeks axis (the gestante schedule metadata row was seeded in plan 01; this adds its items). Uses `week_min`/`week_max` + text `age_label` (window). NO age_months, NO profile_id, NO cross join.

**Components (this plan):** new `GestanteList` (`gestante-list.tsx`); `VaccineCalendarView` gains the shadcn Tabs shell ("Criança (SUS × SBIm)" default, "Gestante").

Full phase artifact list lives in 05-01-PLAN.md §artifacts_this_phase_produces.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Physician sign-off on gestante clinical seed values and week windows</name>
  <action>Pause for physician review of the gestante clinical seed values and gestational-week windows before Task 1 commits/applies them. Do not author or commit clinical values autonomously (STATE.md blocker). See what-built / how-to-verify below.</action>
  <what-built>The gestante seed migration STRUCTURE (idempotent global insert on the gestational_weeks axis) is ready, but the CLINICAL VALUES — the gestante vaccine rows and their gestational-week windows — require physician sign-off before commit (STATE.md blocker). The executor must NOT author clinical values autonomously.</what-built>
  <how-to-verify>
    1. Review the gestante schedule metadata (source='gestante', axis='gestational_weeks', version, effective_date).
    2. Review each proposed gestante `vaccine_schedule_items` row and its window: Hepatite B; dTpa — a partir de 20 semanas (week_min=20, week_max=null); Influenza — qualquer momento (week_min/week_max null); COVID-19; VSR/Abrysvo — 28–36 semanas (week_min=28, week_max=36). Confirm each vaccine, timing window, and the `age_label` text (the exact PT-BR phrase the UI renders per the Copywriting Contract) match the current official gestante recommendations.
    3. Confirm `week_min`/`week_max` are populated (not `age_months`) since gestante sits on the gestational_weeks axis; confirm `week_max = null` correctly encodes "a partir de N semanas".
    4. Approve, correct specific values, or reject. Only physician-approved values may be committed.
  </how-to-verify>
  <resume-signal>Type "approved" once the gestante clinical values and week windows are confirmed, or provide the corrected values.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Gestante seed migration (gestational_weeks axis) + apply to live DB (VAC-03/VAC-04)</name>
  <files>supabase/migrations/20260720000400_seed_vaccine_schedules_gestante.sql</files>
  <read_first>
    - supabase/migrations/20260720000200_seed_vaccine_schedules.sql (SUS seed shape from plan 01 — COPY idempotent global insert)
    - supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql (SBIm seed from plan 02)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md §Gestante Modeling (week_min/week_max semantics, A5)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §seed_vaccine_schedules migration
  </read_first>
  <action>
    Write `supabase/migrations/20260720000400_seed_vaccine_schedules_gestante.sql` using the physician-approved gestante values. Insert the gestante `vaccine_schedule_items`, joining to the existing gestante schedule by `source='gestante'`/`version`, each guarded by `where not exists` on a stable key (schedule_id + vaccine + age_label). Populate `week_min`/`week_max` (structured gestational window) AND the human `age_label` text window; leave `age_months`/`age_months_max` null (gestational_weeks axis). Use `week_max = null` for open-ended "a partir de N semanas" and both values for "N–M semanas". Set `sort_order` for deterministic display order.

    CRITICAL D-07: NO `public.profiles`, NO `cross join`, NO `profile_id`. Global insert only. Clinical-content header comment stating physician approval at the checkpoint.

    Apply this migration to the live Supabase project `acstugafrgrqzvtuznxv` via `supabase db push` (or Supabase MCP / SQL editor if non-interactive). Confirm the gestante items are present (count query on `vaccine_schedule_items` joined to the gestante schedule).
  </action>
  <verify>
    <automated>grep -c 'cross join' supabase/migrations/20260720000400_seed_vaccine_schedules_gestante.sql | grep -qx 0 &amp;&amp; echo NO_CROSS_JOIN_OK</automated>
    <automated>grep -c 'profile' supabase/migrations/20260720000400_seed_vaccine_schedules_gestante.sql | grep -qx 0 &amp;&amp; echo NO_PROFILE_OK</automated>
    <automated>grep -q 'week_min' supabase/migrations/20260720000400_seed_vaccine_schedules_gestante.sql &amp;&amp; echo WEEK_AXIS_OK</automated>
  </verify>
  <done>Gestante seed migration exists (idempotent, global — zero `cross join`/`profile`), populates `week_min`/`week_max` + `age_label`, is applied to the live DB, and the gestante items are queryable including Hepatite B, dTpa (≥20 sem), Influenza, COVID-19, VSR/Abrysvo (28–36 sem).</done>
</task>

<task type="auto">
  <name>Task 2: Tabs shell (Criança | Gestante) + Gestante list-by-vaccine (VAC-03, D-04/D-05)</name>
  <files>app/dashboard/vaccines/page.tsx, components/dashboard/vaccines/vaccine-calendar-view.tsx, components/dashboard/vaccines/gestante-list.tsx</files>
  <read_first>
    - components/ui/tabs.tsx (existing shadcn primitive — reuse, do not re-scaffold)
    - components/dashboard/vaccines/vaccine-calendar-view.tsx (wrap existing Criança two-column layout in a Tabs shell)
    - components/dashboard/vaccines/vaccine-column.tsx (reference for the card/list idiom; gestante-list follows the same visual family)
    - components/dashboard/vaccines/schedule-provenance.tsx (reused for the gestante dataset)
    - app/dashboard/vaccines/page.tsx (add the third gestante read)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md §C2 (Tabs), §C5 (gestante list by vaccine, NOT by trimester), §Copywriting Contract (tab labels, gestante intro + rows)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §vaccine-calendar-view, §gestante-list
  </read_first>
  <action>
    In `app/dashboard/vaccines/page.tsx`, add a third read `getVaccineScheduleWithItems(supabase, "gestante")` and pass the gestante dataset into `<VaccineCalendarView>`.

    In `vaccine-calendar-view.tsx`, introduce a top-level shadcn `Tabs` (client) with two tabs: "Criança (SUS × SBIm)" (default, containing the existing two-column SUS×SBIm layout from plan 02) and "Gestante" (containing the new `<GestanteList>`). The active tab uses the accent indicator (UI-SPEC §Color reserves accent for the active tab). Keep the prop surface forward-compatible for the optional `birthDate` highlight (plan 04). Use `cn()` for class composition.

    Create `components/dashboard/vaccines/gestante-list.tsx` — a single list by vaccine with the window in text (C5): a Card containing the gestante intro copy ("Vacinação da gestante — janela por semana gestacional. Eixo diferente do calendário da criança.") and a list/table with columns Vacina / Janela. Render each item's `vaccine` + its `age_label` window phrase ("a partir de 20 semanas", "28–36 semanas", "qualquer momento") as body text, the window qualifier muted. Do NOT group by trimester (some vaccines cross trimesters). Render the gestante `<ScheduleProvenance>` in the card footer (its own caption, D-09).

    PT-BR strings from the Copywriting Contract. No new npm package or shadcn block.
  </action>
  <verify>
    <automated>grep -q '"gestante"' app/dashboard/vaccines/page.tsx &amp;&amp; echo GESTANTE_READ_OK</automated>
    <automated>grep -qi 'Gestante' components/dashboard/vaccines/vaccine-calendar-view.tsx &amp;&amp; echo TAB_OK</automated>
    <automated>test -f components/dashboard/vaccines/gestante-list.tsx &amp;&amp; echo GESTANTE_LIST_OK</automated>
    <automated>yarn typecheck</automated>
  </verify>
  <done>`yarn typecheck` passes. The page reads the gestante dataset. The view shows two tabs (Criança default, Gestante). The Gestante tab lists the gestante vaccines by vaccine with the text window (not by trimester), including all VAC-03 required vaccines, with its own provenance caption and the fixed advisory visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC route | Authenticated + paid request; adds the third global read (gestante) and client-side Tabs interaction |
| RSC route → Supabase | Same global-read RLS; still no write path |
| migration/service-role → tables | Only write path (gestante seed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `/dashboard/vaccines` route | high | mitigate | The auth + `profile.status === "paid"` gate from plan 01 stays; the gestante read runs behind the same gate. |
| T-05-02 | Tampering | reference tables | high | mitigate | No new write policy; gestante seed applied via migration only (D-07). SELECT-only RLS continues to deny client writes. |
| T-05-04 | Tampering / Safety | gestante seed clinical content | medium | mitigate | Gestante clinical values + week windows gated behind a blocking `checkpoint:human-verify`; per-dataset provenance + fixed advisory render for gestante (D-09). |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this plan (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `yarn typecheck` passes.
- Gestante items live in the DB on the gestational_weeks axis (week_min/week_max populated).
- Two tabs render; Gestante lists all VAC-03 vaccines with text windows, not by trimester.
- Gestante provenance caption + fixed advisory visible.
</verification>

<success_criteria>
- VAC-03: gestante reference (Hepatite B, dTpa ≥20 sem, Influenza, COVID-19, VSR/Abrysvo ≥28 sem) consultable with text windows.
- VAC-04 (extended + criterion 3): gestante is a separate, clearly labeled dataset on its own axis with its own provenance.
</success_criteria>

<output>
Create `.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-03-SUMMARY.md` when done.
</output>
