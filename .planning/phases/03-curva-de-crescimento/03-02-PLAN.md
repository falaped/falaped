---
phase: 03-curva-de-crescimento
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - package.json
  - yarn.lock
  - lib/lms-zscore.ts
  - lib/lms-zscore.spec.ts
  - lib/growth-reference/index.ts
  - lib/growth-reference/index.spec.ts
  - lib/growth-reference/who/weight-for-age-boys.json
  - lib/growth-reference/who/weight-for-age-girls.json
  - lib/growth-reference/who/height-for-age-boys.json
  - lib/growth-reference/who/height-for-age-girls.json
  - lib/growth-reference/who/bmi-for-age-boys.json
  - lib/growth-reference/who/bmi-for-age-girls.json
  - lib/growth-reference/who/head-circumference-for-age-boys.json
  - lib/growth-reference/who/head-circumference-for-age-girls.json
  - lib/growth-classification.ts
  - lib/growth-classification.spec.ts
  - components/dashboard/patients/growth/growth-chart.tsx
  - components/dashboard/patients/growth/growth-position-readout.tsx
  - components/dashboard/patients/growth/growth-section.tsx
autonomous: false
requirements: [GROWTH-02, GROWTH-03]
must_haves:
  truths:
    - "O pediatra vê 4 gráficos por idade (peso/idade, estatura/idade, IMC/idade, PC/idade) selecionáveis por tab, cada um com as medições do paciente plotadas sobre as bandas de referência OMS (D-02)"
    - "As curvas de referência usam o padrão OMS (WHO) como base 0–19a; Intergrowth-21st (período de prematuro) fica para slice posterior (D-01 / Open Question 1)"
    - "Cada gráfico exibe fonte + faixa etária coberta a partir dos metadados do arquivo de referência (D-03)"
    - "Toggle percentil (P3/P15/P50/P85/P97) ↔ escore-z (-3..+3) re-avalia as bandas a partir do MESMO dataset LMS sem recarregar (D-12)"
    - "Para prematuros (<37 sem) a idade corrigida é aplicada automaticamente até 36m com toggle cronológica↔corrigida; plota os dois pontos como par conectado (D-04/D-06)"
    - "Cada medição mostra percentil + escore-z + classificação reusando o mapa de cor de patient-bmi-ui-status (D-13)"
    - "Quando patients.sex é null, mostra estado-vazio 'Informe o sexo do paciente' e não renderiza a curva (Pitfall 5)"
  artifacts:
    - "lib/lms-zscore.ts (+ .spec.ts) — lmsZScore + lmsValueAtZ (math pura, spot-check P50==M)"
    - "lib/growth-reference/index.ts (+ .spec.ts) + 8 arquivos JSON WHO (4 indicadores × 2 sexos) com metadados source/version/ageMin/ageMax"
    - "lib/growth-classification.ts — classificação por z-cutoff WHO mapeada a good/warn/bad"
    - "components/dashboard/patients/growth/{growth-chart,growth-position-readout}.tsx"
    - "recharts@3.9.0 em package.json (pin exato)"
  key_links:
    - "growth-chart consome lib/lms-zscore (bandas) + lib/growth-reference (tabelas+metadados) + computePediatricAge({correctedAgeCutoffMonths:36}) (posição por idade)"
    - "growth-section renderiza os Tabs de indicadores + growth-chart + growth-position-readout"
    - "growth-position-readout reusa o mapa de cor good/warn/bad de lib/patient-bmi-ui-status (SÓ a cor)"
  prohibitions:
    - "NÃO usar dados WHO não verificados na curva clínica sem o checkpoint human-verify (blocker STATE.md / RESEARCH Pitfall 3)"
    - "NÃO criar tabela de banco para os dados de referência — JSON estático versionado (RESEARCH Anti-Pattern)"
    - "NÃO importar Recharts sem 'use client' no growth-chart (RESEARCH Anti-Pattern)"
    - "NÃO tratar as bandas hard-coded de classifyBmiByAge como fonte de verdade da curva — usar z-score WHO (RESEARCH linha 198); reusar SÓ o mapa de cor"
    - "NÃO colorir cada centil com um hue diferente — ramp monocromático azul, só P50 enfatizado (UI-SPEC C5/Color)"
    - "NÃO desenhar past-ceiling de cada curva — respeitar ageMax por arquivo (D-03/A5)"
---

<objective>
Slice vertical "ver a curva de crescimento": sobre o histórico persistido da 03-01, o pediatra vê as 4 curvas OMS por idade com suas medições plotadas, alterna percentil↔escore-z e cronológica↔corrigida, e lê a posição (percentil/z + classificação) de cada medição. Cobre GROWTH-02 (gráficos sobre referência OMS com fonte/faixa) e o restante de GROWTH-03 (posicionamento por idade pediátrica).

Purpose: Entrega a visualização clínica — o segundo passo do MVP em que o dado registrado vira curva legível sobre a referência da Caderneta da Criança.
Output: Math LMS pura + tabelas de referência WHO em JSON versionado + componente Recharts (tabs + toggles + corrected-age) + readout de posição, plugados na growth-section.
</objective>

## Phase Goal

**As a** pediatra, **I want to** visualizar as medições da criança plotadas sobre as curvas de referência OMS por idade, **so that** eu avalie o crescimento (percentil/escore-z) usando a idade pediátrica correta.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-curva-de-crescimento/03-CONTEXT.md
@.planning/phases/03-curva-de-crescimento/03-RESEARCH.md
@.planning/phases/03-curva-de-crescimento/03-PATTERNS.md
@.planning/phases/03-curva-de-crescimento/03-UI-SPEC.md
@lib/compute-pediatric-age.ts
@lib/patient-bmi-ui-status.ts
</context>

## Artifacts this phase produces (Plan 02)

- **Lib (pure math):** `lib/lms-zscore.ts` — `lmsZScore(x, {L,M,S})`, `lmsValueAtZ(z, {L,M,S})`; `lib/lms-zscore.spec.ts`
- **Lib (reference index):** `lib/growth-reference/index.ts` — `getReferenceTable(standard, indicator, sex)` returning `{ rows, source, version, ageMin, ageMax, ageUnit }`; type `GrowthReferenceTable`; `lib/growth-reference/index.spec.ts`
- **Static assets:** `lib/growth-reference/who/{weight-for-age,height-for-age,bmi-for-age,head-circumference-for-age}-{boys,girls}.json` (8 files), each `{ source, standard, sex, indicator, ageUnit, ageMin, ageMax, version, rows: [{ ageMonths, L, M, S }] }`
- **Lib (classification):** `lib/growth-classification.ts` — WHO z-cutoff → `{ label, status: good|warn|bad }`; `.spec.ts`
- **Components:** `components/dashboard/patients/growth/{growth-chart,growth-position-readout}.tsx` (both `"use client"`)
- **Dependency:** `recharts@3.9.0` (exact pin) added to `package.json`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: legitimidade do pacote recharts antes do install</name>
  <action>Validar a legitimidade de `recharts@3.9.0` no npm registry (peer react ^19, sem postinstall, repo público) e obter aprovação humana ANTES de `yarn add recharts@3.9.0`. Checkpoint não auto-aprovável.</action>
  <what-built>Nada instalado ainda. Este checkpoint valida a legitimidade do pacote `recharts` antes do install. A auditoria (03-RESEARCH.md §"Package Legitimacy Audit") marcou `recharts` como [SUS] APENAS por falso-positivo "too-new" no patch 3.9.2 (5 dias); o pacote em si tem 8+ anos, 53M downloads/semana, repo público, sem postinstall. Pin recomendado: `3.9.0` (patch estável ligeiramente anterior).</what-built>
  <how-to-verify>
    1. Confirmar no registro: `npm view recharts@3.9.0 version` → `3.9.0`; `npm view recharts@3.9.0 peerDependencies` inclui `react` na linha `^19`.
    2. Confirmar ausência de postinstall: `npm view recharts@3.9.0 scripts.postinstall` → vazio.
    3. Abrir https://www.npmjs.com/package/recharts e confirmar repo `github.com/recharts/recharts`, downloads na casa dos dezenas de milhões/semana.
    4. Após aprovação, o executor roda `yarn add recharts@3.9.0` (NUNCA `npm install` — projeto yarn-only) e valida install limpo sob React 19 (sem conflito `react-is`; se aparecer, adicionar `resolutions` de `react-is` na linha do React 19).
  </how-to-verify>
  <resume-signal>Digite "aprovado" para instalar recharts@3.9.0, ou "rejeitado" com alternativa.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: confirmar acurácia/fonte das tabelas de referência OMS</name>
  <action>Confirmar com o médico a fonte canônica, as faixas etárias cobertas por indicador (D-03/A5) e a rotulagem PT-BR (A3), e travar a ingestão das LMS WHO até essa confirmação (blocker STATE.md). Exigir o spot-check P50==M / P3/P97 vs WHO antes de confiar nos dados.</action>
  <what-built>As tabelas de referência OMS (L, M, S por indicador/sexo/idade) serão ingeridas como JSON estático versionado. Isto alimenta uma CURVA CLÍNICA — a acurácia é o blocker registrado na STATE.md ([Phase 3] "acurácia das curvas de referência OMS é tarefa de conteúdo — verificar dados/fonte oficiais no momento do build"). Este checkpoint trava a ingestão até o médico confirmar fonte e faixas.</what-built>
  <how-to-verify>
    1. Confirmar a fonte canônica por indicador: WHO Child Growth Standards 0–5a (who.int/tools/child-growth-standards/standards) e WHO Growth Reference 5–19a (who.int/tools/growth-reference-data-for-5to19-years), ou a redistribuição LMS do CDC (cdc.gov/growthcharts/who-data-files.htm).
    2. Confirmar com o médico as faixas etárias cobertas por curva (D-03/A5): peso/idade ~0–10a, estatura/idade e IMC/idade 0–19a, PC/idade ~0–5a.
    3. Confirmar a granularidade mensal das LMS (A4) e a rotulagem PT-BR da classificação (A3: eutrófico/sobrepeso/obesidade/magreza; z-cutoffs -2/+1/+2).
    4. Spot-check obrigatório antes de confiar nos dados: para uma idade+sexo conhecidos, a linha P50 (z=0) DEVE igualar o valor `M` da WHO, e P3/P97 devem bater com a tabela de percentis publicada da WHO dentro do arredondamento (será testado em `lib/growth-reference/index.spec.ts` / `lib/lms-zscore.spec.ts`).
    5. Intergrowth-21st fica para uma slice posterior (Open Question 1 / A2) — este MVP entrega WHO com pontos de idade corrigida; NÃO bloquear neste checkpoint por falta de Intergrowth.
  </how-to-verify>
  <resume-signal>Digite "confirmado" com a fonte/faixas aprovadas para ingerir as LMS WHO, ou aponte correções.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Math LMS pura + tabelas de referência WHO (JSON versionado) + index</name>
  <files>lib/lms-zscore.ts, lib/lms-zscore.spec.ts, lib/growth-reference/index.ts, lib/growth-reference/index.spec.ts, lib/growth-reference/who/weight-for-age-boys.json, lib/growth-reference/who/weight-for-age-girls.json, lib/growth-reference/who/height-for-age-boys.json, lib/growth-reference/who/height-for-age-girls.json, lib/growth-reference/who/bmi-for-age-boys.json, lib/growth-reference/who/bmi-for-age-girls.json, lib/growth-reference/who/head-circumference-for-age-boys.json, lib/growth-reference/who/head-circumference-for-age-girls.json</files>
  <read_first>
    - .planning/phases/03-curva-de-crescimento/03-RESEARCH.md §"Data format — LMS parameters", "Deriving percentile ↔ z-score from LMS", "Storage strategy", "Code Examples" (linhas 341-350)
    - .planning/phases/03-curva-de-crescimento/03-PATTERNS.md §"lib/lms-zscore.ts", "lib/growth-reference/index.ts"
    - lib/patient-bmi-ui-status.ts (analog parcial: shape de lookup puro + presentation)
    - lib/compute-pediatric-age.spec.ts (analog: convenção de .spec.ts com tsx --test)
  </read_first>
  <behavior>
    - `lmsValueAtZ(0, {L,M,S})` retorna exatamente `M` (P50 == mediana) — spot-check contra valor WHO conhecido
    - `lmsZScore(M, {L,M,S})` retorna `0`
    - `lmsZScore` usa o ramo log quando `L === 0`
    - `getReferenceTable("who","weight-for-age","masculino")` retorna rows não-vazio + `source`/`version`/`ageMin`/`ageMax` presentes
    - cada um dos 8 arquivos JSON tem `source`, `version`, `ageMin`, `ageMax`, `ageUnit` e rows com {ageMonths, L, M, S}
    - P3 (z≈-1.881) e P97 (z≈+1.881) de uma linha conhecida batem com a tabela de percentis WHO publicada dentro do arredondamento (Pitfall 3)
  </behavior>
  <action>
    **lib/lms-zscore.ts**: duas funções puras exportadas com JSDoc — `lmsZScore(x, { L, M, S })` = `L !== 0 ? (Math.pow(x/M, L) - 1)/(L*S) : Math.log(x/M)/S`; `lmsValueAtZ(z, { L, M, S })` = `L !== 0 ? M*Math.pow(1 + L*S*z, 1/L) : M*Math.exp(S*z)`. Tipo `Lms = { L: number; M: number; S: number }`. Adicionar um helper `percentileFromZ(z)` (CDF normal padrão) e o mapa de z para os percentis do D-12 (P3≈-1.881, P15≈-1.036, P50=0, P85≈+1.036, P97≈+1.881).
    **JSON WHO** (8 arquivos): ingerir da fonte confirmada no checkpoint. Cada arquivo: `{ source, standard: "WHO", sex, indicator, ageUnit: "months", ageMin, ageMax, version, rows: [{ ageMonths, L, M, S }] }`. Respeitar o teto por indicador (D-03/A5): peso/idade ~0–120m, estatura/idade e IMC/idade 0–228m, PC/idade ~0–60m. Granularidade mensal (A4).
    **lib/growth-reference/index.ts**: importar os 8 JSON estaticamente; expor `getReferenceTable(standard, indicator, sex)` retornando `{ rows, source, version, ageMin, ageMax, ageUnit }`; tipo `GrowthReferenceTable`; JSDoc. Uma responsabilidade por arquivo.
    **specs**: `lms-zscore.spec.ts` com o spot-check P50==M e P3/P97 vs WHO (Pitfall 3 / Wave 0); `growth-reference/index.spec.ts` asseverando metadados presentes por arquivo (Wave 0).
  </action>
  <verify>
    <automated>npx tsx --test lib/lms-zscore.spec.ts lib/growth-reference/index.spec.ts && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lmsValueAtZ(0, lms)` === `lms.M` (spec verde)
    - `lib/lms-zscore.spec.ts` e `lib/growth-reference/index.spec.ts` verdes via tsx --test
    - `ls lib/growth-reference/who/*.json | wc -l` === 8
    - `grep -L '"source"' lib/growth-reference/who/*.json` retorna vazio (todo arquivo tem source)
    - `getReferenceTable` retorna `ageMin`/`ageMax`/`source`/`version` não-nulos
    - `yarn typecheck` passa
  </acceptance_criteria>
  <done>Math LMS pura testada (P50==M, P3/P97 vs WHO); 8 JSON WHO com metadados; index com getReferenceTable; specs verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classificação por z-cutoff WHO (mapa de cor reusado)</name>
  <files>lib/growth-classification.ts, lib/growth-classification.spec.ts</files>
  <read_first>
    - lib/patient-bmi-ui-status.ts (analog: PatientBmiUiStatus = good|warn|bad; mapa de cor — reusar SÓ a cor)
    - .planning/phases/03-curva-de-crescimento/03-RESEARCH.md §"Positioning + Classification (D-13)" (linha 198: usar z-cutoff WHO, não as bandas hard-coded)
    - .planning/phases/03-curva-de-crescimento/03-UI-SPEC.md §"C7", "Color" (mapa good→chart-2, warn→chart-4, bad→destructive)
  </read_first>
  <behavior>
    - z < -2 → status `bad` (magreza / abaixo)
    - -2 ≤ z ≤ +1 → status `good` (eutrofia)
    - +1 < z ≤ +2 → status `warn` (sobrepeso)
    - z > +2 → status `bad` (obesidade)
    - retorna label PT-BR por indicador (ex: `IMC: eutrófico`, `Peso no P75`) e o `status` good/warn/bad
  </behavior>
  <action>
    Criar `lib/growth-classification.ts`: função pura que recebe o z-score (e o indicador) e retorna `{ label: string, status: "good" | "warn" | "bad" }` aplicando os z-cutoffs WHO -2/+1/+2 (RESEARCH linha 198). Reusar o TIPO `PatientBmiUiStatus` e o mapa de cor de `lib/patient-bmi-ui-status.ts` (NÃO reusar `classifyBmiByAge` — as bandas hard-coded não são fonte de verdade da curva). Labels PT-BR conforme UI-SPEC C7 / Copywriting (magreza/eutrófico/sobrepeso/obesidade); wording exato a confirmar com médico (A3 — usar as strings do UI-SPEC como default). Adicionar helper para a label `Peso no P{n}` a partir do percentil. JSDoc. Spec cobrindo os 4 cortes de z.
  </action>
  <verify>
    <automated>npx tsx --test lib/growth-classification.spec.ts && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/growth-classification.spec.ts` verde: z=-3→bad, z=0→good, z=+1.5→warn, z=+2.5→bad
    - reusa `PatientBmiUiStatus` de `lib/patient-bmi-ui-status.ts`
    - `grep -q "classifyBmiByAge" lib/growth-classification.ts` retorna vazio (NÃO reusa a heurística)
    - labels em PT-BR
  </acceptance_criteria>
  <done>Classificação por z-cutoff WHO com cor good/warn/bad reusada; sem as bandas heurísticas; spec verde.</done>
</task>

<task type="auto">
  <name>Task 3: Componente de gráfico Recharts + readout de posição + wiring na growth-section</name>
  <files>components/dashboard/patients/growth/growth-chart.tsx, components/dashboard/patients/growth/growth-position-readout.tsx, components/dashboard/patients/growth/growth-section.tsx</files>
  <read_first>
    - .planning/phases/03-curva-de-crescimento/03-RESEARCH.md §"Pattern 1: Recharts reference bands + patient scatter" (linhas 259-277), "Two points without clutter (D-04)", "Anti-Patterns to Avoid"
    - .planning/phases/03-curva-de-crescimento/03-UI-SPEC.md §"C4", "C5", "C6", "C7", "C8", "Color", "Copywriting Contract"
    - .planning/phases/03-curva-de-crescimento/03-PATTERNS.md §"growth-chart.tsx", "growth-section.tsx"
    - components/dashboard/patients/growth/growth-section.tsx (arquivo criado em 03-01 — a modificar para montar os Tabs + chart + readout)
    - components/dashboard/patients/patient-clinical-overview.tsx (analog: Card + MetricTile visual para o readout)
    - lib/compute-pediatric-age.ts (posicionar por idade; chamar com { correctedAgeCutoffMonths: 36 } e measured_on como `now`)
  </read_first>
  <action>
    **growth-chart.tsx** (`"use client"` — obrigatório, é interativo): Recharts `ComposedChart` dentro de `ResponsiveContainer width="100%" height={360}`. x-axis numérico = idade em meses, `domain={[ageMin, ageMax]}` do arquivo de referência (respeitar teto — D-03). Bandas de referência: para cada centil/z ativo, gerar a linha avaliando `lmsValueAtZ(z, row)` por row da tabela → `<Line dot={false}>`; P50 (z=0) enfatizado `strokeWidth={2}` na cor accent (`--chart-1`/`--primary`), demais bandas `strokeWidth={1}` na ponta muted do ramp azul (NÃO um hue por centil — UI-SPEC C5/Color). Pontos do paciente: `<Scatter>` calculando a idade por medição via `computePediatricAge(patient.birth_date, new Date(measured_on), patient.gestational_age_weeks, { correctedAgeCutoffMonths: 36 })` — usar `totalDays`/meses como x. Prematuro (<37 sem): plotar par cronológico+corrigido conectado (conector faint + 2 marcadores, o do basis ativo enfatizado); toggle `Idade cronológica ↔ Idade corrigida` (C6), auto-corrigida por default quando `<37`; toggle escondido para não-prematuro. Toggle `Percentil ↔ Escore-z` (C5) re-avalia as bandas do MESMO dataset (default Percentil). Empty state quando `patient.sex` é null (Pitfall 5 / Copywriting). Caption de proveniência `Fonte: OMS (WHO) · faixa {ageMin}–{ageMax}` adjacente ao gráfico (D-03 obrigatório). Prop `indicator` torna o componente indicator-agnóstico.
    **growth-position-readout.tsx**: para a medição selecionada/mais recente, tile no visual do MetricTile mostrando percentil + escore-z (`text-3xl tabular-nums`) + label de classificação de `lib/growth-classification.ts`; cor do status via mapa good/warn/bad → `border-chart-2/50 bg-chart-2/10` etc. (UI-SPEC C7). Fora de faixa: `> P97`/`< P3` com o z exato (Open Q2).
    **growth-section.tsx** (modificar): adicionar shadcn `Tabs` com os 4 indicadores (peso/idade, estatura/idade, IMC/idade, PC/idade — C4); um `growth-chart` reusado por tab; renderizar o `growth-position-readout`. Não tilar 4 gráficos densos simultâneos. IMC/PC tabs tratam "sem dado para este indicador" com o copy de chart-empty (C4/C8). Estados loading (Skeleton)/empty/error/populated (C8). Manter o form + histórico da 03-01.
  </action>
  <verify>
    <automated>yarn typecheck && yarn build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `yarn typecheck` e `yarn build` verdes
    - `growth-chart.tsx` inicia com `"use client"` e importa de `recharts`
    - `grep -q "correctedAgeCutoffMonths: 36" components/dashboard/patients/growth/growth-chart.tsx` retorna match (posiciona a 36m)
    - `growth-chart.tsx` renderiza a caption de proveniência com `ageMin`/`ageMax` (D-03) e trata `patient.sex === null` com empty state
    - `growth-section.tsx` usa `Tabs` com 4 indicadores e renderiza `growth-chart` + `growth-position-readout`
    - `grep -q "strokeWidth={2}" components/dashboard/patients/growth/growth-chart.tsx` (P50 enfatizado)
  </acceptance_criteria>
  <done>4 curvas OMS por tab com medições plotadas, toggles percentil/z e cronológica/corrigida, readout de posição classificado, empty state sem sexo, caption de fonte/faixa; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | novo pacote `recharts` entra na árvore de dependências |
| static asset → curva clínica | dados WHO LMS alimentam uma curva usada em decisão clínica (acurácia) |
| client render | o gráfico é client-side; consome medições já escopadas server-side (03-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | install de `recharts` | high | mitigate | slopcheck + checkpoint blocking-human antes do install; pin exato `3.9.0`; verificar ausência de postinstall e peer react ^19 (Task checkpoint) |
| T-03-06 | Tampering (data integrity) | dados WHO LMS incorretos → curva clinicamente enganosa | high | mitigate | checkpoint human-verify de fonte/faixas (blocker STATE.md); spot-check P50==M e P3/P97 vs WHO em spec; exibir source/version/faixa na UI (D-03) |
| T-03-07 | Information disclosure | medição/curva de dado sensível de menor renderizada no cliente | medium | accept | dados já escopados por profile_id+patient_id na leitura server-side (03-01); nenhum novo canal de saída; sem exposição pública |
| T-03-08 | Tampering | valor fora de faixa extrapolando past-ceiling da curva | low | mitigate | clamp da banda a z±3 e respeitar ageMax por arquivo (D-03/A5/Open Q2) |
</threat_model>

<verification>
- `yarn test` verde (lms-zscore, growth-reference, growth-classification)
- `yarn typecheck` e `yarn build` verdes
- 8 JSON WHO com metadados; P50==M spot-check verde
- No perfil: 4 tabs de curva com medições plotadas; toggles percentil/z e cronológica/corrigida funcionam; caption de fonte/faixa visível; empty state quando sexo null
- recharts@3.9.0 pin exato instalado limpo sob React 19
</verification>

<success_criteria>
- Pediatra vê 4 gráficos por idade com medições sobre a referência OMS, fonte + faixa visíveis (GROWTH-02 / D-02/D-03)
- Toggle percentil↔z do mesmo dataset LMS (D-12); prematuros com pontos cronológico+corrigido e toggle (D-04/D-06)
- Posição (percentil/z + classificação) por medição reusando o mapa de cor do BMI card (D-13)
- Medições posicionadas pela idade pediátrica estendida a 36m (GROWTH-03)
</success_criteria>

<output>
Create `.planning/phases/03-curva-de-crescimento/03-02-SUMMARY.md` when done
</output>
