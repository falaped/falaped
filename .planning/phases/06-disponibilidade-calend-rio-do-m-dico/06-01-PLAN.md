---
phase: 06-disponibilidade-calend-rio-do-m-dico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260722000000_availability_overrides_hybrid.sql
  - lib/expand-availability.ts
  - lib/expand-availability.spec.ts
  - lib/schemas/availability.ts
autonomous: false
requirements: [AGENDA-01, AGENDA-02, AGENDA-03, AGENDA-04, AGENDA-05]
user_setup: []

must_haves:
  truths:
    - "expandAvailability aplica precedência híbrida (template → aditivos → subtrativos; folga vence) e preserva minutos locais em dia de transição DST (D-21, WR-01)"
    - "As folgas subtrativas v1 já cadastradas continuam no banco, agora com override_type='subtract' (D-22)"
    - "O schema de override rejeita data ISO ambígua (WR-02) e minutos > 1440 (WR-03), e exige slot_minutes>0 para aditivos"
  artifacts:
    - "supabase/migrations/20260722000000_availability_overrides_hybrid.sql (ALTER+backfill, aplicado à DB viva)"
    - "lib/expand-availability.ts (fn pura v2 híbrida + DST-safe)"
    - "lib/expand-availability.spec.ts (13 casos v1 migrados + 7 casos novos)"
    - "lib/schemas/availability.ts (override schema com WR-02/WR-03 corrigidos)"
  key_links:
    - "expandAvailability recebe overrides[]{type,slotMinutes} — contrato consumido pelo RSC (Plano 03) e pelo client"
    - "override_type/slot_minutes na tabela availability_exceptions — lidos pelos módulos (Plano 02)"
---

<objective>
Estabelecer a fundação de dados e lógica pura do modelo HÍBRIDO de disponibilidade: evoluir a tabela `availability_exceptions` para overrides aditivos/subtrativos preservando as rows v1 (D-20/D-22), reescrever `expandAvailability` para precedência híbrida DST-safe (D-21, corrige WR-01), e endurecer o schema Zod (corrige WR-02/WR-03). Termina com a migração APLICADA à DB viva via checkpoint bloqueante.

Purpose: Todo o resto da fase (módulos, actions, UI) depende deste contrato de dados + da fn pura. A correção dos 4 pitfalls WR-01..04 vive aqui (a validação/expansão).
Output: Migração aplicada, fn pura híbrida testada, schema robusto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-CONTEXT.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-RESEARCH.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migração ALTER+backfill híbrida (D-20/D-22, WR-03)</name>
  <files>supabase/migrations/20260722000000_availability_overrides_hybrid.sql</files>
  <read_first>
    - supabase/migrations/20260721000200_availability_exceptions.sql (a tabela a ALTERAR — NÃO recriar; RLS+4 policies vivem nela, linhas 51-88)
    - supabase/migrations/20260721000100_availability_rules.sql (estilo de CHECK nomeado, linhas 26-31; aplicar teto 1440 aqui também)
    - supabase/migrations/20260720000500_patient_vaccine_doses.sql (molde owner-scoped — REFERÊNCIA apenas; NÃO reemitir policies)
  </read_first>
  <action>
    Criar a migração `20260722000000_availability_overrides_hybrid.sql` com header multi-linha explicando o PORQUÊ (preservar rows v1 per D-22; evoluir para overrides híbridos; forward constraint — Phase 7 escreve appointments por cima, sem FK agora). Aplicar SOMENTE ALTER TABLE em `public.availability_exceptions` — NUNCA drop/recreate (recriar derruba RLS silenciosamente, Pitfall 2/5).
    Adicionar coluna `override_type text not null default 'subtract'` com `check (override_type in ('add','subtract'))` (per D-20: existentes = subtrativas). Seguir com `update public.availability_exceptions set override_type = 'subtract' where override_type is null` (backfill auditável, per D-22).
    Adicionar `constraint availability_exceptions_minute_ceiling check (start_minute is null or (start_minute <= 1440 and end_minute <= 1440))` (teto WR-03; 1440 = fim do dia, coerente com WR-04).
    Adicionar coluna `slot_minutes smallint` (nullable — usada só em aditivos com faixa, AGENDA-05/D-09) e `constraint availability_exceptions_add_needs_range_and_slot check (override_type = 'subtract' or (start_minute is not null and end_minute is not null and slot_minutes is not null and slot_minutes > 0))` (aditivo exige faixa + duração; aditivo dia-inteiro não faz sentido).
    Aplicar também o teto WR-03 em `public.availability_rules` via `alter table public.availability_rules add constraint availability_rules_minute_ceiling check (start_minute <= 1440 and end_minute <= 1440)`.
    Atualizar/adicionar `comment on column` para `override_type` e `slot_minutes` (PT-BR, estilo dos comments existentes). NÃO reemitir enable RLS nem as 4 policies — a tabela já as carrega e o ALTER as preserva.
    NÃO adicionar rename de tabela/coluna (mantém `availability_exceptions` e `exception_date` — decisão do planner per RESEARCH: custo de refactor > benefício cosmético).
  </action>
  <verify>
    <automated>grep -qi 'alter table' supabase/migrations/20260722000000_availability_overrides_hybrid.sql && ! grep -iqE 'drop +table|create +table +public\.availability_exceptions' supabase/migrations/20260722000000_availability_overrides_hybrid.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe e contém `add column override_type`, `default 'subtract'`, um `update ... set override_type = 'subtract'` de backfill, o CHECK de teto 1440 e o CHECK aditivo-precisa-faixa-e-slot.
    - `grep -iE 'drop +table|create +table +public\.availability_exceptions'` NÃO encontra nada (ALTER-only, D-22).
    - Nenhuma reemissão de `enable row level security` nem `create policy` para `availability_exceptions` (grep -c retorna 0 para ambos filtrando comentários).
  </acceptance_criteria>
  <done>Migração ALTER+backfill escrita, preservando a tabela, RLS e rows; teto de minutos aplicado às duas tabelas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever expandAvailability híbrida + DST-safe (D-21, WR-01) e .spec</name>
  <files>lib/expand-availability.ts, lib/expand-availability.spec.ts</files>
  <read_first>
    - lib/expand-availability.ts (v1 — reescrever in-place; manter scaffold, tipos FreeSlot/ExpandResult, iteração zonada linhas 81-98)
    - lib/expand-availability.spec.ts (v1 — 13 casos a migrar exceptions→overrides + 7 novos)
    - lib/compute-pediatric-age.ts (molde de fn pura: sem new Date() interno, sem process.env.TZ)
    - lib/clinic-timezone.ts (CLINIC_TIME_ZONE)
  </read_first>
  <behavior>
    - DST-safe: faixa 08:00–12:00 em America/New_York num dia de spring-forward (2026-03-08) preserva os minutos LOCAIS dos slots (08:00, 08:30, ...) sob TZ=UTC e TZ=America/New_York (corrige WR-01).
    - Aditivo básico (AGENDA-05): dia sem template + override type="add" 19:00–20:00 @30 → 2 slots só do aditivo.
    - Aditivo sobre template com sobreposição → sem slots duplicados (dedupe por start.getTime()).
    - Aditivo + subtrativo na mesma faixa → 0 slots (folga vence, D-21).
    - Subtrativo dia-inteiro (startMinute===null) remove template E aditivos daquele dia → 0 slots.
    - Aditivo com slotMinutes próprio (@20) diferente do template (@30) no mesmo dia → contagens distintas.
    - Precedência combinada: template + aditivo + subtrativo parcial num só dia → conjunto final correto.
    - Todos os 13 casos v1 (D-02, D-09, D-10 ×2, D-04 ×3, D-11 ×3, janela vazia, weekday vazio, D-07 mês) continuam verdes, migrados de `exceptions` para `overrides` com `type`.
  </behavior>
  <action>
    Reescrever `expandAvailability` para o parâmetro `overrides: AvailabilityOverride[]` (substitui `exceptions`). Renomear/estender o tipo exportado: `AvailabilityException` → `AvailabilityOverride` com `type: "add" | "subtract"` e `slotMinutes: number | null` (per RESEARCH assinatura). Manter `AvailabilityBand`, `FreeSlot`, `ExpandResult` e a assinatura de `input` (`rules`, `overrides`, `window{from,to}`, `timeZone`). Manter contrato puro/determinístico (molde computePediatricAge): nunca `new Date()` interno, nunca `process.env.TZ`.
    REMOVER o anti-padrão WR-01: `addMinutes(dayStart, minute, context)` para limites de slot (v1 linhas 120-131) — deriva 1h em transição DST. Substituir por construção wall-clock no zone: uma helper `wallClock(day, minuteOfDay, timeZone)` que deriva h/m do minute-of-day e faz `setHours`/`setMinutes`/`setSeconds`/`setMilliseconds` no context `{ in: tz(timeZone) }`. O AVANÇO de slot também é wall-clock (`wallClock(day, currentMinute + slotMinutes)`), nunca `addMinutes` sobre o instante. Manter a iteração zonada de dias (v1 linhas 81-98: `eachDayOfInterval`, `startOfDay(day, context)`, `new TZDate(day, timeZone).getDay()`).
    Implementar precedência híbrida D-21 no corpo: para cada dia da janela, (1) coletar slots do template recorrente do weekday; (2) UNIR os slots dos overrides aditivos daquela data (dedupe por `start.getTime()` — Pitfall 3, aditivo com seu próprio `slotMinutes`); (3) por último SUBTRAIR os overrides subtrativos daquela data (folga vence). Subtrativo dia-inteiro (`startMinute===null`) remove template E aditivos; subtrativo parcial reusa a checagem de sobreposição meio-aberta v1 (`slotStart < exEnd && slotEnd > exStart`). Manter emit só-slot-inteiro (D-10) e recorte de janela meio-aberto `slotStart >= from && slotStart < to` (`<`, nunca `<=`, D-11). Manter `byDay{freeSlotCount,hasAvailability}` (D-07/mês).
    Atualizar o JSDoc (agora híbrido + precedência D-21 + nota DST-safe WR-01). Migrar TODOS os 13 casos existentes no `.spec.ts` de `exceptions` para `overrides` (subtrativos ganham `type:"subtract"`) e ADICIONAR os 7 casos do bloco <behavior>. O caso DST DEVE rodar/afirmar sob America/New_York num dia de spring-forward.
  </action>
  <verify>
    <automated>yarn test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test` verde (todos os `*.spec.ts` em modules/ lib/ passam), incluindo os 7 casos novos + 13 migrados.
    - `grep -n 'addMinutes(dayStart' lib/expand-availability.ts` retorna vazio (WR-01 removido).
    - `grep -c 'type: "add"\|"add"' lib/expand-availability.spec.ts` > 0 (casos aditivos presentes).
    - `grep -n 'America/New_York' lib/expand-availability.spec.ts` retorna ≥1 linha (caso DST presente).
    - A fn não referencia `new Date()` sem argumento nem `process.env` (grep confirma pureza).
  </acceptance_criteria>
  <done>expandAvailability híbrida DST-safe verde; suite cobre precedência aditivo/subtrativo, dedupe e DST spring-forward.</done>
</task>

<task type="auto">
  <name>Task 3: Endurecer lib/schemas/availability.ts (override schema, WR-02/WR-03)</name>
  <files>lib/schemas/availability.ts</files>
  <read_first>
    - lib/schemas/availability.ts (v1 — editar in-place; manter availabilityRuleSchema linhas 13-37 e saveAvailabilityRulesSchema linhas 40-42)
    - lib/compute-pediatric-age.ts (linhas 84, 91-104: ISO_DATE_ONLY regex + rebuild-and-compare que rejeita 2026-02-30)
  </read_first>
  <action>
    Evoluir `createAvailabilityExceptionSchema` para o schema de override (renomear/adicionar `createAvailabilityOverrideSchema`; manter o export antigo como alias se algum call-site v1 ainda referenciar, ou atualizar depois no Plano 02).
    WR-02: substituir a validação frouxa `Date.parse` (v1 linhas 48-52) do campo de data por regex ISO estrita `^\d{4}-\d{2}-\d{2}$` + reconstrução da data e comparação de ano/mês/dia para rejeitar rollovers (2026-02-30), espelhando `localMidnightFromIso` de compute-pediatric-age.ts. Mensagem PT-BR ("Data inválida (use AAAA-MM-DD).").
    WR-03: adicionar `.max(1440, "O horário não pode passar de 24:00 (1440 min).")` a `start_minute` e `end_minute` (espelha o CHECK do DB). Manter `multipleOf30`, o refine "ambos-ou-nenhum" e "fim > início".
    Adicionar `override_type: z.enum(["add","subtract"])` e `slot_minutes` condicional: obrigatório e `> 0` quando `override_type === "add"` (via refine com path ["slot_minutes"]), nullable/ignorado quando `"subtract"`. Adicionar também um refine "aditivo exige faixa" (start/end não-null quando type="add"). Mensagens PT-BR inline.
    Adicionar/atualizar os `type` inferidos (`CreateAvailabilityOverrideInput`) e um `saveAvailabilitySchema` do batch: `{ rules: array(availabilityRuleSchema), overridesAdd: array(override), overridesRemove: array({ id: z.string().uuid() }) }` — consumido pela action do Plano 02.
    Aplicar o teto WR-03 (`.max(1440)`) também em `availabilityRuleSchema.end_minute`/`start_minute`.
  </action>
  <verify>
    <automated>yarn typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `yarn typecheck` sem erros novos no arquivo.
    - `grep -n 'Date.parse' lib/schemas/availability.ts` retorna vazio (WR-02 removido).
    - `grep -c '1440' lib/schemas/availability.ts` ≥ 2 (teto em rule + override).
    - `grep -n 'z.enum(\["add", "subtract"\]\|"add"' lib/schemas/availability.ts` encontra o enum override_type.
    - `grep -n 'saveAvailabilitySchema\|overridesAdd\|overridesRemove' lib/schemas/availability.ts` encontra o schema do batch.
  </acceptance_criteria>
  <done>Schema de override robusto (WR-02/WR-03 corrigidos), enum de tipo, slot condicional e schema de batch save exportado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: [BLOCKING] Aplicar a migração híbrida à DB viva Supabase</name>
  <what-built>
    A migração `20260722000000_availability_overrides_hybrid.sql` (Task 1) que faz ALTER+backfill em `availability_exceptions` preservando as rows v1. Build/typecheck passam SEM a migração aplicada (tipos vêm da config, não do DB vivo) — logo a verificação de fase daria falso-positivo se a migração não for empurrada agora, ANTES da verificação de fase e dos Planos 02/03.
  </what-built>
  <how-to-verify>
    1. ANTES de aplicar: registrar a contagem atual de folgas — `select count(*) as antes from public.availability_exceptions;` (via `mcp__supabase execute_sql` ou dashboard). Confirmar o estado da tabela via `mcp__supabase list_tables`.
    2. Aplicar a migração: `supabase db push` (requer `SUPABASE_ACCESS_TOKEN`) OU `mcp__supabase apply_migration` com o conteúdo do arquivo `20260722000000_availability_overrides_hybrid.sql`.
    3. Verificar colunas: a tabela `availability_exceptions` agora tem `override_type` (text, default 'subtract') e `slot_minutes` (smallint nullable) — confirmar via `mcp__supabase list_tables`.
    4. Verificar preservação + backfill: `select count(*) as depois, count(*) filter (where override_type='subtract') as subtrativas from public.availability_exceptions;` — `depois` == `antes` (nenhuma row perdida, D-22) e `subtrativas` == `antes` (todas as folgas v1 mapeadas para 'subtract').
    5. Verificar RLS intacta: a tabela ainda tem `rowsecurity=true` e as 4 policies (select/insert/update/delete own) — `select policyname from pg_policies where tablename='availability_exceptions';` retorna 4.
    6. Verificar CHECK de teto em `availability_rules` também aplicado (colunas via list_tables / constraint via pg_constraint).
    7. Rodar `mcp__supabase get_advisors` (type: security) — nenhum aviso NOVO de segurança para `availability_exceptions`/`availability_rules` (RLS enabled, no policy = negação silenciosa NÃO deve aparecer).
  </how-to-verify>
  <resume-signal>Responda "aprovado" (colunas criadas, rows preservadas, subtrativas==antes, RLS+4 policies intactas, get_advisors limpo) ou descreva a divergência.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB vivo | ALTER+backfill roda com privilégio de migration (fora do RLS); risco = perda de rows / RLS derrubada por recreate |
| client → action (Zod) | entrada de data/faixa/override atravessa aqui (validação no Plano 03/02, schema definido aqui) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Information Disclosure / Denial | migração `availability_overrides_hybrid.sql` | high | mitigate | ALTER-only (nunca drop/recreate) — o ALTER preserva RLS + 4 policies + rows; checkpoint bloqueante confirma `get_advisors` limpo e contagem de rows preservada |
| T-06-03a | Tampering | `lib/schemas/availability.ts` (data ISO) | medium | mitigate | WR-02: regex ISO estrita + rebuild-and-compare (rejeita 2026-02-30/07/21/2026) antes do módulo |
| T-06-03b | Tampering | `lib/schemas/availability.ts` + CHECK do DB (teto de minutos) | medium | mitigate | WR-03: `.max(1440)` no Zod + `CHECK (... <= 1440)` no DB nas duas tabelas — impede slot vazando para o dia seguinte |
| T-06-01 | Elevation of Privilege | tabela `availability_exceptions` (herdado v1) | high | mitigate | RLS owner-scoped por `profile_id` (preservada no ALTER); defesa-em-profundidade `.eq(profile_id)` nos módulos (Plano 02) |
| T-06-SC | Tampering | npm/yarn installs | low | accept | Nenhum pacote NOVO nesta fase (`@date-fns/tz`/`date-fns` já auditados OK em RESEARCH §Package Legitimacy) |
</threat_model>

<verification>
- `yarn test` verde (fn pura híbrida + DST + precedência).
- `yarn typecheck` sem erros novos.
- Migração ALTER-only (grep confirma ausência de drop/recreate).
- Migração APLICADA à DB viva com rows preservadas + backfill + RLS + 4 policies intactas + get_advisors limpo (checkpoint Task 4).
</verification>

<success_criteria>
- SC-1/SC-2/SC-3/SC-4/SC-5 (fundação): template recorrente + overrides híbridos persistidos, expansão pura correta nas viradas/fuso/DST, folga vence disponibilidade, aditivo abre horário extra — provados pela suite `.spec` e pela migração aplicada.
</success_criteria>

<artifacts_this_phase_produces>
Novos/modificados por este plano:
- `supabase/migrations/20260722000000_availability_overrides_hybrid.sql` (NOVO) — ALTER+backfill híbrido
- `lib/expand-availability.ts` (REESCRITO) — tipos `AvailabilityOverride` (com `type`, `slotMinutes`), `expandAvailability({rules, overrides, window, timeZone})`, helper `wallClock`
- `lib/expand-availability.spec.ts` (REESCRITO) — 20 casos (13 migrados + 7 híbridos/DST)
- `lib/schemas/availability.ts` (MODIFICADO) — `createAvailabilityOverrideSchema`, `saveAvailabilitySchema`, `override_type` enum, teto 1440, ISO estrita
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-01-SUMMARY.md` when done
</output>
