---
phase: 06-disponibilidade-calend-rio-do-m-dico
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - modules/availability/types.ts
  - modules/availability/list-availability-overrides.ts
  - modules/availability/create-availability-override.ts
  - modules/availability/delete-availability-override.ts
  - actions/availability/save-availability.ts
  - actions/availability/index.ts
autonomous: true
requirements: [AGENDA-01, AGENDA-02, AGENDA-03, AGENDA-05]
user_setup: []

must_haves:
  truths:
    - "Os módulos de override leem/criam/deletam sempre escopados por profile_id (IDOR triple-defense, D-13)"
    - "saveAvailabilityAction recebe o diff {rules, overridesAdd, overridesRemove}, gate paid + Zod safeParse, e reconcilia num único fluxo (D-17)"
    - "O create de override aceita override_type + slot_minutes; o subtrativo dia-inteiro continua válido (D-20)"
  artifacts:
    - "modules/availability/list-availability-overrides.ts, create-availability-override.ts, delete-availability-override.ts"
    - "modules/availability/types.ts (AvailabilityExceptionRow estendida com override_type + slot_minutes)"
    - "actions/availability/save-availability.ts + barrel export"
  key_links:
    - "saveAvailabilityAction → upsertAvailabilityRules + create/delete override (reconcilia o diff)"
    - "listAvailabilityOverrides.select inclui override_type, slot_minutes — consumido pelo RSC (Plano 03)"
---

<objective>
Construir a camada de dados do modelo híbrido: os módulos CRUD de override owner-scoped (evoluindo os módulos de exceção v1) e a action de salvar-em-lote que reconcilia o diff da grade + overrides adicionados/removidos (D-17). Depende da migração aplicada (Plano 01) e dos schemas de batch.

Purpose: A UI editável (Plano 03) salva tudo por um único action; os módulos expõem override_type/slot_minutes para a expansão.
Output: 3 módulos de override, types estendidos, action de batch save + barrel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-CONTEXT.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-RESEARCH.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-PATTERNS.md
@.cursor/skills/supabase-falaped/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender types.ts + módulos CRUD de override (D-20, IDOR D-13)</name>
  <files>modules/availability/types.ts, modules/availability/list-availability-overrides.ts, modules/availability/create-availability-override.ts, modules/availability/delete-availability-override.ts</files>
  <read_first>
    - modules/availability/types.ts (v1 — estender AvailabilityExceptionRow, linhas 22-29)
    - modules/availability/list-availability-exceptions.ts (analog exato do list; adicionar colunas ao .select)
    - modules/availability/create-availability-exception.ts (analog exato do create; stamp profile_id server-side, linha 27)
    - modules/availability/delete-availability-exception.ts (analog exato do delete; double-scoped .eq profile_id + id, linhas 20-23)
  </read_first>
  <action>
    Em `types.ts`: estender `AvailabilityExceptionRow` com `override_type: "add" | "subtract"` e `slot_minutes: number | null`. Adicionar o alias `export type AvailabilityOverrideRow = AvailabilityExceptionRow`. Manter `AvailabilityRuleRow` e o header owner-scope.
    Criar `list-availability-overrides.ts` (evolui `list-availability-exceptions.ts`): uma fn `listAvailabilityOverrides(supabase, profileId)`, `SupabaseClient` como primeiro arg injetado, `.eq("profile_id", profileId)` (backstop IDOR, nunca omitir), `.order("exception_date")`, `[AVAILABILITY]` error tag. ADICIONAR `override_type, slot_minutes` à string do `.select(...)` (senão a expansão híbrida recebe undefined). Retornar `AvailabilityOverrideRow[]`.
    Criar `create-availability-override.ts` (evolui `create-availability-exception.ts`): fn `createAvailabilityOverride(supabase, profileId, input)` com input type incluindo `override_type` e `slot_minutes`. STAMPAR `profile_id` server-side no insert (nunca confiar no cliente — IDOR D-13); incluir `override_type` e `slot_minutes` no payload; `.select(...).single()` com as novas colunas; `[AVAILABILITY]` tag.
    Criar `delete-availability-override.ts` (evolui `delete-availability-exception.ts` VERBATIM): `deleteAvailabilityOverride(supabase, profileId, id)` com o delete DOUBLE-scoped `.eq("profile_id", profileId).eq("id", id)` (nunca por id sozinho; idempotente no-op ao mirar id alheio), `[AVAILABILITY]` tag. Só o nome da fn muda; a tabela permanece `availability_exceptions`.
    NÃO remover os módulos v1 de exceção ainda se algo os importar — o Plano 03 troca os call-sites; se após o Plano 03 ficarem órfãos, a limpeza é do executor. Manter uma fn por arquivo, JSDoc PT-BR nos exports.
  </action>
  <verify>
    <automated>yarn typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `yarn typecheck` sem erros novos.
    - `grep -c 'override_type' modules/availability/list-availability-overrides.ts` ≥ 1 (coluna no select).
    - `grep -n 'profile_id: profileId' modules/availability/create-availability-override.ts` encontra o stamp server-side.
    - `grep -c '.eq("profile_id", profileId)' modules/availability/delete-availability-override.ts` ≥ 1 E `grep -c '.eq("id", id)' modules/availability/delete-availability-override.ts` ≥ 1 (double-scoped).
    - `grep -c 'override_type' modules/availability/types.ts` ≥ 1.
  </acceptance_criteria>
  <done>Módulos de override owner-scoped (list/create/delete) e types estendidos; IDOR triple-defense preservado.</done>
</task>

<task type="auto">
  <name>Task 2: Action de salvar-em-lote saveAvailabilityAction (D-17)</name>
  <files>actions/availability/save-availability.ts, actions/availability/index.ts</files>
  <read_first>
    - actions/availability/save-availability-rules.ts (skeleton gate+validate+result-union a copiar; strings de erro paid verbatim, linhas 31-55)
    - actions/availability/index.ts (barrel v1 — adicionar o novo export)
    - modules/availability/upsert-availability-rules.ts (delete-then-insert da grade — reusar as-is)
    - lib/schemas/availability.ts (saveAvailabilitySchema criado no Plano 01)
  </read_first>
  <action>
    Criar `save-availability.ts` com `"use server"` e `saveAvailabilityAction(input): Promise<{ ok: true } | { ok: false; error: string }>`. Copiar o skeleton do `save-availability-rules.ts`: `createClient()` → `getAuthenticatedUser(supabase)` → se `!profile` retornar "Sessão não encontrada." → se `profile.status !== "paid"` retornar a string de erro paid VERBATIM ("Perfil não ativo. Conclua a configuração da conta em Perfil.") → `saveAvailabilitySchema.safeParse(input)` → em erro `zodErrorToUserMessage(parsed.error)`.
    No try: reconciliar o diff (D-17) — `upsertAvailabilityRules(supabase, profile.id, parsed.data.rules)` (delete-then-insert da grade recorrente); depois `createAvailabilityOverride` para cada item de `parsed.data.overridesAdd` (mapeando `override_type`, `start_minute`, `end_minute`, `slot_minutes`); depois `deleteAvailabilityOverride(supabase, profile.id, id)` para cada `id` de `parsed.data.overridesRemove` (scoped profile_id + id). Ao fim `revalidatePath("/dashboard/agenda")` e `return { ok: true }`. No catch retornar `e instanceof Error ? e.message : "Não foi possível salvar a disponibilidade. Tente novamente."`.
    Exportar `saveAvailabilityAction` + o type de resultado do `actions/availability/index.ts` (padrão do barrel v1). Manter os exports v1 no barrel (o Plano 03 pode remover os órfãos depois).
  </action>
  <verify>
    <automated>yarn typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `yarn typecheck` sem erros novos.
    - `grep -c 'profile.status !== "paid"' actions/availability/save-availability.ts` == 1 (gate paid presente).
    - `grep -n 'safeParse' actions/availability/save-availability.ts` encontra a validação Zod no boundary.
    - `grep -n 'upsertAvailabilityRules\|createAvailabilityOverride\|deleteAvailabilityOverride' actions/availability/save-availability.ts` encontra os 3 passos de reconciliação.
    - `grep -c 'revalidatePath' actions/availability/save-availability.ts` == 1.
    - `grep -n 'saveAvailabilityAction' actions/availability/index.ts` encontra o export no barrel.
  </acceptance_criteria>
  <done>saveAvailabilityAction reconcilia grade + overrides add/remove com gate paid + Zod + result union + revalidate; exportado no barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → saveAvailabilityAction | diff da grade + overrides atravessa aqui; entrada não confiável |
| action → módulos → DB | módulos escopam por profile_id; RLS é a segunda camada |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege (IDOR) | módulos de override (list/create/delete) | high | mitigate | Triple-defense: RLS por profile_id (Plano 01) + `.eq("profile_id", profileId)` em read/create/delete + delete double-scoped (profile_id AND id) + profile_id stampado server-side no create (nunca do cliente) |
| T-06-05 | Elevation of Privilege | `saveAvailabilityAction` | high | mitigate | Gate `getAuthenticatedUser` + `profile.status === "paid"` no boundary do action (RLS `to authenticated` não impõe assinatura) |
| T-06-03 | Tampering | `saveAvailabilityAction` (Zod) | medium | mitigate | `saveAvailabilitySchema.safeParse` no boundary (WR-02/WR-03 já no schema, Plano 01); erros → PT-BR via zodErrorToUserMessage |
| T-06-SC | Tampering | npm/yarn installs | low | accept | Nenhum pacote NOVO nesta fase (RESEARCH §Package Legitimacy: `@date-fns/tz`/`date-fns` já OK) |
</threat_model>

<verification>
- `yarn typecheck` sem erros novos.
- IDOR triple-defense confirmado por grep (profile_id em read/create/delete; delete double-scoped).
- Gate paid presente no action; Zod safeParse no boundary; revalidatePath.
</verification>

<success_criteria>
- SC-1 (grade recorrente salva sem recriar rows por slot), SC-3 (folga por data + aditivo pontual persistidos via override), SC-5 (salvar em lote — um action recebe o diff). Prova: typecheck + grep de gate/scoping/reconciliação.
</success_criteria>

<artifacts_this_phase_produces>
Novos/modificados por este plano:
- `modules/availability/types.ts` (MODIFICADO) — `AvailabilityExceptionRow` + `override_type`, `slot_minutes`; alias `AvailabilityOverrideRow`
- `modules/availability/list-availability-overrides.ts` (NOVO) — `listAvailabilityOverrides`
- `modules/availability/create-availability-override.ts` (NOVO) — `createAvailabilityOverride`
- `modules/availability/delete-availability-override.ts` (NOVO) — `deleteAvailabilityOverride`
- `actions/availability/save-availability.ts` (NOVO) — `saveAvailabilityAction`, `SaveAvailabilityResult`
- `actions/availability/index.ts` (MODIFICADO) — export do batch save
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-02-SUMMARY.md` when done
</output>
