---
phase: 06-disponibilidade-calend-rio-do-m-dico
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - components/dashboard/agenda/calendar-editor.tsx
  - components/dashboard/agenda/calendar-day-week-grid.tsx
  - components/dashboard/agenda/calendar-month-indicator.tsx
  - components/dashboard/agenda/availability-action-panel.tsx
  - app/dashboard/agenda/page.tsx
autonomous: false
requirements: [AGENDA-01, AGENDA-02, AGENDA-03, AGENDA-04, AGENDA-05]
user_setup: []

must_haves:
  truths:
    - "Uma única superfície de calendário editável: abas Dia/Semana/Mês à esquerda + painel de ações à direita; navegação anterior/hoje/próximo entre meses (D-14)"
    - "Toggle Disponibilidade|Folga define a pintura; verde=disponível, folga=neutra (não destructive-red) (D-15)"
    - "Pintura por clique (slot), arraste (período contíguo) e dia-inteiro, passo 30 min; salvar em lote com estado não-salvo visível + confirmar antes de descartar (D-16/D-17)"
    - "Navegação re-expande slots no cliente (fn pura) sem slot duplicado/sumido nas viradas, fuso America/Sao_Paulo (D-11/D-14)"
    - "Mês = indicador (ponto + contagem), sem pintura no mês (D-18)"
  artifacts:
    - "components/dashboard/agenda/calendar-editor.tsx (+ subcomponentes de grid dia/semana, indicador de mês, painel de ações)"
    - "app/dashboard/agenda/page.tsx (RSC: carga rules+overrides, expand da semana, passa ao editor)"
  key_links:
    - "calendar-editor → saveAvailabilityAction (Plano 02) com o diff {rules, overridesAdd, overridesRemove}"
    - "calendar-editor + page.tsx → expandAvailability({rules, overrides,...}) (Plano 01)"
---

<objective>
Substituir a UI read-only v1 (agenda-view + availability-grid + exception-dialog) por um CALENDÁRIO ÚNICO EDITÁVEL (D-14..D-18): pintura por clique/arraste/dia-inteiro com toggle Disponibilidade|Folga, salvar em lote com guarda de descarte, navegação dia/semana/mês re-expandindo slots no cliente. O RSC passa a carregar rules+overrides e a expandir a semana default.

Purpose: Entrega a experiência v2 que o médico pediu no UAT — uma superfície só, pinta e salva em lote.
Output: calendar-editor + subcomponentes + page.tsx migrado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-CONTEXT.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-RESEARCH.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-PATTERNS.md
@.cursor/skills/pediatric-dashboard-design/SKILL.md
</context>

<notes>
CONTEXT.md v2 (D-14..D-18) é o contrato de UI AUTORITATIVO desta fase. `06-UI-SPEC.md` é STALE (v1, editor separado + views read-only) — NÃO seguir para layout/interação. Reusar do UI-SPEC v1 apenas os tokens de cor que ainda valem: verde = disponível; folga = tratamento neutro (bg-muted/hachura + badge "Folga"), NUNCA destructive-red. Um UI-SPEC v2 via `/gsd-ui-phase` é follow-up recomendado — este plano deriva a UI das decisões D-14..D-18 e deixa o polimento visual concreto ao executor.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: RSC page.tsx — carregar overrides, expandir a semana (D-14, migrar exceptions→overrides)</name>
  <files>app/dashboard/agenda/page.tsx</files>
  <read_first>
    - app/dashboard/agenda/page.tsx (v1 — editar in-place; manter gate + Promise.all + janela default de semana)
    - modules/availability/list-availability-overrides.ts (Plano 02 — novo list)
    - lib/expand-availability.ts (Plano 01 — assinatura com `overrides`)
    - lib/clinic-timezone.ts (CLINIC_TIME_ZONE)
  </read_first>
  <action>
    Editar `page.tsx` in-place. Manter o gate auth (`redirect("/auth/login")` se `!profile?.id`) e o PAID gate no RSC (`if (profile.status !== "paid") redirect("/dashboard/link-whatsapp")` — a RLS `to authenticated` não impõe assinatura, T-06-05). Manter o cabeçalho/Separator.
    Trocar `listAvailabilityExceptions` por `listAvailabilityOverrides` no `Promise.all` (scoped por `profile.id`). Manter a janela default de SEMANA no fuso da clínica (`startOfWeek(new Date(), { ...context, weekStartsOn: 1 })` segunda D-11; `addDays(weekStart, 7, context)` meio-aberta).
    Mapear as override rows para o tipo da fn: `{ date: r.exception_date, type: r.override_type, startMinute: r.start_minute, endMinute: r.end_minute, slotMinutes: r.slot_minutes }`. Chamar `expandAvailability({ rules: bands, overrides, window, timeZone: CLINIC_TIME_ZONE })`. Serializar slots (Date → ISO). Passar ao client as rows CRUAS de rules + overrides (para re-expansão client-side na navegação, D-14) além dos slots serializados + byDay + timeZone.
    Trocar `<AgendaView>` por `<CalendarEditor>` (Task 2). Atualizar imports (remover os tipos `AvailabilityException`; usar `AvailabilityOverride`).
  </action>
  <verify>
    <automated>yarn typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `yarn typecheck` sem erros novos.
    - `grep -c 'profile.status !== "paid"' app/dashboard/agenda/page.tsx` == 1 (paid gate RSC preservado).
    - `grep -n 'listAvailabilityOverrides' app/dashboard/agenda/page.tsx` encontra o novo list.
    - `grep -n 'CalendarEditor' app/dashboard/agenda/page.tsx` encontra o novo componente.
    - `grep -n 'overrides' app/dashboard/agenda/page.tsx` encontra o mapeamento (não `exceptions`).
  </acceptance_criteria>
  <done>RSC carrega rules+overrides scoped, expande a semana, passa rows cruas + slots ao CalendarEditor com paid gate.</done>
</task>

<task type="auto">
  <name>Task 2: CalendarEditor + subcomponentes — pintura, toggle, batch save, guarda (D-14..D-18)</name>
  <files>components/dashboard/agenda/calendar-editor.tsx, components/dashboard/agenda/calendar-day-week-grid.tsx, components/dashboard/agenda/calendar-month-indicator.tsx, components/dashboard/agenda/availability-action-panel.tsx</files>
  <read_first>
    - components/dashboard/agenda/agenda-view.tsx (client shell reusável: "use client", Tabs Dia/Semana/Mês, ptBR, helpers localDateOf/localMinuteOf, STEP=30, navegação addDays/addMonths sob tz)
    - components/dashboard/agenda/availability-grid.tsx (reusar cellKey + bandsForDay: paint→bands; toast sonner)
    - lib/expand-availability.ts (Plano 01 — re-expansão client-side na navegação)
    - actions/availability/save-availability.ts (Plano 02 — saveAvailabilityAction com o diff)
    - components/ui/alert-dialog.tsx (guarda "descartar mudanças?")
  </read_first>
  <action>
    Criar `calendar-editor.tsx` (`"use client"`) como a superfície única (substitui agenda-view). Layout D-14: abas Dia/Semana/Mês à ESQUERDA (`Tabs/TabsList/TabsTrigger`, default Semana) + PAINEL DE AÇÕES à direita (`availability-action-panel.tsx`). Navegação anterior/hoje/próximo incluindo entre meses (reusar `addDays`/`addMonths`/`startOfWeek`/`startOfMonth`/`endOfMonth` sob `{ in: tz(timeZone) }`). Re-expansão CLIENT-SIDE: ao navegar/trocar aba, chamar `expandAvailability` sobre rules+overrides crus para a janela da view (D-14 discretion → cliente; a fn é pura/serializável).
    Estado de edição local + diff (D-17): manter `draft` inicial vindo do RSC (rules + overrides), `isDirty = !deepEqual(draft, initial)`. O botão Salvar mostra estado NÃO-SALVO quando dirty. Ao Salvar: computar o diff `{ rules: grade completa, overridesAdd: [], overridesRemove: [ids] }` (reusar `cellKey`/`bandsForDay` de availability-grid para converter células pintadas → bands da grade recorrente) e chamar `saveAvailabilityAction`; toast sonner de sucesso/erro; ao ok, sincronizar `initial = draft`.
    Guarda de descarte (D-17, Pitfall 6): `beforeunload` quando `isDirty` (cobre refresh/fechar aba); AlertDialog "Você tem mudanças não salvas — descartar?" antes de trocar de aba/navegar com `isDirty` (a troca de aba é estado LOCAL → guarda trivial: checar isDirty antes de aplicar a troca). Documentar no código que a saída da rota `/dashboard/agenda` conta com `beforeunload` + confirm nos pontos de saída conhecidos.
    Criar `calendar-day-week-grid.tsx` — CSS grid custom (D-08, sem lib de calendário), coluna por dia, linha por passo de 30 min. Pintura: clique alterna um slot; arraste marca período contíguo na coluna-dia via Pointer Events (`setPointerCapture`, `pointerdown/move/up`, `touch-action: none`); "dia inteiro" marca/desmarca a coluna. CLIQUE-TOGGLE é o BASELINE OBRIGATÓRIO; o arraste é enhancement (D-16, A4) — se o arraste não sair, a fase não bloqueia. O modo pintado vem do toggle ativo.
    Criar `availability-action-panel.tsx` — o painel direito: o TOGGLE ÚNICO Disponibilidade|Folga (D-15), o botão "dia inteiro", o seletor de duração de slot por faixa (D-09, presets como na v1) e o botão Salvar (com o indicador não-salvo D-17). Verde = disponível; folga = neutro (`bg-muted`/hachura + badge "Folga"), NUNCA destructive-red.
    Criar `calendar-month-indicator.tsx` — aba Mês SÓ indicador (D-18): grid mensal Monday-first, ponto + contagem de livres por dia derivados de `byDay`; SEM slots reais e SEM pintura; clicar um dia pode navegar para aquele Dia (a critério visual). Reusar o grid mensal da agenda-view v1.
    Strings de UI em PT-BR. Depois de trocar os call-sites, remover os componentes v1 órfãos (agenda-view.tsx, availability-grid.tsx, exception-dialog.tsx) se nada mais os importar.
  </action>
  <verify>
    <automated>yarn typecheck 2>&1 | tail -15 && yarn lint 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `yarn typecheck` e `yarn lint` sem erros novos.
    - `grep -c 'saveAvailabilityAction' components/dashboard/agenda/calendar-editor.tsx` ≥ 1 (batch save conectado).
    - `grep -n 'beforeunload' components/dashboard/agenda/calendar-editor.tsx` encontra a guarda de descarte.
    - `grep -n 'expandAvailability' components/dashboard/agenda/calendar-editor.tsx` encontra a re-expansão client-side.
    - `grep -n 'setPointerCapture' components/dashboard/agenda/calendar-day-week-grid.tsx` encontra o arraste (enhancement).
    - `grep -niE 'disponibilidade|folga' components/dashboard/agenda/availability-action-panel.tsx` encontra o toggle.
    - `! grep -riE 'destructive' components/dashboard/agenda/availability-action-panel.tsx` (folga não é destructive-red, D-15).
    - **WR-04 (UI↔schema↔DB consistente):** a faixa visível da grade e o seletor de duração alcançam o **fim do dia (1440 = 24:00)** — o médico consegue pintar/bloquear até 24:00, casando com o teto 1440 do schema/DB (Plano 01). Sem cap em 23:30 como na v1.
  </acceptance_criteria>
  <done>Calendário único editável: pintura clique/arraste/dia-inteiro, toggle Disponibilidade|Folga, batch save + guarda de descarte, Mês indicador, navegação client-side.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual/interação do calendário editável (D-14..D-18)</name>
  <what-built>
    O calendário único editável em `/dashboard/agenda`: abas Dia/Semana/Mês à esquerda + painel de ações à direita, toggle Disponibilidade|Folga, pintura clique/arraste/dia-inteiro, salvar em lote com estado não-salvo + confirmar antes de descartar, e Mês como indicador. Backed pela migração aplicada (Plano 01) e pelo saveAvailabilityAction (Plano 02).
  </what-built>
  <how-to-verify>
    1. `yarn dev` e abrir `http://localhost:3000/dashboard/agenda` logado como médico paid.
    2. Layout (D-14): abas Dia/Semana/Mês à esquerda, painel de ações à direita; navegação anterior/hoje/próximo funciona, incluindo trocar de mês.
    3. Toggle (D-15): alternar Disponibilidade|Folga; verde = disponível; folga = tratamento neutro (bg-muted/hachura + badge "Folga"), NÃO vermelho de erro.
    4. Pintura (D-16): clicar um slot alterna aquele horário; arrastar marca um período contíguo; "dia inteiro" marca/desmarca o dia; passo de 30 min.
    5. Salvar em lote (D-17): pintar várias mudanças → o botão Salvar mostra estado "não salvo" → clicar Salvar uma vez persiste tudo (toast de sucesso). Recarregar a página e confirmar que a disponibilidade/folgas persistiram.
    6. Guarda de descarte (D-17): com mudanças não salvas, trocar de aba / tentar sair → aparece o aviso "mudanças não salvas — descartar?".
    7. Aditivo (AGENDA-05): abrir horário extra pontual numa data (override aditivo) e confirmar que o slot extra aparece naquele dia, fora do template.
    8. Folga vence (D-21): numa data com disponibilidade, marcar folga na mesma faixa → os horários daquele intervalo somem.
    9. Mês (D-18): a aba Mês mostra ponto + contagem por dia, sem pintar faixa.
    10. Viradas/fuso (AGENDA-04): navegar semana/mês e confirmar horários corretos nas viradas (segunda como início, sem slot duplicado/sumido), fuso America/Sao_Paulo.
  </how-to-verify>
  <resume-signal>Responda "aprovado" (layout, toggle, pintura, batch save + guarda, aditivo, folga vence, mês indicador, viradas corretas) ou descreva os ajustes necessários.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (client component) → saveAvailabilityAction | o diff pintado atravessa aqui; validado no action (Plano 02) |
| RSC → cliente | rules+overrides crus enviados ao browser para re-expansão; dado do próprio dono (sem exposição cross-profile) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | `app/dashboard/agenda/page.tsx` (RSC) | high | mitigate | Paid gate no RSC (`redirect` se `status !== "paid"`) espelhando o action; auth redirect se sem profile |
| T-06-01 | Information Disclosure (IDOR) | carga RSC de rules+overrides | high | mitigate | `Promise.all` scoped por `profile.id`; RLS por profile_id na leitura (Plano 01); nenhum dado de outro médico chega ao cliente |
| T-06-04 | Tampering | diff enviado ao `saveAvailabilityAction` | medium | mitigate | O cliente não é fonte de verdade: o action re-valida com Zod (WR-02/WR-03) e stampa profile_id server-side (Plano 02); pintura client-side é só UX |
| T-06-SC | Tampering | npm/yarn installs | low | accept | Nenhum pacote NOVO (Pointer Events é API nativa; RESEARCH §Package Legitimacy OK) |
</threat_model>

<verification>
- `yarn typecheck` + `yarn lint` sem erros novos.
- Paid gate RSC preservado; carga scoped por profile_id.
- Checkpoint humano (Task 3): layout D-14, toggle D-15, pintura D-16, batch save + guarda D-17, mês indicador D-18, aditivo AGENDA-05, folga vence D-21, viradas/fuso AGENDA-04.
</verification>

<success_criteria>
- SC-1..SC-5 integralmente pela UI: define disponibilidade recorrente pintando (SC-1), vê horários da duração de slot (SC-2), bloqueia folga + abre aditivo pontual (SC-3), alterna dia/semana/mês corretos nas viradas com mês=indicador (SC-4), pinta clique/arraste/dia-inteiro com toggle e salva em lote com confirmação de descarte (SC-5).
</success_criteria>

<artifacts_this_phase_produces>
Novos/modificados por este plano:
- `app/dashboard/agenda/page.tsx` (MODIFICADO) — carrega overrides, expande semana, monta `<CalendarEditor>`
- `components/dashboard/agenda/calendar-editor.tsx` (NOVO) — superfície única editável (draft/dirty/guard/batch)
- `components/dashboard/agenda/calendar-day-week-grid.tsx` (NOVO) — grid CSS custom + pintura pointer-events
- `components/dashboard/agenda/availability-action-panel.tsx` (NOVO) — toggle Disponibilidade|Folga + dia-inteiro + duração + Salvar
- `components/dashboard/agenda/calendar-month-indicator.tsx` (NOVO) — mês indicador (ponto+contagem)
- (limpeza) `agenda-view.tsx`, `availability-grid.tsx`, `exception-dialog.tsx` removidos se órfãos
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-03-SUMMARY.md` when done
</output>
