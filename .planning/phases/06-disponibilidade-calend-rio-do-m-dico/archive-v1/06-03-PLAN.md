---
phase: 06-disponibilidade-calend-rio-do-m-dico
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - actions/availability/save-availability-rules.ts
  - actions/availability/create-availability-exception.ts
  - actions/availability/delete-availability-exception.ts
  - actions/availability/index.ts
  - actions/index.ts
  - app/dashboard/agenda/page.tsx
  - components/dashboard/agenda/agenda-view.tsx
  - components/dashboard/agenda/availability-grid.tsx
  - components/dashboard/agenda/exception-dialog.tsx
  - components/app-sidebar.tsx
autonomous: true
requirements: [AGENDA-01, AGENDA-02, AGENDA-03, AGENDA-04]
tags: [availability, calendar, actions, ui, rsc]

must_haves:
  truths:
    - "Cada action nova faz getAuthenticatedUser + gate profile.status paid + Zod safeParse + delega ao modulo + retorna result union { ok } (D-13)."
    - "O medico pinta a grade semanal clicavel (D-01), define duracao por faixa (D-09) e salva via saveAvailabilityRulesAction."
    - "O medico adiciona/exclui uma folga (dia inteiro ou parcial, D-04) via o dialog de excecao; exclusao pede confirmacao (AlertDialog)."
    - "A rota /dashboard/agenda computa a janela (default SEMANA, D-06) via startOfWeek({ in: tz, weekStartsOn: 1 }), chama expandAvailability server-side e renderiza dia/semana/mes."
    - "As views dia/semana usam CSS grid custom Tailwind (D-08); o mes mostra indicador leve por dia + contagem (D-07), nunca slots reais."
    - "Item Agenda aparece no sidebar apontando para /dashboard/agenda."
  artifacts:
    - actions/availability/save-availability-rules.ts
    - actions/availability/create-availability-exception.ts
    - actions/availability/delete-availability-exception.ts
    - actions/availability/index.ts
    - app/dashboard/agenda/page.tsx
    - components/dashboard/agenda/agenda-view.tsx
    - components/dashboard/agenda/availability-grid.tsx
    - components/dashboard/agenda/exception-dialog.tsx
  key_links:
    - "page.tsx (RSC) chama expandAvailability (Plano 02) com rules+exceptions (Plano 01) e passa slots + byDay ao componente client."
    - "actions registradas no barrel actions/availability/index.ts E no actions/index.ts raiz; revalidatePath('/dashboard/agenda')."
  prohibitions:
    - "NAO usar components/ui/calendar.tsx como grade de agenda; e date-picker (react-day-picker) so para a data da excecao (D-08)."
    - "NAO adicionar lib de calendario (FullCalendar/react-big-calendar); grade e CSS grid custom (D-08)."
    - "NAO renderizar slots reais na celula do mes (D-07); so dot + contagem."
    - "NAO hardcodar hex; consumir tokens (bg-primary, bg-muted, text-muted-foreground) do design system (UI-SPEC)."
    - "NAO pular o gate paid em nenhum action novo nem no RSC (redirect no RSC, return no action)."
    - "NAO persistir slot; a expansao e so na leitura (D-12)."
---

<objective>
Expor a disponibilidade ao medico: 3 actions (salvar grade, criar/excluir folga) com gate auth+paid+Zod, a rota RSC /dashboard/agenda que expande os slots server-side, e os 3 componentes client (editor de grade clicavel, views dia/semana/mes, dialog de folga) mais o item de sidebar. Fecha AGENDA-01..04.

Purpose: E a superficie que entrega os 4 requisitos ao usuario; consome as tabelas/modulos (Plano 01) e a funcao pura (Plano 02).
Output: actions + rota + 3 componentes + sidebar; nenhuma nova superficie externa (owner-only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-CONTEXT.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-RESEARCH.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-PATTERNS.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

**Actions (sufixo Action, result union):**
- saveAvailabilityRulesAction(input) -> { ok: true } | { ok: false; error }
- createAvailabilityExceptionAction(input) -> { ok: true; exception } | { ok: false; error }
- deleteAvailabilityExceptionAction(input) -> { ok: true } | { ok: false; error }
- Barrels: actions/availability/index.ts + re-export em actions/index.ts.

**Rota:** app/dashboard/agenda/page.tsx (RSC) le rules+exceptions, computa janela (default SEMANA), chama expandAvailability, renderiza a view.

**Componentes client:**
- components/dashboard/agenda/agenda-view.tsx — Tabs Dia/Semana/Mes (default Semana); dia/semana em CSS grid custom; mes = indicador leve por dia + contagem.
- components/dashboard/agenda/availability-grid.tsx — grade semanal clicavel de edicao (D-01) + controle de duracao por faixa (D-09) + CTA Salvar disponibilidade.
- components/dashboard/agenda/exception-dialog.tsx — Dialog "Adicionar folga" com react-day-picker + toggle Dia inteiro/Periodo (D-04) + AlertDialog de exclusao.

**Modificacao:** components/app-sidebar.tsx item { title: "Agenda", url: "/dashboard/agenda" } + import de icone lucide.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Tres actions de availability (gate auth+paid+Zod) + barrels</name>
  <files>actions/availability/save-availability-rules.ts, actions/availability/create-availability-exception.ts, actions/availability/delete-availability-exception.ts, actions/availability/index.ts, actions/index.ts</files>
  <read_first>
    - actions/patient-vaccine-doses/toggle-patient-vaccine-dose.ts L1-70 (cabecalho "use server", imports, gate auth L34-41, gate paid, safeParse L43-49, try/delegate/revalidate/catch L51-70)
    - actions/patient-vaccine-doses/index.ts L1-4 (barrel do dominio)
    - actions/index.ts (barrel raiz; onde re-exportar)
    - lib/zod-error-message.ts (mapear issues Zod -> PT-BR)
    - lib/schemas/availability.ts (Plano 01: saveAvailabilityRulesSchema, createAvailabilityExceptionSchema)
    - modules/availability/upsert-availability-rules.ts, create-availability-exception.ts, delete-availability-exception.ts (Plano 01)
    - 06-PATTERNS.md L210-253 (assignment de actions + barrels)
  </read_first>
  <action>
    Criar os 3 actions seguindo o molde toggle-patient-vaccine-dose.ts: "use server"; import createClient de @/lib/supabase/server, getAuthenticatedUser de @/modules/supabase/get-authenticated-user, os schemas de @/lib/schemas/availability, os modulos de @/modules/availability, revalidatePath de next/cache. Cada action: const supabase = await createClient(); const { profile } = await getAuthenticatedUser(supabase); retorna { ok:false, error:"Sessao nao encontrada." } se sem profile; retorna { ok:false, error:"Perfil nao ativo. Conclua a configuracao da conta em Perfil." } se profile.status diferente de paid; safeParse do input (mensagem PT-BR via lib/zod-error-message.ts em falha); try { delega ao modulo com profile.id; revalidatePath("/dashboard/agenda"); return { ok:true, ... } } catch (e) { return { ok:false, error: mensagem } }.
    - saveAvailabilityRulesAction: valida saveAvailabilityRulesSchema, chama upsertAvailabilityRules(supabase, profile.id, rules).
    - createAvailabilityExceptionAction: valida createAvailabilityExceptionSchema, chama createAvailabilityException(...), retorna { ok:true, exception }.
    - deleteAvailabilityExceptionAction: valida { id } (uuid), chama deleteAvailabilityException(supabase, profile.id, id).
    Exportar tipos de result union por action. Criar actions/availability/index.ts re-exportando as 3 actions + tipos. Adicionar o re-export do dominio availability em actions/index.ts raiz seguindo o padrao dos blocos existentes.
  </action>
  <verify>
    <automated>yarn typecheck && ls actions/availability/save-availability-rules.ts actions/availability/create-availability-exception.ts actions/availability/delete-availability-exception.ts actions/availability/index.ts && grep -q '!== "paid"' actions/availability/save-availability-rules.ts && grep -q 'getAuthenticatedUser' actions/availability/create-availability-exception.ts && grep -q 'availability' actions/index.ts && grep -rq 'safeParse' actions/availability/</automated>
  </verify>
  <acceptance_criteria>
    - yarn typecheck limpo.
    - Os 3 actions existem, cada um com "use server", gate auth (getAuthenticatedUser), gate paid (profile.status diferente de paid), safeParse e result union { ok }.
    - actions/availability/index.ts re-exporta as 3 actions; actions/index.ts raiz inclui o dominio availability.
    - Cada action chama revalidatePath("/dashboard/agenda") no caminho de sucesso.
  </acceptance_criteria>
  <done>3 actions com gate auth+paid+Zod, delegando aos modulos do Plano 01, registradas em ambos os barrels; typecheck limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Rota RSC /dashboard/agenda + item de sidebar</name>
  <files>app/dashboard/agenda/page.tsx, components/app-sidebar.tsx</files>
  <read_first>
    - app/dashboard/vaccines/page.tsx L12-39 (RSC: createClient -> getAuthenticatedUser -> redirect se sem perfil -> gate paid redirect -> reads em Promise.all -> passa dados a componente client; header PT-BR + Separator)
    - components/app-sidebar.tsx L7-13 (imports de icone) e L37-80 (array navMain, grupos title/icon/items)
    - lib/clinic-timezone.ts + lib/expand-availability.ts (Plano 02)
    - modules/availability/list-availability-rules.ts, list-availability-exceptions.ts (Plano 01)
    - 06-RESEARCH.md L396-405 (limites de semana startOfWeek weekStartsOn 1 + { in: tz })
    - 06-PATTERNS.md L257-322 (assignments de page.tsx e sidebar)
    - 06-UI-SPEC.md secao Copywriting (page title "Agenda"; description) e Key Layout (default Semana D-06)
  </read_first>
  <action>
    app/dashboard/agenda/page.tsx (RSC, molde vaccines/page.tsx): createClient -> getAuthenticatedUser; redirect("/auth/login") se sem profile.id; gate paid: se profile.status diferente de paid, redirect("/dashboard/link-whatsapp"). Ler em Promise.all listAvailabilityRules(supabase, profile.id) e listAvailabilityExceptions(supabase, profile.id). Computar a janela default = SEMANA (D-06): weekStart = startOfWeek(new Date(), { in: tz(CLINIC_TIME_ZONE), weekStartsOn: 1 }); weekEnd = addDays(weekStart, 7) (meio-aberto). Mapear as rows snake_case para os tipos camelCase da fn pura (AvailabilityBand/AvailabilityException) e chamar expandAvailability({ rules, exceptions, window: { from: weekStart, to: weekEnd }, timeZone: CLINIC_TIME_ZONE }). Passar slots + byDay + as rows cruas de rules/exceptions ao componente client AgendaView. Header PT-BR: h1 "Agenda" (text-2xl font-semibold tracking-tight), descricao "Configure sua disponibilidade e veja seus horarios por dia, semana ou mes." (text-sm text-muted-foreground), icone lucide + Separator (mesmo layout da pagina de vacinas). Layout flex flex-col gap-6.
    components/app-sidebar.tsx: importar um icone lucide (ex CalendarIcon) no bloco de imports L7-13; adicionar { title: "Agenda", url: "/dashboard/agenda" } ao navMain (novo grupo "Agenda" ou dentro de "Atendimentos" L46-55, seguindo o shape title/icon/items).
  </action>
  <verify>
    <automated>yarn typecheck && test -f app/dashboard/agenda/page.tsx && grep -q 'expandAvailability' app/dashboard/agenda/page.tsx && grep -q 'weekStartsOn: 1' app/dashboard/agenda/page.tsx && grep -q 'CLINIC_TIME_ZONE' app/dashboard/agenda/page.tsx && grep -q '/dashboard/agenda' components/app-sidebar.tsx && grep -q '!== "paid"' app/dashboard/agenda/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - yarn typecheck limpo.
    - page.tsx e RSC: faz gate auth + paid (redirect), le rules+exceptions em Promise.all, chama expandAvailability com window da semana (weekStartsOn 1, { in: tz(CLINIC_TIME_ZONE) }) e passa slots + byDay ao componente client.
    - Nao ha lib de calendario importada; nao usa components/ui/calendar.tsx como grade.
    - components/app-sidebar.tsx tem o item Agenda apontando para /dashboard/agenda.
    - Strings de UI em PT-BR; consome tokens do design system.
  </acceptance_criteria>
  <done>Rota /dashboard/agenda expande slots server-side com janela default de semana e o item Agenda esta no sidebar; typecheck limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Componentes client — editor de grade (D-01/D-09), views dia/semana/mes (D-06/07/08), dialog de folga (D-04)</name>
  <files>components/dashboard/agenda/agenda-view.tsx, components/dashboard/agenda/availability-grid.tsx, components/dashboard/agenda/exception-dialog.tsx</files>
  <read_first>
    - components/dashboard/vaccines/vaccine-calendar-view.tsx L1-11, L32-79 ("use client", props tipadas inline, Tabs para trocar de view, cn() para classes, reuso de fn pura de lib/)
    - components/ui/calendar.tsx (react-day-picker; reuso SO como date-picker da excecao, D-08)
    - 06-RESEARCH.md L213-231 (Pattern 3: CSS grid custom gridTemplateColumns/gridTemplateRows, sticky gutter/header, celula por grid-column/grid-row do indice de 30 min)
    - 06-PATTERNS.md L278-303 (assignments dos componentes client)
    - 06-UI-SPEC.md secoes Spacing (grid density: 24px row / 40px header / 64px gutter), Color (bg-primary reservado, bg-muted para gap/indisponivel), Copywriting (empty/loading/error states, destructive AlertDialog), Key Layout (range 06:00-22:00 auto-extensivel; click-to-toggle baseline; duracao por faixa via Select inline; Tabs Dia/Semana/Mes default Semana; mes = dot + contagem)
    - actions/availability/index.ts (Plano 03 Task 1: saveAvailabilityRulesAction, createAvailabilityExceptionAction, deleteAvailabilityExceptionAction)
  </read_first>
  <action>
    Todos "use client". Strings PT-BR; consumir tokens (bg-primary/bg-muted/text-muted-foreground), nunca hex.
    availability-grid.tsx (editor D-01, greenfield): grade semanal clicavel Seg->Dom (colunas, week starts Monday D-11) x linhas de 30 min (D-03) em CSS grid custom (gridTemplateColumns "4rem repeat(7, minmax(0,1fr))", gridTemplateRows "2.5rem repeat(N, 1.5rem)"; sticky gutter/header). Range visivel default 06:00-22:00, auto-extensivel se uma band salva cair fora (floor 00:00 / ceiling 24:00). Click-to-toggle de celula (baseline obrigatorio; drag opcional reusando a mesma semantica). Celulas contiguas num dia formam uma band (D-02); gap de almoco separa bands. Controle de duracao por faixa (D-09): um Select/Popover compacto ancorado ao topo de cada band contigua (nao um por celula) com presets (10/15/20/30/45/60), default 30 min, text-sm. Derivar as bands da grade pintada e enviar via saveAvailabilityRulesAction; CTA "Salvar disponibilidade" (unico primary, bg-primary), estado "Salvando..."; sucesso/erro via sonner toast (copy do UI-SPEC). Empty state (dashed border) quando nada configurado.
    agenda-view.tsx (views, greenfield na grade): Tabs Dia/Semana/Mes (order Dia . Semana . Mes, default Semana D-06; aba ativa em accent). Dia/Semana (D-08) = CSS grid custom (mesma tecnica; 1 coluna no dia, 7 no semana); slots livres = celulas accent bg-primary/10 posicionadas por grid-column/grid-row a partir do indice de 30 min; gaps/indisponivel = bg-muted; coluna de hoje com marcador accent no topo; comparar limites meio-aberto. Mes (D-07) = grid mensal custom Monday-first; cada celula mostra SO um dot accent quando byDay[date].hasAvailability + a contagem "N livres" em text-sm muted; dias sem disponibilidade em muted sem dot; NUNCA renderizar horarios reais na celula. Navegacao Anterior/Hoje/Proximo (variant outline/ghost). Empty states por view (copy do UI-SPEC). Props tipadas inline recebendo slots + byDay + rules/exceptions da page.tsx.
    exception-dialog.tsx (D-04): Dialog "Adicionar folga" (secondary CTA) contendo o components/ui/calendar.tsx (react-day-picker, locale ptBR) SO como date-picker + um toggle "Dia inteiro" vs "Periodo" (faixa parcial start/end multiplos de 30, D-04); submete via createAvailabilityExceptionAction. Lista de folgas cadastradas com AlertDialog de exclusao ("Excluir folga?", body com {data}, confirm "Excluir" variant destructive, cancel "Cancelar") chamando deleteAvailabilityExceptionAction. Folga = tratamento neutro (bg-muted, badge "Folga"), NAO destructive red. Empty state "Nenhuma folga cadastrada.".
  </action>
  <verify>
    <automated>yarn typecheck && yarn lint && ls components/dashboard/agenda/agenda-view.tsx components/dashboard/agenda/availability-grid.tsx components/dashboard/agenda/exception-dialog.tsx && grep -q 'gridTemplate' components/dashboard/agenda/availability-grid.tsx && grep -q 'gridTemplate' components/dashboard/agenda/agenda-view.tsx && grep -q 'saveAvailabilityRulesAction' components/dashboard/agenda/availability-grid.tsx && [ "$(grep -rl 'react-big-calendar\|@fullcalendar' components/dashboard/agenda/ | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - yarn typecheck e yarn lint limpos.
    - Os 3 componentes existem, todos "use client", strings PT-BR, sem hex hardcoded (tokens do design system).
    - availability-grid.tsx e agenda-view.tsx usam CSS grid custom (gridTemplate...); nenhuma lib de calendario (react-big-calendar/@fullcalendar) importada.
    - agenda-view.tsx: Tabs Dia/Semana/Mes com default Semana; mes mostra dot + contagem, sem horarios reais na celula (D-07).
    - availability-grid.tsx: click-to-toggle + Select de duracao por band + CTA Salvar disponibilidade chamando saveAvailabilityRulesAction.
    - exception-dialog.tsx: reusa components/ui/calendar.tsx so como date-picker; toggle dia inteiro/periodo; AlertDialog de exclusao.
  </acceptance_criteria>
  <done>Editor de grade clicavel com duracao por faixa, tres views (dia/semana/mes) em CSS grid custom, e dialog de folga com date-picker + exclusao confirmada; typecheck e lint limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (medico) -> Server Action | Unico ponto de entrada de escrita; gate auth+paid+Zod aqui. Nenhuma superficie nao-autenticada nova (goal da fase). |
| RSC (page.tsx) -> modules -> DB | Leitura escopada por profile_id + RLS; redirect se nao autenticado/nao paid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | actions/availability/* (bypass do gate paid) | high | mitigate | Gate profile.status paid em TODO action novo + redirect equivalente no RSC page.tsx (padrao do repo); teste de gate no verify (grep). |
| T-06-06 | Tampering | input de faixa/data vindo do cliente | medium | mitigate | Zod safeParse no boundary de cada action (weekday range, multiplos de 30, start<end, data valida, nullability coerente D-04) + CHECK no DB (Plano 01). |
| T-06-07 | Information Disclosure | RSC/actions lendo dados de outro medico | high | mitigate | Todas as leituras/escritas passam profile.id ao modulo (.eq profile_id) + RLS (Plano 01); nenhuma leitura cross-profile; agenda e owner-only. |
| T-06-SC | Tampering | dependencias (nenhum novo pacote neste plano) | low | accept | Nenhum install neste plano; @date-fns/tz auditado e adicionado no Plano 02. |
</threat_model>

<verification>
- `yarn typecheck` e `yarn lint` limpos.
- 3 actions com gate auth+paid+Zod, registradas nos barrels; rota /dashboard/agenda expande slots server-side; 3 componentes client em CSS grid custom sem lib de calendario; item Agenda no sidebar.
- Verificacao humana (UI): abrir /dashboard/agenda logado como perfil paid — grade editavel salva; folga (dia inteiro e parcial) some da grade; Dia/Semana/Mes corretos nas viradas (default Semana).
</verification>

<success_criteria>
- AGENDA-01..04 entregues ao usuario: recorrencia editavel (D-01/D-02), duracao por faixa (D-09), excecoes subtrativas (D-04), tres views corretas no fuso (D-06/07/08/11).
- Zero nova superficie externa; tudo owner-scoped + gate paid.
</success_criteria>

<output>
Create `.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-03-SUMMARY.md` when done
</output>
