---
phase: 06-disponibilidade-calend-rio-do-m-dico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260721000100_availability_rules.sql
  - supabase/migrations/20260721000200_availability_exceptions.sql
  - modules/availability/types.ts
  - modules/availability/list-availability-rules.ts
  - modules/availability/upsert-availability-rules.ts
  - modules/availability/list-availability-exceptions.ts
  - modules/availability/create-availability-exception.ts
  - modules/availability/delete-availability-exception.ts
  - lib/schemas/availability.ts
autonomous: false
requirements: [AGENDA-01, AGENDA-03]
tags: [availability, calendar, supabase, rls, migration]

must_haves:
  truths:
    - "As tabelas availability_rules e availability_exceptions existem no banco com RLS habilitada e 4 policies owner-scoped por profile_id (D-13)."
    - "O medico pode gravar multiplas faixas por dia da semana (sem unique em profile_id+weekday) e cada faixa carrega sua propria slot_minutes (D-02, D-09)."
    - "Uma excecao pode ser dia inteiro (start/end null) ou parcial (faixa preenchida), com CHECK garantindo ambos-ou-nenhum (D-04)."
    - "Todo modulo de leitura/escrita filtra por .eq(profile_id) alem da RLS (defense-in-depth, IDOR)."
  artifacts:
    - supabase/migrations/20260721000100_availability_rules.sql
    - supabase/migrations/20260721000200_availability_exceptions.sql
    - modules/availability/types.ts
    - modules/availability/list-availability-rules.ts
    - modules/availability/upsert-availability-rules.ts
    - modules/availability/list-availability-exceptions.ts
    - modules/availability/create-availability-exception.ts
    - modules/availability/delete-availability-exception.ts
    - lib/schemas/availability.ts
  key_links:
    - "Migracao aplicada ao banco (schema push) ANTES da verificacao; build/typecheck passam sem isso (falso positivo)."
    - "RLS policies usam profile_id in (select id from public.profiles where auth_user_id = auth.uid()); RLS sem policy = negacao silenciosa."
  prohibitions:
    - "NAO criar row-por-slot; so regras + excecoes sao persistidas (D-12)."
    - "NAO adicionar FK/coluna de consulta as tabelas de disponibilidade; Phase 7 escreve appointments por cima (forward constraint)."
    - "NAO usar using(true) global; availability e owner-scoped, nao global-read."
    - "NAO construir SupabaseClient dentro de modulos nem importar next/cache ou next/headers em modules/."
---

<objective>
Criar a fundacao de persistencia da disponibilidade: duas tabelas owner-scoped (availability_rules, availability_exceptions) com RLS + 4 policies na mesma migration, os modulos CRUD escopados por profile_id, os tipos snake_case e o schema Zod de validacao. Aplicar a migracao ao banco.

Purpose: Sem as tabelas + RLS + modulos, nada da agenda persiste; esta e a base sobre a qual a expansao (Plano 02) e a UI/actions (Plano 03) operam.
Output: 2 migrations aplicadas, 5 modulos CRUD, types.ts, schema Zod.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-CONTEXT.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-RESEARCH.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

**Tabelas novas (Postgres):**
- `public.availability_rules` — colunas: id uuid pk, profile_id uuid not null references public.profiles(id) on delete cascade, weekday smallint, start_minute smallint, end_minute smallint, slot_minutes smallint, created_at timestamptz. Indice (profile_id, weekday). SEM unique em (profile_id, weekday) (D-02).
- `public.availability_exceptions` — colunas: id uuid pk, profile_id uuid references profiles(id) on delete cascade, exception_date date not null, start_minute smallint (nullable), end_minute smallint (nullable), created_at timestamptz. Indice (profile_id, exception_date).

**Modulos (uma fn exportada por arquivo):**
- listAvailabilityRules(supabase, profileId) -> AvailabilityRuleRow[]
- upsertAvailabilityRules(supabase, profileId, rules) -> void (substitui a grade inteira do medico)
- listAvailabilityExceptions(supabase, profileId) -> AvailabilityExceptionRow[]
- createAvailabilityException(supabase, profileId, input) -> AvailabilityExceptionRow
- deleteAvailabilityException(supabase, profileId, id) -> void

**Tipos:** AvailabilityRuleRow, AvailabilityExceptionRow (snake_case, em modules/availability/types.ts).

**Schema Zod:** availabilityRuleSchema, saveAvailabilityRulesSchema, createAvailabilityExceptionSchema + tipos z.infer em lib/schemas/availability.ts.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Criar as duas migrations owner-scoped com RLS + 4 policies</name>
  <files>supabase/migrations/20260721000100_availability_rules.sql, supabase/migrations/20260721000200_availability_exceptions.sql</files>
  <read_first>
    - supabase/migrations/20260720000500_patient_vaccine_doses.sql (template EXATO: tabela + comentario PT-BR + indice + enable RLS + 4 policies select/insert/update/delete na MESMA migration; L17-31 tabela/indice, L37-74 RLS/policies)
    - supabase/migrations/20260720000100_rls_vaccine_schedules.sql L2-4 (regra "RLS sem policy = negacao silenciosa; ordem table -> rls -> seed")
    - 06-RESEARCH.md secao Data Model L281-318 (colunas exatas, CHECK constraints, forward constraint Phase 7)
    - 06-PATTERNS.md L34-74 (assignment de migration)
  </read_first>
  <action>
    Criar 20260721000100_availability_rules.sql: table public.availability_rules com as colunas do artifacts_produced. CHECK constraints (per D-03/D-09, 06-RESEARCH L294-296): end_minute maior que start_minute; start_minute modulo 30 igual a 0 e end_minute modulo 30 igual a 0; slot_minutes maior que 0; weekday between 0 and 6. Documentar a convencao de weekday escolhida (0=domingo..6=sabado, casando com date-fns getDay()) em comment on column. Indice idx_availability_rules_profile_weekday on (profile_id, weekday). SEM unique em (profile_id, weekday) (multiplas faixas/dia, D-02). comment on table PT-BR. Depois: alter table enable row level security + as 4 policies (select/insert/update/delete, to authenticated) copiadas do analog trocando o nome da tabela, todas owner-scoped por profile_id in (select id from public.profiles where auth_user_id = auth.uid()).
    Criar 20260721000200_availability_exceptions.sql: table public.availability_exceptions com as colunas do artifacts_produced. CHECK (06-RESEARCH L309-311): (start_minute is null) igual a (end_minute is null) (ambos ou nenhum, D-04); start_minute is null or (end_minute maior que start_minute and start_minute modulo 30 igual a 0 and end_minute modulo 30 igual a 0). Indice idx_availability_exceptions_profile_date on (profile_id, exception_date). comment on table PT-BR explicando "subtrativa; faixa null = dia inteiro (D-04/D-05)". Enable RLS + as mesmas 4 policies owner-scoped. NAO adicionar nenhuma coluna additive/type nem FK de consulta (so subtrativo D-05; forward constraint Phase 7).
    Usar padrao de nomeacao de policy do analog. Timestamp 20260721 mantem ordem cronologica apos as migrations de 20260720.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260721000100_availability_rules.sql && test -f supabase/migrations/20260721000200_availability_exceptions.sql && grep -q 'enable row level security' supabase/migrations/20260721000100_availability_rules.sql && grep -q 'enable row level security' supabase/migrations/20260721000200_availability_exceptions.sql && [ "$(grep -c 'create policy' supabase/migrations/20260721000100_availability_rules.sql)" -ge 4 ] && [ "$(grep -c 'create policy' supabase/migrations/20260721000200_availability_exceptions.sql)" -ge 4 ] && grep -q 'auth.uid()' supabase/migrations/20260721000100_availability_rules.sql</automated>
  </verify>
  <acceptance_criteria>
    - Ambos os arquivos de migration existem com o prefixo de timestamp 20260721.
    - Cada arquivo tem enable row level security + no minimo 4 create policy (select/insert/update/delete) owner-scoped por profile_id via auth.uid().
    - availability_rules NAO tem constraint unique em (profile_id, weekday); tem os 4 CHECK descritos.
    - availability_exceptions tem CHECK (start_minute is null) = (end_minute is null) e nao possui coluna additive/type nem FK de consulta.
    - Nenhuma tabela persiste slot individual (so regras + excecoes).
  </acceptance_criteria>
  <done>As duas migrations existem, seguem o template patient_vaccine_doses, tem RLS + 4 policies owner-scoped e os CHECK constraints de D-03/D-04/D-09.</done>
</task>

<task type="auto">
  <name>Task 2: Tipos, schema Zod e modulos CRUD escopados por profile_id</name>
  <files>modules/availability/types.ts, modules/availability/list-availability-rules.ts, modules/availability/upsert-availability-rules.ts, modules/availability/list-availability-exceptions.ts, modules/availability/create-availability-exception.ts, modules/availability/delete-availability-exception.ts, lib/schemas/availability.ts</files>
  <read_first>
    - modules/patient-vaccine-doses/types.ts (molde de row snake_case + JSDoc de escopo; 06-PATTERNS L177-189)
    - modules/patient-vaccine-doses/get-taken-dose-ids-by-patient.ts L16-34 (query read, client injetado 1o arg, .eq(profile_id), throw new Error("[DOMAIN] ..."), select explicito)
    - modules/patient-vaccine-doses/mark-dose-taken.ts L18-40 (write scoped, stamp profile_id server-side)
    - modules/patient-vaccine-doses/unmark-dose-taken.ts (delete scoped por profile_id + id)
    - lib/schemas/patient-vaccine-dose.ts L8-16 (schema + z.infer + mensagens PT-BR inline)
    - 06-RESEARCH.md L426-441 (excerto pronto de modulo scoped) e L316 (indices)
    - 06-PATTERNS.md L126-206 (assignments de modulos + schema)
  </read_first>
  <action>
    modules/availability/types.ts: exportar AvailabilityRuleRow e AvailabilityExceptionRow em snake_case espelhando as colunas do DB (id, profile_id, weekday|exception_date, start_minute, end_minute, slot_minutes, created_at). JSDoc explicando escopo owner por profile_id. Molde: modules/patient-vaccine-doses/types.ts.
    lib/schemas/availability.ts: availabilityRuleSchema (weekday int 0..6, start_minute/end_minute int multiplos de 30 com start menor que end, slot_minutes int maior que 0), saveAvailabilityRulesSchema (array de availabilityRuleSchema; grade inteira), createAvailabilityExceptionSchema (exception_date string data valida; start_minute/end_minute int nullable multiplos de 30 coerentes: ambos preenchidos ou ambos null, D-04). Mensagens PT-BR inline (ex: "A faixa deve comecar e terminar em multiplos de 30 minutos.", "O horario final deve ser maior que o inicial."). Exportar tipos via z.infer. Molde: lib/schemas/patient-vaccine-dose.ts.
    modules/availability/list-availability-rules.ts: listAvailabilityRules(supabase, profileId) -> select explicito das colunas de availability_rules com .eq("profile_id", profileId); throw new Error("[AVAILABILITY] ...") em erro. Molde: get-taken-dose-ids-by-patient.ts.
    modules/availability/list-availability-exceptions.ts: listAvailabilityExceptions(supabase, profileId) analogo em availability_exceptions.
    modules/availability/upsert-availability-rules.ts: upsertAvailabilityRules(supabase, profileId, rules) que SUBSTITUI a grade inteira do medico (delete-then-insert escopado por profile_id, ou upsert em lote) sempre stampando profile_id server-side (nunca confiar no cliente). throw [AVAILABILITY] em erro.
    modules/availability/create-availability-exception.ts: createAvailabilityException(supabase, profileId, input) -> insert com profile_id stampado, retorna a row criada. Molde: mark-dose-taken.ts.
    modules/availability/delete-availability-exception.ts: deleteAvailabilityException(supabase, profileId, id) -> delete escopado por .eq("profile_id", profileId).eq("id", id). Molde: unmark-dose-taken.ts.
    Uma fn exportada por arquivo, named exports, JSDoc nos exports, SupabaseClient como 1o argumento por injecao. NAO construir client, NAO importar next/cache ou next/headers.
  </action>
  <verify>
    <automated>yarn typecheck && ls modules/availability/list-availability-rules.ts modules/availability/upsert-availability-rules.ts modules/availability/list-availability-exceptions.ts modules/availability/create-availability-exception.ts modules/availability/delete-availability-exception.ts modules/availability/types.ts lib/schemas/availability.ts && [ "$(grep -rl 'next/cache\|next/headers' modules/availability/ | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -c '\.eq("profile_id"' modules/availability/list-availability-rules.ts)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - yarn typecheck passa sem erros.
    - Cada um dos 5 modulos existe, exporta exatamente uma fn nomeada e recebe SupabaseClient como 1o argumento.
    - Toda query filtra por .eq("profile_id", profileId); nenhum modulo importa next/cache ou next/headers.
    - Modulos lancam Error com tag [AVAILABILITY].
    - lib/schemas/availability.ts exporta os 3 schemas + tipos z.infer com mensagens PT-BR.
  </acceptance_criteria>
  <done>Tipos, schema Zod e 5 modulos CRUD existem, escopados por profile_id, typecheck limpo, sem imports proibidos em modules/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Aplicar a migracao ao banco (schema push)</name>
  <files>supabase/migrations/20260721000100_availability_rules.sql, supabase/migrations/20260721000200_availability_exceptions.sql</files>
  <action>Aplicar as duas migrations ao banco Supabase via o mecanismo do projeto (supabase db push com SUPABASE_ACCESS_TOKEN, ou mcp__supabase apply_migration por arquivo) ANTES de qualquer verificacao da fase. Build/typecheck passam sem a migracao aplicada (os tipos nao dependem da tabela viva), criando falso positivo; por isso este passo e obrigatorio e bloqueante. Confirmar tabelas + RLS + 4 policies e get_advisors sem aviso de RLS-sem-policy.</action>
  <what-built>As duas migrations (availability_rules, availability_exceptions) com RLS + policies foram escritas em supabase/migrations/. Elas ainda NAO estao no banco. Build e typecheck passam sem a migracao aplicada (os tipos nao dependem da tabela viva), criando um falso positivo de verificacao; por isso a aplicacao e obrigatoria antes de qualquer verificacao da fase.</what-built>
  <how-to-verify>
    1. Aplicar as migrations ao banco Supabase via o mecanismo do projeto:
       - CLI: definir SUPABASE_ACCESS_TOKEN e rodar `supabase db push` (aplica as migrations pendentes 20260721000100 e 20260721000200), OU
       - MCP: mcp__supabase apply_migration para cada arquivo.
    2. Confirmar que as tabelas existem: no painel Supabase (Table Editor) ou via `mcp__supabase list_tables`, verificar public.availability_rules e public.availability_exceptions com RLS habilitada (badge "RLS enabled") e as 4 policies em cada.
    3. Rodar `mcp__supabase get_advisors` (security) e confirmar que nao ha aviso de "RLS enabled but no policy" para as duas tabelas novas.
    4. Reportar: tabelas criadas? RLS enabled? 4 policies em cada? Advisors limpos para as tabelas novas?
  </how-to-verify>
  <resume-signal>Digite "aprovado" apos confirmar que as duas tabelas existem no banco com RLS + 4 policies e sem aviso de RLS-sem-policy, ou descreva o erro de push.</resume-signal>
  <verify>
    <human-check>As tabelas public.availability_rules e public.availability_exceptions existem no banco com RLS enabled + 4 policies cada; mcp__supabase get_advisors (security) sem aviso de RLS-sem-policy para as tabelas novas.</human-check>
  </verify>
  <done>As duas migrations aplicadas ao banco; ambas as tabelas existem com RLS habilitada + 4 policies owner-scoped; advisors sem aviso de RLS-sem-policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (medico) -> Server Action -> module | Input de faixas/excecoes cruza aqui; nenhuma superficie nao-autenticada nova (goal da fase: zero nova superficie externa). |
| PostgREST direto (Supabase) -> tabelas novas | RLS e a ultima linha de defesa contra IDOR cross-profile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | availability_rules / availability_exceptions (IDOR cross-profile) | high | mitigate | RLS 4 policies owner-scoped por profile_id in (select id from public.profiles where auth_user_id = auth.uid()) + .eq("profile_id", profileId) em todo modulo (defense-in-depth). |
| T-06-02 | Information Disclosure | RLS habilitada sem policy = negacao silenciosa OU policy frouxa = vazamento | high | mitigate | Enable RLS + TODAS as 4 policies na MESMA migration (D-13); get_advisors confirma ausencia de RLS-sem-policy antes da verificacao. |
| T-06-03 | Tampering | input de faixa/data (weekday, minutos, data de excecao) | medium | mitigate | Zod safeParse no boundary (Plano 03 actions) + CHECK constraints no DB (multiplos de 30, end>start, nullability coerente); Supabase parametriza queries. |
| T-06-SC | Tampering | yarn add @date-fns/tz (Plano 02) | low | accept | Pacote auditado no 06-RESEARCH Package Legitimacy Audit (OK, org oficial date-fns, sem postinstall). Instalacao ocorre no Plano 02; nenhum pacote [ASSUMED]/[SUS] neste plano. |
</threat_model>

<verification>
- `yarn typecheck` limpo.
- As duas migrations aplicadas ao banco; tabelas existem com RLS + 4 policies cada; get_advisors sem aviso de RLS-sem-policy.
- Modulos escopados por profile_id, sem imports proibidos.
</verification>

<success_criteria>
- Tabelas availability_rules e availability_exceptions no banco, owner-scoped, RLS + policies (D-13).
- Modulos CRUD e schema Zod prontos para os actions do Plano 03 consumirem.
- Nenhum slot persistido; nenhuma FK de consulta (forward constraint Phase 7 preservada).
</success_criteria>

<output>
Create `.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-01-SUMMARY.md` when done
</output>
