---
phase: 06-disponibilidade-calend-rio-do-m-dico
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - lib/clinic-timezone.ts
  - lib/expand-availability.ts
  - lib/expand-availability.spec.ts
autonomous: true
requirements: [AGENDA-02, AGENDA-04]
tags: [availability, calendar, timezone, pure-function, date-fns]

must_haves:
  truths:
    - "@date-fns/tz esta declarado em package.json (nao phantom) e instalado via yarn."
    - "expandAvailability recebe rules + exceptions + window {from,to} + timeZone e nunca le new Date() ou process.env.TZ internamente (determinismo, D-12)."
    - "Slots respeitam intervalos meio-abertos [start,end); nenhum slot duplicado ou sumido nas viradas de dia/semana/mes (D-11)."
    - "Cada faixa usa sua propria slot_minutes e descarta a sobra que nao divide certo (D-09, D-10)."
    - "Excecao dia-inteiro remove todos os slots da data; excecao parcial remove so os slots que sobrepoem a faixa (D-04)."
    - "Os mesmos inputs com timeZone America/Sao_Paulo produzem os mesmos slots rodando com TZ=UTC e TZ=America/New_York (asserção-chave de fuso)."
  artifacts:
    - lib/clinic-timezone.ts
    - lib/expand-availability.ts
    - lib/expand-availability.spec.ts
  key_links:
    - "expandAvailability usa context { in: tz(CLINIC_TIME_ZONE) } em toda aritmetica de data (o TZ do host nunca vaza)."
    - "byDay resume freeSlotCount + hasAvailability por dia local para alimentar a view de mes (D-07) sem renderizar horarios."
  prohibitions:
    - "NAO usar offset fixo -03:00 cabeado; usar sempre o named zone (correto para datas historicas 1985-2019)."
    - "NAO usar new Date(\"YYYY-MM-DD\") (parseia UTC midnight -> off-by-one BRT); construir instantes via TZDate / { in }."
    - "NAO comparar limites com <= (duplica slot da virada); usar sempre < end."
    - "NAO persistir row-por-slot; a expansao e so na leitura (D-12)."
    - "NAO usar npm install; repo e yarn-only (usar yarn add)."
---

<objective>
Escrever o coracao da fase: a funcao pura testavel expandAvailability (regras + excecoes -> slots livres + resumo por dia), a constante de fuso da clinica, e a suite de testes .spec cobrindo todos os casos de borda travados. Declarar @date-fns/tz em package.json.

Purpose: E o unico codigo de negocio genuinamente novo (D-12); toda correcao de fuso/virada e casos de borda de expansao vivem aqui, provados por teste antes da UI consumir (Plano 03).
Output: @date-fns/tz declarado, lib/clinic-timezone.ts, lib/expand-availability.ts + .spec.ts (yarn test verde).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-CONTEXT.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-RESEARCH.md
@.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

**Dependencia declarada:** @date-fns/tz (^1.4.1) em package.json (via yarn add).

**lib/clinic-timezone.ts:** `export const CLINIC_TIME_ZONE = "America/Sao_Paulo"` (single source of truth do fuso; nao env).

**lib/expand-availability.ts (funcao pura, D-12):**
- Tipos: AvailabilityBand { weekday, startMinute, endMinute, slotMinutes }, AvailabilityException { date, startMinute (nullable), endMinute (nullable) }, FreeSlot { start: Date, end: Date, localDate: string }, ExpandResult { slots: FreeSlot[], byDay: Record<string, { freeSlotCount: number; hasAvailability: boolean }> }.
- Fn: expandAvailability({ rules, exceptions, window: { from, to }, timeZone }): ExpandResult.

**lib/expand-availability.spec.ts:** suite node:test + assert/strict cobrindo D-02/D-04/D-09/D-10/D-11 + fuso.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declarar @date-fns/tz, criar clinic-timezone.ts e escrever a suite .spec (RED)</name>
  <files>package.json, lib/clinic-timezone.ts, lib/expand-availability.spec.ts</files>
  <read_first>
    - lib/compute-pediatric-age.spec.ts L1-13 (estrutura node:test + assert/strict; now/janela explicitos para TZ-independencia; 06-PATTERNS L106-122)
    - lib/compute-pediatric-age.ts L59-83 (padrao de const nomeada exportada no topo)
    - 06-RESEARCH.md L322-382 (assinatura recomendada + lista EXATA de casos .spec) e L102-106 (instalacao yarn add @date-fns/tz; nota phantom)
    - 06-PATTERNS.md L78-122, L354-362 (assignments da fn pura + spec + greenfield de fuso)
  </read_first>
  <behavior>
    - D-02 multiplas faixas/dia: manha 08:00-12:00 + tarde 14:00-18:00 no mesmo weekday -> dois blocos, gap do almoco vazio.
    - D-09 duracao por faixa: manha @30min e tarde @20min no mesmo dia -> contagens diferentes por faixa.
    - D-10 sobra descartada: 14:00-18:00 @45min -> exatamente 14:00, 14:45, 15:30, 16:15, 17:00 (5 slots; ignora 15 min finais).
    - D-10 divisao exata: 08:00-12:00 @30min -> 8 slots, sem sobra.
    - D-04 excecao dia inteiro: data num weekday com faixas -> 0 slots naquele dia, dias vizinhos intactos.
    - D-04 excecao parcial: "saio 16:00" numa quarta 14:00-18:00 -> so 14:00, 14:30, 15:00, 15:30 (remove maior ou igual a 16:00).
    - D-04 excecao parcial que nao sobrepoe: excecao fora das faixas do dia -> nenhum slot removido.
    - D-11 virada de semana (semana comeca na segunda): janela seg 00:00 -> prox. seg 00:00; slot de domingo 23:30-00:00 pertence a semana corrente; sem duplicacao.
    - D-11 virada de dia meio-aberta: slot que termina exatamente em 00:00 pertence ao dia que termina.
    - D-11 fuso fixo: mesmos inputs com timeZone America/Sao_Paulo produzem os mesmos slots rodando com TZ=UTC e TZ=America/New_York.
    - Janela vazia / weekday sem faixa: 0 slots, sem throw.
    - Mes (D-07): byDay conta freeSlotCount por dia e marca hasAvailability.
  </behavior>
  <action>
    Rodar `yarn add @date-fns/tz` (nunca npm) para declarar o pacote em package.json (hoje e phantom em node_modules; nao confiar nisso). Confirmar a entrada em dependencies.
    Criar lib/clinic-timezone.ts: export const CLINIC_TIME_ZONE = "America/Sao_Paulo" com JSDoc (single source; nao ler de env). Molde de const: compute-pediatric-age.ts L59.
    Criar lib/expand-availability.spec.ts (node:test + assert/strict, molde compute-pediatric-age.spec.ts): importar { expandAvailability } de "@/lib/expand-availability" e CLINIC_TIME_ZONE. Escrever UM teste por item do bloco behavior acima, cada um passando window {from,to} e timeZone explicitos (nunca dependendo do relogio). Para a asserção-chave de fuso (D-11 fuso fixo), estruturar o teste de forma que a igualdade valha independente do TZ do processo (a fn recebe timeZone por parametro; comparar os instantes/localDate resultantes). Construir as datas de window via TZDate ou construtor local explicito (nunca new Date("YYYY-MM-DD")). Os testes DEVEM falhar agora (a fn ainda nao existe) - RED.
    Nao inventar API alem da assinatura do 06-RESEARCH L352-357.
  </action>
  <verify>
    <automated>grep -q '@date-fns/tz' package.json && test -f lib/clinic-timezone.ts && grep -q 'America/Sao_Paulo' lib/clinic-timezone.ts && test -f lib/expand-availability.spec.ts && [ "$(grep -c 'test(' lib/expand-availability.spec.ts)" -ge 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - @date-fns/tz aparece em package.json dependencies.
    - lib/clinic-timezone.ts exporta CLINIC_TIME_ZONE = "America/Sao_Paulo".
    - lib/expand-availability.spec.ts tem no minimo 10 casos test() cobrindo todos os itens do bloco behavior, cada um com window + timeZone explicitos.
    - Nenhum teste usa new Date("YYYY-MM-DD") para construir instantes.
    - Rodar a suite agora falha porque expandAvailability ainda nao existe (RED).
  </acceptance_criteria>
  <done>@date-fns/tz declarado, clinic-timezone.ts criado, .spec.ts escrito com todos os casos de borda e falhando por ausencia da implementacao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar expandAvailability ate a suite passar (GREEN)</name>
  <files>lib/expand-availability.ts</files>
  <read_first>
    - lib/compute-pediatric-age.ts L1-9, L84-104, L160-165 (imports date-fns, anti-patterns de data, assinatura deterministica com tempo por parametro)
    - 06-RESEARCH.md L185-231 (Pattern 1 boundaries com { in: tz() }; Pattern 3), L322-382 (assinatura + algoritmo interno passo-a-passo), L396-423 (Code Examples prontos: limites de semana; bandSlots descartando a sobra)
    - 06-PATTERNS.md L78-104 (imports date-fns + @date-fns/tz; determinismo; anti-patterns a herdar)
  </read_first>
  <action>
    Criar lib/expand-availability.ts com a assinatura EXATA do 06-RESEARCH L328-357: os tipos AvailabilityBand, AvailabilityException, FreeSlot, ExpandResult e a fn expandAvailability({ rules, exceptions, window: { from, to }, timeZone }): ExpandResult. Importar de date-fns startOfDay, addMinutes, addDays, eachDayOfInterval (e startOfWeek se necessario) e de @date-fns/tz tz, TZDate.
    Algoritmo (06-RESEARCH L360-366): (1) iterar cada dia local d em [window.from, window.to) via eachDayOfInterval com { in: tz(timeZone) }; (2) para o weekday de d, pegar todas as bands (D-02); (3) para cada band, base = startOfDay(d, { in: tz(timeZone) }), gerar slots avancando slotMinutes e emitindo SOMENTE enquanto addMinutes(slotStart, slotMinutes) <= bandEnd (D-10 descarta sobra); (4) aplicar excecoes da data d: se alguma excecao tem startMinute === null -> remover o dia todo (pula); senao remover slots que sobrepoem [exStart, exEnd) via slot.start < exEnd && slot.end > exStart (D-04); (5) recortar ao window meio-aberto (slot.start >= from && slot.start < to); (6) acumular byDay { freeSlotCount, hasAvailability } (D-07). localDate = "YYYY-MM-DD" no fuso da clinica (formatar com { in: tz(timeZone) }). JSDoc rico documentando a convencao de weekday (0=domingo..6=sabado, casando com a coluna do DB do Plano 01 e date-fns getDay()). Comparar limites SEMPRE com < end, nunca <= (D-11). Rodar yarn test ate todos os casos passarem (GREEN); refatorar se necessario mantendo verde.
  </action>
  <verify>
    <automated>TZ=UTC yarn test && TZ=America/New_York yarn test && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - yarn test passa com todos os casos de lib/expand-availability.spec.ts verdes.
    - A suite passa identicamente com TZ=UTC e com TZ=America/New_York no ambiente (fuso vem do parametro, nao do host).
    - yarn typecheck limpo.
    - expandAvailability nao le new Date() nem process.env.TZ internamente; usa { in: tz(timeZone) } em toda aritmetica.
    - byDay retorna freeSlotCount + hasAvailability por localDate.
  </acceptance_criteria>
  <done>expandAvailability implementada com a assinatura do research, toda a suite verde independente do TZ do processo, typecheck limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependencia externa (npm) -> build | yarn add @date-fns/tz introduz codigo de terceiro na build. |
| (nenhuma superficie de rede nova) | A fn pura nao expoe endpoint; roda server-side e client-side com os mesmos inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | yarn add @date-fns/tz (supply chain) | low | accept | Auditado no 06-RESEARCH Package Legitimacy Audit: OK, publicado pelo org oficial date-fns (monorepo verificado), 32.9M downloads/semana, sem postinstall script. Nenhum pacote [ASSUMED]/[SUS]. |
| T-06-04 | Tampering | expandAvailability recebendo dados corrompidos (faixa invalida) | low | accept | A fn e pura e determinista; validacao de forma acontece antes (Zod no action, CHECK no DB, Plano 01/03). Um input malformado no maximo produz slots errados na leitura do proprio dono, sem cross-profile (fn nao acessa DB). |

Nota: fase owner-only, zero nova superficie externa (goal). A fn pura nao acessa DB nem rede.
</threat_model>

<verification>
- @date-fns/tz declarado em package.json.
- yarn test verde com todos os casos de borda; identico sob TZ=UTC e TZ=America/New_York.
- yarn typecheck limpo.
</verification>

<success_criteria>
- expandAvailability pura, determinista, TZ-independente por parametro (D-11/D-12).
- Todos os casos D-02/D-04/D-09/D-10/D-11 + byDay do mes (D-07) cobertos e verdes.
</success_criteria>

<output>
Create `.planning/phases/06-disponibilidade-calend-rio-do-m-dico/06-02-SUMMARY.md` when done
</output>
