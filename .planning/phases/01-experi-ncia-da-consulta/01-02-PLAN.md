---
phase: 01-experi-ncia-da-consulta
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/compute-pediatric-age.ts
  - lib/compute-pediatric-age.spec.ts
  - lib/format-pediatric-age.ts
  - lib/format-pediatric-age.spec.ts
autonomous: true
requirements: [CONS-01]
must_haves:
  truths:
    - "A single pure helper computes pediatric age from a birth date, banded by age (days / weeks / months+days / years+months) per D-07/D-11"
    - "The engine returns corrected age for preterm infants (gestational < 37 weeks) up to ~24 months corrected, distinct from chronological (D-10)"
    - "Missing, invalid, and future birth dates return explicit status flags (D-09/D-12), never throw, never produce wrong numbers"
    - "Edge cases (end-of-month, leap year, year rollover, newborn, near-local-midnight) are covered by passing tests"
    - "A formatter renders the engine result as PT-BR by-extenso text with correct singular/plural agreement (D-08)"
  artifacts:
    - path: "lib/compute-pediatric-age.ts"
      provides: "Pure pediatric age engine (chronological + corrected, banded, status flags)"
      exports: ["computePediatricAge", "PediatricAge", "AgeBand", "PediatricAgeStatus"]
      min_lines: 60
    - path: "lib/compute-pediatric-age.spec.ts"
      provides: "Edge-case unit tests for the engine"
      contains: "computePediatricAge"
    - path: "lib/format-pediatric-age.ts"
      provides: "PT-BR by-extenso + abbreviated rendering of a PediatricAge result"
      exports: ["formatPediatricAge", "formatPediatricAgeAbbrev"]
    - path: "lib/format-pediatric-age.spec.ts"
      provides: "Plural/singular + band-wording tests"
      contains: "formatPediatricAge"
  key_links:
    - from: "lib/compute-pediatric-age.ts"
      to: "date-fns@4.4.0"
      via: "calendar math (no ms division)"
      pattern: "from \"date-fns\""
---

<objective>
Build the keystone of the phase: ONE pure, tested `lib/compute-pediatric-age.ts` that decomposes a birth date into a banded pediatric age (days / weeks / months+days / years+months per D-07/D-11), computes corrected age for preterm infants (D-10), and returns explicit status flags for missing/invalid/future dates (D-09/D-12) — plus a `lib/format-pediatric-age.ts` that renders the result as PT-BR by-extenso text (D-08). No I/O, no React, no Supabase.

Purpose: This helper replaces the inline, untested, coarse `formatAgeFromBirthDate` (years/months only) and becomes the single source of truth consumed by the patient hero, case header, and assistant context this phase (Plan 04) and the vaccine due/overdue logic in Phase 5. The TDD cycle locks the calendar edge cases (the exact CONS-01 failure surface) before any UI consumes it.
Output: Two pure helpers + two co-located specs, all green under `yarn test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experi-ncia-da-consulta/01-CONTEXT.md
@.planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md
@.planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md
@.planning/phases/01-experi-ncia-da-consulta/01-UI-SPEC.md
</context>

<feature>
  <name>Pediatric age engine + PT-BR formatter</name>
  <files>lib/compute-pediatric-age.ts, lib/compute-pediatric-age.spec.ts, lib/format-pediatric-age.ts, lib/format-pediatric-age.spec.ts</files>
  <read_first>
    - lib/brazilian-date-form.ts (lines 1-2 imports; ~66-71 the calendar-equality guard — the local-calendar parsing analog; copy its `new Date(y, m-1, d)` construction style, NOT `new Date("YYYY-MM-DD")` and NOT the `T12:00:00` noon hack)
    - lib/patient-bmi-ui-status.spec.ts (lines 1-49: the `node:test` + `node:assert/strict` + `tsx --test` harness style to mirror exactly)
    - modules/falaped-assistant/lib/formatters.ts (lines 9-28: the deprecated `formatAgeFromBirthDate` anti-pattern this replaces — years/months only, noon hack, no days/weeks/corrected)
    - .planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md §"Pattern 1: Single tested age engine" (recommended return shape) + §"Anti-Patterns to Avoid" + §"Common Pitfalls" (Pitfall 1)
    - .planning/phases/01-experi-ncia-da-consulta/01-UI-SPEC.md §"Age badge copy rules" (the exact PT-BR band wording + plural agreement the formatter must produce)
  </read_first>
  <behavior>
    - Newborn: birth 2 days ago → status "ok", band "days", parts {days: 2}; 1 day → "1 dia" via formatter (singular).
    - 0 days (born today) → band "days", parts {days: 0}; formatter "0 dias".
    - Weeks band: 35 days old → band "weeks", parts {weeks: 5}; boundary at ~12 weeks (84 days) switches to months+days per D-11.
    - Months+days band: birth 2025-03-12, now 2025-06-24 → band "months_days", parts {months: 3, days: 12}; formatter "3 meses e 12 dias". When days remainder is 0 → omit days clause ("4 meses").
    - End-of-month trap: birth 2025-01-31, now 2025-03-01 → months computed via intervalToDuration (calendar-correct), not manual month subtraction.
    - Leap year: birth 2024-02-29, now 2025-03-01 (non-leap) → exactly 1 year; band years_months; document and test the Feb-29 anniversary policy.
    - Year rollover: birth 2024-12-20, now 2025-01-05 → correct day/week count across the year boundary.
    - Years+months band: birth 2023-02-10, now 2025-06-10 → "2 anos e 4 meses"; 0 months remainder → "3 anos".
    - Missing DOB (null/undefined) → status "missing_birth_date" (D-09), no parts.
    - Invalid DOB ("2025-13-40", "abc") → status "invalid" (D-12).
    - Future DOB (birth after now) → status "future" (D-12).
    - Near-local-midnight: passing an explicit `now` near local midnight does NOT flip the day count vs UTC — engine builds local-midnight dates and uses differenceInDays of two local-midnight dates.
    - Corrected age (D-10): gestationalAgeWeeks = 32, chronological 3 months → subtract (40-32)=8 weeks; result has `corrected` with its own band/parts and `appliesUntilMonths: 24`. When gestationalAgeWeeks >= 37 or null → no `corrected` field (full-term). When corrected age exceeds ~24 months corrected → no `corrected` field (stop correcting).
    - Formatter: `formatPediatricAge(result)` → full by-extenso PT-BR per UI-SPEC band wording with singular/plural agreement; `formatPediatricAgeAbbrev(result)` → "5 d" / "6 sem" / "3m 12d" / "2a 4m" for the tight case header.
  </behavior>
  <action>
    RED first. Write `lib/compute-pediatric-age.spec.ts` and `lib/format-pediatric-age.spec.ts` using `import test from "node:test"` + `import assert from "node:assert/strict"` (mirror `lib/patient-bmi-ui-status.spec.ts`). Every test passes an explicit `now` as the second arg so it is deterministic. Cover every case in <behavior>. Run `yarn test` and confirm the new specs FAIL (engine/formatter not yet implemented). Commit `test(01-02): add failing pediatric age engine + formatter specs`.

    GREEN. Implement `lib/compute-pediatric-age.ts` exporting types `AgeBand = "days" | "weeks" | "months_days" | "years_months"`, `PediatricAgeStatus = "ok" | "missing_birth_date" | "invalid" | "future"`, `PediatricAge = { status, band?, totalDays?, parts?: { years?, months?, weeks?, days? }, corrected?: { band, parts, appliesUntilMonths } }`, and the function `computePediatricAge(birthDateIso: string | null | undefined, now: Date = new Date(), gestationalAgeWeeks?: number | null): PediatricAge`. Build a local-midnight `Date` from the ISO string via `new Date(Number(y), Number(m)-1, Number(d))` after a `/^(\d{4})-(\d{2})-(\d{2})$/` parse (reject non-matching → "invalid"); normalize `now` to local midnight too. Use date-fns `differenceInDays`, `differenceInWeeks`, `intervalToDuration`, `isAfter`, `isValid` — never `(now-birth)/86400000`, never manual month-number subtraction. Band per D-07/D-11: 0–28 days → days; <84 days (≈12 weeks) → weeks; <24 months → months_days; else years_months. Corrected-age branch: define a single named constant `FULL_TERM_GESTATIONAL_WEEKS = 40` with a comment citing A3/D-10 (one-line change point, not scattered magic numbers) and `CORRECTED_AGE_CUTOFF_MONTHS = 24`; subtract `(FULL_TERM_GESTATIONAL_WEEKS - gestationalAgeWeeks)` weeks when `gestationalAgeWeeks < 37`, recompute the band on the corrected total, and only populate `corrected` while corrected age ≤ cutoff. JSDoc on the exported function.

    Implement `lib/format-pediatric-age.ts` exporting `formatPediatricAge(age: PediatricAge): string` and `formatPediatricAgeAbbrev(age: PediatricAge): string` per the UI-SPEC band wording and singular/plural rules. The formatter NEVER does date math — it only renders the engine result. Status flags ("missing_birth_date"/"invalid"/"future") are rendered by the component (UI-SPEC copy), so the formatter may return an empty string or a sentinel for non-"ok" status; document which.

    Run `yarn test` until all specs pass. Commit `feat(01-02): implement pediatric age engine + PT-BR formatter`. REFACTOR only if needed (keep band logic readable); re-run `yarn test`; commit `refactor(01-02): ...` if applicable.
  </action>
  <verify>
    <automated>yarn test 2>&1 | grep -Eq "(pass|ok)" && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/compute-pediatric-age.ts` exports `computePediatricAge`, `PediatricAge`, `AgeBand`, `PediatricAgeStatus`
    - `lib/compute-pediatric-age.ts` imports calendar functions from "date-fns" and contains no `86400000` / `/ 1000 / 60` ms-division age math (grep: `grep -v '^\s*//' lib/compute-pediatric-age.ts | grep -c '86400000'` == 0)
    - `lib/compute-pediatric-age.ts` contains a named `FULL_TERM_GESTATIONAL_WEEKS` constant
    - `lib/format-pediatric-age.ts` exports `formatPediatricAge` and `formatPediatricAgeAbbrev`
    - `yarn test` runs `lib/compute-pediatric-age.spec.ts` and `lib/format-pediatric-age.spec.ts` and exits 0 (all cases in <behavior> green, including leap/end-of-month/year-rollover/newborn/missing/invalid/future/corrected)
    - `yarn typecheck` exits 0
  </acceptance_criteria>
  <implementation>Pure date-fns calendar math, local-midnight construction, banded result, corrected-age branch with named constants; formatter is render-only. See <action> for the RED→GREEN→REFACTOR sequence.</implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Pure in-process helper; no I/O, no network, no DB, no user input beyond a string already validated upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | birthDateIso string input | mitigate | Strict regex parse + isValid guard; malformed/future input returns a status flag, never throws, never yields a wrong number that could mislead a clinical age display. |
| T-01-05 | Information Disclosure | age computation | accept | No PII persisted or logged here; the engine receives a date and returns a structure. Display/logging is the consumer's concern (handled in Plan 04). |
| T-01-SC | Tampering | npm installs | mitigate | No new packages (date-fns@4.4.0 already installed; RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `yarn test` green including all edge cases enumerated in <behavior>.
- `yarn typecheck` green.
- No ms-division age math present (grep gate above).
</verification>

<success_criteria>
- One tested pure engine returns banded chronological + corrected age with status flags.
- Formatter produces correct PT-BR by-extenso + abbreviated strings with plural agreement.
- All CONS-01 edge cases (D-07/D-09/D-10/D-11/D-12) covered by passing tests.
</success_criteria>

<output>
Create `.planning/phases/01-experi-ncia-da-consulta/01-02-SUMMARY.md` when done.
</output>
