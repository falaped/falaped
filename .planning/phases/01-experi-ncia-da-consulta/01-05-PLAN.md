---
phase: 01-experi-ncia-da-consulta
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-04"]
files_modified:
  - lib/compute-elapsed-ms.ts
  - lib/compute-elapsed-ms.spec.ts
  - modules/cases/types.ts
  - modules/cases/get-case-by-id.ts
  - modules/cases/pause-consultation.ts
  - modules/cases/resume-consultation.ts
  - actions/cases/pause-consultation.ts
  - actions/cases/resume-consultation.ts
  - actions/cases/index.ts
  - hooks/use-consultation-timer.ts
  - components/dashboard/cases/consultation-timer-widget.tsx
  - components/dashboard/cases/case-detail-content.tsx
autonomous: false
requirements: [CONS-02, CONS-03]
must_haves:
  truths:
    - "Opening a case shows a floating consultation timer counting elapsed time live (CONS-02, D-02 auto-start)"
    - "Elapsed stays correct after reload/navigation — computed from persisted started_at, never a resetting counter (CONS-03)"
    - "Pause/resume works; paused time is excluded from elapsed and pause state survives reload (D-03)"
    - "The widget is a floating panel draggable anywhere, position persisted per device (D-04)"
  artifacts:
    - path: "lib/compute-elapsed-ms.ts"
      provides: "Pure elapsed-from-timestamp calculation, unit-tested"
      exports: ["computeElapsedMs"]
    - path: "hooks/use-consultation-timer.ts"
      provides: "Client hook: setInterval repaints; elapsed from timestamp"
      exports: ["useConsultationTimer"]
    - path: "components/dashboard/cases/consultation-timer-widget.tsx"
      provides: "Draggable floating timer widget with pause/resume controls"
      min_lines: 60
    - path: "modules/cases/pause-consultation.ts"
      provides: "Ownership-scoped pause write"
      exports: ["pauseConsultation"]
  key_links:
    - from: "components/dashboard/cases/consultation-timer-widget.tsx"
      to: "actions/cases/pause-consultation.ts"
      via: "pauseConsultationAction / resumeConsultationAction"
      pattern: "(pause|resume)ConsultationAction"
    - from: "hooks/use-consultation-timer.ts"
      to: "lib/compute-elapsed-ms.ts"
      via: "computeElapsedMs(opts, Date.now())"
      pattern: "computeElapsedMs"
---

<objective>
Deliver the consultation timer vertical slice: a floating, draggable widget showing live elapsed time anchored on the persisted `cases.started_at` (CONS-02, auto-start D-02), surviving reload/navigation by computing from the timestamp (CONS-03), with pause/resume using the Plan-01 accumulator columns (D-03). Spans a pure tested `computeElapsedMs`, ownership-scoped pause/resume actions+modules, the `useConsultationTimer` hook (setInterval repaints only), and the draggable widget mounted in the case detail.

Purpose: Makes CONS-02/03 user-observable — open a case and a drift-free timer is already running, pauses/resumes correctly, never resets on refresh. Depends on Plan 01 (pause columns + started_at) and Plan 04 (already touched `case-detail-content.tsx` / `get-case-by-id.ts`; this plan adds pause columns + the widget mount without collision).
Output: tested elapsed helper, pause/resume slice, timer hook, draggable widget, case-detail mount.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md
@.planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md
@.planning/phases/01-experi-ncia-da-consulta/01-UI-SPEC.md
@.planning/phases/01-experi-ncia-da-consulta/01-01-SUMMARY.md
@.planning/phases/01-experi-ncia-da-consulta/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure elapsed helper (failing test first) + extend case types/select</name>
  <files>lib/compute-elapsed-ms.ts, lib/compute-elapsed-ms.spec.ts, modules/cases/types.ts, modules/cases/get-case-by-id.ts</files>
  <read_first>
    - lib/patient-bmi-ui-status.spec.ts (node:test + node:assert/strict + tsx --test harness)
    - .planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md §"Pattern 2: Elapsed-from-timestamp timer" (the elapsed formula) + §"Validation Architecture" (extract pure computeElapsedMs)
    - modules/cases/types.ts (CaseWithPatient — add two pause columns); modules/cases/get-case-by-id.ts (CaseDetail lines 29-44 + top-level case .select lines 65-99 — add the pause columns; Plan 04 already added gestational_age_weeks to the patient sub-select, keep it)
    - .planning/phases/01-experi-ncia-da-consulta/01-01-SUMMARY.md (exact pause column names + started_at default)
  </read_first>
  <behavior>
    - Running (no endedAt/pausedAt): elapsed = now - started_at - pausedMs (e.g. started 10min ago, pausedMs 0 -> ~600000).
    - Paused (pausedAt set): elapsed frozen at pausedAt - started_at - pausedMs (does not grow as now advances).
    - Ended (endedAt set): elapsed = endedAt - started_at - pausedMs (final).
    - Reload simulation: same inputs, later now -> running grows by delta; paused unchanged. Never negative (clamp 0).
    - Accumulator: started 20min ago, pausedMs 300000, running -> ~900000.
  </behavior>
  <action>
    RED: write `lib/compute-elapsed-ms.spec.ts` covering every <behavior> case, passing explicit `now` for determinism (mirror patient-bmi-ui-status.spec.ts). `yarn test` must FAIL. Commit `test(01-05): add failing computeElapsedMs spec`.
    GREEN: implement `lib/compute-elapsed-ms.ts` exporting `computeElapsedMs(opts: { startedAt: string; endedAt: string | null; pausedMs: number; pausedAt: string | null }, now: number): number` = `Math.max(0, end - anchor - pausedMs)` where `anchor = new Date(startedAt).getTime()` and `end = endedAt ? Date(endedAt) : pausedAt ? Date(pausedAt) : now`. JSDoc. `yarn test` green. Commit `feat(01-05): implement computeElapsedMs`.
    Types: add `consultation_paused_ms: number` + `consultation_paused_at: string | null` to `CaseWithPatient` (types.ts) and to `CaseDetail` + the top-level case `.select(...)` in get-case-by-id.ts. Keep the Plan-04 `gestational_age_weeks` patient sub-select and the `.eq("user_phone", phone)` scoping unchanged.
  </action>
  <verify>
    <automated>grep -q "computeElapsedMs" lib/compute-elapsed-ms.ts && grep -q "consultation_paused_ms" modules/cases/types.ts && grep -q "consultation_paused_ms" modules/cases/get-case-by-id.ts && grep -q "gestational_age_weeks" modules/cases/get-case-by-id.ts && yarn test && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/compute-elapsed-ms.ts` exports `computeElapsedMs`; spec covers running/paused/ended/reload/accumulator and passes under `yarn test`
    - CaseWithPatient + CaseDetail + the case select include `consultation_paused_ms` and `consultation_paused_at`
    - `gestational_age_weeks` (from Plan 04) still present in the patient sub-select
    - `yarn test` and `yarn typecheck` exit 0
  </acceptance_criteria>
  <done>Pure elapsed helper green; case types/select carry the pause fields without dropping Plan-04 changes.</done>
</task>

<task type="auto">
  <name>Task 2: Pause/resume slice (modules + actions + barrel)</name>
  <files>modules/cases/pause-consultation.ts, modules/cases/resume-consultation.ts, actions/cases/pause-consultation.ts, actions/cases/resume-consultation.ts, actions/cases/index.ts</files>
  <read_first>
    - modules/cases/update-case-status.ts (lines 8-61: the user_phone resolution + ownership-scoped update — the exact pattern; cases are scoped by user_phone, never id alone)
    - actions/cases/update-case-status.ts (lines 1-32: auth + paid gate + result union + revalidatePath)
    - actions/cases/index.ts (the barrel to extend) + the root actions/index.ts re-export `from "./cases"`
    - .planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md §"actions/cases/pause-consultation.ts" + §"modules/cases/pause-consultation.ts" (accumulator semantics)
  </read_first>
  <action>
    `modules/cases/pause-consultation.ts` (export `pauseConsultation(supabase, caseId, profileId)`): resolve `user_phone` from `authenticated_users` (like update-case-status), read the case row scoped by `user_phone`, and if not already paused (`consultation_paused_at` null) and not ended, set `consultation_paused_at = new Date().toISOString()`. Throw `Error("[CASES] ...")` on failure. One exported fn, SupabaseClient injected, no next/cache.
    `modules/cases/resume-consultation.ts` (export `resumeConsultation(...)`): resolve user_phone, read current row scoped by user_phone; if `consultation_paused_at` set, compute `addedMs = Date.now() - new Date(consultation_paused_at).getTime()`, write `consultation_paused_ms = current_paused_ms + addedMs` and `consultation_paused_at = null`. Ownership filter on the update (`.eq("id", caseId).eq("user_phone", userPhone)`).
    Actions `actions/cases/pause-consultation.ts` + `resume-consultation.ts`: "use server", `getAuthenticatedUser`, gate `profile.status === "paid"`, validate `caseId` is a non-empty string, delegate to the module (passing `profile.id`), `revalidatePath("/dashboard/cases")` + `` `/dashboard/cases/${caseId}` ``, return `{ ok: true } | { ok: false; error }`. Export `pauseConsultationAction` / `resumeConsultationAction`. Add both to `actions/cases/index.ts` (root barrel re-exports the whole module, so no root edit needed — verify).
  </action>
  <verify>
    <automated>grep -q "pauseConsultation" modules/cases/pause-consultation.ts && grep -q "user_phone" modules/cases/pause-consultation.ts && grep -q "consultation_paused_ms" modules/cases/resume-consultation.ts && grep -q "pauseConsultationAction" actions/cases/index.ts && grep -q "resumeConsultationAction" actions/cases/index.ts && yarn typecheck && yarn lint</automated>
  </verify>
  <acceptance_criteria>
    - both modules resolve user_phone and scope reads/writes by it (never by id alone)
    - resume accumulates into `consultation_paused_ms` and clears `consultation_paused_at`
    - both actions enforce auth + paid gate, validate caseId, return result unions, revalidate paths
    - `actions/cases/index.ts` exports both new actions
    - `yarn typecheck` + `yarn lint` exit 0
  </acceptance_criteria>
  <done>Pause/resume mutate durable case state, ownership-scoped, behind the paid gate.</done>
</task>

<task type="auto">
  <name>Task 3: Timer hook + draggable widget + case-detail mount</name>
  <files>hooks/use-consultation-timer.ts, components/dashboard/cases/consultation-timer-widget.tsx, components/dashboard/cases/case-detail-content.tsx</files>
  <read_first>
    - hooks/use-audio-recorder.ts (client hook structure)
    - components/dashboard/cases/case-report.tsx (lines 1-25: the @dnd-kit DndContext + sensors import convention to reuse; note it uses useSortable — the widget needs free-drag useDraggable instead)
    - components/dashboard/cases/case-detail-header.tsx (lines 8-22: StatusBadge active ping treatment to reuse for the running dot)
    - components/dashboard/cases/case-detail-content.tsx (line 92 header; this RSC fetched caseDetail incl. started_at, ended_at, and now the pause columns — mount the widget here; Plan 04 owns the header age, this plan owns the widget mount)
    - .planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md §"Pattern 2" + §"Code Examples" (draggable widget) ; .planning/phases/01-experi-ncia-da-consulta/01-UI-SPEC.md §2 "Consultation Timer Widget" (full visual/interaction contract + copy)
  </read_first>
  <action>
    `hooks/use-consultation-timer.ts` (export `useConsultationTimer(opts: { startedAt; endedAt; pausedMs; pausedAt })`): `running = !endedAt && !pausedAt`; `useEffect` sets `setInterval(() => force(n => n+1), 1000)` ONLY while running (repaint, never a counter); returns `computeElapsedMs(opts, Date.now())`. No date math inline — wrap the Plan-1-task helper.
    `components/dashboard/cases/consultation-timer-widget.tsx` ("use client"): a `Card`-toned fixed floating panel (`bg-card border border-border rounded-lg shadow-md p-3 z-50`), default anchored `bottom-6 right-6`. Use `@dnd-kit` `DndContext` + `useDraggable`; whole header bar = drag handle (`cursor-grab`, `min-h-11`, `GripVerticalIcon` at `opacity-60`); persist `{x,y}` to `localStorage` on `DragEnd`, clamp to viewport on load. Elapsed readout `text-base font-semibold tabular-nums` formatted `mm:ss` / `h:mm:ss`. Running: primary pinging dot (reuse StatusBadge treatment), honor `prefers-reduced-motion`. Paused: "Pausado" `Badge variant="outline" text-muted-foreground`, static dot, frozen elapsed. Ended: "Encerrado" chip, final elapsed, controls hidden. One toggle `Button variant="ghost" size="icon" min-h-11 min-w-11` with `PauseIcon`/`PlayIcon`, aria-labels "Pausar consulta"/"Retomar consulta", calling `pauseConsultationAction`/`resumeConsultationAction` from `@/actions`; refresh via `useRouter().refresh()` after the action resolves. All copy verbatim per UI-SPEC §Timer copy.
    `components/dashboard/cases/case-detail-content.tsx`: mount `<ConsultationTimerWidget ... />` passing `started_at`, `ended_at`, `consultation_paused_ms`, `consultation_paused_at`, and `caseId`. Place it at the case-detail level (sibling of the main stack) so it floats over the page. Do NOT modify the header age render (Plan 04 owns it).
  </action>
  <verify>
    <automated>grep -q "useConsultationTimer" hooks/use-consultation-timer.ts && grep -q "computeElapsedMs" hooks/use-consultation-timer.ts && grep -q "useDraggable" components/dashboard/cases/consultation-timer-widget.tsx && grep -q "Pausar consulta" components/dashboard/cases/consultation-timer-widget.tsx && grep -q "ConsultationTimerWidget" components/dashboard/cases/case-detail-content.tsx && yarn typecheck && yarn lint && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/use-consultation-timer.ts` exports `useConsultationTimer`, wraps `computeElapsedMs`, and `setInterval` only repaints (no incrementing counter)
    - `consultation-timer-widget.tsx` uses `useDraggable`, persists position to localStorage, has running/paused/ended states with the verbatim UI-SPEC copy, and 44px touch targets
    - `case-detail-content.tsx` mounts `ConsultationTimerWidget` with started_at/ended_at/pause fields/caseId
    - `yarn typecheck`, `yarn lint`, `yarn test` exit 0
  </acceptance_criteria>
  <done>Live, draggable, pausable timer renders in the case detail; elapsed is timestamp-derived and survives reload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the timer end-to-end (live, reload, pause, drag)</name>
  <what-built>A floating draggable consultation timer auto-started on case open, with pause/resume persisted to the cases pause columns and elapsed computed from started_at.</what-built>
  <how-to-verify>
    Run `yarn dev`, open an active case, then:
    1. CONS-02: confirm the timer is already counting up live on open (no "Iniciar" button — D-02 auto-start), seconds ticking.
    2. CONS-03: note the elapsed value, refresh the page (F5) -> elapsed continues from the correct value, not reset to 0. Navigate away and back -> still correct.
    3. D-03 pause: click "Pausar consulta" -> elapsed freezes, "Pausado" chip shows, dot static. Wait ~30s, refresh -> still frozen (pause state survived reload). Click "Retomar consulta" -> elapsed resumes WITHOUT counting the paused interval (the ~30s paused is excluded).
    4. D-04 drag: drag the widget by its handle to another corner -> it moves; refresh -> it stays where you dropped it (localStorage), clamped inside the viewport.
    5. Close the case -> "Encerrado" chip, final elapsed, pause/resume controls gone.
    6. Reduced motion: with OS reduce-motion on, the running dot does not ping (static).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any drift/reset/pause-accounting/drag-persistence issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → pause/resume action → DB | Mutates durable consultation state on `cases` |
| browser localStorage | Stores only widget x/y position (no PII) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Elevation of Privilege | pause/resume actions | mitigate | Both actions call `getAuthenticatedUser` + gate `profile.status === "paid"`; modules resolve `user_phone` from `authenticated_users` and scope every read/write by it (never by case id alone), inheriting cases RLS (20260604000003). |
| T-01-13 | Tampering | pause accumulator write | mitigate | resume reads the current row (scoped by user_phone) before accumulating, so a forged caseId for another tenant returns no row and the write no-ops/throws; elapsed clamps at 0 to resist negative-time tampering. |
| T-01-14 | Information Disclosure | localStorage widget position | accept | Stores only `{x,y}` coordinates per device; no patient/case data. Acceptable (A5). |
| T-01-SC | Tampering | npm installs | mitigate | No new packages — `@dnd-kit/core@6.3.1` already installed (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `computeElapsedMs` spec green (running/paused/ended/reload/accumulator).
- Pause/resume actions gated + ownership-scoped (grep + typecheck + lint).
- Hook wraps the pure helper; widget draggable with correct states (grep + lint).
- Human gate confirms live tick, reload survival, pause accounting, drag persistence (Task 4).
</verification>

<success_criteria>
- CONS-02: live timer counting on case open (auto-start).
- CONS-03: elapsed survives reload/navigation (timestamp-derived).
- D-03/D-04: pause/resume correct and persisted; widget draggable + position persisted.
</success_criteria>

<output>
Create `.planning/phases/01-experi-ncia-da-consulta/01-05-SUMMARY.md` when done.
</output>
