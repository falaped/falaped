---
phase: 01-experi-ncia-da-consulta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260628000000_patients_add_gestational_age.sql
  - supabase/migrations/20260628000100_cases_add_consultation_pause.sql
  - modules/cases/create-dashboard-case-with-patient.ts
autonomous: false
requirements: [CONS-01, CONS-02, CONS-03]
must_haves:
  truths:
    - "patients table has a nullable gestational_age_weeks column"
    - "cases table has consultation_paused_ms (default 0) and consultation_paused_at columns"
    - "cases.started_at has a DB default of now() AND is set explicitly on dashboard case creation (D-02 auto-start anchor)"
    - "schema changes are pushed to the live database so type/build checks reflect real columns"
  artifacts:
    - path: "supabase/migrations/20260628000000_patients_add_gestational_age.sql"
      provides: "Additive nullable gestational_age_weeks column on patients"
      contains: "gestational_age_weeks"
    - path: "supabase/migrations/20260628000100_cases_add_consultation_pause.sql"
      provides: "consultation_paused_ms + consultation_paused_at columns and started_at default"
      contains: "consultation_paused"
    - path: "modules/cases/create-dashboard-case-with-patient.ts"
      provides: "Explicit started_at write on case insert (auto-start anchor)"
      contains: "started_at"
  key_links:
    - from: "supabase/migrations"
      to: "live database"
      via: "supabase db push"
      pattern: "supabase db push"
---

<objective>
Lay the persistent-state foundation for the consultation experience: add the gestational-age column on `patients` (corrected-age input, D-10), add the pause accumulator columns on `cases` (D-03), all reusing the existing `cases.started_at`/`ended_at` rather than a standalone timer (D-01), guarantee the timer anchor exists by giving `cases.started_at` a `now()` default and writing it explicitly on dashboard case creation (D-02 auto-start, A2), then push the schema so downstream type/build checks reflect real columns.

Purpose: Two additive, idempotent migrations + one auto-start write are the durable substrate that the age display slice (Plan 04) and the timer slice (Plan 05) consume. Pushing the schema here prevents a false-positive verification state where TypeScript passes against config-derived types while the live DB lacks the columns.
Output: Two new migration files, an explicit `started_at` insert value in `createDashboardCaseWithPatient`, and a pushed live schema.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experi-ncia-da-consulta/01-CONTEXT.md
@.planning/phases/01-experi-ncia-da-consulta/01-RESEARCH.md
@.planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write both additive migrations + auto-start anchor write</name>
  <files>supabase/migrations/20260628000000_patients_add_gestational_age.sql, supabase/migrations/20260628000100_cases_add_consultation_pause.sql, modules/cases/create-dashboard-case-with-patient.ts</files>
  <read_first>
    - supabase/migrations/20260325140000_cases_add_assistant_turn_queue.sql (additive `add column if not exists` style — the exact pattern to copy)
    - supabase/migrations/20260327120000_patients_sex_enum.sql (line ~20: the `comment on column` idiom)
    - supabase/migrations/20260604000002_rls_patients.sql + 20260604000003_rls_cases.sql (confirm RLS is table-level so additive nullable columns inherit it — no new policy needed)
    - modules/cases/create-dashboard-case-with-patient.ts (the `.insert({ ... })` block at lines 67-81 does NOT set `started_at` — A2)
    - .planning/phases/01-experi-ncia-da-consulta/01-PATTERNS.md §"Migration style" and §"supabase/migrations/<ts>_cases_add_consultation_pause.sql" (accumulator model + A2 note)
  </read_first>
  <action>
    Create `supabase/migrations/20260628000000_patients_add_gestational_age.sql`: `alter table public.patients add column if not exists gestational_age_weeks integer;` plus a `comment on column public.patients.gestational_age_weeks is 'Idade gestacional ao nascer em semanas (20–42). NULL quando desconhecida; usada para idade corrigida de prematuros.';`. Nullable, no backfill, no constraint (range is enforced at the Zod boundary in Plan 04, not the DB, to keep the column purely additive).

    Create `supabase/migrations/20260628000100_cases_add_consultation_pause.sql` with three idempotent statements: (a) `alter table public.cases add column if not exists consultation_paused_ms bigint not null default 0;` (b) `add column if not exists consultation_paused_at timestamptz;` (c) `alter table public.cases alter column started_at set default now();` — the accumulator pause model (D-03 per PATTERNS §accumulator) plus the A2 anchor default. Add `comment on column` lines explaining `consultation_paused_ms` = total accumulated paused milliseconds and `consultation_paused_at` non-null ⇒ currently paused. Use `set default now()` (not `add column`) because `started_at` already exists on the live table; guard nothing else — setting a default never fails on an existing column.

    In `modules/cases/create-dashboard-case-with-patient.ts`, add `started_at: new Date().toISOString()` to the `.insert({ ... })` object (lines 67-81), so the auto-start anchor (D-02) is set explicitly on dashboard case creation regardless of the DB default. This is the belt-and-suspenders for A2: the default covers any other insert path; the explicit write makes dashboard auto-start deterministic. Do not change any ownership filter or other field.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260628000000_patients_add_gestational_age.sql && test -f supabase/migrations/20260628000100_cases_add_consultation_pause.sql && grep -q "gestational_age_weeks" supabase/migrations/20260628000000_patients_add_gestational_age.sql && grep -q "consultation_paused_ms" supabase/migrations/20260628000100_cases_add_consultation_pause.sql && grep -q "set default now()" supabase/migrations/20260628000100_cases_add_consultation_pause.sql && grep -q "started_at" modules/cases/create-dashboard-case-with-patient.ts && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260628000000_patients_add_gestational_age.sql` contains `add column if not exists gestational_age_weeks integer`
    - `supabase/migrations/20260628000100_cases_add_consultation_pause.sql` contains `consultation_paused_ms bigint not null default 0`, `consultation_paused_at timestamptz`, and `alter column started_at set default now()`
    - `modules/cases/create-dashboard-case-with-patient.ts` `.insert` object includes `started_at: new Date().toISOString()`
    - `yarn typecheck` exits 0
  </acceptance_criteria>
  <done>Both migration files exist with additive idempotent statements; the create-case insert sets started_at explicitly; typecheck is green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push schema to the live database</name>
  <what-built>Two additive migrations (patients.gestational_age_weeks; cases.consultation_paused_ms + consultation_paused_at + started_at default) ready to apply.</what-built>
  <how-to-verify>
    Run `supabase db push` from the repo root (the project is linked; `SUPABASE_ACCESS_TOKEN` is set in this environment). This applies the two new migrations to the live Supabase project. If the push prompts for interactive confirmation that cannot be suppressed in non-TTY, run it in your own terminal and paste the result. After it succeeds:
    1. Confirm output lists both `20260628000000_patients_add_gestational_age` and `20260628000100_cases_add_consultation_pause` as applied.
    2. (Optional sanity) In the Supabase dashboard → Table editor, confirm `patients.gestational_age_weeks` and `cases.consultation_paused_ms` / `consultation_paused_at` exist and `cases.started_at` shows default `now()`.
    This task is mandatory and blocking — build/type checks pass from config-derived types even if the live DB lacks columns, so verification would be a false positive without this push.
  </how-to-verify>
  <resume-signal>Type "applied" once `supabase db push` reports both migrations applied, or paste the error if it failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → action → DB | Patient/case writes cross here; gestational age is child-health PII |
| migration → live DB | Schema change applied with elevated credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | patients.gestational_age_weeks (child PII) | mitigate | Additive column inherits existing table-level RLS on `patients` (20260604000002); no new policy and no public exposure. All reads/writes remain scoped by `profile_id` in the existing module layer. |
| T-01-02 | Tampering | cases pause columns | mitigate | Columns inherit `cases` RLS (20260604000003, dual profile_id/user_phone anchor); writes only via Plan 05 actions that scope by user_phone, never by id alone. |
| T-01-03 | Denial of Service | started_at default change | accept | `alter column set default` is metadata-only, no table rewrite, no lock contention on this size. Additive + reversible. |
| T-01-SC | Tampering | npm/supabase installs | mitigate | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new deps). `supabase db push` uses the already-installed pinned CLI. |
</threat_model>

<verification>
- Both migration files present and grep-match the expected column statements.
- `yarn typecheck` green.
- `supabase db push` applied both migrations to the live project (Task 2 human confirmation).
</verification>

<success_criteria>
- patients.gestational_age_weeks exists (nullable) on the live DB.
- cases.consultation_paused_ms (default 0) and consultation_paused_at exist; cases.started_at defaults to now().
- Dashboard case creation writes started_at explicitly (D-02 anchor guaranteed).
</success_criteria>

<output>
Create `.planning/phases/01-experi-ncia-da-consulta/01-01-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces (all 5 plans)

Newly-created symbols/files — the drift-verification pass MUST treat these as introduced by Phase 1, not pre-existing drift.

**New functions / exports:**
- `computePediatricAge` (lib/compute-pediatric-age.ts) + types `PediatricAge`, `AgeBand`, `PediatricAgeStatus`
- `formatPediatricAge`, `formatPediatricAgeAbbrev` (lib/format-pediatric-age.ts)
- `computeElapsedMs` (lib/compute-elapsed-ms.ts)
- `useConsultationTimer` (hooks/use-consultation-timer.ts)
- `pauseConsultation`, `resumeConsultation` (modules/cases/*)
- `pauseConsultationAction`, `resumeConsultationAction` (actions/cases/*)
- Named constants `FULL_TERM_GESTATIONAL_WEEKS`, `CORRECTED_AGE_CUTOFF_MONTHS` (lib/compute-pediatric-age.ts)

**New components:**
- `ConsultationTimerWidget` (components/dashboard/cases/consultation-timer-widget.tsx)
- `PatientFormGestationalAgeField` (components/dashboard/patients/patient-form/patient-form-gestational-age-field.tsx)

**New DB columns:**
- `patients.gestational_age_weeks` (integer, nullable)
- `cases.consultation_paused_ms` (bigint, default 0)
- `cases.consultation_paused_at` (timestamptz, nullable)
- `cases.started_at` default set to `now()` (existing column, default added)

**New files (non-symbol):**
- supabase/migrations/20260628000000_patients_add_gestational_age.sql
- supabase/migrations/20260628000100_cases_add_consultation_pause.sql
- scripts/repro-report-pdf.ts
- *.spec.ts: lib/compute-pediatric-age.spec.ts, lib/format-pediatric-age.spec.ts, lib/compute-elapsed-ms.spec.ts

**Repurposed (not new, behavior changed):**
- `formatAgeFromBirthDate` (modules/falaped-assistant/lib/formatters.ts) → thin adapter over computePediatricAge
- gestational_age_weeks added to Patient / CasePatientDetail types and PATIENT_SELECT / get-case-by-id selects
- consultation_paused_* added to CaseWithPatient / CaseDetail types and the cases select
</artifacts_this_phase_produces>
