---
phase: 04-documentos-cl-nicos-novos
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - app/dashboard/prescriptions/new/page.tsx
  - app/dashboard/prescriptions/new/prescription-wizard-wrapper.tsx
  - components/dashboard/prescriptions/prescription-wizard.tsx
  - components/app-sidebar.tsx
autonomous: false
requirements: [DOC-05]

must_haves:
  truths:
    - "O médico abre um receituário em branco (mesmo layout de receita, corpo/medicamentos vazios) e gera o PDF sem a exigência de pelo menos um medicamento — para colar receitas prontas (DOC-05 / D-14)"
    - "É um MODO da receita existente, não um tipo de documento novo — parity visual garantida por reuso do componente"
  artifacts:
    - "app/dashboard/prescriptions/new/page.tsx aceita ?mode=blank e o thread para o wizard"
    - "components/dashboard/prescriptions/prescription-wizard.tsx com prop blankMode"
  key_links:
    - "blankMode=true → pula o guard de min-1-medicamento no handleGenerate, inicia com medicamentos vazios, CTA 'Gerar receituário', toast 'Receituário gerado. Download iniciado.'"
    - "app-sidebar.tsx grupo Serviços ganha Receituário em branco → /dashboard/prescriptions/new?mode=blank"
---

## Phase Goal

**As a** pediatra, **I want to** gerar um receituário em branco (layout de receita, corpo vazio), **so that** eu cole receitas prontas que já mantenho, com o mesmo cabeçalho/rodapé das minhas receitas.

<objective>
Entregar o **receituário em branco (DOC-05)** como um **modo "em branco/vazio" do fluxo de receita existente** (D-14) — NÃO um tipo de documento novo. Edição in-place mínima: um flag `blankMode` no `prescription-wizard`, passado via `?mode=blank` na rota de nova receita, que pula o guard de mínimo-1-medicamento, inicia com medicamentos/corpo vazios, troca o rótulo do CTA e o toast de sucesso. Menos código novo, casamento visual garantido pelo reuso do componente.

Purpose: Fecha DOC-05 com o menor delta possível, reusando toda a stack de receitas (payload, PDF, storage, escopo profile_id — herdados).
Output: Entrada "Receituário em branco" que abre a receita em modo vazio e gera o PDF.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentos-cl-nicos-novos/04-CONTEXT.md
@.planning/phases/04-documentos-cl-nicos-novos/04-PATTERNS.md
@.planning/phases/04-documentos-cl-nicos-novos/04-UI-SPEC.md

# Arquivos a editar in-place
@app/dashboard/prescriptions/new/page.tsx
@app/dashboard/prescriptions/new/prescription-wizard-wrapper.tsx
@components/dashboard/prescriptions/prescription-wizard.tsx
@components/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar blankMode ao prescription-wizard (guard/estado/CTA/toast)</name>
  <files>components/dashboard/prescriptions/prescription-wizard.tsx</files>
  <read_first>
    components/dashboard/prescriptions/prescription-wizard.tsx (state machine lines 113-132, buildPayload 252-269, handleGenerate 315-347 incl. o guard de min-1-medicamento na ~linha 317, CTA e toast de sucesso);
    04-PATTERNS.md §"DOC-05 delta"; 04-UI-SPEC.md §"DOC-05 Receituário em branco"
  </read_first>
  <action>
    Adicionar uma prop opcional `blankMode?: boolean` (default false) ao PrescriptionWizard. Quando `blankMode` for true:
    - NÃO exigir pelo menos um medicamento no handleGenerate — pular o guard de mínimo-1-medicamento (o guard que hoje bloqueia geração sem medicamentos) para que um receituário vazio possa ser gerado.
    - Iniciar o estado com a lista de medicamentos vazia e corpo vazio (não pré-popular nada).
    - Trocar o rótulo do CTA de geração para "Gerar receituário" (mantendo o busy "Gerando…").
    - Trocar o toast de sucesso para "Receituário gerado. Download iniciado.".
    Quando `blankMode` for false, o comportamento atual permanece intacto (não regredir a receita normal). Não introduzir um novo tipo de documento nem nova action — reusar generatePrescriptionAction e todo o pipeline existente (payload/PDF/storage/escopo profile_id herdados). Manter PT-BR e o estilo/variantes de botão existentes (parity visual — D-14).
  </action>
  <verify>
    <automated>yarn tsc --noEmit && yarn eslint components/dashboard/prescriptions/prescription-wizard.tsx --max-warnings=0 && grep -q "blankMode" components/dashboard/prescriptions/prescription-wizard.tsx && grep -q "Gerar receituário" components/dashboard/prescriptions/prescription-wizard.tsx</automated>
  </verify>
  <done>Prop blankMode existe; em modo blank o guard de min-1-medicamento é pulado, estado inicia vazio, CTA "Gerar receituário", toast "Receituário gerado. Download iniciado."; modo normal inalterado.</done>
</task>

<task type="auto">
  <name>Task 2: Thread ?mode=blank pela rota/wrapper e item na sidebar</name>
  <files>app/dashboard/prescriptions/new/page.tsx, app/dashboard/prescriptions/new/prescription-wizard-wrapper.tsx, components/app-sidebar.tsx</files>
  <read_first>
    app/dashboard/prescriptions/new/page.tsx (searchParams type + PrescriptionWizardWrapper props), app/dashboard/prescriptions/new/prescription-wizard-wrapper.tsx (props → PrescriptionWizard);
    components/app-sidebar.tsx (grupo Serviços)
  </read_first>
  <action>
    Em new/page.tsx: estender o tipo de searchParams com `mode?: string`; após await searchParams, derivar `blankMode = resolved.mode === "blank"`; passar `blankMode` como prop ao PrescriptionWizardWrapper. Opcionalmente ajustar o subtítulo quando blankMode (ex.: manter genérico; não obrigatório). Não quebrar os params existentes (templateId/patientId/caseId).
    Em prescription-wizard-wrapper.tsx: adicionar `blankMode?: boolean` às props e repassá-lo a <PrescriptionWizard blankMode={blankMode} />. Manter a lógica de key/_t inalterada.
    Em components/app-sidebar.tsx: no grupo "Serviços" adicionar { title: "Receituário em branco", url: "/dashboard/prescriptions/new?mode=blank" } — APENAS append; preservar os itens adicionados pelas fatias 04-01/04-02/04-03/04-04.
  </action>
  <verify>
    <automated>yarn tsc --noEmit && yarn eslint app/dashboard/prescriptions/new components/app-sidebar.tsx --max-warnings=0 && grep -q "mode=blank" components/app-sidebar.tsx && grep -q "blankMode" app/dashboard/prescriptions/new/page.tsx && grep -q "blankMode" app/dashboard/prescriptions/new/prescription-wizard-wrapper.tsx</automated>
  </verify>
  <done>typecheck+lint limpos; ?mode=blank derivado na rota e threadado via wrapper ao wizard; item "Receituário em branco" na sidebar (itens anteriores preservados); params existentes intactos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verificar receituário em branco (modo vazio) e não-regressão da receita normal</name>
  <action>Verificar manualmente o modo em branco (?mode=blank) gerando um receituário vazio e confirmando que a receita normal segue exigindo pelo menos um medicamento. Sem migrations novas — reusa a stack de receitas.</action>
  <what-built>Receituário em branco como modo vazio da receita existente (?mode=blank), sem migrations novas (reusa a stack de receitas). Verificação de comportamento visual do modo vazio.</what-built>
  <how-to-verify>
    1. Abrir /dashboard/prescriptions/new?mode=blank (ou clicar "Receituário em branco" na sidebar).
    2. Confirmar que o wizard abre sem medicamentos e permite gerar o PDF sem adicionar medicamento (guard pulado); CTA lê "Gerar receituário".
    3. Gerar e confirmar que o PDF baixa no mesmo layout de receita, corpo vazio, sem página em branco extra; toast "Receituário gerado. Download iniciado.".
    4. Abrir /dashboard/prescriptions/new (modo normal) e confirmar que a receita normal continua exigindo pelo menos um medicamento (sem regressão).
  </how-to-verify>
  <resume-signal>Digite "approved" quando o receituário em branco gerar PDF vazio corretamente e a receita normal seguir inalterada, ou descreva problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action | payload de receita (agora possivelmente vazio) cruza aqui — reusa generatePrescriptionAction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Elevation of Privilege | generatePrescriptionAction (reuso) | high | mitigate | herda o escopo profile_id + gate da stack de receitas existente; blankMode não altera auth/escopo — apenas pula o guard de UI de min-1-medicamento |
| T-04-24 | Tampering | receita normal regredida pelo flag | medium | mitigate | blankMode default false; caminho normal inalterado; checkpoint verifica que a receita normal ainda exige medicamento |
</threat_model>

<verification>
- `yarn tsc --noEmit` + `yarn eslint` limpos.
- Manual: ?mode=blank gera PDF vazio sem exigir medicamento (CTA "Gerar receituário"); receita normal inalterada (ainda exige medicamento).
- Item "Receituário em branco" na sidebar.
</verification>

<success_criteria>
- DOC-05 entregue: receituário em branco como modo vazio do fluxo de receita existente (D-14), layout de receita, corpo vazio, PDF sem página em branco extra.
- Nenhum tipo de documento novo, nenhuma migration nova — reuso total da stack de receitas.
- Receita normal sem regressão. Item na sidebar (D-12).
</success_criteria>

<output>
Create `.planning/phases/04-documentos-cl-nicos-novos/04-05-SUMMARY.md` when done.
</output>
