---
phase: 10-livro-caixa-de-ganhos-painel
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - modules/profiles/types.ts
  - modules/profiles/update-profile.ts
  - modules/supabase/get-authenticated-user.ts
  - lib/schemas/profile.ts
  - actions/profile/update-profile.ts
  - lib/schemas/procedure-catalog-item.ts
  - modules/procedure-catalog/types.ts
  - modules/procedure-catalog/list-procedure-catalog-items.ts
  - modules/procedure-catalog/create-procedure-catalog-item.ts
  - modules/procedure-catalog/update-procedure-catalog-item.ts
  - modules/procedure-catalog/delete-procedure-catalog-item.ts
  - modules/procedure-catalog/delete-procedure-catalog-item.spec.ts
  - actions/procedure-catalog/create-procedure-catalog-item.ts
  - actions/procedure-catalog/update-procedure-catalog-item.ts
  - actions/procedure-catalog/delete-procedure-catalog-item.ts
  - actions/procedure-catalog/index.ts
  - actions/index.ts
  - app/dashboard/profile/page.tsx
  - app/dashboard/profile/profile-content.tsx
  - components/dashboard/profile/procedure-catalog-card.tsx
autonomous: false
requirements: [EARN-01]

must_haves:
  truths:
    # --- UI-SPEC ## UI Considerations — as 5 rows `covered` de S5 ---
    - "S5 empty: com o catálogo vazio, a mensagem `Nenhum procedimento cadastrado.` aparece DENTRO da borda da lista e a linha de adicionar continua visível (empty state auto-resolvível). Com `consultation_price_cents` nulo, o campo abre vazio com placeholder e hint — nada pré-preenche e nada faz default para zero."
    - "S5 loading: no submit o primário fica `disabled` e o label vira `Adicionando…` (linha de adicionar) ou o rótulo de salvar procedimento (linha em edição)."
    - "S5 error: `FieldError` por campo, com as strings exatas do contrato de moeda; em preço de catálogo o valor zero É aceito e só o negativo é rejeitado, com `O preço não pode ser negativo.`"
    - "S5 populated: a lista tem borda com divisores; a linha em edição acontece NO LUGAR, com os botões `Salvar procedimento` e `Descartar edição`; a remoção fica atrás de um diálogo de confirmação cuja cópia tranquiliza que o snapshot protege o histórico."
    - "S5 partial: a edição no lugar abre em UMA linha enquanto as outras seguem em repouso; o botão `Descartar edição` existe justamente porque toque não tem tecla Esc."
    - statement: "S5 overflow: com muitos procedimentos no `Card` do Perfil, decidir no checkpoint visual se a lista precisa de cap de altura com scroll."
      verification: backstop
    - statement: "S5 zero-one-many: onde aparecer uma contagem de procedimentos, ela concorda em número — `1 procedimento` no singular e `N procedimentos` no plural."
      verification: backstop
    - statement: "S5 long-text: com um nome de procedimento longo, tanto na linha em repouso quanto na linha em edição, o preço e os botões não são empurrados para fora da linha."
      verification: backstop
    # --- O landmine e a travessia da coluna nova ---
    - "A string de `.select()` hardcoded de `modules/supabase/get-authenticated-user.ts` inclui `consultation_price_cents`. Sem isso o valor volta undefined em runtime, sem erro em lugar nenhum, e o diálogo de encerramento abriria com o campo vazio para sempre."
    - "`consultation_price_cents` atravessa as CINCO camadas: a coluna no banco (10-01), o tipo em `modules/profiles/types.ts`, o payload em `modules/profiles/update-profile.ts`, o schema em `lib/schemas/profile.ts`, o mapeamento campo-a-campo em `actions/profile/update-profile.ts` e a string de select de `get-authenticated-user.ts`."
    - "`actions/profile/update-profile.ts` continua SEM gate de assinatura — Perfil é onde o usuário não-pago conclui a conta. Os três actions NOVOS do catálogo TÊM o gate de assinatura."
    - "As três mutações de catálogo filtram por `id` E por `profile_id` no módulo, nunca por `id` sozinho, e `delete-procedure-catalog-item.spec.ts` afirma isso com um mock que grava as chamadas de filtro."
    - "O componente de catálogo tem estado próprio e actions próprios — NÃO entra no `useForm` do perfil (ciclo de vida diferente, gate diferente, actions diferentes)."
    - "Cada mutação de catálogo é seguida de `revalidatePath(\"/dashboard/profile\")` no action e de `router.refresh()` no cliente."
    - "O card novo `Preços` fica entre `Informações do perfil` e `Logos`, e o bloco de procedimentos vive DENTRO dele como bloco titulado (um nível de profundidade só, sem `Card` aninhado)."
  artifacts:
    - lib/schemas/procedure-catalog-item.ts
    - modules/procedure-catalog/types.ts
    - modules/procedure-catalog/list-procedure-catalog-items.ts
    - modules/procedure-catalog/create-procedure-catalog-item.ts
    - modules/procedure-catalog/update-procedure-catalog-item.ts
    - modules/procedure-catalog/delete-procedure-catalog-item.ts
    - modules/procedure-catalog/delete-procedure-catalog-item.spec.ts
    - actions/procedure-catalog/create-procedure-catalog-item.ts
    - actions/procedure-catalog/update-procedure-catalog-item.ts
    - actions/procedure-catalog/delete-procedure-catalog-item.ts
    - actions/procedure-catalog/index.ts
    - components/dashboard/profile/procedure-catalog-card.tsx
  key_links:
    - "`profiles.consultation_price_cents` -> string de select de `get-authenticated-user.ts` -> `AuthenticatedUserProfile` -> pré-preenchimento do diálogo de encerramento (10-04)"
    - "`procedure_catalog_items.price_cents` -> `listProcedureCatalogItems` -> pré-preenchimento de cada linha de procedimento (10-04) -> copiado por snapshot em `financial_entries.amount_cents`"
    - "`app/dashboard/profile/page.tsx` carrega `listProcedureCatalogItems` junto do carregamento de templates já existente"
  prohibitions:
    - "Nenhum gate de assinatura adicionado a `actions/profile/update-profile.ts` — adicionar ali é regressão do fluxo de onboarding do usuário não-pago."
    - "Nenhuma mutação de catálogo filtrando só por id — sempre id junto de profile_id."
    - "Nenhum `Card` aninhado dentro do card `Preços` — o bloco de procedimentos é um bloco titulado."
    - "Nenhum campo de preço pré-preenchido com zero quando o valor no banco é nulo."
    - "Nenhum rótulo de botão genérico na linha de catálogo — os rótulos são `Salvar procedimento` e `Descartar edição`; a palavra `Cancelar` fica reservada para os diálogos de confirmação."
    - "Nenhuma reordenação por arraste, detecção de duplicado, importação em lote ou habilitar/desabilitar por item — nada disso foi pedido."
    - "Nenhum `await` de action em componente cliente sem `router.refresh()` em seguida."
    - "Nenhum valor monetário abreviado; nenhum hex nem rgb em componente; nenhum peso de fonte 700."
    - "Nenhum arredondamento de `gap-1.5`/`py-1.5` (6px) nem de `size-7` (28px) para a escala de 4px."
---

<objective>
O pré-requisito do gatilho: o médico cadastra o valor da consulta e um catálogo de procedimentos com
preço, no Perfil. Sem isso o diálogo de encerramento não tem o que oferecer.

Purpose: DV-3 acrescentou esta superfície ao roadmap (dois blocos novos de tela + uma tabela). Ela
vem ANTES do diálogo de encerramento porque é a fonte dos valores que ele pré-preenche. E carrega o
esquecimento mais provável da fase inteira — a string de `.select()` hardcoded de
`get-authenticated-user.ts`, que faz o preço "salvar, sumir no reload e não dar erro em lugar nenhum".

Output: o card `Preços` no Perfil com o campo de valor da consulta ligado ao form existente, o editor
de catálogo com add/edit/remove, quatro módulos + três actions owner-scoped, e um spec de ownership.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-livro-caixa-de-ganhos-painel/10-UI-SPEC.md
@.planning/phases/10-livro-caixa-de-ganhos-painel/10-PATTERNS.md
@.planning/phases/10-livro-caixa-de-ganhos-painel/10-02-SUMMARY.md
</context>

## Artifacts this phase produces (plano 10-03)

**Arquivos novos:** `lib/schemas/procedure-catalog-item.ts`, `modules/procedure-catalog/types.ts`,
`modules/procedure-catalog/list-procedure-catalog-items.ts`,
`modules/procedure-catalog/create-procedure-catalog-item.ts`,
`modules/procedure-catalog/update-procedure-catalog-item.ts`,
`modules/procedure-catalog/delete-procedure-catalog-item.ts`,
`modules/procedure-catalog/delete-procedure-catalog-item.spec.ts`,
`actions/procedure-catalog/{create,update,delete}-procedure-catalog-item.ts`,
`actions/procedure-catalog/index.ts`, `components/dashboard/profile/procedure-catalog-card.tsx`

**Funções/valores exportados novos:** `ProcedureCatalogItem`, `procedureCatalogItemSchema`,
`ProcedureCatalogItemFormData`, `listProcedureCatalogItems`, `createProcedureCatalogItem`,
`updateProcedureCatalogItem`, `deleteProcedureCatalogItem`, `createProcedureCatalogItemAction`,
`updateProcedureCatalogItemAction`, `deleteProcedureCatalogItemAction` (cada action com seu
tipo `…Result`), `ProcedureCatalogCard`

**Símbolo novo em tipo existente:** `Profile.consultation_price_cents` (em `modules/profiles/types.ts`)
e `UpdateProfilePayload.consultation_price_cents` (em `modules/profiles/update-profile.ts`)

**Arquivos editados:** `modules/supabase/get-authenticated-user.ts` (string de select),
`lib/schemas/profile.ts`, `actions/profile/update-profile.ts`, `actions/index.ts`,
`app/dashboard/profile/page.tsx`, `app/dashboard/profile/profile-content.tsx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A travessia de consultation_price_cents pelas cinco camadas + o card Preços</name>
  <files>modules/profiles/types.ts, modules/supabase/get-authenticated-user.ts, modules/profiles/update-profile.ts, lib/schemas/profile.ts, actions/profile/update-profile.ts, app/dashboard/profile/profile-content.tsx</files>
  <read_first>
    - modules/supabase/get-authenticated-user.ts (LEIA O ARQUIVO INTEIRO — a string de `.select()` está nas linhas 36-42 e é uma STRING LITERAL de colunas; note também que a função devolve um objeto vazio castado em caso de falha nas linhas 34 e 46, então uma checagem de "perfil ausente" nunca dispara; e note que ESTE arquivo usa ponto-e-vírgula, ao contrário da maioria dos análogos da fase)
    - modules/profiles/types.ts (o tipo `Profile`, usado como `AuthenticatedUserProfile`)
    - modules/profiles/update-profile.ts (o tipo `UpdateProfilePayload` e a função de update)
    - lib/schemas/profile.ts (o `updateProfileFormSchema` — todos os campos são string com transform de vazio para undefined)
    - actions/profile/update-profile.ts (LEIA O ARQUIVO INTEIRO — o mapeamento de payload lista campo por campo, e o action NÃO tem gate de assinatura por design)
    - app/dashboard/profile/profile-content.tsx (a sequência de `Card`: `Informações do perfil` na linha ~267, `Logos` na ~549, `Aparência` na ~677, `Plano` na ~731 e a zona de perigo na ~777 — o card novo entra entre 548 e 549)
    - lib/money.ts e lib/formatters.ts (o contrato de moeda criado em 10-02: `parseBrlToCents` e `formatCentsToBRL`)
    - .planning/phases/10-livro-caixa-de-ganhos-painel/10-UI-SPEC.md § "S5 — Perfil" e § "Currency contract"
  </read_first>
  <behavior>
    - Salvar `250,00` no campo de valor da consulta e recarregar a página mostra `250,00` de volta no campo (não vazio) — é este teste que prova a string de select corrigida.
    - Deixar o campo de valor da consulta vazio e salvar grava nulo, não zero.
    - Digitar `-5` no campo de valor da consulta bloqueia o submit com a mensagem de preço negativo.
    - `updateProfileAction` continua funcionando para um perfil cujo status NÃO é pago (nenhum gate novo).
  </behavior>
  <action>
Fazer `consultation_price_cents` atravessar as cinco camadas de TypeScript. A coluna já existe no
banco (plano `10-01`); nada disso funciona se qualquer uma das cinco for esquecida, e a que
falha em SILÊNCIO é a primeira.

**1. `modules/supabase/get-authenticated-user.ts` — o landmine.** A chamada de `.select()` recebe uma
string literal com a lista de colunas de `profiles`. Acrescentar `consultation_price_cents` a essa
string, logo depois de `default_location_city`. **Se isso for esquecido, o valor volta `undefined` em
runtime, o TypeScript não reclama por causa do cast de tipo, e o único sintoma é o preço "salvar e
sumir no reload" sem nenhum erro.** Manter o estilo de ponto-e-vírgula deste arquivo.

**2. `modules/profiles/types.ts`.** Acrescentar `consultation_price_cents: number | null` ao tipo
`Profile`, na mesma posição relativa da string de select.

**3. `modules/profiles/update-profile.ts`.** Acrescentar `consultation_price_cents?: number | null`
ao tipo `UpdateProfilePayload`. A função em si não muda (ela faz spread do payload).

**4. `lib/schemas/profile.ts`.** Acrescentar `consultation_price_cents` ao
`updateProfileFormSchema` como campo de FORMULÁRIO — ou seja, uma string (o form manda string vazia
quando não está setado, é a convenção de todos os outros campos do schema). Transform: `trim`; string
vazia vira `undefined` (grava nulo, NÃO zero); senão passa por `parseBrlToCents`, e um retorno nulo
vira erro `Valor inválido. Use apenas números, ex.: 250,00.`; um valor negativo vira erro
`O preço não pode ser negativo.` O resultado é centavos inteiros. Zero É aceito aqui (é um preço, não
um lançamento).

**5. `actions/profile/update-profile.ts`.** Acrescentar `consultation_price_cents:
parsed.data.consultation_price_cents ?? null` ao objeto de payload — o mapeamento é campo a campo,
então um campo não listado simplesmente não é salvo. **NÃO adicionar gate de assinatura a este
action** — ele não tem por design, porque Perfil é onde o usuário não-pago conclui a conta;
adicionar é regressão de fluxo.

**6. `app/dashboard/profile/profile-content.tsx` — o card `Preços`.** Inserir um `Card` novo entre o
card `Logos` e o que vem antes dele, com a mesma forma dos vizinhos
(`Card` > `CardHeader` > `CardTitle` + `CardDescription`, depois `CardContent`). Título `Preços`,
descrição `Valores usados ao encerrar um caso. Você pode ajustar cada valor na hora do lançamento.`
Dentro do `CardContent`:
- Um `Field` com rótulo `Valor da consulta (R$)`, ligado ao `useForm` de perfil que já existe (o campo
  viaja em `updateProfileAction`), seguindo o contrato de moeda: `type="text"`, `inputMode="decimal"`,
  `autoComplete="off"`, `placeholder="ex.: 250,00"`, `className="tabular-nums"`. Largura CONTIDA
  (`max-w-xs`) — um campo de dinheiro com a largura do card lê como área de texto. `FieldDescription`:
  `Valor padrão usado ao encerrar um caso. Você pode ajustar na hora.` `FieldError` abaixo. O valor
  inicial vem do perfil já em centavos, formatado como decimal simples PT-BR sem prefixo de moeda (o
  prefixo vive no rótulo). **Quando o valor no banco é nulo, o campo abre VAZIO com só o placeholder
  — nunca pré-preenchido com zero, que seria submetido por inércia.**
- Um `FieldSeparator`, depois o bloco titulado de procedimentos: um `span` em
  `text-xs font-semibold uppercase tracking-wide text-muted-foreground` com a palavra `Procedimentos`,
  e abaixo o `<ProcedureCatalogCard>` da Task 2. **Sem `Card` aninhado** — um nível de profundidade só.
  </action>
  <verify>
    <automated>yarn typecheck &amp;&amp; yarn lint &amp;&amp; yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "consultation_price_cents" modules/supabase/get-authenticated-user.ts` retorna 1 ou mais, e a ocorrência está DENTRO da string literal passada para `.select(`.
    - `grep -c "consultation_price_cents" modules/profiles/types.ts` retorna 1.
    - `grep -c "consultation_price_cents" modules/profiles/update-profile.ts` retorna 1.
    - `grep -c "consultation_price_cents" lib/schemas/profile.ts` retorna 1 ou mais.
    - `grep -c "consultation_price_cents" actions/profile/update-profile.ts` retorna 1 ou mais.
    - `grep -c 'profile.status' actions/profile/update-profile.ts` retorna 0 — nenhum gate de assinatura foi adicionado.
    - `lib/schemas/profile.ts` contém, literalmente, a mensagem `O preço não pode ser negativo.`
    - `app/dashboard/profile/profile-content.tsx` contém `Preços`, `Valor da consulta (R$)`, `Valor padrão usado ao encerrar um caso. Você pode ajustar na hora.` e `max-w-xs`.
    - `yarn typecheck`, `yarn lint` e `yarn test` saem todos com 0.
    - Comportamento: salvar `250,00` no campo, recarregar `/dashboard/profile`, e o campo volta com `250,00` — não vazio.
    - Comportamento: limpar o campo e salvar grava nulo (o campo volta vazio no reload, não com zero).
  </acceptance_criteria>
  <done>`consultation_price_cents` atravessa as cinco camadas de TypeScript, incluindo a string de select hardcoded; o card `Preços` existe no Perfil com o campo de valor da consulta contido e sem default de zero; e o action de perfil continua sem gate de assinatura.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Catálogo de procedimentos — módulos owner-scoped, actions com gate e o editor no Perfil</name>
  <files>lib/schemas/procedure-catalog-item.ts, modules/procedure-catalog/types.ts, modules/procedure-catalog/list-procedure-catalog-items.ts, modules/procedure-catalog/create-procedure-catalog-item.ts, modules/procedure-catalog/update-procedure-catalog-item.ts, modules/procedure-catalog/delete-procedure-catalog-item.ts, modules/procedure-catalog/delete-procedure-catalog-item.spec.ts, actions/procedure-catalog/create-procedure-catalog-item.ts, actions/procedure-catalog/update-procedure-catalog-item.ts, actions/procedure-catalog/delete-procedure-catalog-item.ts, actions/procedure-catalog/index.ts, actions/index.ts, app/dashboard/profile/page.tsx, components/dashboard/profile/procedure-catalog-card.tsx</files>
  <read_first>
    - modules/exam-catalog/get-exam-catalog-items.ts (o arquivo INTEIRO, 23 linhas — é o molde 1:1 do módulo de listagem: cliente injetado primeiro, `profileId` segundo, JSDoc, `.eq("profile_id", …)`, `.order("name")`, throw com tag entre colchetes, e o cast final de array com fallback)
    - modules/patient-growth/delete-measurement.ts (o molde de mutação owner-scoped: o encadeamento de filtros e o comentário que explica por que NUNCA filtrar só por id)
    - modules/patient-growth/delete-measurement.spec.ts (o arquivo INTEIRO, 89 linhas — o mock de `SupabaseClient` que GRAVA as chamadas de filtro e afirma o escopo; copiar verbatim e trocar tabela, colunas e função)
    - actions/patient-growth/create-measurement.ts (o molde canônico de action: gate de autenticação, gate de assinatura, `safeParse`, chamada ao módulo, `revalidatePath`, result union, e o precedente de a conversão de unidade viver no action)
    - lib/schemas/appointment.ts (o molde de docblock PT-BR nomeando o boundary)
    - actions/index.ts (o formato dos blocos de reexport)
    - app/dashboard/profile/page.tsx (o carregamento existente de templates nas linhas 15-18 — o molde de onde acrescentar a carga do catálogo)
    - components/dashboard/profile/profile-content.tsx (a forma de `Card`, e o card da zona de perigo por volta da linha 777, que é o precedente de token dos blocos destrutivos)
    - components/dashboard/cases/case-detail-actions.tsx linhas 133-170 (o molde de diálogo de confirmação destrutivo, com o botão de confirmação sendo um `Button variant="destructive"` no rodapé em vez do primitivo de ação)
    - .planning/phases/10-livro-caixa-de-ganhos-painel/10-UI-SPEC.md § "Procedure catalog editor" (a tabela de estados de linha: repouso, edição no lugar, linha de adicionar) e § "Copywriting Contract"
  </read_first>
  <precondition>A tabela `public.procedure_catalog_items` está aplicada ao banco vivo (checkpoint de `10-01` resolvido). Sem isso todo action de catálogo falha em runtime.</precondition>
  <behavior>
    - `deleteProcedureCatalogItem` filtra a exclusão por `id` E por `profile_id` — o spec afirma as duas chamadas de filtro.
    - `updateProcedureCatalogItem` filtra o update por `id` E por `profile_id`.
    - `listProcedureCatalogItems` filtra por `profile_id` e ordena por nome.
    - Um action de catálogo com perfil cujo status não é pago devolve `{ ok: false }` com a mensagem de perfil não ativo.
    - Criar um item com nome só de espaços devolve `{ ok: false }` com a mensagem de nome obrigatório.
    - Criar um item com preço `0,00` é ACEITO (procedimento gratuito é catalogável); com `-1` é rejeitado.
  </behavior>
  <action>
**`lib/schemas/procedure-catalog-item.ts` (novo).** Docblock PT-BR nomeando o boundary e declarando
que `profile_id` é estampado server-side. Exportar `procedureCatalogItemSchema` com `name` (string
obrigatória, `trim`, não-vazia com mensagem `Informe o nome do procedimento.`, máximo 120 caracteres)
e `price` (string do form transformada por `parseBrlToCents`; vazio vira `Informe o valor.`;
inparseável vira `Valor inválido. Use apenas números, ex.: 250,00.`; negativo vira
`O preço não pode ser negativo.`; **zero é aceito**). Exportar `ProcedureCatalogItemFormData` como
`z.infer` e o tipo de input.

**`modules/procedure-catalog/types.ts`.** `ProcedureCatalogItem` com `id`, `profile_id`, `name`,
`price_cents`, `created_at`, `updated_at`.

**Os quatro módulos**, um por arquivo, espelhando `modules/exam-catalog/get-exam-catalog-items.ts`
na forma e usando a tag de domínio `[PROCEDURE_CATALOG]` nos throws:
- `list-procedure-catalog-items.ts` — `(supabase, profileId)`, `.select("id, name, price_cents")`,
  `.eq("profile_id", profileId)`, `.order("name", { ascending: true })`.
- `create-procedure-catalog-item.ts` — `(supabase, profileId, { name, priceCents })`, insert com
  `profile_id` estampado do argumento, `.select("id").single()`, devolve o id.
- `update-procedure-catalog-item.ts` — `(supabase, id, profileId, { name, priceCents })`,
  `.update(...).eq("id", id).eq("profile_id", profileId)`.
- `delete-procedure-catalog-item.ts` — `(supabase, id, profileId)`,
  `.delete().eq("id", id).eq("profile_id", profileId)`.
Os dois últimos levam JSDoc dizendo explicitamente que o duplo filtro é o backstop de posse contra
IDOR e que filtrar só por id é proibido. Nenhum módulo importa nada de cache nem de headers.

**`modules/procedure-catalog/delete-procedure-catalog-item.spec.ts` (novo, exigido por SC-4).**
Copiar o mock gravador de `modules/patient-growth/delete-measurement.spec.ts` verbatim, trocando o
nome da tabela e a função sob teste. Dois `test()` no mínimo: um afirmando que a exclusão inclui um
filtro por `profile_id` com o valor do perfil (a mensagem do assert deve dizer que filtrar só por id
é proibido) e um afirmando que inclui o filtro por `id`. Runner `node:test` + `assert/strict`, sem
framework e sem fixture.

**Os três actions** em `actions/procedure-catalog/`, cada um `"use server"`, na ordem canônica:
`createClient()`, `getAuthenticatedUser`, `{ ok: false, error: "Sessão não encontrada." }` sem
`profile?.id`, `{ ok: false, error: "Perfil não ativo. Conclua a configuração da conta em Perfil." }`
quando `profile.status !== "paid"` (estes actions NOVOS **têm** o gate, ao contrário do de perfil),
`safeParse` no boundary (o de delete valida só que o id é um uuid), chamada ao módulo passando
`profile.id`, `revalidatePath("/dashboard/profile")`, result union, `catch` com narrowing para
`Error` e fallback PT-BR. Barrel `actions/procedure-catalog/index.ts` reexportando as três funções e
os três tipos de resultado; e um bloco novo de reexport em `actions/index.ts`.

**`app/dashboard/profile/page.tsx`.** Acrescentar `listProcedureCatalogItems(supabase, profile.id)`
à carga já existente (no mesmo estilo do carregamento de templates) e passar o resultado para o
`profile-content`.

**`components/dashboard/profile/procedure-catalog-card.tsx` (novo, `"use client"`).** Estado próprio
e actions próprios — **não entra no `useForm` do perfil** (actions diferentes, gate diferente, ciclo
de vida diferente). Recebe a lista inicial como prop do RSC.

Layout: uma lista com borda (`rounded-lg border border-border divide-y divide-border`), uma linha por
item, e uma linha de adicionar persistente no fim.
- *Linha em repouso:* `px-4 py-3 flex items-center justify-between gap-3` — o nome em `text-sm`, o
  preço em `text-sm tabular-nums font-medium` via `formatCentsToBRL`, um botão fantasma de ícone de
  lápis com `aria-label="Editar procedimento"` e um botão fantasma de ícone de lixeira com
  `aria-label="Remover procedimento"` em `text-destructive`.
- *Linha em edição (no lugar):* a mesma linha troca para um `Input` de nome (`flex-1`) + um `Input` de
  moeda (`w-32`, contrato de moeda) + um botão `size="sm"` rotulado `Salvar procedimento` + um botão
  `size="sm" variant="ghost"` rotulado `Descartar edição`. **Apenas UMA linha editável por vez.** Esc
  também descarta, mas o botão de descartar **fica** — um usuário de toque não tem tecla Esc e
  ficaria preso na linha em edição. Os rótulos são esses e não os genéricos: a palavra `Cancelar`
  está reservada para os diálogos de confirmação, onde é a redação do primitivo.
- *Linha de adicionar (sempre visível, última):* `bg-muted/30` — `Input` com
  `placeholder="Nome do procedimento"` + `Input` de moeda com `placeholder="ex.: 120,00"` + botão
  `size="sm"` rotulado `Adicionar`. Limpa em sucesso e devolve o foco ao campo de nome, para uma
  segunda entrada rápida.
- *Vazio:* a mensagem `Nenhum procedimento cadastrado.` DENTRO da borda da lista, com o corpo
  `Cadastre os procedimentos que você cobra além da consulta (ex.: frenectomia, laserterapia).`, sem
  CTA (a linha de adicionar já está na tela). A linha de adicionar continua visível.
- *Remoção:* atrás de um diálogo de confirmação com título `Remover procedimento?`, descrição
  `"{nome}" sai do catálogo. Os lançamentos já registrados não mudam — eles guardaram o nome e o
  preço do momento da cobrança.` e confirmação `Remover` num `Button variant="destructive"` no
  rodapé. Essa descrição é a tranquilização que o snapshot merece; sem ela, apagar um item do
  catálogo parece apagar histórico.
- Cada mutação: primário `disabled` + label de pendência (`Adicionando…` / o rótulo de salvar) sob
  `useTransition`; em sucesso, `toast.success` com `Procedimento adicionado.` / `Procedimento
  atualizado.` / `Procedimento removido.` e **`router.refresh()`**; em falha,
  `toast.error(getFriendlyToastMessage(result.error))`.
- Nada de reordenação por arraste, nada de detecção de duplicado, nada de importação em lote, nada de
  habilitar/desabilitar por item — nenhuma dessas coisas foi pedida, e a tabela análoga do repo
  também não tem nenhuma.
  </action>
  <verify>
    <automated>yarn test 2>&amp;1 | tail -20 &amp;&amp; yarn typecheck &amp;&amp; yarn lint &amp;&amp; yarn build 2>&amp;1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test` sai com 0 e a saída inclui os testes de `modules/procedure-catalog/delete-procedure-catalog-item.spec.ts`.
    - `yarn typecheck`, `yarn lint` e `yarn build` saem todos com 0.
    - `grep -c 'eq("profile_id"' modules/procedure-catalog/delete-procedure-catalog-item.ts` retorna 1 e `grep -c 'eq("id"' …` retorna 1.
    - `grep -c 'eq("profile_id"' modules/procedure-catalog/update-procedure-catalog-item.ts` retorna 1 e `grep -c 'eq("id"' …` retorna 1.
    - `grep -lc 'profile.status !== "paid"' actions/procedure-catalog/*.ts` lista os três arquivos de action.
    - `grep -c "revalidatePath" actions/procedure-catalog/create-procedure-catalog-item.ts actions/procedure-catalog/update-procedure-catalog-item.ts actions/procedure-catalog/delete-procedure-catalog-item.ts` retorna 1 para cada.
    - `grep -cE "next/(cache|headers)" modules/procedure-catalog/*.ts` retorna 0.
    - `grep -c "\[PROCEDURE_CATALOG\]" modules/procedure-catalog/*.ts` retorna 1 ou mais em cada um dos quatro módulos.
    - `components/dashboard/profile/procedure-catalog-card.tsx` contém, literalmente: `Nenhum procedimento cadastrado.`, `Salvar procedimento`, `Descartar edição`, `Adicionar`, `Remover procedimento?`, `Procedimento adicionado.`, `Procedimento atualizado.`, `Procedimento removido.`, `aria-label="Editar procedimento"` e `aria-label="Remover procedimento"`.
    - `grep -c "router.refresh()" components/dashboard/profile/procedure-catalog-card.tsx` retorna 3 ou mais (um por mutação).
    - `grep -v '^\s*//' components/dashboard/profile/procedure-catalog-card.tsx | grep -cE '<Card\b'` retorna 0 — o bloco de procedimentos não abre um `Card` aninhado.
    - `grep -c "useForm" components/dashboard/profile/procedure-catalog-card.tsx` retorna 0 — o componente não entra no form do perfil.
    - `grep -icE "#[0-9a-f]{3,6}\b|rgb\(" components/dashboard/profile/procedure-catalog-card.tsx` retorna 0; `grep -c "font-bold" …` retorna 0.
    - `app/dashboard/profile/page.tsx` contém `listProcedureCatalogItems`.
    - `actions/index.ts` contém um bloco de reexport de `./procedure-catalog`.
    - Comportamento: adicionar `Frenectomia` com preço `120,00` faz a linha aparecer na lista sem F5; editar o preço para `150,00` no lugar persiste; remover pede confirmação e some. Um item com preço `0,00` é aceito; com `-1` mostra a mensagem de preço negativo.
  </acceptance_criteria>
  <done>O médico cadastra, edita no lugar e remove procedimentos com preço no card `Preços` do Perfil; as três mutações são owner-scoped com gate de assinatura, o spec de ownership da exclusão está verde, e o RSC do Perfil carrega a lista.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint visual: card Preços no Perfil e editor de catálogo (S5)</name>
  <what-built>
O card `Preços` no Perfil (S5): o campo `Valor da consulta (R$)` ligado ao form de perfil existente, e
o editor de catálogo de procedimentos com adicionar, editar-no-lugar e remover-com-confirmação. Mais
a travessia de `consultation_price_cents` pelas cinco camadas de TypeScript — incluindo a string de
`.select()` hardcoded que faz o valor sumir em silêncio se for esquecida.
  </what-built>
  <how-to-verify>
1. Abrir `/dashboard/profile`. Confirmar que o card `Preços` aparece **entre** `Informações do perfil`
   e `Logos`, com a descrição correta, e que o bloco `Procedimentos` está DENTRO dele (sem card
   aninhado — um nível de profundidade só).
2. **O landmine.** Com o valor da consulta em branco, digitar `250,00`, salvar, e então **recarregar a
   página com F5**. O campo tem de voltar com `250,00`. Se voltar vazio, a coluna não está na string
   de `.select()` de `get-authenticated-user.ts` — é exatamente o bug que este checkpoint existe para
   pegar, e ele não emite nenhum erro.
3. Limpar o campo e salvar. Recarregar: o campo tem de voltar **vazio**, com só o placeholder — nunca
   com zero.
4. Digitar `-5` e salvar: mensagem `O preço não pode ser negativo.` sob o campo.
5. Confirmar que o campo de valor da consulta é **contido** (não ocupa a largura do card).
6. **Catálogo vazio:** confirmar `Nenhum procedimento cadastrado.` dentro da borda da lista, com o
   corpo citando frenectomia e laserterapia, e a linha de adicionar ainda visível.
7. Adicionar `Frenectomia` / `120,00`. A linha aparece **sem F5**, o toast lê `Procedimento
   adicionado.`, os campos limpam e o foco volta para o campo de nome. Adicionar um segundo item.
8. Clicar no lápis de uma linha: ela troca para edição NO LUGAR, com os botões `Salvar procedimento` e
   `Descartar edição` (não os rótulos genéricos). Confirmar que as OUTRAS linhas seguem em repouso.
   Alterar o preço, salvar, confirmar persistência sem F5.
9. Abrir a edição de novo e apertar **Esc** — descarta. Abrir de novo e clicar `Descartar edição` —
   descarta também (é a saída para quem usa toque).
10. Clicar na lixeira: confirmar o título `Remover procedimento?` e que a descrição tranquiliza que os
    lançamentos já registrados não mudam porque guardaram nome e preço do momento. Confirmar e ver a
    linha sair.
11. Adicionar um procedimento com preço `0,00` — tem de ser **aceito** (procedimento gratuito é
    catalogável). Tentar `-1` — tem de mostrar a mensagem de preço negativo.
12. **Backstop overflow (S5):** cadastrar ~20 procedimentos e decidir aqui, olhando a tela, se a lista
    precisa de cap de altura com scroll dentro do card. Registrar a decisão no SUMMARY.
13. **Backstop nome longo (S5):** cadastrar um procedimento com nome de ~80 caracteres. Confirmar que
    nem o preço nem os botões são empurrados para fora da linha — na linha em repouso E na linha em
    edição.
14. **Backstop contagem (S5):** se alguma contagem de procedimentos aparecer na tela, confirmar que ela
    concorda em número (singular com um item, plural com vários).
15. Testar com um usuário cujo status **não** é pago: salvar o perfil (incluindo o valor da consulta)
    tem de continuar funcionando; as mutações de catálogo têm de recusar com a mensagem de perfil não
    ativo.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu (item por item). Se o item 2 falhar, o problema é a string de `.select()`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → actions de catálogo | nome do procedimento, preço em reais como string e id do item cruzam aqui como dado não confiável |
| browser → `updateProfileAction` | o valor da consulta em reais cruza aqui; este action deliberadamente não tem gate de assinatura |
| Server Action → Postgres | mutações em `procedure_catalog_items`; `profile_id` vem da sessão, nunca do payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-16 | Elevation of Privilege | `id` de item de catálogo vindo do cliente (IDOR) | high | mitigate | Toda mutação filtra por `id` E por `profile_id` no módulo, nunca só por `id`; a RLS owner-scoped da tabela é a segunda camada; `delete-procedure-catalog-item.spec.ts` afirma os dois filtros com um mock gravador |
| T-10-17 | Elevation of Privilege | actions de catálogo sem gate de assinatura | high | mitigate | Os três actions novos checam `profile.status !== "paid"` antes de qualquer escrita (SC-4). Note que `getAuthenticatedUser` devolve objeto vazio castado em falha, então o gate real é `!profile?.id` mais a checagem de status |
| T-10-18 | Tampering | preço negativo ou absurdo no catálogo | medium | mitigate | Zod rejeita negativo com mensagem PT-BR e aplica teto abaixo do teto de `integer`; a constraint `procedure_catalog_items_price_non_negative` no banco é a última linha; zero é aceito de propósito |
| T-10-19 | Tampering | nome de procedimento vazio ou só de espaços | low | mitigate | Zod com `trim` + não-vazio; a constraint `procedure_catalog_items_name_not_blank` no banco fecha o caminho direto por PostgREST |
| T-10-20 | Elevation of Privilege | gate de assinatura adicionado por engano em `updateProfileAction` | medium | mitigate | Prohibition explícita neste plano + critério de aceite que grepa a ausência: adicionar o gate ali trava o onboarding do usuário não-pago |
| T-10-21 | Information Disclosure | erro cru de banco no toast do editor de catálogo | medium | mitigate | Módulo lança com tag de domínio; action converte em result union; o helper de mensagem amigável remove a tag antes do toast |
| T-10-22 | Repudiation | remover um item do catálogo "apagar" faturamento | low | accept | Não apaga: o snapshot (D-05) já copiou nome e centavos para cada lançamento. A cópia do diálogo de confirmação declara isso ao usuário. É a prova de que D-05 valeu a pena |
| T-10-SC | Tampering | instalações npm/pip/cargo | low | accept | Zero pacotes novos (RESEARCH § Package Legitimacy Audit). Nenhum item `[ASSUMED]`/`[SUS]`/`[SLOP]` |
</threat_model>

<verification>
- `yarn test` verde incluindo o spec de ownership da exclusão de catálogo.
- `yarn typecheck`, `yarn lint`, `yarn build` saem com 0.
- `consultation_price_cents` aparece em cinco arquivos: a string de select, o tipo de perfil, o
  payload de update, o schema e o mapeamento do action.
- `grep -c 'profile.status' actions/profile/update-profile.ts` retorna 0.
- Nenhum módulo de `modules/procedure-catalog/` importa cache ou headers.
- O checkpoint visual de 15 itens foi aprovado, com a decisão do item 12 (cap de altura) registrada.
</verification>

<success_criteria>
- O médico cadastra o valor da consulta e um catálogo de procedimentos com preço, e os valores
  sobrevivem a um reload (o landmine da string de select está fechado).
- As três mutações de catálogo são owner-scoped, gateadas por assinatura e cobertas por spec.
- O Perfil ganha um card e um bloco titulado, sem card aninhado e sem nova linguagem visual.
- O fluxo de onboarding do usuário não-pago continua intacto.
</success_criteria>

<output>
Create `.planning/phases/10-livro-caixa-de-ganhos-painel/10-03-SUMMARY.md` when done
</output>
