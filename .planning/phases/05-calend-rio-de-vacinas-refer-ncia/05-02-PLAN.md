---
phase: 05-calend-rio-de-vacinas-refer-ncia
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql
  - app/dashboard/vaccines/page.tsx
  - components/dashboard/vaccines/vaccine-calendar-view.tsx
  - components/dashboard/vaccines/vaccine-column.tsx
autonomous: false
requirements: [VAC-02, VAC-04]
must_haves:
  truths:
    - "Inside the Criança view, the doctor sees SUS/PNI and Particular (SBIm) as two parallel columns grouped by age band, side by side (VAC-02)"
    - "Both columns read across at the same age-band rhythm; where one dataset lacks a band an explicit empty marker is shown rather than silent misalignment"
    - "The SBIm dataset shows its own provenance caption (Fonte: SBIm {version} · vigência {date}) distinct from the SUS caption"
  artifacts:
    - "supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql (physician-approved SBIm items)"
    - "components/dashboard/vaccines/vaccine-calendar-view.tsx (now renders two columns)"
  key_links:
    - "page.tsx → getVaccineScheduleWithItems(supabase, 'SBIm') second read"
    - "VaccineCalendarView renders SUS + SBIm columns in a two-column grid"
    - "age-band union across SUS+SBIm drives aligned rows"
---

<objective>
Second vertical slice: add the particular/SBIm dataset side by side with SUS inside the Criança view (VAC-02), each column keeping its own provenance (VAC-04). Extends the working foundation from plan 01 — the same route, module, and column component now render two aligned datasets for direct comparison.

Purpose: The doctor can compare "what's due at 2 meses" in SUS vs SBIm at a glance — the core D-01 two-column comparison.
Output: A physician-approved SBIm seed, a two-column layout in the view, and per-dataset provenance for both columns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-CONTEXT.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan adds NO new tables, types, or routes. It reuses `getVaccineScheduleWithItems` (plan 01) with `source="SBIm"` and extends existing components.

**Migration (this plan):** `20260720000300_seed_vaccine_schedules_sbim.sql` — idempotent global insert of the SBIm `vaccine_schedule_items` (the SBIm schedule metadata row was seeded in plan 01; this adds its items). NO profile_id, NO cross join.

**Components extended (this plan):** `VaccineCalendarView` now accepts and renders both SUS and SBIm datasets in a two-column grid; `VaccineColumn` gains age-band alignment support (explicit empty marker where a band is absent).

Full phase artifact list lives in 05-01-PLAN.md §artifacts_this_phase_produces.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Physician sign-off on SBIm clinical seed values</name>
  <action>Pause for physician review of the SBIm clinical seed values before Task 1 commits/applies them. Do not author or commit clinical values autonomously (STATE.md blocker). See what-built / how-to-verify below.</action>
  <what-built>The SBIm seed migration STRUCTURE (idempotent global insert of the SBIm child-calendar items) is ready, but the CLINICAL VALUES — the SBIm vaccine/dose/age rows — require physician sign-off before commit (STATE.md blocker). The executor must NOT author clinical values autonomously.</what-built>
  <how-to-verify>
    1. Review the SBIm schedule metadata (source='SBIm', axis='child_age', version, effective_date) — confirm it matches the current official SBIm calendar (e.g. "SBIm 2025").
    2. Review every proposed SBIm `vaccine_schedule_items` row: vaccine, dose, `age_months`/`age_months_max`, `age_label`. Confirm each matches the current official SBIm (Sociedade Brasileira de Imunizações) recommendations.
    3. Confirm the SBIm age milestones align with the SUS age bands where the vaccines coincide, so the two columns read across (VAC-02 comparison).
    4. Approve, correct specific values, or reject. Only physician-approved values may be committed.
  </how-to-verify>
  <resume-signal>Type "approved" once the SBIm clinical values are confirmed, or provide the corrected vaccine/dose/age values.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: SBIm seed migration + apply to live DB (VAC-02/VAC-04)</name>
  <files>supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql</files>
  <read_first>
    - supabase/migrations/20260720000200_seed_vaccine_schedules.sql (the SUS seed from plan 01 — COPY its idempotent global-insert shape for the SBIm items)
    - supabase/migrations/20260710030100_seed_guidance_templates.sql (clinical-content header convention)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §seed_vaccine_schedules migration
  </read_first>
  <action>
    Write `supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql` using the physician-approved SBIm values from the preceding checkpoint. Insert the SBIm `vaccine_schedule_items` rows, joining to the existing SBIm schedule by `source='SBIm'`/`version`, each guarded by `where not exists` on the stable key (schedule_id + vaccine + dose + age_label) so re-running is safe. Populate BOTH structured `age_months`/`age_months_max` AND the human `age_label`; set `sort_order` by ascending age; leave `week_min`/`week_max` null (child_age axis).

    CRITICAL D-07: NO `public.profiles`, NO `cross join`, NO `profile_id`. Global insert only. Add the clinical-content header comment stating physician approval at the checkpoint.

    Then apply this migration to the live Supabase project `acstugafrgrqzvtuznxv` via `supabase db push` (or Supabase MCP `apply_migration` / SQL editor if non-interactive). Confirm the SBIm items are present: a count query on `vaccine_schedule_items` joined to the SBIm schedule returns the seeded count. (The tables + RLS already exist from plan 01; only this seed is new.)
  </action>
  <verify>
    <automated>grep -c 'cross join' supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql | grep -qx 0 &amp;&amp; echo NO_CROSS_JOIN_OK</automated>
    <automated>grep -c 'profile' supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql | grep -qx 0 &amp;&amp; echo NO_PROFILE_OK</automated>
    <automated>grep -q 'where not exists' supabase/migrations/20260720000300_seed_vaccine_schedules_sbim.sql &amp;&amp; echo IDEMPOTENT_OK</automated>
  </verify>
  <done>SBIm seed migration exists (idempotent, global — zero `cross join`/`profile`), is applied to the live DB, and the SBIm items are queryable. Structured `age_months` + `age_label` populated per item.</done>
</task>

<task type="auto">
  <name>Task 2: Render SUS × SBIm two-column layout with aligned bands (VAC-02, D-01)</name>
  <files>app/dashboard/vaccines/page.tsx, components/dashboard/vaccines/vaccine-calendar-view.tsx, components/dashboard/vaccines/vaccine-column.tsx</files>
  <read_first>
    - app/dashboard/vaccines/page.tsx (add the second SBIm read; reuse the existing module)
    - components/dashboard/vaccines/vaccine-calendar-view.tsx (extend to two columns)
    - components/dashboard/vaccines/vaccine-column.tsx (add empty-band marker support)
    - components/dashboard/vaccines/schedule-provenance.tsx (reused per column — no change expected)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md §C3 (two-column grid, alignment, explicit empty marker), §Copywriting Contract (column headers "SUS/PNI" / "Particular (SBIm)")
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §vaccine-column
  </read_first>
  <action>
    In `app/dashboard/vaccines/page.tsx`, add a second read `getVaccineScheduleWithItems(supabase, "SBIm")` alongside the existing SUS read, and pass both datasets into `<VaccineCalendarView>`.

    In `vaccine-calendar-view.tsx`, render the two datasets as parallel columns in a responsive grid: `grid grid-cols-1 md:grid-cols-2 gap-4` (C3) — SUS/PNI left, Particular (SBIm) right. On narrow widths the columns stack (SUS above SBIm), each keeping its own column header; never merge into one list. Keep the prop surface forward-compatible for the Gestante tab (plan 03) and the optional `birthDate` highlight (plan 04) — do not hard-code assumptions that block those.

    In `vaccine-column.tsx`, support age-band alignment: compute the union of distinct age bands across SUS + SBIm (by `age_months`/`age_label`), render every band in BOTH columns, and where a dataset has no vaccine for a band show an explicit empty marker (`—`) rather than silently omitting the row and misaligning the columns (C3). The column header comes from the dataset label ("SUS/PNI", "Particular (SBIm)"). Each column card still renders its own `<ScheduleProvenance>` in the footer (D-09) — SUS shows the SUS caption, SBIm shows the SBIm caption; do not collapse to one shared caption.

    Do NOT color SUS vs SBIm with different hues and do NOT color-code vaccines (UI-SPEC §Color) — distinction is column position + header label only. PT-BR strings. No new package or shadcn block.
  </action>
  <verify>
    <automated>grep -q '"SBIm"' app/dashboard/vaccines/page.tsx &amp;&amp; echo SBIM_READ_OK</automated>
    <automated>grep -q 'md:grid-cols-2' components/dashboard/vaccines/vaccine-calendar-view.tsx &amp;&amp; echo TWO_COLUMN_GRID_OK</automated>
    <automated>yarn typecheck</automated>
  </verify>
  <done>`yarn typecheck` passes. The page reads both SUS and SBIm. The view renders two parallel columns in a `md:grid-cols-2` grid, aligned by the union of age bands with `—` for missing bands, each with its own provenance caption. Visiting `/dashboard/vaccines` shows SUS/PNI and Particular (SBIm) side by side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC route | Authenticated + paid request; adds one more global read (SBIm) |
| RSC route → Supabase | Same global-read RLS governs both reads; still no write path |
| migration/service-role → tables | Only write path (SBIm seed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `/dashboard/vaccines` route | high | mitigate | The auth + `profile.status === "paid"` gate established in plan 01 remains on the route; the added SBIm read is behind the same gate. |
| T-05-02 | Tampering | reference tables | high | mitigate | No new write policy is introduced; SBIm seed is applied via migration only (D-07). The SELECT-only RLS from plan 01 continues to deny client writes. |
| T-05-04 | Tampering / Safety | SBIm seed clinical content | medium | mitigate | SBIm clinical values gated behind a blocking `checkpoint:human-verify` (physician sign-off); per-dataset provenance + fixed advisory render for SBIm (D-09). |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this plan (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `yarn typecheck` passes.
- SBIm items live in the DB and render in the right column aligned with SUS bands.
- Both columns show distinct provenance captions; the fixed advisory remains visible.
- No color divergence between SUS and SBIm columns.
</verification>

<success_criteria>
- VAC-02: SBIm calendar by age is shown side by side with SUS.
- VAC-04 (extended): SBIm modeled as versioned data with its own provenance visible.
- Columns align by age band with explicit empty markers.
</success_criteria>

<output>
Create `.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-02-SUMMARY.md` when done.
</output>
