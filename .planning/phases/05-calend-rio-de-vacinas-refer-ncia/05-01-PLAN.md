---
phase: 05-calend-rio-de-vacinas-refer-ncia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260720000000_vaccine_schedules.sql
  - supabase/migrations/20260720000100_rls_vaccine_schedules.sql
  - supabase/migrations/20260720000200_seed_vaccine_schedules.sql
  - modules/vaccines/types.ts
  - modules/vaccines/get-vaccine-schedule-with-items.ts
  - app/dashboard/vaccines/page.tsx
  - components/dashboard/vaccines/vaccine-calendar-view.tsx
  - components/dashboard/vaccines/vaccine-column.tsx
  - components/dashboard/vaccines/schedule-provenance.tsx
  - components/app-sidebar.tsx
autonomous: false
requirements: [VAC-01, VAC-04]
must_haves:
  truths:
    - "A paid, authenticated doctor opens /dashboard/vaccines and sees the SUS/PNI calendar grouped by age band, rendered end-to-end from the seed tables"
    - "Each dataset shows its own provenance caption (Fonte: {source} {version} · vigência {date}) plus the fixed advisory 'Confira sempre contra o calendário oficial atual.'"
    - "A second doctor sees the exact same schedule rows (data is global, not owner-scoped)"
    - "The two reference tables reject any client write (no INSERT/UPDATE/DELETE policy); only migrations/service-role can mutate them"
  artifacts:
    - "supabase/migrations/20260720000000_vaccine_schedules.sql (vaccine_schedules + vaccine_schedule_items tables, NO profile_id)"
    - "supabase/migrations/20260720000100_rls_vaccine_schedules.sql (global-read SELECT-only RLS)"
    - "supabase/migrations/20260720000200_seed_vaccine_schedules.sql (physician-approved clinical seed)"
    - "modules/vaccines/get-vaccine-schedule-with-items.ts (read module, no profile_id filter)"
    - "app/dashboard/vaccines/page.tsx (RSC route, auth + paid gate)"
    - "components/dashboard/vaccines/vaccine-calendar-view.tsx"
  key_links:
    - "page.tsx → getVaccineScheduleWithItems(supabase, 'SUS') → vaccine_schedules join vaccine_schedule_items"
    - "global-read RLS (using true) makes the SELECT succeed for any authenticated user"
    - "sidebar navMain entry → /dashboard/vaccines"
---

<objective>
Foundation vertical slice for the vaccine reference calendar. Stands up the two seed tables (`vaccine_schedules` + `vaccine_schedule_items`) with the deliberate global-read RLS divergence (D-07), applies them to the live DB, seeds the physician-approved clinical content (checkpoint-gated), and proves the whole stack end-to-end by rendering the SUS/PNI calendar at `/dashboard/vaccines` grouped by age band with per-dataset provenance (VAC-01, VAC-04).

Purpose: Deliver the thinnest end-to-end path — DB → RLS → read module → RSC route → view — so a paid doctor can consult the SUS/PNI calendar by age. Every later slice (SBIm column, Gestante tab, patient highlight) extends this working spine.
Output: Two reference tables live in Supabase, seeded clinical data, a `/dashboard/vaccines` route in the sidebar rendering one dataset column with provenance and the fixed advisory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-CONTEXT.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md
@.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols created across the whole phase (this plan creates the foundation subset; later plans extend the same files):

**Tables (this plan):**
- `public.vaccine_schedules` — columns: `id` (uuid pk), `source` (text: 'SUS'|'SBIm'|'gestante'), `axis` (text: 'child_age'|'gestational_weeks', default 'child_age'), `version` (text), `effective_date` (date), `notes` (text), `created_at` (timestamptz). NO `profile_id`.
- `public.vaccine_schedule_items` — columns: `id` (uuid pk), `schedule_id` (uuid fk → vaccine_schedules, on delete cascade), `vaccine` (text), `dose` (text nullable), `age_months` (integer nullable), `age_months_max` (integer nullable), `week_min` (integer nullable), `week_max` (integer nullable), `age_label` (text not null), `sort_order` (integer default 0), `notes` (text nullable). NO `profile_id`.

**Index (this plan):** `idx_vaccine_schedule_items_schedule` on `(schedule_id, sort_order)`.

**RLS policies (this plan):** `"Vaccine schedules readable by authenticated"` (SELECT, to authenticated, using true); `"Vaccine schedule items readable by authenticated"` (SELECT, to authenticated, using true). NO insert/update/delete policies on either table.

**Types (this plan):** `VaccineSource`, `VaccineAxis`, `VaccineSchedule`, `VaccineScheduleItem`, `VaccineScheduleWithItems` in `modules/vaccines/types.ts`.

**Module fns (this plan):** `getVaccineScheduleWithItems(supabase, source)` in `modules/vaccines/get-vaccine-schedule-with-items.ts`.

**Routes (this plan):** `/dashboard/vaccines` (`app/dashboard/vaccines/page.tsx`).

**Components (this plan):** `VaccineCalendarView` (`vaccine-calendar-view.tsx`), `VaccineColumn` (`vaccine-column.tsx`), `ScheduleProvenance` (`schedule-provenance.tsx`).

**Nav (this plan):** sidebar entry `{ title: "Vacinas", url: "/dashboard/vaccines" }`.

**Later plans (02/03/04) add:** SBIm second column, `GestanteList` component, `groupItemsByAgeBand` helper (if extracted), patient-profile entry link, current-age highlight logic. No new tables.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create migrations — two global reference tables + global-read RLS (D-06/D-07/D-08)</name>
  <files>supabase/migrations/20260720000000_vaccine_schedules.sql, supabase/migrations/20260720000100_rls_vaccine_schedules.sql</files>
  <read_first>
    - supabase/migrations/20260710020400_exam_catalog_items.sql (table + comment + index analog — KEEP shape, DROP the profile_id column and its index per PATTERNS.md)
    - supabase/migrations/20260709000000_patient_measurements.sql (multi-column table + comment analog)
    - supabase/migrations/20260710000100_rls_referrals.sql (RLS "enable + policies same file" rule — KEEP the enable/same-file structure, DROP the owner-scoped `using ( profile_id in (select ...) )` predicate per PATTERNS.md)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md §Pattern 1 and §Pattern 2
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §vaccine_schedules migration and §rls_vaccine_schedules migration (DROP callouts)
  </read_first>
  <action>
    Write two migration files with timestamp-prefixed names matching the repo convention (use dated prefixes 20260720000000 and 20260720000100).

    File 1 `20260720000000_vaccine_schedules.sql`: create `public.vaccine_schedules` and `public.vaccine_schedule_items` exactly as specified in the artifacts section above. The parent table holds `source`, `axis` (default 'child_age'), `version`, `effective_date` (date), `notes`, `created_at`. The child table holds `schedule_id` (fk on delete cascade), `vaccine`, `dose`, structured `age_months`/`age_months_max` (child_age axis) and `week_min`/`week_max` (gestational_weeks axis), the human `age_label` (not null), `sort_order` (default 0), `notes`. Create index `idx_vaccine_schedule_items_schedule` on `(schedule_id, sort_order)`.

    CRITICAL D-07 divergence: NEITHER table has a `profile_id` column and NEITHER has the `profile_id` index the exam-catalog analog uses. Do NOT copy the `profile_id uuid not null references public.profiles(id)` line or its index — this is shared reference data, not owner-scoped. Add a table comment on `vaccine_schedules` stating it is versioned global reference data, read-only, without owner scoping (reference decision D-07 and D-08 by their identifiers in the comment prose). Omit the `updated_at`/`set_updated_at` trigger (seed-only table; no app updates) per the PATTERNS.md note.

    File 2 `20260720000100_rls_vaccine_schedules.sql`: enable row level security on BOTH tables and create exactly two SELECT policies in the SAME file (repo rule: enable + policies together, else silent total denial). Policy 1 `"Vaccine schedules readable by authenticated"` for select to authenticated using (true). Policy 2 `"Vaccine schedule items readable by authenticated"` for select to authenticated using (true). Create NO insert, update, or delete policies on either table — writes are seed-only via migration (D-07). Add a SQL comment on its own line stating that the absence of a profile_id filter and the absence of write policies is deliberate reference-data design, so a future maintainer does not "fix" it by adding an owner filter.
  </action>
  <verify>
    <automated>grep -c 'profile_id' supabase/migrations/20260720000000_vaccine_schedules.sql | grep -qx 0 &amp;&amp; echo NO_PROFILE_ID_OK</automated>
    <automated>grep -vE '^[[:space:]]*--' supabase/migrations/20260720000100_rls_vaccine_schedules.sql | grep -Eiq 'create policy.*(insert|update|delete)|for[[:space:]]+(insert|update|delete)' &amp;&amp; echo HAS_WRITE_POLICY_FAIL || echo SELECT_ONLY_OK</automated>
    <automated>grep -q 'using (true)' supabase/migrations/20260720000100_rls_vaccine_schedules.sql &amp;&amp; echo GLOBAL_READ_OK</automated>
  </verify>
  <done>Both migration files exist. The tables migration contains zero occurrences of `profile_id`. The RLS migration enables RLS on both tables, creates two SELECT-only `using (true)` policies, and contains no insert/update/delete policy statements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Physician sign-off on SUS/PNI clinical seed values</name>
  <action>Pause for physician review of the SUS/PNI clinical seed values before Task 2 commits them. Do not author or commit clinical values autonomously (STATE.md blocker). See what-built / how-to-verify below.</action>
  <what-built>The seed migration STRUCTURE (idempotent global insert of three schedule metadata rows: SUS, SBIm, gestante) is ready, but the CLINICAL VALUES — the actual vaccine/dose/age rows for the SUS/PNI child calendar — require physician sign-off before commit. This is the STATE.md blocker: "[Phase 5] Acurácia dos dados PNI/SBIm deve ser verificada com o médico contra as fontes oficiais atuais." The executor must NOT author clinical values autonomously.</what-built>
  <how-to-verify>
    1. Review the SUS/PNI schedule metadata row: source='SUS', axis='child_age', version, effective_date. Confirm the version label and vigência date match the current official PNI calendar (e.g. "PNI 2025", "2025-01-01").
    2. Review every proposed `vaccine_schedule_items` row for the SUS schedule: vaccine name, dose label, `age_months` / `age_months_max`, and the human `age_label`. Confirm each vaccine, dose, and recommended age matches the current official PNI/SUS calendar from the Ministério da Saúde.
    3. Confirm the age granularity chosen (which age milestones appear: ao nascer, 2, 3, 4, 6, 9, 12, 15 meses...) reflects the official schedule.
    4. Approve, correct specific values, or reject. Only physician-approved values may be committed. The seed file header must state that the clinical content was approved at this checkpoint (mirror the seed_guidance_templates.sql header convention).
  </how-to-verify>
  <resume-signal>Type "approved" once the SUS clinical values are confirmed, or provide the corrected vaccine/dose/age values to use.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write idempotent global seed migration (VAC-01/VAC-04, physician-approved)</name>
  <files>supabase/migrations/20260720000200_seed_vaccine_schedules.sql</files>
  <read_first>
    - supabase/migrations/20260710020600_seed_exam_catalog.sql (idempotent `insert ... select ... from (values ...) where not exists` analog — KEEP the idempotency shape, DROP the per-profile `cross join public.profiles` per PATTERNS.md D-07 divergence)
    - supabase/migrations/20260710030100_seed_guidance_templates.sql (idempotent seed + clinical-content header convention to COPY)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-RESEARCH.md §Pattern 3
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §seed_vaccine_schedules migration
  </read_first>
  <action>
    Write `supabase/migrations/20260720000200_seed_vaccine_schedules.sql` using the physician-approved values from the preceding checkpoint. Insert the three schedule metadata rows (SUS/child_age, SBIm/child_age, gestante/gestational_weeks) idempotently via `insert ... select ... from (values ...) where not exists (select 1 from public.vaccine_schedules s where s.source = v.source and s.version = v.version)`. Then insert the SUS schedule's `vaccine_schedule_items` rows, joining on the parent schedule by `source`/`version`, each guarded by a `where not exists` on a stable key (schedule_id + vaccine + dose + age_label).

    CRITICAL D-07 divergence: do NOT fan out per profile. There is NO `public.profiles`, NO `cross join`, NO `profile_id` anywhere in this seed. Insert ONCE, GLOBALLY.

    For the SUS child items, populate BOTH the structured `age_months` (and `age_months_max` for windows like 12–15 meses) AND the human `age_label` text the UI renders — the structured field is required for Phase 6's per-age diff (do not store age as text only). Set `sort_order` so items sort by ascending age within the schedule. Leave `week_min`/`week_max` null for child_age items.

    Add the clinical-content header comment (mirror seed_guidance_templates.sql) stating the values were physician-approved at the human-verify checkpoint and the executor did not author them.

    Scope: this plan seeds ONLY the SUS dataset items to prove the foundation slice end-to-end. The SBIm items are added in plan 02 and gestante items in plan 03 (the schedule metadata rows for all three may be inserted here since they are cheap and idempotent, but SBIm/gestante items are deferred to their slices).
  </action>
  <verify>
    <automated>grep -c 'cross join' supabase/migrations/20260720000200_seed_vaccine_schedules.sql | grep -qx 0 &amp;&amp; echo NO_CROSS_JOIN_OK</automated>
    <automated>grep -c 'profile' supabase/migrations/20260720000200_seed_vaccine_schedules.sql | grep -qx 0 &amp;&amp; echo NO_PROFILE_OK</automated>
    <automated>grep -q 'where not exists' supabase/migrations/20260720000200_seed_vaccine_schedules.sql &amp;&amp; echo IDEMPOTENT_OK</automated>
  </verify>
  <done>Seed migration exists with physician-approved SUS values, is idempotent (`where not exists`), inserts globally (zero occurrences of `cross join` and `profile`), and populates structured `age_months` plus `age_label` for each SUS item.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: [BLOCKING] Apply the 3 vaccine migrations to the live DB in order</name>
  <action>[BLOCKING] Apply the three migrations to the live Supabase DB in order (table → rls → seed) before verification. Build/typecheck pass without this push (false positive), so the phase cannot be verified until it is done. See how-to-verify below.</action>
  <what-built>All three vaccine migrations are written (tables, global-read RLS, physician-approved SUS seed). They must now be applied to the LIVE Supabase project `acstugafrgrqzvtuznxv` in strict order — table → rls → seed — because the app reads types/data from the live DB and build/typecheck pass WITHOUT the push (false-positive verification). This mirrors the referrals migration close-out precedent in STATE.md. [BLOCKING] Schema push required.</what-built>
  <how-to-verify>
    1. Apply the migrations to the live DB IN THIS ORDER via `supabase db push` (or the Supabase MCP `apply_migration` / SQL editor if the CLI is non-interactive): first `20260720000000_vaccine_schedules.sql`, then `20260720000100_rls_vaccine_schedules.sql`, then `20260720000200_seed_vaccine_schedules.sql`. Applying RLS before/without the SELECT policy causes silent total denial (zero rows, no error) — order matters.
    2. Confirm both tables exist with NO `profile_id` column (e.g. via `list_tables` or `select column_name from information_schema.columns where table_name = 'vaccine_schedules'`).
    3. Confirm the two SELECT-only policies exist and there are no write policies.
    4. Confirm the SUS schedule row and its items are present (`select count(*) from vaccine_schedule_items i join vaccine_schedules s on s.id = i.schedule_id where s.source = 'SUS'` returns the seeded count).
  </how-to-verify>
  <resume-signal>Type "pushed" once all three migrations are applied to the live DB in order and the SUS rows are confirmed present.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Read layer + route + minimal view — SUS renders end-to-end (VAC-01/VAC-04)</name>
  <files>modules/vaccines/types.ts, modules/vaccines/get-vaccine-schedule-with-items.ts, app/dashboard/vaccines/page.tsx, components/dashboard/vaccines/vaccine-calendar-view.tsx, components/dashboard/vaccines/vaccine-column.tsx, components/dashboard/vaccines/schedule-provenance.tsx, components/app-sidebar.tsx</files>
  <read_first>
    - modules/prescriptions/get-prescriptions-by-profile-id.ts (module shape to COPY — SupabaseClient injected first, typed return, throw `[DOMAIN]` on error; DROP the `.eq("profile_id", ...)` line per PATTERNS.md)
    - modules/referrals/types.ts (types file convention)
    - app/dashboard/referrals/page.tsx (RSC route analog — auth preamble, header block, dashed empty-state card)
    - app/dashboard/patients/new/page.tsx line ~19 (the exact paid-gate expression: `if (profile.status !== "paid") redirect("/dashboard/link-whatsapp")`)
    - components/app-sidebar.tsx §navMain "Serviços" group (self-modify to add the nav entry)
    - lib/growth-reference/index.ts (provenance metadata shape concept for the caption)
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-UI-SPEC.md §C1, §C3, §C6, §C7, §Copywriting Contract
    - .planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-PATTERNS.md §get-vaccine-schedule-with-items, §page.tsx, §schedule-provenance, §app-sidebar
  </read_first>
  <behavior>
    - `getVaccineScheduleWithItems(supabase, "SUS")` returns the SUS schedule joined with its items ordered by `sort_order` ascending; returns null when the dataset is unseeded.
    - The module builds its query with `.eq("source", source)` and NO `.eq("profile_id", ...)` — a second doctor calling it gets the identical rows.
    - The module throws `Error` tagged `[VACCINES]` on a Supabase error.
  </behavior>
  <action>
    Create `modules/vaccines/types.ts` with named exports: `VaccineSource = "SUS" | "SBIm" | "gestante"`, `VaccineAxis = "child_age" | "gestational_weeks"`, `VaccineSchedule` (mirrors the parent row in snake_case), `VaccineScheduleItem` (mirrors the child row in snake_case: `age_months`, `age_months_max`, `week_min`, `week_max`, `age_label`, `sort_order`, etc.), and `VaccineScheduleWithItems` (schedule + `vaccine_schedule_items` array). PascalCase types, named exports only.

    Create `modules/vaccines/get-vaccine-schedule-with-items.ts` — one exported async function `getVaccineScheduleWithItems(supabase, source)` taking the injected `SupabaseClient` first and a `VaccineSource`. Select the schedule columns plus the nested `vaccine_schedule_items(...)` join, filter with `.eq("source", source)`, order the referenced items by `sort_order` ascending, use `.maybeSingle()`, throw `Error("[VACCINES] Failed to fetch schedule ...")` on error, and return the data (null when unseeded). Add an inline comment at the query stating there is intentionally NO `profile_id` filter because this is global reference data (reference D-07 by identifier) so no one adds an owner filter by reflex. Do not import `next/cache` or `next/headers`.

    Create `app/dashboard/vaccines/page.tsx` as a Server Component: `createClient()` → `getAuthenticatedUser(supabase)` → `if (!profile?.id) redirect("/auth/login")` → the paid gate `if (profile.status !== "paid") redirect("/dashboard/link-whatsapp")` (D-10). Read the SUS schedule via the module. Render the page shell per C1 (`flex flex-col gap-6`, `max-w-6xl`, header h1 "Calendário de vacinas" + muted description from the Copywriting Contract, a Syringe/Calendar lucide icon). Render `<VaccineCalendarView>` passing the SUS dataset. Show the defensive dashed empty-state card (C7, "Calendário indisponível" copy) when the SUS read returns null.

    Create `components/dashboard/vaccines/vaccine-calendar-view.tsx` (client component). For this foundation slice it renders a SINGLE column (SUS) via `<VaccineColumn>` inside the "Criança" area — the Tabs shell and the SBIm/Gestante siblings are added in later plans, so keep the component's prop surface forward-compatible (accept the SUS dataset now; plan 02 adds SBIm, plan 03 adds gestante, plan 04 adds an optional `birthDate`). Use `cn()` from `lib/utils.ts` for class composition.

    Create `components/dashboard/vaccines/vaccine-column.tsx` — renders one dataset as a Card with the column header ("SUS/PNI") and the items grouped by age band (group by distinct `age_label`/`age_months`, ordered by `sort_order`), each band listing its vaccine/dose as body text (C3). NOT a vaccine×age grid, NOT an accordion. Render the `<ScheduleProvenance>` in the card footer.

    Create `components/dashboard/vaccines/schedule-provenance.tsx` — renders the per-dataset caption `Fonte: {source} {version} · vigência {effective_date}` (format the date with date-fns to PT-BR "mmm/yyyy", already installed) read from the dataset's own metadata, PLUS the fixed persistent advisory text `Confira sempre contra o calendário oficial atual.` in muted/info treatment (`text-muted-foreground`, never destructive red). Per D-09, this is per-dataset, not a single detached global banner.

    Modify `components/app-sidebar.tsx` — add `{ title: "Vacinas", url: "/dashboard/vaccines" }` to the navMain "Serviços" group alongside "Encaminhamentos". The existing `isNavItemActive` fallback handles the new route; no other sidebar change needed.

    All user-facing strings PT-BR. Do NOT introduce any new npm package or shadcn block (RESEARCH: none needed).
  </action>
  <verify>
    <automated>grep -c 'profile_id' modules/vaccines/get-vaccine-schedule-with-items.ts | grep -qx 0 &amp;&amp; echo MODULE_NO_OWNER_FILTER_OK</automated>
    <automated>grep -q 'status !== "paid"' app/dashboard/vaccines/page.tsx &amp;&amp; echo PAID_GATE_OK</automated>
    <automated>grep -q '/dashboard/vaccines' components/app-sidebar.tsx &amp;&amp; echo SIDEBAR_OK</automated>
    <automated>yarn typecheck</automated>
  </verify>
  <done>`yarn typecheck` passes. The read module has zero `profile_id` occurrences and filters by `source`. The route enforces both the auth redirect and the `status !== "paid"` gate. The sidebar links to `/dashboard/vaccines`. Visiting `/dashboard/vaccines` as a paid user renders the SUS/PNI column grouped by age band with its provenance caption and the fixed advisory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC route (`/dashboard/vaccines`) | Authenticated request; `source` param (if any) is untrusted input |
| RSC route → Supabase (publishable/authenticated key) | RLS governs read access; no write path exists in the app |
| migration/service-role → tables | The ONLY write path to reference data (seed-only, D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `/dashboard/vaccines` route | high | mitigate | Enforce `getAuthenticatedUser` + `profile.status === "paid"` gate on the route (D-10). RLS `to authenticated` does NOT enforce the subscription tier — the paid gate is an app-layer rule and is verified by grep `status !== "paid"`. |
| T-05-02 | Tampering | `vaccine_schedules` / `vaccine_schedule_items` | high | mitigate | RLS enabled with ONLY SELECT policies; NO insert/update/delete policies, so all client writes (publishable/authenticated key) are denied. Only migrations/service-role can mutate. Verified by grep asserting no write-policy keywords in the RLS migration. |
| T-05-03 | Information Disclosure (IDOR) | read module | low | accept | N/A by design — reference data is global and identical for every doctor; there is no per-tenant row to leak. The absent `profile_id` filter is the deliberate D-07 divergence, NOT the CONCERNS.md IDOR bug. Documented in the module + migration comments so it is not "fixed" by reflex. |
| T-05-04 | Tampering / Safety | seed clinical content | medium | mitigate | Clinical seed values gated behind a blocking `checkpoint:human-verify` (physician sign-off, STATE.md blocker); UI carries the fixed advisory "Confira sempre contra o calendário oficial atual." (D-09). |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this phase (RESEARCH §Package Legitimacy Audit: N/A). Any new package discovered mid-execution triggers the legitimacy gate before install. |
</threat_model>

<verification>
- `yarn typecheck` passes with the new module, types, route, and components.
- Both reference tables exist in the live DB with NO `profile_id` column (confirmed at the push checkpoint).
- Two SELECT-only RLS policies exist; no write policies (confirmed at push checkpoint + grep gate).
- SUS schedule + items seeded and visible; a paid doctor sees the SUS/PNI calendar at `/dashboard/vaccines`.
- Provenance caption + fixed advisory render for the SUS dataset.
</verification>

<success_criteria>
- VAC-01: A paid doctor consults the SUS/PNI reference calendar by age at `/dashboard/vaccines`.
- VAC-04 (partial): the SUS dataset is modeled as versioned data (source/version/effective_date) with provenance visible on screen.
- Global-read RLS live: two SELECT-only policies, no write policies, no `profile_id` column.
- Seed clinical content committed only after physician sign-off.
</success_criteria>

<output>
Create `.planning/phases/05-calend-rio-de-vacinas-refer-ncia/05-01-SUMMARY.md` when done.
</output>
