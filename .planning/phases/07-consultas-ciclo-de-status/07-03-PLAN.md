---
phase: 07-consultas-ciclo-de-status
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - components/dashboard/agenda/appointment-create-dialog.tsx
  - components/dashboard/agenda/pending-requests-panel.tsx
  - components/dashboard/agenda/calendar-day-week-grid.tsx
  - components/dashboard/agenda/calendar-editor.tsx
  - app/dashboard/agenda/page.tsx
autonomous: false
requirements: [APPT-01, APPT-02, APPT-03]
must_haves:
  truths:
    - "Clicking a free/available slot opens the Nova consulta dialog; selecting an existing patient creates a Confirmada appointment that appears immediately in the cell"
    - "The calendar renders all 5 statuses distinctly: pendente (dashed light) vs confirmada (solid filled), falta (destructive tint + UserX) vs cancelada (hatch + strikethrough), realizada (muted)"
    - "The Pedidos a confirmar panel lists pending requests with Confirmar / Recusar actions wired to transitionAppointmentStatusAction"
    - "A confirmed appointment exposes a status menu with only the legal transitions; final-state appointments are read-only"
    - "The RSC loads appointments alongside availability rules+overrides for the week window and passes them to the calendar"
  artifacts:
    - components/dashboard/agenda/appointment-create-dialog.tsx
    - components/dashboard/agenda/pending-requests-panel.tsx
    - components/dashboard/agenda/calendar-day-week-grid.tsx
    - components/dashboard/agenda/calendar-editor.tsx
    - app/dashboard/agenda/page.tsx
  key_links:
    - "free-slot left-click reaches appointment creation without a mode-switch detour, coexisting with Phase 6's availability menu (disambiguation rule)"
    - "cell status matched by starts_at against the expanded FreeSlot.start (expandAvailability); active booking takes visual precedence over historical (D-07)"
    - "double-booking result union renders as an inline dialog error, never a raw error"
    - "all fills/borders/badges consume oklch tokens only (no hex/rgb); all copy PT-BR from UI-SPEC"
---

<objective>
Integrate appointments into the Phase 6 editable calendar per the approved UI-SPEC: a creation Dialog opened by clicking a free slot (patient search via the existing patients domain), 5-status rendering on the grid (pendente≠confirmada, falta≠cancelada), a "Pedidos a confirmar" panel with Confirmar/Recusar, a status-transition menu with legal-only transitions, and the RSC loading appointments alongside availability. Covers APPT-01 (creation UI), APPT-02 (status rendering + transition menu), APPT-03 (pending panel confirm/reject).

Purpose: This is the doctor-facing surface where SC-1..3 become observable. It EXTENDS the Phase 6 calendar (does NOT rebuild it), reuses the vendored shadcn primitives, and consumes tokens only. Ends with a [BLOCKING] visual checkpoint because the 5-status distinctions and the creation flow require human verification.
Output: appointment-create-dialog, pending-requests-panel, extended calendar grid/editor, RSC parallel load of appointments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-consultas-ciclo-de-status/07-UI-SPEC.md
@.planning/phases/07-consultas-ciclo-de-status/07-RESEARCH.md
@.planning/phases/07-consultas-ciclo-de-status/07-PATTERNS.md
@components/dashboard/agenda/calendar-editor.tsx
@components/dashboard/agenda/calendar-day-week-grid.tsx
@components/dashboard/agenda/availability-cell-menu.tsx
@app/dashboard/agenda/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Creation dialog + pending-requests panel</name>
  <files>components/dashboard/agenda/appointment-create-dialog.tsx, components/dashboard/agenda/pending-requests-panel.tsx</files>
  <read_first>
    - CREATE: components/dashboard/agenda/appointment-create-dialog.tsx — analogs: components/ui/dialog.tsx, components/ui/command.tsx / combobox.tsx (patient search), components/dashboard/agenda/availability-cell-menu.tsx (client state + controlled open + PT-BR copy), calendar-editor.tsx lines 13-14 (toast from sonner, Loader2 from lucide)
    - CREATE: components/dashboard/agenda/pending-requests-panel.tsx — analogs: components/ui/card.tsx, components/ui/badge.tsx, components/ui/alert-dialog.tsx, calendar-editor.tsx lines 16-27 (AlertDialog import block + destructive-confirm usage)
    - Patient search reuses (D-04): modules/patients/get-patients-by-profile-id.ts, modules/patients/find-patient-by-profile-id-name-responsible.ts
    - Actions: actions/appointments (createAppointmentAction, transitionAppointmentStatusAction)
    - ALL copy verbatim from 07-UI-SPEC.md §Copywriting Contract, §Empty states, §Error states, §Destructive/final-state actions; layout from §Key Layout 1 and §Key Layout 3
  </read_first>
  <action>
    Create two client components ("use client"):
    (a) `appointment-create-dialog.tsx`: a shadcn `Dialog` (centered modal, not the virtual popover). Title "Nova consulta"; description "{data} · {horário} — escolha o paciente para agendar neste horário." with the locked slot data/time derived from the clicked slot (D-01 — no time editing). Body: a `Command`(cmdk)/`Combobox` patient search delegating to `getPatientsByProfileId` / `findPatientByProfileIdNameAndResponsible` (scoped by profile_id); client-side filter over the profile's patients is acceptable. Each result row: patient name (`text-base`) + responsável (`text-sm text-muted-foreground`), optional avatar. States: before-typing "Comece a digitar para buscar.", loading skeleton rows, no-results "Nenhum paciente encontrado.", selected shows the chosen patient with a "trocar" affordance. NO patient creation here (D-04). Footer: primary CTA "Agendar consulta" (accent, disabled until a patient is selected; "Agendando..." + Loader2 while saving), secondary "Cancelar" (`variant="ghost"`). On submit call `createAppointmentAction`; on `ok:true` toast "Consulta agendada." and close (the cell renders Confirmada, D-05); on the double-booking result union render an INLINE error inside the dialog (not a raw error) with the CTA re-enabled; on no-patient Zod error show field-level `text-destructive text-sm` "Selecione um paciente para agendar.".
    (b) `pending-requests-panel.tsx`: a `Card` titled "Pedidos a confirmar" with a count badge "{n} pendente(s)". Each request row: patient name (`text-base`) + responsável (`text-sm muted`) + slot "{data} {horário}" + a "Pendente" badge (`variant="outline"` text-primary), with inline **Confirmar** (`Button size="sm"`, accent → transitionAppointmentStatusAction pending→confirmed, toast "Pedido confirmado.") and **Recusar** (`Button size="sm" variant="outline"` → destructive `AlertDialog` title "Recusar este pedido?", body "A consulta de {paciente} em {data} {horário} será cancelada e o horário liberado. Esta ação não pode ser desfeita.", confirm "Recusar" variant destructive, cancel "Voltar" → pending→canceled, toast "Pedido recusado."). Empty state: dashed-border pattern `rounded-xl border border-dashed p-8 text-center` with "Nenhum pedido a confirmar." + "Os pedidos de agendamento aparecem aqui quando a assistente marca uma consulta.". Controls ≥44px in one axis.
    Consume oklch tokens only (no hex/rgb). All strings PT-BR verbatim from UI-SPEC.
  </action>
  <verify>
    <automated>yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - appointment-create-dialog.tsx contains the verbatim CTA "Agendar consulta", saving label "Agendando...", title "Nova consulta", and the no-patient message "Selecione um paciente para agendar." (grep each).
    - The dialog delegates patient search to the existing patients domain (grep `getPatientsByProfileId` OR `findPatientByProfileIdNameAndResponsible`) and does NOT create patients (no createPatientAction import).
    - The dialog renders the double-booking result union inline (branches on the action result `ok:false`) and does not render `error.message`/raw Postgres text.
    - pending-requests-panel.tsx contains "Pedidos a confirmar", "{n} pendente(s)" pattern, "Confirmar", "Recusar", the AlertDialog title "Recusar este pedido?", and the empty-state copy "Nenhum pedido a confirmar." (grep each).
    - `grep -riE "#[0-9a-f]{3,6}|rgb\(" components/dashboard/agenda/appointment-create-dialog.tsx components/dashboard/agenda/pending-requests-panel.tsx | grep -v '^#' | wc -l` returns 0 (tokens only, no hardcoded colors).
    - `yarn typecheck` passes.
  </acceptance_criteria>
  <done>Creation dialog (patient search via patients domain, Confirmada on success, inline double-booking error) and pending-requests panel (Confirmar/Recusar wired to the transition action, destructive AlertDialog on Recusar, dashed empty state) exist, PT-BR verbatim, tokens only, typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend the calendar grid/editor for 5-status rendering + transition menu, and load appointments in the RSC</name>
  <files>components/dashboard/agenda/calendar-day-week-grid.tsx, components/dashboard/agenda/calendar-editor.tsx, app/dashboard/agenda/page.tsx</files>
  <read_first>
    - MODIFY: components/dashboard/agenda/calendar-day-week-grid.tsx — analog: itself (per-cell rendering lines 264-309, CellState type line 35, onCellMenu/onDragSelect seam; gesture logic lines 135-223 unchanged; text-xs precedent lines 251/261)
    - MODIFY: components/dashboard/agenda/calendar-editor.tsx — analog: itself (props lines 230-238, local RuleRow/OverrideRow types lines 48-65, toast/Loader2 lines 13-14); FreeSlot type: lib/expand-availability.ts lines 61-68
    - MODIFY: app/dashboard/agenda/page.tsx — analog: itself (parallel load lines 38-41, auth+paid gate lines 32-36, CLINIC_TIME_ZONE + week-window lines 44-47)
    - New module: modules/appointments/list-appointments-by-profile-id.ts (from Plan 02)
    - Status color system + rendering rules verbatim from 07-UI-SPEC.md §Appointment status color system, §Key Layout 2 and 4
  </read_first>
  <action>
    (a) `calendar-day-week-grid.tsx`: extend per-cell rendering to overlay appointment status treatment when a cell is booked, and route left-click on a **free/available** cell to appointment creation (disambiguation rule: free/available cell → Nova consulta; empty cell → keep the Phase 6 "Disponibilidade" menu; right-click folga unchanged). Do NOT change the pointer-events gesture logic — add an appointment layer via the existing callback seam. Render each status per the UI-SPEC triple (fill/border + lucide icon + inline status): Pendente `bg-primary/10` + dashed `border-primary/60` + Clock; Confirmada `bg-primary/70 text-primary-foreground` + solid `border-primary` + CalendarCheck; Realizada `bg-muted` + `border-border` + Check; Falta `bg-destructive/10` + `border-destructive/40` text-destructive + UserX; Cancelada `bg-muted` + diagonal-hatch overlay + line-through name + CalendarX. In-grid text uses `text-xs`. A booked slot occupies exactly one cell (D-01). Active booking (pendente/confirmada) takes visual precedence over historical (realizada/falta/cancelada) in a re-booked cell (D-07). On a confirmed cell expose a `dropdown-menu`/detail popover with only the legal transitions (Marcar como realizada, Marcar falta, Cancelar consulta via destructive AlertDialog); final states show read-only detail with no transition items (D-06).
    (b) `calendar-editor.tsx`: add an `appointments: AppointmentRow[]` prop (snake_case crude rows, mirroring the local RuleRow/OverrideRow types). Host the creation dialog + pending-requests panel (panel adjacent to the calendar: right side on ≥lg, stacked on smaller viewports per UI-SPEC §Key Layout 3). Map cell status by matching appointment `starts_at` against the expanded `FreeSlot.start`. Reuse the already-imported toast/Loader2.
    (c) `app/dashboard/agenda/page.tsx`: add `listAppointmentsByProfileId(supabase, profile.id, weekStart, weekEnd)` to the existing `Promise.all` parallel load, map the rows, and pass `appointments` to `<CalendarEditor>`. The auth+paid gate and CLINIC_TIME_ZONE/week-window derivation are unchanged. On a load failure surface "Não foi possível carregar as consultas. Atualize a página para tentar novamente." per UI-SPEC.
    Month view stays indicator-only (D-18): may add an appointment count per day, never renders appointment cells/times.
  </action>
  <verify>
    <automated>yarn typecheck && yarn build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx adds `listAppointmentsByProfileId` to the parallel load (grep) and passes an `appointments` prop to CalendarEditor (grep `appointments=`).
    - calendar-editor.tsx declares an `appointments` prop of an appointment row array type (grep `appointments`).
    - calendar-day-week-grid.tsx references all 5 status treatments: grep for the lucide icons `CalendarCheck`, `UserX`, `CalendarX`, `Clock`, `Check` (all 5 present) and the distinguishing fills `bg-primary/70` (confirmada) and `bg-destructive/10` (falta) and `border-dashed` (pendente).
    - A confirmed-cell transition affordance offers only legal transitions and references the transition action (grep `transitionAppointmentStatusAction`); final states render no transition items.
    - `grep -riE "#[0-9a-f]{3,6}|rgb\(" components/dashboard/agenda/calendar-day-week-grid.tsx | grep -v '^#' | wc -l` returns 0 (tokens only).
    - `yarn typecheck` passes and `yarn build` completes without error.
  </acceptance_criteria>
  <done>The grid renders 5 distinct statuses with icon+fill+border triples, free-slot click opens creation, confirmed cells expose legal-only transitions, final states are read-only; the editor hosts the dialog + pending panel; the RSC loads appointments in parallel and passes them down; typecheck + build clean; tokens only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: Visual verification of the appointment UI (creation, 5-status distinctions, pending panel, transitions)</name>
  <action>Checkpoint task — no code changes. Run the app and verify the flows in how-to-verify below with human eyes. Do NOT mark the phase verified until the human confirms.</action>
  <what-built>
    The full appointment UI on the Phase 6 calendar: click-a-free-slot creation dialog (patient search + Confirmada on success), 5-status grid rendering, the "Pedidos a confirmar" panel with Confirmar/Recusar, and the confirmed-appointment status-transition menu. The RSC now loads appointments alongside availability. This checkpoint verifies the visual distinctions (SC-2 falta≠cancelada, SC-3 pendente≠confirmada) and the end-to-end creation + double-booking flows, which require human eyes.
  </what-built>
  <how-to-verify>
    1. Run the app (`yarn dev`) and open `/dashboard/agenda` (Dia or Semana view) as a paid profile.
    2. CREATE (APPT-01, SC-1): click a free/available (light blue) slot → the "Nova consulta" dialog opens with the locked {data} · {horário}. Search a patient by name/responsável, select one, click "Agendar consulta" → the cell shows the Confirmada treatment (solid filled blue + CalendarCheck) and a "Consulta agendada." toast appears.
    3. DOUBLE-BOOKING (APPT-04, SC-4): attempt to create a second appointment on the SAME confirmed slot → the dialog shows the inline friendly message "Este horário já foi ocupado por outra consulta. Escolha outro horário livre." (never a raw Postgres/23P01 error).
    4. STATUS DISTINCTIONS (SC-2): drive a confirmed appointment to Falta and another to Cancelada → confirm Falta shows destructive tint + UserX icon + "Falta" badge, while Cancelada shows the neutral muted fill + diagonal hatch + strikethrough name + "Cancelada" badge. They must be unmistakably different.
    5. PENDENTE vs CONFIRMADA (SC-3): with a pending request present (or seed one), confirm the pendente cell is dashed + light (`bg-primary/10`) and the confirmada cell is solid filled (`bg-primary/70`) — legible at the 24px cell size.
    6. PENDING PANEL (APPT-03): in "Pedidos a confirmar", click Confirmar on a pending request → it flips to Confirmada in the grid + "Pedido confirmado." toast; click Recusar on another → the destructive AlertDialog appears, confirm → the slot is released + "Pedido recusado." toast.
    7. FINAL STATES (D-06): open a realizada/falta/cancelada appointment → confirm the status menu shows NO further transitions (read-only detail + historical badge).
    8. TOKENS/COPY: confirm all strings are PT-BR and no color looks off-palette (tokens only).
  </how-to-verify>
  <resume-signal>Type "approved" once creation, the friendly double-booking message, the falta≠cancelada and pendente≠confirmada distinctions, the pending panel confirm/reject, and final-state read-only are all confirmed — or describe the issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → Server Action | The dialog/panel call the gated, validated actions from Plan 02; the UI performs no privileged DB access |
| RSC → module | The RSC loads appointments under the auth+paid gate and profile_id scope (inherited page gate) |
| DB error → UI | The double-booking result union renders as friendly inline/toast copy, never a raw error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Information Disclosure | raw Postgres/23P01 error surfaced in the dialog/toast | medium | mitigate | UI only reads the action's discriminated result union (friendly PT-BR strings); never renders error.message/details |
| T-07-10 | Elevation of Privilege | patient search exposing another profile's patients | high | mitigate | search delegates to getPatientsByProfileId / findPatientByProfileIdNameAndResponsible (already profile_id-scoped, D-04); no cross-profile query added |
| T-07-11 | Spoofing | client claiming a status transition the cycle forbids | high | mitigate | UI only offers legal transitions; the action re-validates via isLegalTransition + compare-and-set (Plan 02) — UI affordance is not the security boundary |
| T-07-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase; all shadcn primitives already vendored in components/ui/ (UI-SPEC §Registry Safety: no third-party registries) |
</threat_model>

<verification>
- `yarn typecheck` + `yarn build` pass.
- Grid renders 5 distinct statuses (icon+fill+border), free-slot click opens creation, pending panel confirm/reject wired, final states read-only, RSC loads appointments in parallel.
- Tokens only (no hex/rgb) in new/modified agenda components; all copy PT-BR verbatim from UI-SPEC.
- [BLOCKING] visual checkpoint confirms SC-2 (falta≠cancelada), SC-3 (pendente≠confirmada), creation, and the friendly double-booking message.
</verification>

<success_criteria>
- SC-1 / APPT-01: doctor creates a Confirmada appointment from a free slot via the dialog, linked to an existing patient.
- SC-2 / APPT-02: the 5-status cycle renders with falta visually distinct from cancelada.
- SC-3 / APPT-03: doctor confirms/rejects pending requests from the panel; pendente is visually distinct from confirmada.
- SC-4 (UI side): a double-booking attempt shows the friendly "horário já ocupado" message, never a raw error.
</success_criteria>

<output>
Create `.planning/phases/07-consultas-ciclo-de-status/07-03-SUMMARY.md` when done.
</output>
