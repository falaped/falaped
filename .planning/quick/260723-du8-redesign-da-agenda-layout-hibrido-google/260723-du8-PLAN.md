---
phase: quick-260723-du8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/agenda/calendar-time-grid.tsx
  - components/dashboard/agenda/booking-rail.tsx
  - components/dashboard/agenda/calendar-editor.tsx
  - components/dashboard/agenda/appointment-status-style.ts
  - components/dashboard/agenda/calendar-day-week-grid.tsx
  - components/dashboard/agenda/appointment-detail-menu.tsx
autonomous: false
requirements: [APPT-01, APPT-02, APPT-03, AGENDA-05]
user_setup: []

must_haves:
  truths:
    - "The Dia/Semana view renders as a time-grid (hour axis left, day columns, week starts Monday) with appointment blocks positioned by start time + duration (not one cell per appointment), a now-line on today's column, and today's column highlighted"
    - "A fixed Calendly-style booking rail sits to the right of the grid on ≥lg and stacks below on smaller viewports: mini month date-picker, patient search (reusing the patients domain), duration chips 15/30/45/60/90, a list of the selected day's FREE slots (from expandAvailability minus occupied), and an 'Agendar consulta' CTA wired to createAppointmentAction"
    - "The 5 statuses read distinctly on the time-grid using only Falaped tokens: Pendente = primary dashed light + Clock; Confirmada = solid primary + CalendarCheck; Realizada = muted + Check; Falta = destructive + UserX; Cancelada = muted + diagonal hatch + strikethrough + CalendarX"
    - "Clicking a free time on the grid still opens creation, availability painting (drag + right-click folga) from Phase 6 still works, and the future-only booking rule is respected"
    - "Dia, Semana and Mês are all first-class views (toggle); Mês stays indicator-only"
  artifacts:
    - components/dashboard/agenda/calendar-time-grid.tsx
    - components/dashboard/agenda/booking-rail.tsx
    - components/dashboard/agenda/appointment-status-style.ts
    - components/dashboard/agenda/calendar-editor.tsx
  key_links:
    - "booking rail 'Agendar consulta' → createAppointmentAction; double-booking result union renders inline friendly (never raw error)"
    - "free-slot list in the rail derived from expandAvailability slots for the selected day minus slots occupied by active appointments (pending/confirmed)"
    - "time-grid block position computed from starts_at/ends_at in CLINIC_TIME_ZONE (wall-clock), anchored to the same window the grid renders"
    - "status style shared by grid, detail menu and rail via a single exported APPOINTMENT_STATUS_STYLE (tokens only, no hex/rgb)"
---

<objective>
Redesenhar SÓ A CAMADA DE UI da Agenda (Fase 7) para o layout híbrido Google Agenda × Calendly aprovado no mockup `260723-du8-DESIGN-MOCKUP.html`: (1) uma GRADE DE TEMPO para Dia/Semana com blocos posicionados por horário+duração, linha de "agora", coluna de hoje destacada, semana começando segunda; (2) um TRILHO DE AGENDAMENTO FIXO estilo Calendly ao lado da grade; (3) os 5 status legíveis usando SÓ tokens Falaped (forma+tom on-token, sem cor inventada); (4) Dia/Semana/Mês como visões de primeira classe (Mês indicador-only). Consome os contratos já entregues (actions/appointments, modules/appointments, lib/expand-availability) SEM alterá-los.

Purpose: O médico não conseguiu ler a agenda com a grade de células pequenas 24px; a grade de tempo + trilho fixo aprovados priorizam leitura do dia e agendamento em poucos cliques. Termina com um checkpoint [BLOCKING] de verificação visual humana (mesmo modelo do 07-03 Task 3).
Output: nova grade de tempo (`calendar-time-grid.tsx`), trilho de agendamento (`booking-rail.tsx`), estilo de status compartilhado (`appointment-status-style.ts`), e o host (`calendar-editor.tsx`) recomposto para o layout grade+trilho.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260723-du8-redesign-da-agenda-layout-hibrido-google/260723-du8-DESIGN-MOCKUP.html
@.planning/phases/07-consultas-ciclo-de-status/07-UI-SPEC.md
@components/dashboard/agenda/calendar-editor.tsx
@components/dashboard/agenda/calendar-day-week-grid.tsx
@components/dashboard/agenda/appointment-create-dialog.tsx
@components/dashboard/agenda/pending-requests-panel.tsx
@components/dashboard/agenda/appointment-detail-menu.tsx
@components/dashboard/agenda/availability-cell-menu.tsx
@components/dashboard/agenda/calendar-month-indicator.tsx
@app/dashboard/agenda/page.tsx
@lib/expand-availability.ts
@lib/clinic-timezone.ts
</context>

<constraints_recap>
- CAMADA DE UI APENAS. NÃO tocar: supabase/migrations/*, modules/appointments/*, actions/appointments/*, lib/schemas/appointment.ts, lib/expand-availability.ts, app/dashboard/agenda/page.tsx (o RSC já carrega tudo — só consumir os props que ele já passa: rules, overrides, appointments, patients, timeZone). Se um dado necessário JÁ está no props do CalendarEditor, use-o; NÃO adicione carga no RSC.
- Tokens oklch apenas nos arquivos tocados — `grep` por hex/rgb deve dar 0.
- Cópia PT-BR verbatim do mockup/07-UI-SPEC (títulos, CTA, empty/error states, legenda).
- Reusar primitivos shadcn já vendorizados em components/ui/. NÃO adicionar dependência nem registry novo.
- Reaproveitar a lógica já existente do appointment-create-dialog (duração, erro inline de double-booking, filtro client-side de pacientes) — mover/compartilhar, não reescrever do zero.
</constraints_recap>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Grade de tempo (blocos posicionados) + estilo de status compartilhado</name>
  <files>components/dashboard/agenda/calendar-time-grid.tsx, components/dashboard/agenda/appointment-status-style.ts, components/dashboard/agenda/calendar-day-week-grid.tsx, components/dashboard/agenda/appointment-detail-menu.tsx</files>
  <read_first>
    - MOCKUP (fonte visual): `.planning/quick/260723-du8-redesign-da-agenda-layout-hibrido-google/260723-du8-DESIGN-MOCKUP.html` — estrutura `.fp-grid` (gutter `52px` + colunas-dia), `.fp-times`/`.fp-col`/`.row` (altura de hora `--hour-h`), `.fp-now` (linha de agora), `.fp-col.today` (coluna de hoje), `.fp-ev` (bloco absoluto por `top`=offset da hora e `height`=duração), classes `.ev-pend/.ev-conf/.ev-done/.ev-miss/.ev-canc`, e a função `top(h)` que converte hora→pixels.
    - EXISTENTE a reusar: `components/dashboard/agenda/calendar-day-week-grid.tsx` — extrair de lá o objeto `APPOINTMENT_STATUS_STYLE`, os tipos `CellState`, `CellAppointment`, `DayColumn`, `MenuAnchor`, `PaintMode`, `isActiveAppointmentStatus`, as consts `STEP`/`DAY_END`, os helpers `minutesToLabel`/`dateCellKey`/`DAY_LABELS`, e toda a lógica de gesto Pointer Events (pointerDown/Enter/Move/Up/contextMenu, `setPointerCapture`, limiar de 6px, distinção clique-vs-arraste) — esta lógica de PINTURA DE DISPONIBILIDADE não pode quebrar (Fase 6).
    - `lib/expand-availability.ts` (FreeSlot: `{ start, end, localDate }`) — NÃO alterar; a grade se ancora nos minutos livres via o `cellStateOf` já provido pelo pai.
    - `components/dashboard/agenda/appointment-detail-menu.tsx` — importa `APPOINTMENT_STATUS_STYLE`, `CellAppointment`, `MenuAnchor` de `./calendar-day-week-grid`; após o move desses símbolos, reapontar os imports (ver ação).
    - 07-UI-SPEC.md §Appointment status color system (contrato dos 5 status, verbatim).
  </read_first>
  <action>
    Objetivo: transformar Dia/Semana de uma grade célula-a-célula (30 min × 1.5rem) numa GRADE DE TEMPO com blocos absolutos (estilo Google Agenda) PRESERVANDO a pintura de disponibilidade da Fase 6.

    (a) EXTRAIR o contrato de status para um módulo próprio `appointment-status-style.ts` ("use client" não necessário — é dado puro/estilo): mover para lá `APPOINTMENT_STATUS_STYLE` e o tipo `CellAppointment` VERBATIM (mesmas classes token-only já existentes: pendente `bg-primary/10 border border-dashed border-primary/60 text-primary` + Clock; confirmada `bg-primary/70 border border-primary text-primary-foreground` + CalendarCheck; realizada `bg-muted border border-border text-muted-foreground` + Check; falta `bg-destructive/10 border border-destructive/40 text-destructive` + UserX; cancelada `bg-muted border border-border text-muted-foreground` + hatch + strike + CalendarX). Reexportar de `calendar-day-week-grid.tsx` os mesmos símbolos (re-export) para não quebrar imports antigos, OU reapontar todos os call-sites; escolha a rota com menos churn e deixe o build limpo. Reapontar o import em `appointment-detail-menu.tsx` para `./appointment-status-style` onde couber (mantendo `MenuAnchor` de onde ele viver).

    (b) CRIAR `calendar-time-grid.tsx` ("use client"), a nova grade de tempo Dia/Semana, substituindo o papel visual de `calendar-day-week-grid.tsx` no Dia/Semana. Props (espelhar o que o pai já sabe prover — mesmos callbacks do grid atual para não mudar o pai além do necessário): `days: DayColumn[]`, `minuteRows: number[]`, `cellStateOf`, `appointmentOf`, `isCellBookable`, `onDragSelect`, `onCellMenu`, `onAppointmentCreate`, `onAppointmentSelect`, e um novo `appointmentsForColumn(localDate) => PositionedAppointment[]` (ver abaixo) OU derive os blocos internamente a partir de um novo prop `appointments` cru — prefira derivar posição no PAI e passar blocos já posicionados para manter a grade "burra"/testável. Layout do mockup:
      - Cabeçalho: gutter vazio + uma célula por dia com `dow` (seg/ter/…) e `dnum`; coluna de hoje com o número destacado (mesmo tratamento token do mockup `.today`, usando `bg-primary text-primary-foreground` num badge circular).
      - Corpo: coluna-gutter de horas (rótulo `HH:00` por hora, `text-xs text-muted-foreground`) + N colunas-dia. Cada coluna é `relative`; as linhas de hora são divisórias (`border-b`). Altura de hora fixa (defina `HOUR_H` em px, análogo ao `--hour-h` do mockup) — a janela de horas vem de `minuteRows` (primeiro/último minuto), NÃO hardcode 08–19.
      - Coluna de hoje: fundo com destaque leve on-token (ex.: `bg-primary/5`), sem cor inventada.
      - Linha de AGORA: só na coluna de hoje, posicionada por `top = (nowMinuteOfDay - windowStartMinute) / 60 * HOUR_H`; ponto + linha finos usando `bg-primary` (o mockup usa vermelho `--now`, mas para ficar SÓ em token Falaped use `bg-primary` ou `bg-destructive` — escolha `bg-primary` para não colidir com o vermelho reservado a Falta; documente a escolha no JSDoc). O "agora" pode congelar na montagem (mesmo padrão `nowMs` do editor).
      - BLOCOS de consulta: um `<div>` ABSOLUTO por consulta na sua coluna-dia, `top` = offset do `starts_at` e `height` = `(duração em min)/60 * HOUR_H` (mínimo ~ meia linha para legibilidade), com `overflow-hidden`. Conteúdo: ícone lucide do status + nome do paciente (`text-xs`) + horário/label; classes de fill/borda vindas do `APPOINTMENT_STATUS_STYLE` (as mesmas classes token já existentes; a hachura da Cancelada continua via o overlay `repeating-linear-gradient` com `var(--color-muted-foreground)`, já token-only). Clicar um bloco → `onAppointmentSelect(appointment, anchor)`. Blocos sobrepostos (raro) podem simplesmente empilhar/estreitar por índice — não precisa de algoritmo perfeito de colisão nesta iteração; garanta que ativos (pendente/confirmada) fiquem à frente (z maior) do histórico (D-07).
      - CLICAR HORÁRIO LIVRE: manter a INTERAÇÃO da Fase 6. Como a grade agora tem blocos absolutos por cima, a camada de fundo (linhas de hora) precisa continuar recebendo clique/arraste para pintura E o clique num horário livre precisa abrir "Nova consulta". Reaproveite a lógica de Pointer Events do grid atual: as células de disponibilidade continuam sendo alvos clicáveis (uma faixa clicável por linha de 30 min por coluna, `STEP`), posicionadas atrás dos blocos. Preserve: arraste esquerdo → `onDragSelect`; clique esquerdo em livre+futuro → `onAppointmentCreate`; clique esquerdo em vazio → `onCellMenu(...,"left")`; botão direito → `onCellMenu(...,"right")` (folga); livre no passado → no-op. NÃO altere os limiares nem a semântica; só o LAYOUT visual muda (as células agora são finas divisórias de 30 min dentro da grade de hora, não blocos 24px isolados).
      - Disponível (livre) = `bg-primary/25 hover:bg-primary/35` na faixa de 30 min (inalterado); folga = `bg-muted`; vazio = hover `bg-muted`.
    Consumir tokens oklch apenas (sem hex/rgb). Toda cópia PT-BR.
  </action>
  <verify>
    <automated>yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `appointment-status-style.ts` existe e exporta `APPOINTMENT_STATUS_STYLE` com as 5 chaves de status (grep `pending`, `confirmed`, `done`, `no_show`, `canceled`) e as classes distintivas `bg-primary/70` (confirmada) e `bg-destructive/10` (falta) e `border-dashed` (pendente).
    - `calendar-time-grid.tsx` referencia os 5 ícones lucide `CalendarCheck`, `UserX`, `CalendarX`, `Clock`, `Check` (via o style compartilhado) e posiciona blocos absolutamente (grep `absolute` e um cálculo de `top`/`height` a partir de duração/hora).
    - A lógica de gesto de disponibilidade da Fase 6 permanece: grep em `calendar-time-grid.tsx` por `onDragSelect`, `onCellMenu`, `setPointerCapture` (ou `PointerCapture`), e a distinção clique-vs-arraste (grep `moved`).
    - `appointment-detail-menu.tsx` continua compilando com os imports reapontados (nenhum import quebrado para símbolos movidos).
    - `grep -riE "#[0-9a-f]{3,6}|rgb\(" components/dashboard/agenda/calendar-time-grid.tsx components/dashboard/agenda/appointment-status-style.ts | grep -v '^#' | wc -l` retorna 0.
    - `yarn typecheck` passa.
  </acceptance_criteria>
  <done>Nova grade de tempo com blocos absolutos por horário/duração, linha de agora na coluna de hoje, coluna de hoje destacada, gutter de horas; estilo de status extraído e compartilhado (5 status token-only); pintura de disponibilidade da Fase 6 preservada (drag + menus); imports reapontados; typecheck limpo; tokens only.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trilho de agendamento fixo (Calendly) + recompor o host CalendarEditor</name>
  <files>components/dashboard/agenda/booking-rail.tsx, components/dashboard/agenda/calendar-editor.tsx</files>
  <read_first>
    - MOCKUP: aside `.fp-rail` — título "Nova consulta", subtítulo "Escolha o dia, a duração e um horário livre."; `.fp-mini` (mini date-picker do mês, semana começando seg, dias `pastday`/`avail`/`sel`); campo Paciente `.fp-search`; chips de duração `.fp-chips` (15/30&nbsp;min/45/60/90); rótulo do dia longo + `.fp-slots` (lista de horários livres, `.fp-slot` selecionável); CTA `.fp-book` "Agendar consulta"; rodapé `.fp-tz` "Horário de Brasília".
    - EXISTENTE a reaproveitar (NÃO reescrever a lógica): `appointment-create-dialog.tsx` — filtro client-side de pacientes (`filtered`), submit via `createAppointmentAction` com `ends_at = starts_at + duração`, erro INLINE amigável de double-booking (`setInlineError`/`result.error`), presets `DURATION_PRESETS = [15,30,45,60,90]`, estado selecionado + "trocar". O trilho é o caminho PRIMÁRIO aprovado; o dialog modal pode COEXISTIR (aberto ao clicar num bloco livre da grade) OU o trilho pode substituí-lo — RECOMENDAÇÃO: manter ambos (dialog para clique-no-slot, trilho para o fluxo deliberado), compartilhando os mesmos presets e a mesma chamada de action. Não duplicar a lista de pacientes: o pai já recebe `patients`.
    - `calendar-editor.tsx` — o host: já possui `patients`, `appointments`, `expandAvailability` client-side (para o Mês), `slotStartMs`/`isCellBookable`/`nowMs`, `weekDays`, `dayColumns`, `labelsOfInstant`, `pendingRequests`, o `<AppointmentCreateDialog>` e o `<PendingRequestsPanel>`. Reutilize esses helpers para derivar os slots livres do dia selecionado no trilho.
    - `components/ui/*`: card, button, badge, command, avatar, calendar (react-day-picker) se disponível — senão construir o mini-grid do mês à mão como no mockup. Verifique `ls components/ui` antes de assumir.
    - 07-UI-SPEC §Copywriting Contract, §Empty/Error states.
  </read_first>
  <action>
    (a) CRIAR `booking-rail.tsx` ("use client"): o trilho de agendamento fixo estilo Calendly. Props: `patients: Patient[]`, `selectedDate: string` (YYYY-MM-DD), `onSelectedDateChange`, `freeSlots: { minute: number; label: string; startsAt: string }[]` (horários LIVRES do dia selecionado já derivados pelo pai a partir de expandAvailability menos os ocupados por consultas ATIVAS), `timeZone`, e `onCreated?` (para o pai reagir pós-sucesso). Estrutura do mockup:
      - `Card` com título "Nova consulta" (`text-xl`) e subtítulo "Escolha o dia, a duração e um horário livre." (`text-sm text-muted-foreground`).
      - Mini date-picker do mês (semana começando segunda): reusar `components/ui/calendar` (react-day-picker, `weekStartsOn: 1`, `locale ptBR`) se existir; senão um grid 7-col à mão. Dias com disponibilidade recebem realce leve on-token; o dia selecionado usa `bg-primary text-primary-foreground`. Selecionar um dia chama `onSelectedDateChange`.
      - Campo "Paciente": busca reusando o MESMO padrão do dialog (Command/cmdk, filtro client-side por nome/responsável sobre `patients`, estado selecionado com "trocar", empty states "Comece a digitar para buscar." / "Nenhum paciente encontrado." verbatim). Não cria paciente (D-04).
      - Chips de "Duração": botões `15`, `30 min`, `45`, `60`, `90` (aria-pressed no ativo) usando `DURATION_PRESETS` — extraia esse array para um ponto compartilhado (ex.: exporte de `appointment-create-dialog.tsx` ou coloque num pequeno módulo) para não divergir.
      - Rótulo do dia longo (ex.: "quinta-feira, 23 de julho", `capitalize`) + lista `.fp-slots` de horários LIVRES (`freeSlots`): cada slot um botão selecionável (`aria-pressed`), `font-variant-numeric: tabular-nums`, ativo com `bg-primary text-primary-foreground`. Empty: "Nenhum horário livre neste dia." (curto, PT-BR) quando `freeSlots` vazio.
      - CTA "Agendar consulta" (full-width, accent; desabilitada até paciente + slot selecionados; "Agendando..." + Loader2 ao salvar) → `createAppointmentAction({ patient_id, starts_at: slot.startsAt, ends_at: startsAt + duração })`. Sucesso: toast "Consulta agendada.", limpa a seleção, chama `onCreated`. Falha (double-booking/indisponível/genérico): erro INLINE amigável `rounded-lg border border-destructive/40 bg-destructive/10 ... text-destructive` com `result.error` (nunca erro cru).
      - Rodapé: "Horário de Brasília" (`text-xs text-muted-foreground`) — sem hora dinâmica obrigatória.
    (b) RECOMPOR `calendar-editor.tsx` para o layout grade+trilho aprovado:
      - Trocar `<CalendarDayWeekGrid>` por `<CalendarTimeGrid>` nas abas Dia e Semana (mesmos props/callbacks; adicionar o que a nova grade precisar para os blocos posicionados — derive as posições no editor a partir de `appointments` + `labelsOfInstant` + a janela de `minuteRows`).
      - Layout: em Dia/Semana, a grade fica à esquerda (`min-w-0 flex-1`) e o TRILHO FIXO à direita em `≥lg` (`lg:w-80 lg:shrink-0`), empilhado abaixo em telas menores (o container já usa `flex flex-col gap-6 lg:flex-row`). O `PendingRequestsPanel` continua existindo — posicione-o DENTRO/ABAIXO do trilho (mesma coluna direita) ou logo abaixo dele, para não competir com a grade. Decida o arranjo mais limpo e documente no JSDoc; o trilho de agendamento é o caminho primário.
      - Estado novo no editor: `selectedRailDate` (default = hoje/dia útil), e um memo `railFreeSlots` que roda `expandAvailability` (client-side, já importado) para o dia selecionado e REMOVE os minutos ocupados por consultas ATIVAS (pending/confirmed) daquele dia — reutilize `appointmentByCell`/`cellStateOf`/`isCellBookable` (só futuro é agendável). Passar ao `<BookingRail>`.
      - Mês permanece `<CalendarMonthIndicator>` (indicador-only, inalterado); o toggle Dia/Semana/Mês (TabsList) permanece.
      - Manter intactos: gate de disponibilidade (drag/menus), batch save, guarda de descarte, `AppointmentCreateDialog` (clique-no-slot), `AppointmentDetailMenu`, `AvailabilityCellMenu`, `PendingRequestsPanel`.
      - Após criar pelo trilho ou pelo dialog, a agenda depende de revalidação do RSC (as actions já revalidam) — não implementar mutação otimista complexa; confie no reload do server component como hoje.
    Tokens oklch apenas; cópia PT-BR verbatim.
  </action>
  <verify>
    <automated>yarn typecheck && yarn build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `booking-rail.tsx` existe e contém verbatim: "Nova consulta", "Escolha o dia, a duração e um horário livre.", o CTA "Agendar consulta", o label de salvamento "Agendando...", "Horário de Brasília" (grep cada).
    - O trilho reusa o domínio patients por filtro client-side sobre a prop `patients` (grep `patients`) e NÃO importa `createPatientAction`.
    - O trilho chama `createAppointmentAction` (grep) e renderiza o erro inline (branch em `result.ok === false` / `result.error`), nunca `error.message` cru.
    - Os chips de duração cobrem 15/30/45/60/90 (grep `15`, `30`, `45`, `60`, `90` ou o array `DURATION_PRESETS` compartilhado).
    - `calendar-editor.tsx` renderiza `<CalendarTimeGrid` (grep) nas abas Dia e Semana e `<BookingRail` (grep) no layout, e mantém `<CalendarMonthIndicator` no Mês.
    - `grep -riE "#[0-9a-f]{3,6}|rgb\(" components/dashboard/agenda/booking-rail.tsx components/dashboard/agenda/calendar-editor.tsx | grep -v '^#' | wc -l` retorna 0.
    - `yarn typecheck` passa e `yarn build` completa sem erro.
  </acceptance_criteria>
  <done>Trilho de agendamento fixo (mini date-picker seg-first, busca de paciente reusando patients, chips 15/30/45/60/90, lista de horários livres do dia, CTA → createAppointmentAction com erro inline) existe; o host recompõe grade-de-tempo + trilho fixo (≥lg à direita, empilhado abaixo em telas menores), preserva pending-panel/dialog/menus/disponibilidade, Mês indicador-only; typecheck + build limpos; tokens only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: Verificação visual humana do redesign híbrido da agenda</name>
  <action>Tarefa de checkpoint — sem mudança de código. Rode o app e verifique os fluxos abaixo com olhos humanos. NÃO marque como verificado até o humano confirmar.</action>
  <what-built>
    O redesign híbrido Google Agenda × Calendly da Agenda (Fase 7), SÓ na camada de UI: grade de tempo Dia/Semana com blocos posicionados por horário/duração, linha de agora, coluna de hoje destacada; trilho de agendamento fixo à direita (mini date-picker, busca de paciente, chips de duração, horários livres, "Agendar consulta"); 5 status distintos usando só tokens Falaped; Dia/Semana/Mês como visões de primeira classe (Mês indicador-only). Consome os contratos já entregues (actions/modules/expand) sem alterá-los.
  </what-built>
  <how-to-verify>
    1. Rode `yarn dev` e abra `/dashboard/agenda` como perfil pago.
    2. GRADE DE TEMPO (Semana): confirme o eixo de horas à esquerda, colunas por dia começando SEGUNDA, coluna de HOJE destacada, e a LINHA DE AGORA na coluna de hoje. As consultas aparecem como BLOCOS posicionados pelo horário e com altura proporcional à duração (não mais uma célula 24px por consulta).
    3. TRILHO FIXO (≥lg): à direita da grade há o trilho "Nova consulta" com mini date-picker do mês, busca de paciente, chips de duração (15/30/45/60/90) e a lista de horários LIVRES do dia selecionado. Em janela estreita (<lg) o trilho empilha ABAIXO da grade. Selecione um dia, um paciente, uma duração e um horário livre → clique "Agendar consulta" → toast "Consulta agendada." e a consulta aparece na grade.
    4. DOUBLE-BOOKING: tente agendar (pelo trilho OU pelo clique num slot) sobre um horário já ocupado → mensagem inline amigável "Este horário já foi ocupado por outra consulta. Escolha outro horário livre." (nunca erro cru/23P01).
    5. 5 STATUS (só tokens): confirme que Pendente (tracejado leve + Clock), Confirmada (sólido azul + CalendarCheck), Realizada (cinza + Check), Falta (vermelho + UserX) e Cancelada (cinza + hachura diagonal + nome riscado + CalendarX) são inconfundíveis — em especial Pendente≠Confirmada e Falta≠Cancelada. Nenhuma cor fora da paleta.
    6. DISPONIBILIDADE (Fase 6 intacta): arraste numa área vazia para pintar disponibilidade; botão direito para folga; clique num horário livre abre "Nova consulta". Nada disso pode ter quebrado.
    7. MÊS: alterne para Mês → continua indicador-only (contagem/pílulas por dia), convivendo com a nova grade de tempo em Dia/Semana. O toggle Dia/Semana/Mês funciona.
    8. PEDIDOS A CONFIRMAR: a fila continua acionável (Confirmar/Recusar) no layout novo.
  </how-to-verify>
  <resume-signal>Digite "approved" quando a grade de tempo, o trilho fixo (e o empilhamento responsivo), os 5 status distintos, a criação ponta-a-ponta, a mensagem amigável de double-booking, a disponibilidade da Fase 6 e o Mês indicador estiverem todos confirmados — ou descreva os problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → Server Action | Trilho e dialog chamam as actions já gated/validadas (createAppointmentAction, transitionAppointmentStatusAction); a UI não faz acesso privilegiado ao DB |
| DB error → UI | O double-booking (23P01) já vira result union amigável na action; a UI só renderiza a string PT-BR, nunca o erro cru |
| RSC → UI props | O RSC já carrega appointments/patients sob o gate auth+paid + escopo profile_id; esta task só consome os props, sem nova carga |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-du8-01 | Information Disclosure | erro cru do Postgres/23P01 exposto no trilho/dialog | medium | mitigate | UI lê só o result union da action (strings PT-BR amigáveis); nunca renderiza error.message/detalhe |
| T-du8-02 | Elevation of Privilege | busca de paciente vazando pacientes de outro perfil | high | mitigate | filtro client-side sobre a prop `patients` já escopada por profile_id no RSC (D-04); nenhuma query cross-profile adicionada |
| T-du8-03 | Spoofing | cliente forjando transição/agendamento fora da regra | high | mitigate | a UI só oferece ações legais; as actions revalidam (isLegalTransition, slot-free check, exclusion constraint) — a UI não é a fronteira de segurança |
| T-du8-SC | Tampering | npm/pip/cargo installs | high | accept | Nenhum pacote novo; todos os primitivos shadcn já vendorizados em components/ui/ (07-UI-SPEC §Registry Safety: sem registries de terceiros) |
</threat_model>

<verification>
- `yarn typecheck` + `yarn build` passam.
- Dia/Semana renderizam como grade de tempo com blocos posicionados, linha de agora, coluna de hoje; trilho fixo à direita (≥lg) empilhando abaixo em telas menores; 5 status distintos token-only; Mês indicador-only; disponibilidade da Fase 6 intacta.
- Tokens oklch apenas (sem hex/rgb) nos arquivos tocados; cópia PT-BR verbatim do mockup/UI-SPEC.
- [BLOCKING] checkpoint visual confirma grade de tempo, trilho, 5 status, criação ponta-a-ponta, double-booking amigável, disponibilidade e Mês.
</verification>

<success_criteria>
- APPT-01: médico cria consulta a partir do trilho fixo (e do clique num slot livre) ligada a um paciente existente; erro de double-booking amigável.
- APPT-02: os 5 status renderizam distintos na grade de tempo (Pendente≠Confirmada, Falta≠Cancelada), token-only.
- APPT-03: fila "Pedidos a confirmar" segue acionável no layout novo.
- AGENDA-05 (leitura): a agenda prioriza a leitura das consultas do dia (grade de tempo + trilho), sem quebrar a edição de disponibilidade da Fase 6.
</success_criteria>

<output>
Create `.planning/quick/260723-du8-redesign-da-agenda-layout-hibrido-google/260723-du8-SUMMARY.md` when done.
</output>
