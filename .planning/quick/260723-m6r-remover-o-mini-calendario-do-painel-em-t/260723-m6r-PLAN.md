---
phase: quick-260723-m6r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/agenda/calendar-time-grid.tsx
  - components/dashboard/agenda/calendar-month-indicator.tsx
  - components/dashboard/agenda/booking-rail.tsx
  - components/dashboard/agenda/availability-panel.tsx
  - components/dashboard/agenda/agenda-side-panel.tsx
  - components/dashboard/agenda/calendar-editor.tsx
autonomous: false
requirements: [M-1, M-2, M-3, M-4, M-5]

must_haves:
  truths:
    - "O mini calendário (react-day-picker) some do painel lateral nos DOIS modos (Consulta e Disponibilidade/Folga) — M-5"
    - "Clicar no cabeçalho do dia na Semana seleciona e destaca aquele dia; clicar na célula do Mês faz o mesmo — M-1/M-2"
    - "Na visão Dia o dia exibido É o dia selecionado — M-1"
    - "O painel opera sobre um único dia selecionado global, mantido ao trocar de modo — M-4"
    - "Recorrência em Disponibilidade/Folga pré-seleciona o weekday do dia selecionado; multi-seleção permanece — M-5"
    - "Clicar num slot livre continua criando consulta (inalterado)"
  artifacts:
    - components/dashboard/agenda/calendar-time-grid.tsx
    - components/dashboard/agenda/calendar-month-indicator.tsx
    - components/dashboard/agenda/booking-rail.tsx
    - components/dashboard/agenda/availability-panel.tsx
    - components/dashboard/agenda/agenda-side-panel.tsx
    - components/dashboard/agenda/calendar-editor.tsx
  key_links:
    - "onSelectDay da grade Semana + célula do Mês → setSelectedRailDate no calendar-editor (M-4)"
    - "selectedRailDate → prop selectedLocalDate para destaque na grade e no Mês (M-2)"
    - "weekday do dia selecionado → prefill do toggle de weekday na Recorrência do availability-panel (M-5)"
---

<objective>
Remover o mini calendário (react-day-picker) do painel lateral em TODOS os modos (Consulta e Disponibilidade/Folga). A seleção do dia passa a acontecer clicando no calendário real: cabeçalho do dia na Semana, célula na Mês; na Dia o dia exibido É o selecionado. O painel só reflete o dia selecionado global (M-4) e destaca-o no calendário (M-2), distinto de "hoje".

Purpose: Eliminar a duplicidade "mini calendário no painel × calendário real", tornando o calendário grande o único ponto de seleção de dia — decisões M-1..M-5 LOCKED em CONTEXT.md.
Output: 6 componentes de UI atualizados (grade clicável + destaque, Mês seleciona+destaca, painel sem picker, editor com dia global).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260723-m6r-remover-o-mini-calendario-do-painel-em-t/260723-m6r-CONTEXT.md
@CLAUDE.md
@components/dashboard/agenda/calendar-editor.tsx
@components/dashboard/agenda/calendar-time-grid.tsx
@components/dashboard/agenda/calendar-month-indicator.tsx
@components/dashboard/agenda/booking-rail.tsx
@components/dashboard/agenda/availability-panel.tsx
@components/dashboard/agenda/agenda-side-panel.tsx

Regras UI: tokens oklch apenas (sem hex/rgb), PT-BR verbatim, sem deps novas, CAMADA DE UI APENAS.
NÃO editar: lib/**, actions/**, modules/**, supabase/migrations/**, app/dashboard/agenda/page.tsx.

Contexto-chave já existente no calendar-editor.tsx:
- `selectedRailDate` (state, default = skipWeekend(hoje) no fuso da clínica) — já satisfaz M-3/M-4 como dia global; hoje repassado via `onSelectedDateChange={setSelectedRailDate}`.
- `selectedRailDayLongLabel` (rótulo longo PT-BR), `weekdayOf(localDate, timeZone)`, `todayLocal`, `localDateOf`, `skipWeekend`, `context`/`tz`.
- Grade recebe `days: DayColumn[]` (cada um com `localDate`, `weekdayLabel`, `dayNumber`, `isToday`). Mês recebe `onSelectDay(day: Date)` e hoje navega para a aba Dia.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Seleção de dia no calendário real (grade Semana/Dia + Mês) com destaque, ponta a ponta</name>
  <files>components/dashboard/agenda/calendar-time-grid.tsx, components/dashboard/agenda/calendar-month-indicator.tsx, components/dashboard/agenda/calendar-editor.tsx</files>
  <action>
Fatia vertical M-1/M-2/M-4: tornar a seleção de dia possível clicando no calendário real e refleti-la no dia global do editor, com destaque token-only.

calendar-time-grid.tsx (cabeçalho de dias):
- Adicionar duas props opcionais ao componente: `selectedLocalDate?: string` e `onSelectDay?: (localDate: string) => void`.
- Trocar o `<div>` de cada célula de cabeçalho (o bloco que renderiza `weekdayLabel` + `dayNumber`) por um `<button type="button">` acessível quando `onSelectDay` existe (mantém o mesmo layout flex/py). Ao clicar, chamar `onSelectDay(day.localDate)`. Marcar `aria-pressed={day.localDate === selectedLocalDate}` no cabeçalho.
- Destaque do dia SELECIONADO (M-2), distinto do destaque de "hoje": quando `day.localDate === selectedLocalDate`, aplicar um anel token-only no cabeçalho (ex.: `ring-2 ring-primary rounded-md`) e usar `bg-primary/5` na coluna correspondente do corpo. NÃO mexer no tratamento de `isToday` (o círculo `bg-primary` do número permanece); os dois estados coexistem visualmente. Slots livres e blocos de consulta permanecem INALTERADOS.
- Acessibilidade: incluir no `aria-label` do cabeçalho algo como "Selecionar dia" + rótulo do dia, em PT-BR.

calendar-month-indicator.tsx (célula do dia):
- Adicionar prop `selectedLocalDate?: string`. A célula já é um `<button>` que chama `onSelectDay(day)` — mantê-la. Adicionar destaque token-only quando `localDate === selectedLocalDate` (ex.: anel `ring-2 ring-primary` na célula), distinto do círculo de "hoje". Ajustar o texto de rodapé PT-BR se ainda disser "para editar a disponibilidade dele" → substituir por copy coerente com selecionar o dia (ex.: "Clique num dia para selecioná-lo.").

calendar-editor.tsx (fio condutor / dia global):
- Passar `selectedLocalDate={selectedRailDate}` e `onSelectDay={setSelectedRailDate}` para AMBAS as instâncias de CalendarTimeGrid (Dia e Semana). Na Semana isso ativa a seleção pelo cabeçalho; na Dia o cabeçalho da única coluna também seleciona.
- SINCRONIZAR a visão Dia com o dia selecionado (M-1): quando a aba ativa for "dia", a coluna renderizada deve refletir `selectedRailDate`. Derivar a data exibida na Dia a partir de `selectedRailDate` (parsear "YYYY-MM-DD" e ancorar no fuso via `TZDate`, seguindo o padrão de `selectedRailDayLongLabel`) em vez de depender só de `dayCursor`; a navegação prev/hoje/próximo da aba Dia deve ATUALIZAR `selectedRailDate` (mantendo skipWeekend) para permanecer coerente. NÃO alterar a lógica de Semana/Mês da navegação, nem `positionedFor`/`cellStateOf`/criação de consulta.
- Mês: manter o comportamento de `onSelectDay(day)` selecionando o dia — em vez de (ou além de) navegar para a aba Dia, chamar `setSelectedRailDate(format(day, "yyyy-MM-dd", context))` para que o dia clicado no Mês vire o dia global; passar `selectedLocalDate={selectedRailDate}` ao CalendarMonthIndicator. Manter a navegação para a aba Dia se já existia (a decisão M-1 diz que na Dia o dia exibido é o selecionado — coerente).

Preservar TUDO o mais: toggle de modo, applyAvailabilityIntent, save-na-hora, dialog de criação, menu de detalhe, linha de agora.
  </action>
  <verify>
    <automated>yarn typecheck</automated>
  </verify>
  <done>`yarn typecheck` passa. Cabeçalho do dia (Semana) e célula (Mês) são `<button>` que chamam a seleção; o dia selecionado recebe destaque token-only distinto de "hoje"; a visão Dia reflete o dia selecionado. Grade de slots/blocos inalterada.</done>
</task>

<task type="auto">
  <name>Task 2: Remover o mini calendário do painel (booking-rail + availability-panel) e limpar plumbing órfão</name>
  <files>components/dashboard/agenda/booking-rail.tsx, components/dashboard/agenda/availability-panel.tsx, components/dashboard/agenda/agenda-side-panel.tsx, components/dashboard/agenda/calendar-editor.tsx</files>
  <action>
M-5: o painel deixa de renderizar o Calendar/mini date-picker nos dois modos; o dia agora vem só do calendário real (Task 1). As props de data no painel viram read-only.

booking-rail.tsx:
- Remover o import de `Calendar` (@/components/ui/calendar) e o bloco JSX que renderiza o `<Calendar mode="single" .../>` (o comentário do mini date-picker e o componente).
- Remover o handler `handleDayPick` e o memo `selectedAsDate` (ficam órfãos).
- Remover a prop `onSelectedDateChange` da assinatura e do tipo de props (o rail não muda mais a data por conta própria). Manter `selectedDate` (read-only, ainda usado pelo `useEffect` que zera o slot ao trocar de dia) e `selectedDayLongLabel`.
- Ajustar o subtítulo PT-BR "Escolha o dia, a duração e um horário livre." → coerente com não escolher o dia aqui (ex.: "Escolha a duração e um horário livre."). Manter paciente, chips de duração, rótulo do dia longo, lista de horários livres, CTA, rodapé "Horário de Brasília" e o erro inline.

availability-panel.tsx:
- Remover o import de `Calendar` e o bloco JSX do `<Calendar mode="single" .../>`.
- Remover `handleDayPick` e `selectedAsDate` (órfãos).
- Remover a prop `onSelectedDateChange` da assinatura/tipo. Manter `selectedDate` (read-only) para: (a) Período/Dia inteiro operarem sobre ele (já usado em `handleApply` via `date: selectedDate`), (b) exibir o rótulo do dia — receber `selectedDayLongLabel` como nova prop e exibi-lo (ex.: sob o subtítulo), já que o picker que antes mostrava o dia sumiu.
- RECORRÊNCIA pré-seleciona o weekday (M-5): quando o escopo for "recurring", pré-selecionar no estado `weekdays` o weekday correspondente ao `selectedDate`. Derivar o weekday a partir de `selectedDate` no fuso da clínica — receber uma nova prop `selectedWeekday: number` do pai (o editor calcula via o helper `weekdayOf(selectedDate, timeZone)` já existente; NÃO importar date-fns tz aqui para não recriar a lógica de fuso). Ao entrar em "recurring" (ou ao mudar o dia enquanto em "recurring"), semear `weekdays` com `new Set([selectedWeekday])` SE o médico ainda não tiver mexido; a multi-seleção manual permanece intacta (o toggle continua adicionando/removendo). Implementar de forma que trocar o dia selecionado pré-selecione o novo weekday sem apagar seleções que o médico adicionou manualmente — preferir semear apenas quando `weekdays` estiver vazio ou ao transicionar de escopo para "recurring".

agenda-side-panel.tsx:
- Remover a prop `onSelectedDateChange` (não repassa mais aos filhos). Passar `selectedDayLongLabel` também ao `AvailabilityPanel` e `selectedWeekday` (nova prop repassada do pai) ao `AvailabilityPanel`. Manter `selectedDate`, `selectedDayLongLabel`, `freeSlots`, `onApply`, `savingAvailability`.

calendar-editor.tsx:
- Remover `onSelectedDateChange={setSelectedRailDate}` das duas instâncias de `AgendaSidePanel` (o painel não muda mais a data). O `setSelectedRailDate` continua sendo wired pela grade/Mês (Task 1).
- Passar `selectedWeekday={weekdayOf(selectedRailDate, timeZone)}` ao `AgendaSidePanel` (que repassa ao AvailabilityPanel). Reusar o helper `weekdayOf` já existente — NÃO editar lib/.
- Se algum plumbing de date-change do picker ficar órfão após as remoções, removê-lo. `setSelectedRailDate` NÃO deve ficar sem uso (é usado por grade + Mês).
  </action>
  <verify>
    <automated>! grep -nE "from \"@/components/ui/calendar\"" components/dashboard/agenda/booking-rail.tsx components/dashboard/agenda/availability-panel.tsx</automated>
    <automated>yarn build</automated>
  </verify>
  <done>`yarn build` passa. `Calendar` (react-day-picker) NÃO é mais importado em booking-rail.tsx nem em availability-panel.tsx. O painel exibe o rótulo longo do dia selecionado nos dois modos e opera sobre ele; Recorrência pré-seleciona o weekday do dia; a multi-seleção manual continua. `onSelectedDateChange` removida da cadeia painel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Removido o mini calendário do painel lateral (Consulta e Disponibilidade/Folga) e habilitada a seleção de dia clicando no calendário real (cabeçalho do dia na Semana, célula na Mês), com destaque token-only distinto de "hoje". Dia global compartilhado entre os dois modos; visão Dia mostra o dia selecionado; Recorrência pré-seleciona o weekday do dia.
  </what-built>
  <how-to-verify>
Rodar `yarn dev` e abrir /dashboard/agenda (usuário pago).
1. Mini calendário SUMIU: no painel lateral, tanto em "Consulta" quanto em "Disponibilidade/Folga", NÃO há mais o mini date-picker de mês.
2. Semana: clicar no cabeçalho de um dia (ex.: "Qui 23") seleciona e DESTACA aquele dia (anel/realce, distinto do círculo de "hoje"); o painel passa a listar os horários livres desse dia e o rótulo longo reflete o dia.
3. Mês: clicar numa célula seleciona/destaca o dia; ao ir para a Dia, o dia exibido É o selecionado.
4. Dia: o dia exibido corresponde ao selecionado; navegar prev/próximo muda o dia selecionado.
5. Trocar entre os modos Consulta ↔ Disponibilidade/Folga mantém o MESMO dia (M-4).
6. Disponibilidade/Folga → escopo "Recorrência": o weekday do dia selecionado já vem pré-marcado; ainda é possível marcar/desmarcar outros dias.
7. Consulta: clicar num horário livre da lista + escolher paciente + duração ainda AGENDA (fluxo intacto).
8. Clicar num slot livre na grade ainda abre "Nova consulta" (inalterado).
Confirmar que não há hex/rgb novos e a copy está em PT-BR.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
UI-only, sem novos limites de confiança, sem instalação de pacotes, sem alteração de actions/modules. Nenhuma superfície STRIDE nova introduzida (o gate de auth/paid e o escopo por profile_id permanecem intactos nos actions existentes, não tocados). Sem registro STRIDE aplicável.
</threat_model>

<verification>
- `yarn typecheck` limpo após Task 1.
- `yarn build` limpo após Task 2.
- Gate sem hex/rgb nos arquivos tocados (contagem normalizada, ignorando comentários):
  `grep -rhvE '^\s*//|^\s*\*' components/dashboard/agenda/calendar-time-grid.tsx components/dashboard/agenda/calendar-month-indicator.tsx components/dashboard/agenda/booking-rail.tsx components/dashboard/agenda/availability-panel.tsx components/dashboard/agenda/agenda-side-panel.tsx components/dashboard/agenda/calendar-editor.tsx | grep -icE '#[0-9a-f]{3,6}\b|rgb\(' ` deve retornar `0`.
- Gate de remoção do Calendar: `grep -lE 'from "@/components/ui/calendar"' components/dashboard/agenda/booking-rail.tsx components/dashboard/agenda/availability-panel.tsx` não retorna nenhum dos dois arquivos.
- Checkpoint humano [BLOCKING] aprovado.
</verification>

<success_criteria>
- Mini calendário removido dos dois modos do painel; sem import de `Calendar` em booking-rail.tsx e availability-panel.tsx.
- Seleção de dia via cabeçalho (Semana) e célula (Mês), com destaque token-only distinto de "hoje".
- Dia global único compartilhado; Dia exibe o selecionado; Recorrência pré-seleciona o weekday.
- Slot livre ainda cria consulta; save-na-hora e menus intactos.
- typecheck + build limpos; zero hex/rgb novos; PT-BR.
</success_criteria>

<output>
Create `.planning/quick/260723-m6r-remover-o-mini-calendario-do-painel-em-t/260723-m6r-SUMMARY.md` when done
</output>
