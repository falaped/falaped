---
phase: 260724-f3j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/dashboard/agenda/calendar-time-grid.tsx
  - components/dashboard/agenda/calendar-month-indicator.tsx
  - components/dashboard/agenda/agenda-side-panel.tsx
  - components/dashboard/agenda/availability-panel.tsx
  - components/dashboard/agenda/booking-rail.tsx
  - components/dashboard/agenda/calendar-editor.tsx
  - components/dashboard/agenda/appointment-detail-menu.tsx
autonomous: false
requirements: [E-1, E-2, E-3, E-4, E-5]
must_haves:
  truths:
    - "E-1: célula de disponibilidade LIVRE aparece VERDE (fundo claro + borda/acento) em Dia/Semana e no ponto/indicador do Mês; folga = cinza (--muted); vazio = branco (--background)."
    - "E-1: os 5 status de consulta (pendente azul-tracejado, confirmada azul-sólido, realizada cinza, falta vermelho, cancelada cinza-hachura) permanecem INALTERADOS."
    - "E-2: criar consulta OK, aplicar disponibilidade e aplicar folga fecham o drawer; em erro o drawer permanece aberto com o erro inline; toast de sucesso (com Desfazer p/ disp/folga) mantido."
    - "E-3: apenas o HEADER do dia selecionado fica marcado (sem realce na coluna/células) e exibe um Badge 'Selecionado'; distinto do 'hoje' (número em bolinha)."
    - "E-4: um ÚNICO botão abre o drawer; dentro do drawer um toggle de 3 opções Consulta | Disponibilidade | Folga escolhe a ação; slot livre segue abrindo o drawer em Consulta com horário pré-marcado."
    - "E-5: não existe mais a seção 'Pedidos a confirmar' na agenda; um bloco pendente clicado oferece Confirmar (pending→confirmed) e Recusar (pending→canceled) no menu de detalhe."
  artifacts:
    - app/globals.css
    - components/dashboard/agenda/calendar-time-grid.tsx
    - components/dashboard/agenda/calendar-month-indicator.tsx
    - components/dashboard/agenda/agenda-side-panel.tsx
    - components/dashboard/agenda/availability-panel.tsx
    - components/dashboard/agenda/booking-rail.tsx
    - components/dashboard/agenda/calendar-editor.tsx
    - components/dashboard/agenda/appointment-detail-menu.tsx
  key_links:
    - "Variáveis CSS de disponibilidade verde definidas em app/globals.css (oklch) e consumidas via var() nas classes das células/indicador."
    - "onCreated (BookingRail) e onApplied (AvailabilityPanel/AgendaSidePanel) sobem até o CalendarEditor e disparam setDrawerOpen(false)."
    - "AppointmentDetailMenu ramo 'pending' usa isLegalTransition/transitionAppointmentStatusAction já existentes (pending→confirmed | pending→canceled)."
---

<objective>
Lote de refinamentos de UI da Agenda (Fase 7, branch redesign/agenda-hibrida), entregando E-1..E-5 exatamente como TRAVADO em CONTEXT.md. CAMADA DE UI apenas — nenhuma mudança em lib/**, actions/**, modules/**, supabase/migrations/** ou app/dashboard/agenda/page.tsx. Editar app/globals.css é permitido (só cor/tokens).

Purpose: reduzir fricção da leitura visual (verde = livre) e do fluxo de ação (1 botão, drawer que fecha, pedidos direto no bloco).
Output: um verde de disponibilidade em oklch centralizado no globals.css + 7 componentes da agenda ajustados; sem deps novas; sem hex/rgb nos .tsx tocados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260724-f3j-agenda-cores-do-fundo-disponivel-verde-f/260724-f3j-CONTEXT.md
@CLAUDE.md
@app/globals.css
@components/dashboard/agenda/calendar-time-grid.tsx
@components/dashboard/agenda/calendar-month-indicator.tsx
@components/dashboard/agenda/calendar-editor.tsx
@components/dashboard/agenda/agenda-side-panel.tsx
@components/dashboard/agenda/availability-panel.tsx
@components/dashboard/agenda/booking-rail.tsx
@components/dashboard/agenda/appointment-detail-menu.tsx
@components/dashboard/agenda/appointment-status-style.ts

Interface notes (já lidos):
- CalendarEditor mantém `drawerOpen`/`setDrawerOpen`, `drawerMode` ("consulta" | "disponibilidade"), `preselectedMinute`, `applyAvailabilityIntent`. Os 2 botões de trigger vivem em ~L1068-1093. `PendingRequestsPanel` é renderizado em ~L1132 e ~L1155; `pendingRequests` memo em ~L524-538; import em ~L45-48.
- AgendaSidePanel expõe `initialMode`, `onApply`, e renderiza BookingRail SEM `onCreated` hoje. `PanelMode = "consulta" | "disponibilidade"`.
- BookingRail já tem `onCreated?: () => void` chamado após sucesso (L138).
- AvailabilityPanel emite `onApply(intent)` mas NÃO sabe do resultado; `type`/`setType` controlam Disponibilidade|Folga.
- appointment-transitions.ts: `pending: ["confirmed","canceled"]`; `isLegalTransition` e `transitionAppointmentStatusAction` já existem — REUSAR, não alterar.
- AppointmentDetailMenu hoje só ramifica `isConfirmed`; pendente cai no `else` (nenhum item).
- Badge já importado em appointment-detail-menu; existe em components/ui/badge.tsx.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: E-1 verde de disponibilidade (globals.css) ponta-a-ponta em Dia/Semana + Mês</name>
  <files>app/globals.css, components/dashboard/agenda/calendar-time-grid.tsx, components/dashboard/agenda/calendar-month-indicator.tsx</files>
  <action>
Definir em app/globals.css DUAS variáveis novas em oklch (NUNCA hex/rgb), no bloco :root e no bloco .dark, seguindo o padrão oklch já usado no arquivo: `--calendar-available` (fundo verde claro, legível) e `--calendar-available-strong` (verde mais forte p/ borda/acento/hover/ponto). Escolher valores oklch de verde (hue ~145-160) coerentes com a saturação suave da paleta (lightness alta no fundo, média no strong; no dark ajustar lightness/alpha como os demais tokens). Expor as vars no `@theme`/`@theme inline` NÃO é necessário — consumir direto via `var(--calendar-available)` na classe utilitária arbitrária.

Em calendar-time-grid.tsx, na camada de fundo READ-ONLY das células (bloco `className={cn(...)}` do `<button>` de faixa, hoje com `state === "available" && "bg-primary/25 hover:bg-primary/35"`): trocar o fill de disponível para VERDE via `var(--calendar-available)` como background e `var(--calendar-available-strong)` no hover (usar classes utilitárias arbitrárias do Tailwind com `[background-color:var(--calendar-available)]` e variante hover análoga). Manter `state === "off"` em `bg-muted` (cinza) e `state === "empty"` em branco/`--background` (o empty hoje só tem hover; garantir que o fundo base não pinte — vazio = branco do container). NÃO tocar nos blocos de consulta (APPOINTMENT_STATUS_STYLE) nem na linha de agora.

Em calendar-month-indicator.tsx, alinhar a sinalização de disponibilidade ao verde: o ponto `bg-primary` (indicador de `summary?.hasAvailability`, ~L114) e, se aplicável, o fundo do dia com disponibilidade passam a usar o verde via `var(--calendar-available-strong)` (ponto) — usar classe arbitrária `[background-color:var(--calendar-available-strong)]`. NÃO alterar o realce de hoje (bolinha primary) nem a barra de seleção (isso é E-3, Task 2).

Toda a cor via variável CSS oklch centralizada — zero hex/rgb nos .tsx. PT-BR nos rótulos.
  </action>
  <verify>
    <automated>grep -nE "calendar-available" app/globals.css | grep -v '^[0-9]*:[[:space:]]*/\*' | grep -c oklch</automated>
    <automated>yarn typecheck</automated>
  </verify>
  <done>globals.css tem `--calendar-available` e `--calendar-available-strong` em oklch (:root e .dark); disponível pinta VERDE via var() em Dia/Semana e o ponto do Mês é verde; folga=cinza, vazio=branco preservados; os 5 status de consulta inalterados; `yarn typecheck` passa.</done>
</task>

<task type="auto">
  <name>Task 2: E-3 (só header + Badge "Selecionado") + E-4 (1 botão, toggle 3 opções) + E-2 (drawer fecha)</name>
  <files>components/dashboard/agenda/calendar-time-grid.tsx, components/dashboard/agenda/calendar-month-indicator.tsx, components/dashboard/agenda/agenda-side-panel.tsx, components/dashboard/agenda/availability-panel.tsx, components/dashboard/agenda/booking-rail.tsx, components/dashboard/agenda/calendar-editor.tsx</files>
  <action>
E-3 (só o header marcado + tag): em calendar-time-grid.tsx REMOVER o realce que ultrapassa o header — no `<div>` da coluna-dia (~L260-270) apagar o ramo `day.localDate === selectedLocalDate && "bg-primary/5 ring-2 ring-inset ring-primary/40"` (a coluna volta ao normal; manter `day.isToday && "bg-primary/5"`). No HEADER do dia selecionado, MANTER o `selectedTab` reduzido ao próprio header e ADICIONAR um Badge pequeno "Selecionado" (importar `Badge` de `@/components/ui/badge`), renderizado dentro do `headerContent` só quando `isSelected`, visualmente DISTINTO do "hoje" (número em bolinha). Em calendar-month-indicator.tsx, o realce de seleção da célula já é só a barra `border-b-2 border-primary` (aceitável como "só o dia marcado") — adicionar também um Badge/indicador "Selecionado" discreto na célula selecionada OU manter apenas a barra; escolha discricionária, mas garantir que NÃO haja fundo/realce que confunda com hoje. PT-BR: texto do badge = "Selecionado".

E-4 (1 botão + toggle 3 opções): em calendar-editor.tsx SUBSTITUIR os DOIS botões de trigger (~L1068-1093, "+ Nova consulta" e "Disponibilidade") por UM ÚNICO `<Button>` (label PT-BR discricionário, ex.: "+ Nova marcação"). O clique zera `preselectedMinute`, define o modo inicial e abre o drawer. Ampliar o tipo de modo para 3 opções propagando um `initialAction: "consulta" | "disponibilidade" | "folga"` ao AgendaSidePanel. Em agenda-side-panel.tsx, trocar o toggle de 2 opções por 3 (`Consulta | Disponibilidade | Folga`), mantendo o padrão de botões segmentados já existente; "Consulta" renderiza BookingRail; "Disponibilidade" e "Folga" renderizam AvailabilityPanel — e o painel recebe uma prop `initialType: "available" | "off"` (available p/ Disponibilidade, off p/ Folga). Em availability-panel.tsx, aceitar `initialType` e semear o estado `type` com ele (via estado inicial + `useEffect` que ressincroniza quando `initialType` muda entre aberturas, mesmo padrão do `initialMode` no side-panel). Slot livre da grade continua abrindo o drawer em Consulta com horário pré-marcado (handleAppointmentCreate já faz isso — só ajustar para o novo shape de modo inicial).

E-2 (drawer fecha ao executar): fazer o sucesso de CADA ação subir até o CalendarEditor e chamar `setDrawerOpen(false)`.
- BookingRail já expõe `onCreated` — em agenda-side-panel.tsx PASSAR `onCreated` (nova prop, repassada ao BookingRail) e em calendar-editor.tsx ligar `onCreated={() => setDrawerOpen(false)}` no AgendaSidePanel.
- AvailabilityPanel: adicionar prop `onApplied?: () => void`; após um `onApply` bem-sucedido o AVISO é que o painel não conhece o resultado — então mover a decisão de fechar para o editor: em calendar-editor.tsx, no `applyAvailabilityIntent`, no ramo `result.ok` (sucesso), chamar `setDrawerOpen(false)` ANTES do toast (o toast de sucesso com Desfazer permanece); no ramo de erro (`else`) NÃO fechar (mantém drawer + erro). Isso cobre disponibilidade E folga (ambas passam por `applyAvailabilityIntent`). Se preferir manter o fechamento no painel, repassar `onApplied` só é chamado em sucesso — mas a fonte da verdade do sucesso está no editor, então fechar no editor é o caminho canônico; `onApplied` fica opcional/não usado.

Sem hex/rgb nos .tsx; PT-BR; sem deps novas (Badge/Sheet já vendorizados).
  </action>
  <verify>
    <automated>yarn typecheck</automated>
    <automated>grep -cE "setDrawerMode|Disponibilidade\"" components/dashboard/agenda/calendar-editor.tsx; grep -oE "Selecionado" components/dashboard/agenda/calendar-time-grid.tsx | head -1</automated>
  </verify>
  <done>Um único botão de trigger no editor abre o drawer; o toggle interno tem 3 opções (Consulta|Disponibilidade|Folga) com o tipo pré-selecionado no AvailabilityPanel; só o header do dia selecionado fica marcado + Badge "Selecionado" (sem realce de coluna/células); criar consulta / aplicar disponibilidade / aplicar folga fecham o drawer em sucesso e NÃO fecham em erro; `yarn typecheck` passa.</done>
</task>

<!-- planner-discipline-allow: PendingRequestsPanel -->
<task type="auto">
  <name>Task 3: E-5 remover PendingRequestsPanel + Confirmar/Recusar no bloco pendente</name>
  <files>components/dashboard/agenda/calendar-editor.tsx, components/dashboard/agenda/appointment-detail-menu.tsx</files>
  <action>
E-5 remover a seção: em calendar-editor.tsx APAGAR o import de `PendingRequestsPanel`/`PendingRequest` (~L45-48), o `pendingRequests` memo (~L524-538) e as duas renderizações `<PendingRequestsPanel requests={pendingRequests} />` (dentro de TabsContent "dia" e "semana"). Remover quaisquer estados/props órfãos que só existiam para o painel. O arquivo `pending-requests-panel.tsx` pode permanecer no repo sem uso (não referenciado). Ajustar comentários que mencionam o painel abaixo da grade para refletir a remoção (sem deixar o literal que o gate negativo procura — ver hygiene abaixo).

E-5 confirmar/recusar no bloco pendente: em appointment-detail-menu.tsx ADICIONAR um ramo para `appointment.status === "pending"` no bloco de transições (hoje só há `isConfirmed`). Definir `const isPending = appointment.status === "pending"`. Quando `isPending`, renderizar dois itens de menu (mesmo padrão visual de MENU_ITEM):
  - "Confirmar" → `runTransition(snapshotFor("confirmed", "Consulta confirmada."))` (pending→confirmed).
  - "Recusar" → abre o AlertDialog destrutivo já existente com `setConfirmCancel(snapshotFor("canceled", "Consulta recusada."))` (pending→canceled; reusa o fluxo resiliente de cancelar). Ajustar o título/descrição do AlertDialog para ler bem tanto p/ cancelar quanto p/ recusar, OU manter genérico ("A consulta ... será cancelada e o horário liberado.") — discricionário, PT-BR.
Reusar `transitionAppointmentStatusAction` e a máquina `APPOINTMENT_TRANSITIONS` já existentes (pending já permite ambos) — NÃO alterar modules/actions. Manter o ramo `isConfirmed` e o `isFinal` intactos.

Sem hex/rgb; PT-BR; sem deps novas.
  </action>
  <verify>
    <automated>grep -c "PendingRequestsPanel" components/dashboard/agenda/calendar-editor.tsx</automated>
    <automated>grep -cE "isPending|Confirmar|Recusar" components/dashboard/agenda/appointment-detail-menu.tsx</automated>
    <automated>yarn typecheck</automated>
    <automated>yarn build</automated>
  </verify>
  <done>`grep -c "PendingRequestsPanel" calendar-editor.tsx` == 0; o menu de detalhe de um bloco pendente oferece Confirmar e Recusar via transições legais existentes; `yarn typecheck` e `yarn build` passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificação visual E-1..E-5</name>
  <what-built>
E-1: fundo de disponibilidade verde (var oklch em globals.css), folga cinza, vazio branco — em Dia/Semana e no ponto do Mês; 5 status de consulta inalterados.
E-2: drawer fecha ao criar consulta / aplicar disponibilidade / aplicar folga (e permanece aberto em erro).
E-3: só o header do dia selecionado marcado + Badge "Selecionado" (sem realce de coluna/células), distinto do "hoje".
E-4: um único botão abre o drawer com toggle de 3 opções Consulta | Disponibilidade | Folga; slot livre abre em Consulta pré-marcado.
E-5: sem seção "Pedidos a confirmar"; bloco pendente confirmável/recusável pelo menu.
  </what-built>
  <how-to-verify>
1. `yarn dev` → abrir /dashboard/agenda.
2. E-1: numa disponibilidade LIVRE em Dia e Semana, o fundo deve ser VERDE (não azul); folga cinza; vazio branco. No Mês, o ponto/indicador do dia com disponibilidade deve ser verde. Confirmar que consulta pendente segue azul-tracejada, confirmada azul-sólida, realizada/cancelada cinza, falta vermelha (inalterados).
3. E-4: confirmar que há UM ÚNICO botão de ação (não dois). Clicar → drawer abre; alternar entre Consulta | Disponibilidade | Folga; ao escolher Folga o painel já vem com Tipo=Folga. Clicar num slot livre na grade → drawer abre em Consulta com o horário marcado.
4. E-2: criar uma consulta → drawer FECHA + toast. Aplicar uma disponibilidade → FECHA + toast com Desfazer. Aplicar uma folga → FECHA + toast. Forçar um erro (ex.: agendar em horário já ocupado) → drawer PERMANECE aberto com erro inline.
5. E-3: selecionar um dia (clicar no header em Semana ou num dia no Mês) → só o HEADER fica marcado + tag "Selecionado"; a coluna/células NÃO ficam realçadas; visualmente distinto do "hoje".
6. E-5: não deve existir a seção "Pedidos a confirmar" abaixo da grade. Clicar num bloco PENDENTE (azul-tracejado) → menu com Confirmar e Recusar; Confirmar torna-o confirmado; Recusar (via confirmação) cancela.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `yarn typecheck` limpo após cada task; `yarn build` limpo na Task 3.
- Gate hex/rgb = 0 (normalizado, ignorando comentários) nos .tsx tocados; verde exclusivamente em oklch (globals.css) consumido via var().
- `grep -c "PendingRequestsPanel" components/dashboard/agenda/calendar-editor.tsx` == 0.
- Um único `<Button>` de trigger do drawer no calendar-editor.tsx.
- Nenhuma edição em lib/**, actions/**, modules/**, supabase/migrations/**, app/dashboard/agenda/page.tsx.
</verification>

<success_criteria>
E-1..E-5 entregues conforme CONTEXT.md (decisões LOCKED), UI-only, verde em oklch centralizado no globals.css, sem hex/rgb nos .tsx, sem deps novas, checkpoint visual humano aprovado.
</success_criteria>

<output>
Create `.planning/quick/260724-f3j-agenda-cores-do-fundo-disponivel-verde-f/260724-f3j-SUMMARY.md` when done
</output>
