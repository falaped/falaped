---
description: Supabase Auth – client/server, middleware, getAuthenticatedUser, protected routes
globs: "lib/supabase/**/*.ts,middleware.ts,app/(auth)/**/*,modules/supabase/get-authenticated-user.ts"
alwaysApply: false
---

# Supabase Auth

## Fluxo de Dados

```
auth.users (Supabase Auth)
    ↓ auth_user_id
profiles (ponte)
    ↑ profile_id
authenticated_users
    └── phone (user_phone) ← chave para filtrar dados
```

Após login: `auth.user.id` → `profiles` → `authenticated_users` → `user_phone`. Todas as queries usam `user_phone`.

**Signup:** ao criar conta em `auth.users`, o trigger `handle_new_auth_user()` insere em `public.profiles` (first_name, surname, email, phone) e em `public.authenticated_users` (profile_id, phone, status = 'unpaid'); só quando phone está presente.

## Arquivos em lib/supabase/

| Arquivo | Uso |
|---------|-----|
| `client.ts` | `createBrowserClient` para Client Components |
| `server.ts` | `createServerClient` para Server Components, Route Handlers, Server Actions |
| `middleware.ts` | Função para refresh de sessão; uso no middleware |

## Middleware

- Refresh da sessão em toda requisição.
- Rotas `/dashboard/*` e `/` → exigir sessão; redirecionar para `/login` se não autenticado.
- Rotas `/login`, `/callback` → se já autenticado, redirecionar para `/dashboard`.

```ts
// middleware.ts
import { createServerClient } from '@supabase/ssr';

export async function middleware(req: NextRequest) {
  const supabase = createServerClient(...);
  const { data: { session } } = await supabase.auth.getSession();

  const isAuth = !!session;
  const isAuthRoute = req.nextUrl.pathname.startsWith('/login') || req.nextUrl.pathname.startsWith('/callback');

  if (!isAuth && !isAuthRoute) return NextResponse.redirect(new URL('/login', req.url));
  if (isAuth && isAuthRoute) return NextResponse.redirect(new URL('/dashboard', req.url));

  return response;
}
```

## getAuthenticatedUser

- `modules/supabase/get-authenticated-user.ts`: `getAuthenticatedUser(supabase)` retorna `{ profile, authenticatedUser } | null`.
- Use em Server Components, Server Actions e Route Handlers para validar sessão e obter dados do médico. Para `user_phone` (queries que exigem status paid): use `result.authenticatedUser?.status === 'paid'` e `result.authenticatedUser.phone`.

## Rotas de Auth

- `(auth)/login/page.tsx`: formulário ou magic link; redireciona ao callback.
- `(auth)/callback/route.ts`: troca de código por sessão; redireciona para `/dashboard` após sucesso.

## Proteção de Páginas

- Middleware protege rotas; não é necessário checar sessão em cada página.
- Para dados sensíveis: obter `result` via `getAuthenticatedUser(supabase)` e redirecionar ou 403 se null; quando precisar de user_phone (paid), usar `result.authenticatedUser`.

## Checklist

- [ ] Client para browser; Server para server
- [ ] Middleware com refresh e redirect
- [ ] getAuthenticatedUser usado em Server Components/Actions
- [ ] Callback route para OAuth/código
